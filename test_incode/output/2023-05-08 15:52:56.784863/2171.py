#!/usr/bin/python3
port = 8080
if __name__ == '__main__':
    #A program that iterates over a port.
    #The port is opened and listened on.
    #The program will listen for connections on
    #port 8080 and print out the contents of the 
    #port 8080 port.
    #The program will then exit when the user 
    #presses the 'q' key.
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    sock.listen(1)
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('0.0.0.0', port))
    
    #Create a socket object using AF_INET and SOCK_STREAM 
    #and bind to the port number 8080

