#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that removes all ports.
    remove_ports()
    #A function that removes ports one by one.
    remove_ports(ports[0])
    #A function that removes ports one by one and prints them.
    remove_ports(ports[0],ports[1])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4],ports[5])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4],ports[5],ports[6])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4],ports[5],ports[6],ports[7])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4],ports[5],ports[6],ports[7],ports[8])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4],ports[5],ports[6],ports[7],ports[8],ports[9])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4],ports[5],ports[6],ports[7],ports[8],ports[9],ports[10])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4],ports[5],ports[6],ports[7],ports[8],ports[9],ports[10],ports[11])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4],ports[5],ports[6],ports[7],ports[8],ports[9],ports[10],ports[11],ports[12])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4],ports[5],ports[6],ports[7],ports[8],ports[9],ports[10],ports[11],ports[12],ports[13])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4],ports[5],ports[6],ports[7],ports[8],ports[9],ports[10],ports[11],ports[12],ports[13],ports[14])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4],ports[5],ports[6],ports[7],ports[8],ports[9],ports[10],ports[11],ports[12],ports[13],ports[14],ports[15])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4],ports[5],ports[6],ports[7],ports[8],ports[9],ports[10],ports[11],ports[12],ports[13],ports[14],ports[15],ports[16])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4],ports[5],ports[6],ports[7],ports[8],ports[9],ports[10],ports[11],ports[12],ports[13],ports[14],ports[15],ports[16],ports[17])
    #A function that removes ports one by one and prints them all.
    remove_ports(ports[0],ports[1],ports[2],ports[3],ports[4],ports[5],ports[6],ports[7],ports[8],ports[9],ports[10],ports[11],ports[12],ports[13],ports[14],ports[15],ports[16],ports[17],ports[18])
    #A function that removes ports one by one and prints them all.
    remove_ports(

