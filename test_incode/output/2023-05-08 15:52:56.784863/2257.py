#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that converts a list of numbers.
    def convert(numbers):
        numbers = [int(x) for x in numbers]
        return numbers
    #A function that converts a list to a string.
    def convert(numbers):
        numbers = [str(x) for x in numbers]
        return numbers
    #A function that converts a list to a tuple.
    def convert(numbers):
        numbers = [tuple(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a dictionary.
    def convert(numbers):
        numbers = [dict(zip(map(str, x), y.split(','))) for x, y in numbers]
        return numbers
    #A function that converts a list to a set.
    def convert(numbers):
        numbers = [set(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a frozenset.
    def convert(numbers):
        numbers = [frozenset(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a list.
    def convert(numbers):
        numbers = [list(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a tuple.
    def convert(numbers):
        numbers = [tuple(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a set.
    def convert(numbers):
        numbers = [set(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a frozenset.
    def convert(numbers):
        numbers = [frozenset(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a list.
    def convert(numbers):
        numbers = [list(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a tuple.
    def convert(numbers):
        numbers = [tuple(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a set.
    def convert(numbers):
        numbers = [set(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a frozenset.
    def convert(numbers):
        numbers = [frozenset(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a list.
    def convert(numbers):
        numbers = [list(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a tuple.
    def convert(numbers):
        numbers = [tuple(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a set.
    def convert(numbers):
        numbers = [set(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a frozenset.
    def convert(numbers):
        numbers = [frozenset(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a list.
    def convert(numbers):
        numbers = [list(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a tuple.
    def convert(numbers):
        numbers = [tuple(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a set.
    def convert(numbers):
        numbers = [set(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a frozenset.
    def convert(numbers):
        numbers = [frozenset(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a list.
    def convert(numbers):
        numbers = [list(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a tuple.
    def convert(numbers):
        numbers = [tuple(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a set.
    def convert(numbers):
        numbers = [set(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a frozenset.
    def convert(numbers):
        numbers = [frozenset(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a list.
    def convert(numbers):
        numbers = [list(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a tuple.
    def convert(numbers):
        numbers = [tuple(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a set.
    def convert(numbers):
        numbers = [set(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a frozenset.
    def convert(numbers):
        numbers = [frozenset(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a list.
    def convert(numbers):
        numbers = [list(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a tuple.
    def convert(numbers):
        numbers = [tuple(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a set.
    def convert(numbers):
        numbers = [set(map(int, x.split(','))) for x in numbers]
        return numbers
    #A function that converts a list to a frozenset.
    def convert(numbers):
        numbers = [frozenset(map(int, x.split(','))) for x in numbers]
        return numbers
    #A 

