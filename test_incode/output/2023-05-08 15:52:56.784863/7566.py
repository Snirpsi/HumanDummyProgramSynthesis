#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that adds a list of numbers.
    def add(numbers):
        return sum(numbers)
    #A function that multiplies a list of numbers.
    def multiply(numbers):
        return numbers[0]*numbers[1]
    #A function that subtracts a list of numbers.
    def subtract(numbers):
        return numbers[0]-numbers[1]
    #A function that divides a list of numbers.
    def divide(numbers):
        return numbers[0]/numbers[1]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the quotient of a list of numbers.
    def quotient(numbers):
        return reduce(lambda x,y: x/y, numbers)
    #A function that returns the remainder of a list of numbers.
    def remainder(numbers):
        return reduce(lambda x,y: x-y, numbers)
    #A function that returns the absolute value of a list of numbers.
    def abs(numbers):
        return reduce(lambda x,y: abs(x), numbers)
    #A function that returns the square root of a list of numbers.
    def sqrt(numbers):
        return reduce(lambda x,y: x**(1/2), numbers)
    #A function that returns the square of a list of numbers.
    def square(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the inverse of a list of numbers.
    def inv(numbers):
        return reduce(lambda x,y: x/y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial2(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial3(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial4(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial5(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial6(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial7(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial8(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial9(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial10(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial11(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial12(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial13(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial14(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial15(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial16(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial17(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial18(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial19(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial20(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial21(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial22(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial23(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial24(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial25(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial26(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial27(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #A function that returns the factorial of a list of numbers.
    def factorial28(numbers):
        return reduce(lambda x,y: x*y, numbers)
    #

