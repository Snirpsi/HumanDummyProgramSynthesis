#!/usr/bin/python3
url = "http://snirps.ddns.net"
if __name__ == '__main__':
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())
    file.close()
    
    #A function that opens a http request.
    #It returns a string containing the response.
    #The response is then saved to a file.
    #The file is then closed after use.
    req = urllib.request.Request(url)
    response = urllib.request.urlopen(req)
    file = open("response.html", "w")
    file.write(response.read())

