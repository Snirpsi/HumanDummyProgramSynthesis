#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that prints all ports.
    def print_ports(ports):
        for port in ports:
            print(port)
    #A function that prints all the ports.
    def print_all_ports():
        print_ports(ports)
    #A function that prints all the ports.
    def print_all_ports_reversed():
        print_ports(ports[::-1])
    #A function that prints all the ports.
    def print_all_ports_reversed_reversed():
        print_ports(ports[::-1] + ports[::-1])
    #A function that prints all the ports.
    def print_all_ports_reversed_reversed_reversed():
        print_ports(ports[::-1] + ports[::-1] + ports[::-1])
    #A function that prints all the ports.
    def print_all_ports_reversed_reversed_reversed_reversed():
        print_ports(ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1] + ports[::-1]

