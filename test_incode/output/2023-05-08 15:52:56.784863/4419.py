#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that enumerates all ports.
    def enumerate_ports():
        for port in ports:
            yield port
    #A function that enumerates all available network interfaces.
    def enumerate_iface():
        for iface in netifaces.interfaces():
            yield iface
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddrs():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifnames():
        for iface in netifaces.interfaces():
            for name in netifaces.ifaddresses(iface):
                yield name
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in netifaces.ifaddresses(iface):
                yield addr
    #A function that enumerates all available network interfaces.
    def enumerate_ifaddresses():
        for iface in netifaces.interfaces():
            for addr in ne

