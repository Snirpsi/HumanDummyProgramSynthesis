#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that adds numbers.
    def add(numbers):
        return numbers[0] + numbers[1] + numbers[2]
    #A function that subtracts numbers.
    def subtract(numbers):
        return numbers[0] - numbers[1] - numbers[2]
    #A function that multiplies numbers.
    def multiply(numbers):
        return numbers[0] * numbers[1] * numbers[2]
    #A function that divides numbers.
    def divide(numbers):
        return numbers[0] / numbers[1] / numbers[2]
    #A function that returns the sum of the numbers.
    def sum(numbers):
        return sum(numbers)
    #A function that returns the product of the numbers.
    def product(numbers):
        return product(numbers)
    #A function that returns the square of the numbers.
    def square(numbers):
        return square(numbers)
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[0]
    #A function that returns the square of the numbers.

