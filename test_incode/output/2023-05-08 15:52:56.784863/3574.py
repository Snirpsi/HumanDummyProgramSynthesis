#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A endless loop that stores a list of numbers.
    #The loop will run until a condition is met.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.
    #The loop will then print the list elements.
    #The condition is that the list has at least two elements.


