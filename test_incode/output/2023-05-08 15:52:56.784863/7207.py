#!/usr/bin/python3
if __name__ == '__main__':
    #A program that iterates over user input.
    #It will print out a message for each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program will ask the user to input a number.
    #The program will then print the number times each letter the user inputs.
    #The program 

