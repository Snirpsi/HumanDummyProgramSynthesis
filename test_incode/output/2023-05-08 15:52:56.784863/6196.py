#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A endless loop that opens a list of words.
    while True:
        #A loop that prints every word in the list.
        for word in words:
            print(word)
        #A loop that asks the user for input.
        word = input("Enter a word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another word: ")
        #A loop that checks if the word is contained in the list.
        if word in words:
            print("The word is in the list")
        else:
            print("The word is not in the list")
        #A loop that asks the user for another input.
        word = input("Enter another 

