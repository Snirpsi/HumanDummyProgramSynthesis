#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that enumerates all ports.
    def enumerate_ports():
        for port in ports:
            yield port
    #A function that prints all the ports.
    def print_ports():
        for port in enumerate_ports():
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed():
        for port in reversed(enumerate_ports()):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed():
        for port in reversed(reversed(enumerate_ports())):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(enumerate_ports()))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(enumerate_ports()))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that prints all the ports.
    def print_ports_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed_reversed():
        for port in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(enumerate_ports())))))):
            print(port)
    #A function that 

