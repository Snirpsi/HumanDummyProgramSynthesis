#!/usr/bin/python3
fruirs = ["apple","banana","orange"]
if __name__ == '__main__':
    #A program that adds fruits.
    #It will ask the user to input a fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fruit name.
    #It will then add that fruit to the fruirs list.
    #It will then ask the user to input another fru

