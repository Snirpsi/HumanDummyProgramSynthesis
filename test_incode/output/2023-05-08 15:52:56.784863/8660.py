#!/usr/bin/python3
fruirs = ["apple","banana","orange"]
if __name__ == '__main__':
    #A program that requests fruits.
    #It asks the user to input a fruit name.
    #It then prints the fruit name.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It then prints the number.
    #It asks the user to input a number.
    #It 

