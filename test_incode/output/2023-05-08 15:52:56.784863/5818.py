#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A program that stores numbers.
    #The program will ask the user to enter a number.
    #The program will then store that number in an array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter another number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then store that number in another array called numbers.
    #The program will then print the array to the screen.
    #The program will then ask the user to enter a number.
    #The program will then 

