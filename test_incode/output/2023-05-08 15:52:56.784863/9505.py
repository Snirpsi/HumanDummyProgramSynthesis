#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that converts numbers.
    def convert(number):
        return number * 2
    #A function that converts numbers.
    def convert(number):
        return number * 3
    #A function that converts numbers.
    def convert(number):
        return number * 4
    #A function that converts numbers.
    def convert(number):
        return number * 5
    #A function that converts numbers.
    def convert(number):
        return number * 6
    #A function that converts numbers.
    def convert(number):
        return number * 7
    #A function that converts numbers.
    def convert(number):
        return number * 8
    #A function that converts numbers.
    def convert(number):
        return number * 9
    #A function that converts numbers.
    def convert(number):
        return number * 10
    #A function that converts numbers.
    def convert(number):
        return number * 11
    #A function that converts numbers.
    def convert(number):
        return number * 12
    #A function that converts numbers.
    def convert(number):
        return number * 13
    #A function that converts numbers.
    def convert(number):
        return number * 14
    #A function that converts numbers.
    def convert(number):
        return number * 15
    #A function that converts numbers.
    def convert(number):
        return number * 16
    #A function that converts numbers.
    def convert(number):
        return number * 17
    #A function that converts numbers.
    def convert(number):
        return number * 18
    #A function that converts numbers.
    def convert(number):
        return number * 19
    #A function that converts numbers.
    def convert(number):
        return number * 20
    #A function that converts numbers.
    def convert(number):
        return number * 21
    #A function that converts numbers.
    def convert(number):
        return number * 22
    #A function that converts numbers.
    def convert(number):
        return number * 23
    #A function that converts numbers.
    def convert(number):
        return number * 24
    #A function that converts numbers.
    def convert(number):
        return number * 25
    #A function that converts numbers.
    def convert(number):
        return number * 26
    #A function that converts numbers.
    def convert(number):
        return number * 27
    #A function that converts numbers.
    def convert(number):
        return number * 28
    #A function that converts numbers.
    def convert(number):
        return number * 29
    #A function that converts numbers.
    def convert(number):
        return number * 30
    #A function that converts numbers.
    def convert(number):
        return number * 31
    #A function that converts numbers.
    def convert(number):
        return number * 32
    #A function that converts numbers.
    def convert(number):
        return number * 33
    #A function that converts numbers.
    def convert(number):
        return number * 34
    #A function that converts numbers.
    def convert(number):
        return number * 35
    #A function that converts numbers.
    def convert(number):
        return number * 36
    #A function that converts numbers.
    def convert(number):
        return number * 37
    #A function that converts numbers.
    def convert(number):
        return number * 38
    #A function that converts numbers.
    def convert(number):
        return number * 39
    #A function that converts numbers.
    def convert(number):
        return number * 40
    #A function that converts numbers.
    def convert(number):
        return number * 41
    #A function that converts numbers.
    def convert(number):
        return number * 42
    #A function that converts numbers.
    def convert(number):
        return number * 43
    #A function that converts numbers.
    def convert(number):
        return number * 44
    #A function that converts numbers.
    def convert(number):
        return number * 45
    #A function that converts numbers.
    def convert(number):
        return number * 46
    #A function that converts numbers.
    def convert(number):
        return number * 47
    #A function that converts numbers.
    def convert(number):
        return number * 48
    #A function that converts numbers.
    def convert(number):
        return number * 49
    #A function that converts numbers.
    def convert(number):
        return number * 50
    #A function that converts numbers.
    def convert(number):
        return number * 51
    #A function that converts numbers.
    def convert(number):
        return number * 52
    #A function that converts numbers.
    def convert(number):
        return number * 53
    #A function that converts numbers.
    def convert(number):
        return number * 54
    #A function that converts numbers.
    def convert(number):
        return number * 55
    #A function that converts numbers.
    def convert(number):
        return number * 56
    #A function that converts numbers.
    def convert(number):
        return number * 57
    #A function that converts numbers.
    def convert(number):
        return number * 58
    #A function that converts numbers.
    def convert(number):
        return number * 59
    #A function that converts numbers.
    def convert(number):
        return number * 60
    #A function that converts numbers.
    def convert(number):
        return number * 61
    #A function that converts numbers.
    def convert(number):

