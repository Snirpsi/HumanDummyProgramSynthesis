#!/usr/bin/python3
if __name__ == '__main__':
    #A program that iterates over user input.
    #It will print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter the user inputs.
    #It will also print out a message for each letter 

