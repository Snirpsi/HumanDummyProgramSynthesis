#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that calculates a list of numbers.
    #The function takes a list of numbers and returns them as a list.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes one argument, the list of numbers, and returns the
    #list as a single number.
    #The function takes 

