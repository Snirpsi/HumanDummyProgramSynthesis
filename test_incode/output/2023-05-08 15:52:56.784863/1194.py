#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A minimal webserver that requests a list of numbers.
    #The server will respond with a JSON list of numbers.
    #The client will then parse that JSON list into numbers.
    #The client will then print the list of numbers.
    #The client will then exit the server.
    
    #The server will listen on port 8000.
    #The client will connect to the server on port 8000.
    #The client will then send a GET request to the server on port 8000.
    #The server will respond with a JSON list of numbers.
    #The client will then parse that JSON list into numbers.
    #The client will then print the list of numbers.
    #The client will then exit the server.
    
    #The server will listen on port 8000.
    #The client will connect to the server on port 8000.
    #The client will then send a GET request to the server on port 8000.
    #The server will respond with a JSON list of numbers.
    #The client will then parse that JSON list into numbers.
    #The client will then print the list of numbers.
    #The client will then exit the server.
    
    #The server will listen on port 8000.
    #The client will connect to the server on port 8000.
    #The client will then send a GET request to the server on port 8000.
    #The server will respond with a JSON list of numbers.
    #The client will then parse that JSON list into numbers.
    #The client will then print the list of numbers.
    #The client will then exit the server.

