#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that opens numbers.txt and reads numbers from it.
    numbers = open(sys.argv[1],'r').read().splitlines()
    #A function that prints all the numbers from numbers.txt and writes them to a new file called numbers.txt.
    numbers = map(int, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)
    numbers = map(str, numbers)

