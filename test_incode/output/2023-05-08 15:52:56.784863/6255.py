#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A program that iterates over a list of numbers.
    #The program will print each number in turn.
    for number in numbers:
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order.
    #The program will print each number in turn.
    for number in reversed(numbers):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(numbers)):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(numbers))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(numbers)))): 
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and prints them in reverse order and in reverse order.
    #The program will print each number in turn.
    for number in reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(reversed(numbers))))):
        print(number)
        
    #A program that iterates over a list of numbers and print

