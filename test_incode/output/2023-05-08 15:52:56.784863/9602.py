#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A endless loop that opens a list of numbers.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program will then ask the user to input a number until the user enters 0.
    #The program will then print out the list starting with that number.
    #The program 

