#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that opens words.txt and reads words from it
    wordlist = open("words.txt","r")
    words = wordlist.read().split()
    wordlist.close()
    #A function that prints all the words in the list
    for word in words:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[-1:]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[-1:]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the words in the list except the last
    for word in words[:-1]:
        print(word)
    #A function that prints all the words in the list except the first
    for word in words[1:]:
        print(word)
    #A function that prints all the word

