#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A program that converts ports.
    #It will listen on all ports and convert them to IPv4
    #It will also listen on all ports and convert them to IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert them to IPv4 and IPv6
    #It will also listen on all ports and convert 

