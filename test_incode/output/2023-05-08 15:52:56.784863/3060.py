#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that requests a list of numbers.
    #The function will return the list as a string.
    #The function will also return the list as a list.
    #The function will also return the list as a dictionary.
    #The function will also return the list as a tuple.
    #The function will also return the list as a set.
    #The function will also return the list as a frozenset.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function will also return the list as a tuple.
    #The function will also return the list as a list.
    #The function 

