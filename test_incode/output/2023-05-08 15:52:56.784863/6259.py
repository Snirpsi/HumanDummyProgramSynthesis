#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A program that requests words.
    #The program will print out each word in the list.
    #The program will ask the user for input.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask the user for input again.
    #The program will then print the word in the list that matches the input.
    #The program will ask

