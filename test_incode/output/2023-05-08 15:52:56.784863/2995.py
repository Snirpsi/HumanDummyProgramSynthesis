#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    ports.extend(range(443))
    #A function that adds ports.
    ports.extend(range(666))
    #A function that adds ports.
    ports.extend(range(8080))
    #A function that adds ports.
    ports.extend(range(80))
    #A function that adds ports.
    

