#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A program that opens a list of words.
    #The program will ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will then ask the user for their name.
    #The program will then print the list of words that 
    #are in the list "words"
    #The program will 

