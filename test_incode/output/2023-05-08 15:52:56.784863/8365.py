#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A minimal webserver that calculates a list of numbers.
    #It will accept a GET request and return a JSON response with all the numbers.
    #The response will contain all the numbers in an array.
    #The response will also contain an HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header that tells the client to send back a JSON response.
    #The response will also contain a HTTP header 

