#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that calculates all ports.
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = ports[:2]
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports

