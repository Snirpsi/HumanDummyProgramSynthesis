#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A endless loop that removes a list of numbers.
    #The list will continue to grow as long as you keep adding numbers to it.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the list.
    #The program will exit after it reaches the end of the 

