#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that requests a list of numbers.
    #It will print them out in reverse order.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a number.
    #The function will then return the number entered.
    #The function will ask the user for input until they enter a 

