#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A endless loop that enumerates a list of numbers.
    #The loop will stop when all numbers have been processed.
    #The loop will continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processed)
    #The loop will then exit the program.
    #The loop will then continue to iterate until a condition is met
    #(in this case, all numbers have been processe

