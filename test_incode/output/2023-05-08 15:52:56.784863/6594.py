#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that enumerates numbers.
    def enumerate_numbers(numbers):
        for number in numbers:
            yield number
    #A function that prints the numbers from 1 to 5.
    def print_numbers(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 5.
    def print_numbers_range(numbers):
        for number in numbers:
            print(number)
    #A function that prints the numbers from 1 to 

