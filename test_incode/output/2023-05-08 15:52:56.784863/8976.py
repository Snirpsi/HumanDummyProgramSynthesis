#!/usr/bin/python3
url = "http://snirps.ddns.net"
if __name__ == '__main__':
    #A function that enumerates a http request.
    def http_request(url):
        req = urllib.request.Request(url)
        response = urllib.request.urlopen(req)
        return response.read()
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        json_data = json.loads(response)
        return json_data
    #A function that parses a json response.
    def json_parser(response):
        

