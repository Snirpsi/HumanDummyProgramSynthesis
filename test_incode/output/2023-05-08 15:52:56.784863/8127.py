#!/usr/bin/python3
if __name__ == '__main__':
    #A function that calculates user input.
    #The user input will be stored as a string.
    #The function will then be called and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called again and will return the user input.
    #The function will then be called 

