#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that opens words.txt and reads it.
    wordfile = open("words.txt","r")
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.txt and their frequencies.
    wordlist = wordfile.read().split()
    wordfile.close()
    #A function that prints all the words in words.

