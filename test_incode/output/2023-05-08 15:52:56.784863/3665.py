#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A program that adds a list of numbers.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program will ask the user for a list of numbers.
    #The program will then add each number in the list to the sum.
    #The program will then print the sum.
    #The program 

