#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that converts numbers.
    def convert(numbers):
        return sum(numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts numbers to words.
    def convert(numbers):
        return " ".join(str(number) for number in numbers)
    #A function that converts 

