#!/usr/bin/python3
port = 8080
if __name__ == '__main__':
    #A function that opens a port.
    server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    server.bind(('0.0.0.0', port))
    server.listen(1)
    while True:
        client, address = server.accept()
        print('Connected by', address)
        
        #A function that sends a message to the client.
        client.send('Hello World!')
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.
        message = input('> ')
        
        #A function that sends the message to the client.
        client.send(message)
        
        #A function that closes the client.
        client.close()
        
        #A function that waits for an input from the user.

