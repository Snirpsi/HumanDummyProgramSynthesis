#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A program that opens a list of numbers.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then prints the numbers in reverse order.
    #It then asks the user for an input.
    #It then 

