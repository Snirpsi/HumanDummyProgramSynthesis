#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt) as file:
        for line in file:
            print(line)
            
    #A endless loop that opens numbers.txt and prints every line until it reaches the end of the file.
    with open(numbers.txt

