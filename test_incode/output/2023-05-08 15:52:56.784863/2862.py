#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that opens a list of numbers.
    #The function will then print out each number in the list.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.
    #The function will then ask the user for another number.
    #The function will then print the number the user entered.

