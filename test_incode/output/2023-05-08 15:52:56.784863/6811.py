#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that opens a list of numbers.
    #It then prints the numbers in the list.
    #It then asks the user for an input of how many numbers to print.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the numbers in the list starting from the number the user entered.
    #It then asks the user for an input of how many numbers to skip.
    #It then prints the number

