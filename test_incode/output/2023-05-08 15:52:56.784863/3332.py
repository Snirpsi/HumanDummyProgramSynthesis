#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A program that opens a list of words.
    #The program will then ask the user to enter a word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user input.
    #The program will then ask the user to enter another word.
    #The program will then print out the word that matches the user 

