#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A endless loop that opens a list of numbers.
    #Each time the program is run, it reads a number from the user
    #and prints it to the screen.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #The program then asks the user to input a number.
    #

