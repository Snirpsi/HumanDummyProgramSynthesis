#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A program that converts a list of numbers.
    #The program will ask the user for a list of
    #numbers and then print out the list of
    #numbers in reverse order.
    #The program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number in
    #the list that is the same as the number
    #the user entered.
    
    #This program will ask the user for a number
    #and will then print out the number 

