#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that converts numbers.
    def convert(numbers):
        return sum(numbers)
    #A function that converts numbers to strings.
    def convert(numbers):
        return str(sum(numbers))
    #A function that converts numbers to integers.
    def convert(numbers):
        return sum(map(int, numbers))
    #A function that converts numbers to floats.
    def convert(numbers):
        return sum(map(float, numbers))
    #A function that converts numbers to booleans.
    def convert(numbers):
        return sum(map(bool, numbers))
    #A function that converts numbers to lists.
    def convert(numbers):
        return sum(map(list, numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return sum(map(tuple, numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return sum(map(set, numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return sum(map(dict, numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return sum(map(set, numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return sum(map(tuple, numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return sum(map(dict, numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return sum(map(set, numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return sum(map(tuple, numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return sum(map(dict, numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return sum(map(set, numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return sum(map(tuple, numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return sum(map(dict, numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return sum(map(set, numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return sum(map(tuple, numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return sum(map(dict, numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return sum(map(set, numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return sum(map(tuple, numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return sum(map(dict, numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return sum(map(set, numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return sum(map(tuple, numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return sum(map(dict, numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return sum(map(set, numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return sum(map(tuple, numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return sum(map(dict, numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return sum(map(set, numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return sum(map(tuple, numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return sum(map(dict, numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return sum(map(set, numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return sum(map(tuple, numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return sum(map(dict, numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return sum(map(set, numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return sum(map(tuple, numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return sum(map(dict, numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return sum(map(set, numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return sum(map(tuple, numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return sum(map(dict, numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return sum(map(set, numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return sum(map(tuple, numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return sum(map(dict, numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return sum(map(set, numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return sum(map(tuple, numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return sum(

