#!/usr/bin/python3
if __name__ == '__main__':
    #A program that converts user input.
    #The program will ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John Doe
    #Age: 25
    #
    #The program will then ask the user for their name and age.
    #The program will then print out the name and age of the user
    #in a format that looks like this:
    #Name: John 

