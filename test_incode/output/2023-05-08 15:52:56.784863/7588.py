#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that stores numbers.
    storeNumbers(numbers)
    #A function that prints numbers.
    printNumbers(numbers)
    #A function that prints numbers and stores them in another list.
    storeNumbers2(numbers)

