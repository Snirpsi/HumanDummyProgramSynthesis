#!/usr/bin/python3
url = "http://snirps.ddns.net"
if __name__ == '__main__':
    #A function that enumerates a http request.
    def enumerate_request(request):
        request = request.replace('http://', '')
        request = request.replace('https://', '')
        request = request.replace('http://', '')
        request = request.replace('https://', '')
        return request
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser')
        table = soup.find('table', attrs={'class': 'table table-striped'})
        rows = table.find_all('tr')
        parsed_data = []
        for row in rows:
            parsed_data.append(row.find_all('td')[0].text.strip())
        return parsed_data
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser')
        table = soup.find('table', attrs={'class': 'table table-striped'})
        rows = table.find_all('tr')
        parsed_data = []
        for row in rows:
            parsed_data.append(row.find_all('td')[1].text.strip())
        return parsed_data
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser')
        table = soup.find('table', attrs={'class': 'table table-striped'})
        rows = table.find_all('tr')
        parsed_data = []
        for row in rows:
            parsed_data.append(row.find_all('td')[2].text.strip())
        return parsed_data
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser')
        table = soup.find('table', attrs={'class': 'table table-striped'})
        rows = table.find_all('tr')
        parsed_data = []
        for row in rows:
            parsed_data.append(row.find_all('td')[3].text.strip())
        return parsed_data
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser')
        table = soup.find('table', attrs={'class': 'table table-striped'})
        rows = table.find_all('tr')
        parsed_data = []
        for row in rows:
            parsed_data.append(row.find_all('td')[4].text.strip())
        return parsed_data
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser')
        table = soup.find('table', attrs={'class': 'table table-striped'})
        rows = table.find_all('tr')
        parsed_data = []
        for row in rows:
            parsed_data.append(row.find_all('td')[5].text.strip())
        return parsed_data
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser')
        table = soup.find('table', attrs={'class': 'table table-striped'})
        rows = table.find_all('tr')
        parsed_data = []
        for row in rows:
            parsed_data.append(row.find_all('td')[6].text.strip())
        return parsed_data
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser')
        table = soup.find('table', attrs={'class': 'table table-striped'})
        rows = table.find_all('tr')
        parsed_data = []
        for row in rows:
            parsed_data.append(row.find_all('td')[7].text.strip())
        return parsed_data
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser')
        table = soup.find('table', attrs={'class': 'table table-striped'})
        rows = table.find_all('tr')
        parsed_data = []
        for row in rows:
            parsed_data.append(row.find_all('td')[8].text.strip())
        return parsed_data
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser')
        table = soup.find('table', attrs={'class': 'table table-striped'})
        rows = table.find_all('tr')
        parsed_data = []
        for row in rows:
            parsed_data.append(row.find_all('td')[9].text.strip())
        return parsed_data
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser')
        table = soup.find('table', attrs={'class': 'table table-striped'})
        rows = table.find_all('tr')
        parsed_data = []
        for row in rows:
            parsed_data.append(row.find_all('td')[10].text.strip())
        return parsed_data
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser')
        table = soup.find('table', attrs={'class': 'table table-striped'})
        rows = table.find_all('tr')
        parsed_data = []
        for row in rows:
            parsed_data.append(row.find_all('td')[11].text.strip())
        return parsed_data
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser')
        table = soup.find('table', attrs={'class': 'table table-striped'})
        rows = table.find_all('tr')
        parsed_data = []
        for row in rows:
            parsed_data.append(row.find_all('td')[12].text.strip())
        return parsed_data
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser')
        table = soup.find('table', attrs={'class': 'table table-striped'})
        rows = table.find_all('tr')
        parsed_data = []
        for row in rows:
            parsed_data.append(row.find_all('td')[13].text.strip())
        return parsed_data
    #A function that parses the response and returns the parsed data.
    def parse_response(response):
        soup = BeautifulSoup(response, 'html.parser

