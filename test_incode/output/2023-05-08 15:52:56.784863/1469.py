#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A program that removes numbers.
    #The program should ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the list.
    #The program should then ask the user to enter a number and then remove it.
    #The program should then print out the numbers removed from the 

