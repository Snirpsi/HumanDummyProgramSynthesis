#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that returns a list of numbers.
    def numbers_generator():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator2():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator3():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator4():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator5():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator6():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator7():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator8():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator9():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator10():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator11():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator12():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator13():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator14():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator15():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator16():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator17():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator18():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator19():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator20():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator21():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator22():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator23():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator24():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator25():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator26():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator27():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator28():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator29():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator30():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator31():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator32():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator33():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator34():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator35():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator36():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator37():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 10.
    def numbers_generator38():
        for i in numbers:
            yield i
    #A function that returns a generator that yields numbers from 1 to 

