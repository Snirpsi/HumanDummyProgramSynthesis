#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].upper())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].lower())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].title())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].capitalize())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].title())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].lower())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].title())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].capitalize())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].title())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].lower())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].title())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].lower())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].title())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].lower())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].lower())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].lower())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #It returns the string with the number converted to a word.
    def convert(word,num):
        word = str(word)
        num = num
        word = word.replace(word[num],word[num].lower())
        return word
    #A function that converts words.
    #It takes two arguments: a string and a number.
    #

