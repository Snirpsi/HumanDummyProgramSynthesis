#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A endless loop that opens numbers.txt and prints every number in it
    while True:
        numbers = open('numbers.txt').read().splitlines()
        for number in numbers:
            print(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if number == 'done':
                break
            else:
                numbers.append(number)
        
        #A while loop which waits for user input until a number is entered
        while True:
            number = input('Enter a number: ')
            if 

