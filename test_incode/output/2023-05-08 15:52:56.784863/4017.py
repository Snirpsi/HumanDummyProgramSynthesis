#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that opens a list of numbers.
    numbers_opener = opener(numbers)
    #A function that closes a list of numbers.
    numbers_closer = closer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the contents of a list.
    numbers_printer = printer(numbers)
    #A function that prints the content

