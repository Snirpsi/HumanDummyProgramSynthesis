#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A program that removes numbers.
    #The program will ask the user for numbers.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another number.
    #The program will then remove all numbers from the
    #users input.
    #The program will then print out the result.
    #The program will ask the user for another 

