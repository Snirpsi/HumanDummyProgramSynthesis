#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that calculates a list of numbers.
    #The function takes one argument, a list of numbers.
    #The function returns another list, containing the sum of the numbers in the list.
    #The function also takes one argument, a number.
    #The function adds the number to the list.
    #The function returns the list.
    sumOfNumbers = sum(numbers)
    print(sumOfNumbers)

