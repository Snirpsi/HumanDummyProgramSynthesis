#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A minimal webserver that converts a list of numbers.
    #It will accept GET requests and convert them to numbers.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also print a message to the console when GET requests are received.
    #It will also 

