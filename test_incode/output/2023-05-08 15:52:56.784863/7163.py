#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that returns numbers.
    #It takes no arguments and returns numbers.
    #It takes one argument and that argument is a list
    #of numbers. 
    #It then prints the numbers in the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the length of the list.
    #It then prints the 

