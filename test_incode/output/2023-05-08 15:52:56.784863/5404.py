#!/usr/bin/python3
fruirs = ["apple","banana","orange"]
if __name__ == '__main__':
    #A program that converts fruits.
    #It will take input from the user and print out the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then convert the fruit name to its corresponding number.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit number.
    #The program will ask the user for input until the user enters EOF.
    #The program will then print the fruit name.
    #The program will ask the user for 

