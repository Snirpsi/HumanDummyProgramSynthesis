#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that opens a list of numbers.
    #The function then prints them out in reverse order.
    #The function then closes the list.
    #The function then opens a new list with numbers from 1 to 9.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.
    #The function then prints out the numbers in that new list.
    #The function then closes the new list.


