#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that adds numbers.
    def add(numbers):
        return numbers[0] + numbers[1] + numbers[2]
    #A function that subtracts numbers.
    def subtract(numbers):
        return numbers[0] - numbers[1] - numbers[2]
    #A function that multiplies numbers.
    def multiply(numbers):
        return numbers[0] * numbers[1] * numbers[2]
    #A function that divides numbers.
    def divide(numbers):
        return numbers[0] / numbers[1] / numbers[2]
    #A function that returns the product of the numbers.
    def product(numbers):
        return numbers[0] * numbers[1] * numbers[2]
    #A function that returns the square of the numbers.
    def square(numbers):
        return numbers[0] * numbers[1] * numbers[2]
    #A function that returns the absolute value of the numbers.
    def abs(numbers):
        return abs(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that returns the square root of the numbers.
    def sqrt(numbers):
        return sqrt(numbers[0])
    #A function that 

