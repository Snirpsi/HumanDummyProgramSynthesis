#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A function that stores ports.
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using ports[port].
    ports = ports + [443,444,445,446,447,448,449,450]
    #The ports are stored in a list.
    #The list can be accessed using port

