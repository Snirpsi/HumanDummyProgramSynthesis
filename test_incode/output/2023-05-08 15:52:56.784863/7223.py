#!/usr/bin/python3
if __name__ == '__main__':
    #A function that adds user input.
    #It will print a message to the user and ask the user to input their name.
    #If the user inputs a name that already exists, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that already exists, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a name that doesn't exist, it will print an error message and ask the user to input their name.
    #If the user inputs a name that doesn't exist and they input a 

