#!/usr/bin/python3
fruirs = ["apple","banana","orange"]
if __name__ == '__main__':
    #A function that returns fruits.
    fruits = fruirs()
    #A function that returns fruits sorted by length.
    fruits.sort(key=len)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.
    fruits.sort(key=str.lower)
    #A function that returns fruits sorted by name.


