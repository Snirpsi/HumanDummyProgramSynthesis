#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A program that stores ports.
    #It will listen on all ports and store them in a list.
    #It will also listen on port 80 and store them in a list.
    #It will also listen on port 443 and store them in a list.
    #It will also listen on port 22 and store them in a list.
    #It will also listen on port 3 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 8080 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 80 and store them in a list.
    #It will also listen on port 443 and store them in a list.
    #It will also listen on port 22 and store them in a list.
    #It will also listen on port 3 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 8080 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 80 and store them in a list.
    #It will also listen on port 443 and store them in a list.
    #It will also listen on port 22 and store them in a list.
    #It will also listen on port 3 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 8080 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 80 and store them in a list.
    #It will also listen on port 443 and store them in a list.
    #It will also listen on port 22 and store them in a list.
    #It will also listen on port 3 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 8080 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 80 and store them in a list.
    #It will also listen on port 443 and store them in a list.
    #It will also listen on port 22 and store them in a list.
    #It will also listen on port 3 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 8080 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 80 and store them in a list.
    #It will also listen on port 443 and store them in a list.
    #It will also listen on port 22 and store them in a list.
    #It will also listen on port 3 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 8080 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 80 and store them in a list.
    #It will also listen on port 443 and store them in a list.
    #It will also listen on port 22 and store them in a list.
    #It will also listen on port 3 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 8080 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 80 and store them in a list.
    #It will also listen on port 443 and store them in a list.
    #It will also listen on port 22 and store them in a list.
    #It will also listen on port 3 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 8080 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 80 and store them in a list.
    #It will also listen on port 443 and store them in a list.
    #It will also listen on port 22 and store them in a list.
    #It will also listen on port 3 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 8080 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 80 and store them in a list.
    #It will also listen on port 443 and store them in a list.
    #It will also listen on port 22 and store them in a list.
    #It will also listen on port 3 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 8080 and store them in a list.
    #It will also listen on port 666 and store them in a list.
    #It will also listen on port 80 and store them in a list.
    #It will also listen on 

