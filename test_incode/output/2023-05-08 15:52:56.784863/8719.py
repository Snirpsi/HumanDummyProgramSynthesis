#!/usr/bin/python3
port = 8080
if __name__ == '__main__':
    #A program that stores a port.
    
    #The port is stored as a string
    
    #The port is stored as a int
    
    #The port is stored as a float
    
    #The port is stored as a list
    
    #The port is stored as a tuple
    
    #The port is stored as a dict
    
    #The port is stored as a set
    
    #The port is stored as a frozenset
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozenset
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozenset
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored as a frozendict
    
    #The port is stored 

