#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that adds a list of numbers.
    def addNumbers(numbers):
        return sum(numbers)
    #A function that subtracts a list of numbers.
    def subtractNumbers(numbers):
        return sum(numbers)
    #A function that multiplies a list of numbers.
    def multiplyNumbers(numbers):
        return sum(numbers)
    #A function that divides a list of numbers.
    def divideNumbers(numbers):
        return sum(numbers)
    #A function that prints the sum of the numbers in the list.
    def printSum(numbers):
        print(sum(numbers))
    #A function that prints the difference of the numbers in the list.
    def printDifference(numbers):
        print(sum(numbers)-sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct2(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct3(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct4(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct5(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct6(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct7(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct8(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct9(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct10(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct11(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct12(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct13(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct14(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct15(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct16(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct17(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct18(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct19(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct20(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct21(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct22(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct23(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct24(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct25(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct26(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct27(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct28(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct29(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct30(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct31(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct32(numbers):
        print(sum(numbers)*sum(numbers))
    #A function that prints the product of the numbers in the list.
    def printProduct33(

