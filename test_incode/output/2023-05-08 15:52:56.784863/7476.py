#!/usr/bin/python3
if __name__ == '__main__':
    #A program that converts user input.
    #It will ask the user for an input of their choice.
    #The program will then convert that input to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that number to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that number to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that number to a number.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that number to a number.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that number to a number.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that number to a number.
    #The program will then convert that string to a number.
    #The program will then convert that number to a string.
    #The program will then convert that string to a number.
    #The program will then convert that number 

