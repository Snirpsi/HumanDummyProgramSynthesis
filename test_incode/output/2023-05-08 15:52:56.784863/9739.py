#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that stores a list of numbers.
    storeNumbers(numbers)
    #A function that prints the numbers in the list.
    printNumbers(numbers)
    #A function that prints the numbers in the list with their indexes.
    printNumbers(numbers, 0)
    #A function that prints the numbers in the list with their indexes.
    printNumbers(numbers, 1)
    #A function that prints the numbers in the list with their indexes.
    printNumbers(numbers, 2)
    #A function that prints the numbers in the list with their indexes.
    printNumbers(numbers, 3)
    #A function that prints the numbers in the list with their indexes.
    printNumbers(numbers, 4)
    #A function that prints the numbers in the list with their indexes.
    printNumbers(numbers, 5)
    #A function that prints the numbers in the list with their indexes.
    printNumbers(numbers, 6)
    #A function that prints the numbers in the list with their indexes.
    printNumbers(numbers, 7)
    #A function that prints the numbers in the list with their indexes.
    printNumbers(numbers, 8)
    #A function that prints the numbers in the list with their indexes.
    printNumbers(numbers, 9)

