#!/usr/bin/python3
fruirs = ["apple","banana","orange"]
if __name__ == '__main__':
    #A endless loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print(fruit)
            
    #A for loop that iterates over fruits.
    for fruit in fruirs:
        print(fruit)
        
    #A while loop that iterates over fruits.
    while True:
        fruit = input("Enter a fruit: ")
        if fruit == "quit":
            break
        else:
            print

