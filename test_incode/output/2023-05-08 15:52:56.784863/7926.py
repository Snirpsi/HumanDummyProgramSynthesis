#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A minimal webserver that adds a list of words.
    #The server will respond with a list of words and a status code.
    #The status code will be 200 OK if the list is OK.
    #The status code will be 404 if the list is NOT OK.
    #The status code will be 500 Internal Server Error if the list is NOT OK.
    #The status code will be 503 Service Unavailable if the list is NOT OK.
    
    #The server will respond with a list of words and a status code.
    #The status code will be 200 OK if the list is OK.
    #The status code will be 404 if the list is NOT OK.
    #The status code will be 500 Internal Server Error if the list is NOT OK.
    #The status code will be 503 Service Unavailable if the list is NOT OK.
    
    #The server will respond with a list of words and a status code.
    #The status code will be 200 OK if the list is OK.
    #The status code will be 404 if the list is NOT OK.
    #The status code will be 500 Internal Server Error if the list is NOT OK.
    #The status code will be 503 Service Unavailable if the list is NOT OK.
    
    #The server will respond with a list of words and a status code.
    #The status code will be 200 OK if the list is OK.
    #The status code will be 404 if the list is NOT OK.
    #The status code will be 500 Internal Server Error if the list is NOT OK.
    #The status code will be 503 Service Unavailable if the list is NOT OK.
    
    #The server will respond with a list of words and a status code.
    #The status code will be 200 OK if the list is OK.
    #The status code will be 404 if the list is NOT OK.
    #The status code will be 500 Internal Server Error if the list is NOT OK.
    #The status code will be 503 Service Unavailable if the list is NOT OK.
    
    #The server will respond with a list of words and a status code.
    #The status code will be 200 OK if the list is OK.
    #The status code will be 404 if the list is NOT OK.
    #The status code will be 500 Internal Server Error if the list is NOT OK.
    #The status code will be 503 Service Unavailable if the list is NOT OK.
    
    #The server will respond with a list of words and a status code.
    #The status code will be 200 OK if the list is OK.
    #The status code will be 404 if the list is NOT OK.
    #The status code will be 500 Internal Server Error if the list is NOT OK.
    #The status code will be 503 Service Unavailable if the list is NOT OK.
    
    #The server will respond with a list of words and a status code.
    #The status code will be 200 OK if the list is OK.
    #The status code will be 404 if the list is NOT OK.
    #The status code will be 500 Internal Server Error if the list is NOT OK.
    #The status code will be 503 Service Unavailable if the list is NOT OK.
    
    #The server will respond with a list of words and a status code.
    #The status code will be 200 OK if the list is OK.
    #The status code will be 404 if the list is NOT OK.
    #The status code will be 500 Internal Server Error if the list is NOT OK.
    #The status code will be 503 Service Unavailable if the list is NOT OK.
    
    #The server will respond with a list of words and a status code.
    #The status code will be 200 OK if the list is OK.
    #The status code will be 404 if the list is NOT OK.
    #The status code will be 500 Internal Server Error if the list is NOT OK.
    #The status code will be 503 Service Unavailable if the list is NOT OK.
    
    #The server will respond with a list of words and a status code.
    #The status code will be 200 OK if the list is OK.
    #The status code will be 404 if the list is NOT OK.
    #The status code will be 500 Internal Server Error if the list is NOT OK.
    #The status code will be 503 Service Unavailable if the list is NOT OK.
    
    #The server will respond with a list of words and a status code.
    #The status code will be 200 OK if the list is OK.
    #The status code will be 404 if the list is NOT OK.
    #The status code will be 500 Internal Server Error if the list is NOT OK.
    #The status code will be 503 Service Unavailable if the list is NOT OK.
    
    #The server will respond with a list of words and a status code.
    #The status code will be 200 OK if the list is OK.
    #The status code will be 404 if the list is NOT OK.
    #The status code will be 500 Internal Server Error if the list is NOT OK.
    #The status code will be 503 Service Unavailable if the list is NOT OK.
    
    #The server will respond with a list of words and a status code.
    #The status code will be 200 OK if the list is OK.
    #The status code will be 404 if the list is NOT OK.
    #The status code will be 500 Internal Server Error if the list is NOT OK.
    #The status code will be 503 Service Unavailable if the list is NOT OK.
    
    #

