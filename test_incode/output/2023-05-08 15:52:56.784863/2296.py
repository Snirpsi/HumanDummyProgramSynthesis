#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that adds words.
    def add(word):
        return word + word
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #A function that prints all the words in a list.
    def print_list(words):
        for word in words:
            print(word)
    #

