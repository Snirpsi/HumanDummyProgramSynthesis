#!/usr/bin/python3
fruirs = ["apple","banana","orange"]
if __name__ == '__main__':
    #A program that stores fruits.
    #It will ask the user to input a fruit name.
    #It will then display the fruit name.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.
    #It will ask the user to input a number.
    #It will then display the number.

