#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that opens a list of words.
    wordlist = open("wordlist.txt","r")
    #A function that reads words from the file.
    words = wordlist.read().split()
    #A function that prints out the words in the list.
    print(words)
    #A function that prints out the words in the list with spaces between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore between each word and an underscore between each word.
    print(" ".join(words))
    #A function that prints out the words in the list with spaces between each word and an underscore 

