#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A function that converts ports.
    ports = ports_converter(ports)
    
    #A function that converts ports to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ports.
    ports = ports_converter(ips)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    
    #A function that converts ips to ips.
    ips = ports_ips(ports)
    

