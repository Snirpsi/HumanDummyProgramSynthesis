#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A minimal webserver that calculates a list of words.
    #The server will accept GET requests and will respond with a list of words.
    #The server will also accept POST requests and will respond with a list of words.
    #The server will also accept PUT requests and will respond with a list of words.
    #The server will also accept DELETE requests and will respond with a list of words.
    #The server will also accept OPTIONS requests and will respond with a list of words.
    #The server will also accept TRACE requests and will respond with a list of words.
    #The server will also accept CONNECT (aka CONNECT-REQUEST) requests and will respond with a list of words.
    #The server will also accept CONNECT requests and will respond with a list of words.
    #The server will also accept PATCH requests and will respond with a list of words.
    #The server will also accept HEAD requests and will respond with a list of words.
    #The server will also accept OPTIONS requests and will respond with a list of words.
    #The server will also accept TRACE requests and will respond with a list of words.
    #The server will also accept CONNECT (aka CONNECT-REQUEST) requests and will respond with a list of words.
    #The server will also accept CONNECT requests and will respond with a list of words.
    #The server will also accept PATCH requests and will respond with a list of words.
    #The server will also accept HEAD requests and will respond with a list of words.
    #The server will also accept OPTIONS requests and will respond with a list of words.
    #The server will also accept TRACE requests and will respond with a list of words.
    #The server will also accept CONNECT (aka CONNECT-REQUEST) requests and will respond with a list of words.
    #The server will also accept CONNECT requests and will respond with a list of words.
    #The server will also accept PATCH requests and will respond with a list of words.
    #The server will also accept HEAD requests and will respond with a list of words.
    #The server will also accept OPTIONS requests and will respond with a list of words.
    #The server will also accept TRACE requests and will respond with a list of words.
    #The server will also accept CONNECT (aka CONNECT-REQUEST) requests and will respond with a list of words.
    #The server will also accept CONNECT requests and will respond with a list of words.
    #The server will also accept PATCH requests and will respond with a list of words.
    #The server will also accept HEAD requests and will respond with a list of words.
    #The server will also accept OPTIONS requests and will respond with a list of words.
    #The server will also accept TRACE requests and will respond with a list of words.
    #The server will also accept CONNECT (aka CONNECT-REQUEST) requests and will respond with a list of words.
    #The server will also accept CONNECT requests and will respond with a list of words.
    #The server will also accept PATCH requests and will respond with a list of words.
    #The server will also accept HEAD requests and will respond with a list of words.
    #The server will also accept OPTIONS requests and will respond with a list of words.
    #The server will also accept TRACE requests and will respond with a list of words.
    #The server will also accept CONNECT (aka CONNECT-REQUEST) requests and will respond with a list of words.
    #The server will also accept CONNECT requests and will respond with a list of words.
    #The server will also accept PATCH requests and will respond with a list of words.
    #The server will also accept HEAD requests and will respond with a list of words.
    #The server will also accept OPTIONS requests and will respond with a list of words.
    #The server will also accept TRACE requests and will respond with a list of words.
    #The server will also accept CONNECT (aka CONNECT-REQUEST) requests and will respond with a list of words.
    #The server will also accept CONNECT requests and will respond with a list of words.
    #The server will also accept PATCH requests and will respond with a list of words.
    #The server will also accept HEAD requests and will respond with a list of words.
    #The server will also accept OPTIONS requests and will respond with a list of words.
    #The server will also accept TRACE requests and will respond with a list of words.
    #The server will also accept CONNECT (aka CONNECT-REQUEST) requests and will respond with a list of words.
    #The server will also accept CONNECT requests and will respond with a list of words.
    #The server will also accept PATCH requests and will respond with a list of words.
    #The server will also accept HEAD requests and will respond with a list of words.
    #The server will also accept OPTIONS requests and will respond with a list of words.
    #The server will also accept TRACE requests and will respond with a list of words.
    #The server will also accept CONNECT (aka CONNECT-REQUEST) requests and will respond with a list of words.
    #The server will also accept CONNECT requests and will respond with a list of words.
    #The server will also accept PATCH requests and will respond with a list of words.
    #The server will also accept HEAD requests and will respond with a list of words.
    #The server will also accept OPTIONS requests and will respond with a list of words.
    #The server 

