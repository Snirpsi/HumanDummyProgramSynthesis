#!/usr/bin/python3
if __name__ == '__main__':
    #A program that opens user input.txt and outputs user input to a text file.
    #The program will ask the user for their name and store it in a variable.
    #The program will then ask the user for their age and store it in a variable.
    #The program will then ask the user for their gender and store it in a variable.
    #The program will then ask the user for their height and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their height and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their height and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store it in a variable.
    #The program will then ask the user for their weight and store 

