#!/usr/bin/python3
fruirs = ["apple","banana","orange"]
if __name__ == '__main__':
    #A program that stores fruits.
    #It will ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then ask the user for their name and store it in the fruits list.
    #The program will then 

