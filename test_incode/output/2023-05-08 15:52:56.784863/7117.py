#!/usr/bin/python3
fruirs = ["apple","banana","orange"]
if __name__ == '__main__':
    #A function that stores fruits.
    def storeFruits(fruits):
        #Store the fruits in the global variable fruits
        global fruits
        fruits = fruits
    #A function that prints the fruits stored in the global variable fruits
    def printFruits():
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruits):
        #Print the fruits stored in the global variable fruits
        print(fruits)
    #A function that prints the fruits stored in the global variable fruits
    def printFruits(fruit

