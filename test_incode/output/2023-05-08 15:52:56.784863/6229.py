#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that removes all ports.
    def cleanPorts():
        for port in ports:
            sock.close()
            sock = None
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort2():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort3():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort4():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort5():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort6():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort7():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort8():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort9():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort10():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort11():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort12():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort13():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort14():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort15():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort16():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort17():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort18():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(('0.0.0.0', port))
        port = sock.getsockname()[1]
        sock.close()
    
    #A function that opens up a new socket and binds it to the port given.
    def openPort19():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)

