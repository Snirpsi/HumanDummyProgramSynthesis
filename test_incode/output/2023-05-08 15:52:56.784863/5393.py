#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that stores words.
    words = []
    
    #A function that prints all the words in the list.
    def print_words():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words2():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words3():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words4():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words5():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words6():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words7():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words8():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words9():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words10():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words11():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words12():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words13():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words14():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words15():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words16():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words17():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words18():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words19():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words20():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words21():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words22():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words23():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words24():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words25():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words26():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words27():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words28():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words29():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words30():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words31():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words32():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words33():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words34():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words35():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words36():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words37():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words38():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words39():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words40():
        for word in words:
            print(word)
    
    #A function that prints all the words in the list.
    def print_words41():
        for 

