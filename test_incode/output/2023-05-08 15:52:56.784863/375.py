#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A endless loop that calculates a list of numbers.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        
    #A for loop that prints numbers until the user enters 0.
    for number in numbers:
        print(number)
        
    #A while loop that prints numbers until the user enters 0.
    while True:
        number = input("Enter a number: ")
        if number == '0':
            break
        print(number)
        


