#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that requests all ports.
    #It will block until a port is requested.
    ports = range(0,65535)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    #It will block until a port is requested.
    def print_ports():
        for port in port

