#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that converts numbers.
    def convert(numbers):
        return list(map(lambda x:x*2, numbers))
    #A function that converts numbers to strings.
    def convert(numbers):
        return list(map(lambda x:str(x), numbers))
    #A function that converts numbers to lists.
    def convert(numbers):
        return list(map(lambda x:list(x), numbers))
    #A function that converts numbers to tuples.
    def convert(numbers):
        return list(map(lambda x:tuple(x), numbers))
    #A function that converts numbers to dictionaries.
    def convert(numbers):
        return list(map(lambda x:dict(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:frozenset(x), numbers))
    #A function that converts numbers to sets.
    def convert(numbers):
        return list(map(lambda x:set(x), number

