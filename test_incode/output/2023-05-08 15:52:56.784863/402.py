#!/usr/bin/python3
if __name__ == '__main__':
    #A function that converts user input.
    #It will ask the user to input a number.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a number.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a number.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert that number to a string.
    #It will then ask the user to input a string.
    #It will then convert that string to a number.
    #It will then ask the user to input a string.
    #It will then convert 

