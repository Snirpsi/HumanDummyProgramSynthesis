#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that opens a list of numbers.
    #The function then prints the numbers in the list.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #The function then asks the user for an input.
    #The function then prints the numbers that match the input.
    #

