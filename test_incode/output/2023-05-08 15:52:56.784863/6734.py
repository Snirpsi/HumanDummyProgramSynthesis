#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A function that converts ports.
    #The ports are converted to a string.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.
    #The list is converted to a string.
    #The string is converted to a list.
    #The string is converted to a list.


