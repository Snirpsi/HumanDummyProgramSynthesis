#!/usr/bin/python3
port = 8080
if __name__ == '__main__':
    #A function that opens a port.
    server_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    server_socket.bind(('0.0.0.0', port))
    server_socket.listen(1)
    
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n<h1>Hello World!</h1>')
        connection.close()
        
    #A function that accepts a connection and sends it to the client.
    while True:
        connection, client_address = server_socket.accept()
        connection.sendall(b'HTTP/1.1 200 OK\r\nServer: Python\r\nContent-Type: text/html\r\n\r\n

