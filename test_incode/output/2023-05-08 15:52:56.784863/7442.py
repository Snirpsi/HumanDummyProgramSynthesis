#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A program that stores words.
    #The program will ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.
    #The program will then ask the user for their name and store it in a list.


