#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that opens words.txt and reads words from it
    wordlist = open("words.txt").read().split()
    #A function that prints all the words in wordlist
    for word in wordlist:
        print(word)
    #A function that prints all the words in wordlist except for hello
    for word in wordlist:
        if word != "hello":
            print(word)
    #A function that prints all the words in wordlist except for world
    for word in wordlist:
        if word != "world":
            print(word)
    #A function that prints all the words in wordlist except for !
    for word in wordlist:
        if word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world":
            print(word)
    #A function that prints all the words in wordlist except for ! and hello
    for word in wordlist:
        if word != "!" and word != "hello":
            print(word)
    #A function that prints all the words in wordlist except for ! and world
    for word in wordlist:
        if word != "!" and word != "world":
            print(word)
    #A function that prints all the words in wordlist except for ! and !
    for word in wordlist:
        if word != "!" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the words in wordlist except for hello and world
    for word in wordlist:
        if word != "hello" and word != "world" and word != "!":
            print(word)
    #A function that prints all the word

