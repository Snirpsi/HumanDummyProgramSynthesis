#!/usr/bin/python3
url = "http://snirps.ddns.net"
if __name__ == '__main__':
    #A function that iterates over a http request.
    #The request is made using urllib2.urlopen()
    #The response is returned as a string.
    #The response is then parsed using BeautifulSoup
    #The html is then converted to a BeautifulSoup object.
    #The html is then converted to a dictionary using beautifulsoup4
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a json string using json.dumps()
    #The json string is then converted back to a dictionary using json.loads()
    #The dictionary is then converted to a 

