#!/usr/bin/python3
fruirs = ["apple","banana","orange"]
if __name__ == '__main__':
    #A function that returns fruits.
    fruits = fruirs()
    #A function that returns fruits sorted by fruit.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fruit name.
    fruits.sort(key = fruirs.index)
    #A function that returns fruits sorted by fru

