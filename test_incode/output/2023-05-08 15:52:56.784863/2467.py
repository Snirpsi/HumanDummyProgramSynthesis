#!/usr/bin/python3
port = 8080
if __name__ == '__main__':
    #A program that adds a port.
    
    #Create a socket and bind the port to it.
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(("", port))
    
    #Listen for connections.
    sock.listen(1)
    
    #Accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and print out the client address.
    conn, addr = sock.accept()
    print("Connected by", addr)
    
    #Now accept a connection and 

