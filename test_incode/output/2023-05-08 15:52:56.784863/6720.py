#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A endless loop that adds a list of words.
    for word in words:
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):
        word = words.pop()
        print(word)
        
    #A while loop that prints words in reverse order.
    while(True):

