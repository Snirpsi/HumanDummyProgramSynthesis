#!/usr/bin/python3
port = 8080
if __name__ == '__main__':
    #A function that iterates over a port.
    #It will print out each line of the port and exit the program.
    while True:
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send: ')
        print('Sending line: ' + line)
        sock.sendall(bytes(line, 'utf-8'))
        line = input('Enter a line to send

