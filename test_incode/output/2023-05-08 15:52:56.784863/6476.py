#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A function that converts ports.
    ports = ports.split(',')
    ports = [int(p) for p in ports]
    ports = ports[::-1]
    ports = [p for p in ports if p > 0]
    ports = [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = ports + [p for p in ports if p < 65535]
    ports = list(set(ports))
    ports.sort()
    ports = ports[:len(ports)]
    ports = ports + [p for p in ports if p > 65535]
    ports = port

