#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that adds words.
    def add(word):
        return word + word
    #A function that prints all the words in the list.
    def printWords(words):
        for word in words:
            print(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWords(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates2(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates3(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates4(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates5(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates6(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates7(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates8(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates9(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates10(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates11(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates12(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates13(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates14(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates15(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates16(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates17(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates18(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates19(words):
        seen = set()
        for word in words:
            if word not in seen:
                print(word)
                seen.add(word)
    #A function that prints all the words in the list without duplicates.
    def printUniqueWordsWithoutDuplicates20

