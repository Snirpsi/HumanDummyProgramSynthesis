#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A function that adds ports.
    ports = add_ports(ports)
    #A function that removes ports.
    ports = remove_ports(ports)
    #A function that prints the ports.
    ports = print_ports(ports)
    #A function that prints the ports.
    ports = print_ports(ports)

