#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that opens numbers.txt and reads numbers from it.
    #It then prints them out to the screen.
    numbers = open(numbers.txt).read().splitlines()
    print(numbers)
    #A function that opens numbers.txt and reads numbers from it.
    #It then prints them out to the screen.
    numbers = open(numbers.txt).read().splitlines()
    print(numbers)

