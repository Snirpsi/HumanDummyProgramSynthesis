#!/usr/bin/python3
if __name__ == '__main__':
    #A program that converts user input.
    #It will take user input from the command line.
    #It will then convert that input into an integer and print it.
    #It will then convert that integer into a string and print it.
    #It will then convert that string into a float and print it.
    #It will then convert that float into a string and print it.
    #It will then convert that string into a list and print it.
    #It will then convert that list into a tuple and print it.
    #It will then convert that tuple into a list and print it.
    #It will then convert that list into a dictionary and print it.
    #It will then convert that dictionary into a list and print it.
    #It will then convert that list into a set and print it.
    #It will then convert that set into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print it.
    #It will then convert that list into a list and print 

