#!/usr/bin/python3
url = "http://snirps.ddns.net"
if __name__ == '__main__':
    #A function that returns a http request.
    def request():
        return requests.get(url)
    #A function that returns a http response.
    def response():
        return requests.get(url)
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def headers():
        return requests.get(url).headers
    #A function that returns the content of a web page.
    def content():
        return requests.get(url).content
    #A function that returns the headers of a web page.
    def 

