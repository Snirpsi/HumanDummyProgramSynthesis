#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that enumerates all ports.
    ports = range(0,65535)
    
    #A function that prints the ports.
    def print_ports(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_2(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_3(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_4(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_5(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_6(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_7(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_8(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_9(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_10(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_11(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_12(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_13(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_14(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_15(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_16(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_17(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_18(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_19(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_20(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_21(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_22(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_23(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_24(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_25(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_26(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_27(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_28(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_29(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_30(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_31(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_32(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_33(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_34(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_35(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_36(ports):
        for port in ports:
            print(port)
    
    #A function that prints the ports.
    def print_ports_37

