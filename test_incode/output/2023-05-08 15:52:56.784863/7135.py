#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that removes a list of numbers.
    #The function should return a new list without those numbers.
    #The function should return None if the list was empty.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only numbers.
    #The function should return an empty list if the list contained only letters.
    #The function should return an empty list if the list contained only spaces.
    #The function should return an empty list if the list contained only punctuation.
    #The function should return an empty list if the list contained only 

