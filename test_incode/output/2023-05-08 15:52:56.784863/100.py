#!/usr/bin/python3
if __name__ == '__main__':
    #A minimal webserver that requests user input.
    #The user can then request a URL from the browser to send data to.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back the requested data back to the browser.
    #The server will then send back

