#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that converts a list of numbers.
    #It will take a list of numbers and convert them into 
    #a string.
    #The function will return a string with the 
    #numbers separated by commas.
    #The function will also take a list of numbers and 
    #convert them into a list.
    #The function will return a list with the 
    #numbers separated by commas.
    numbers = numbers_converter(numbers)
    print(numbers)

