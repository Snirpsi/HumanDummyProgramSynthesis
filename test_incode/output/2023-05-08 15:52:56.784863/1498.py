#!/usr/bin/python3
fruirs = ["apple","banana","orange"]
if __name__ == '__main__':
    #A program that stores fruits.
    #It will ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the quantity of the fruit they want to store
    #and it will store it in the fruits list.
    #The program will then ask the user to input the name of the fruit they want to store
    #

