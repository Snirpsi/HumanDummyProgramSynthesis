#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that returns a list of words.
    #The function takes in a list of words and returns a list of words.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function also takes in a list as a parameter.
    #The function then prints out the words in the list one by one.
    #The function 

