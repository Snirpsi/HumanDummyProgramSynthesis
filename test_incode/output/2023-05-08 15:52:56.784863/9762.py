#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that adds words.
    #The function takes one parameter, the word list.
    #The function returns another parameter, the word list.
    #The function adds the word "hello" to the word list.
    #The function adds the word "world" to the word list.
    #The function adds the word "!" to the word list.
    #The function returns the word list.
    words = add_words(words)
    print(words)

