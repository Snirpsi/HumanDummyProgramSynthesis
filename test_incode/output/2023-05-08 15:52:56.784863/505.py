#!/usr/bin/python3
if __name__ == '__main__':
    #A function that converts user input.
    #The user input is converted into a list.
    #The list is then converted into a dictionary.
    #The dictionary is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a string.
    #The list is then converted into a string.
    #The string is then converted into a list.
    #The string is then converted into a 

