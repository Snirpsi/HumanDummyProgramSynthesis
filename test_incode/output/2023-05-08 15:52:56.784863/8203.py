#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A endless loop that adds a list of numbers.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.
    #The program will then print out the list.
    #The program will then ask the user to input a number.
    #The program will then add that number to the list.

