#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that enumerates a list of numbers.
    #The function returns two values: the index and the value.
    #The function uses list comprehension to iterate over each number in the list.
    #The function then prints the index and the value.
    
    #The function uses list comprehension to iterate over each number in the list.
    #The function then prints the index and the value.
    
    #The function uses list comprehension to iterate over each number in the list.
    #The function then prints the index and the value.
    
    #The function uses list comprehension to iterate over each number in the list.
    #The function then prints the index and the value.

