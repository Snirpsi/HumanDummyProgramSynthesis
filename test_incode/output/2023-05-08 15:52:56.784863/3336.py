#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that converts a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(numbers):
        numbers = list(map(int, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of numbers to a list of strings.
    def convert(numbers):
        numbers = list(map(str, numbers))
        numbers.sort()
        return numbers
    #A function that converts a list of strings to a list of numbers.
    def convert(number

