#!/usr/bin/python3
if __name__ == '__main__':
    #A minimal webserver that calculates user input.
    #It receives user input from the user and sends it to the server.
    #The server then calculates the user input and returns it.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the user.
    #The server then sends the result back to the 

