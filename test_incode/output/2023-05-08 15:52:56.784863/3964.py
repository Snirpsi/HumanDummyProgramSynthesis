#!/usr/bin/python3
url = "http://snirps.ddns.net"
if __name__ == '__main__':
    #A function that calculates a http request.
    def http_request(url):
        try:
            response = requests.get(url)
            return response.text
        except:
            return "No response from server"
    #A function that calculates the time it takes to download a file.
    def download_file(url, file_name):
        start = time.time()
        response = requests.get(url)
        end = time.time()
        duration = end - start
        print("Downloading file from {} to {}".format(url, file_name))
        with open(file_name, "wb") as file:
            file.write(response.content)
        print("File downloaded to {}".format(file_name))
        print("Total time taken to download {} is {} seconds".format(file_name, duration))
    #A function that calculates the time it takes to download a file.
    def download_file(url, file_name):
        start = time.time()
        response = requests.get(url)
        end = time.time()
        duration = end - start
        print("Downloading file from {} to {}".format(url, file_name))
        with open(file_name, "wb") as file:
            file.write(response.content)
        print("File downloaded to {}".format(file_name))
        print("Total time taken to download {} is {} seconds".format(file_name, duration))
    #A function that calculates the time it takes to download a file.
    def download_file(url, file_name):
        start = time.time()
        response = requests.get(url)
        end = time.time()
        duration = end - start
        print("Downloading file from {} to {}".format(url, file_name))
        with open(file_name, "wb") as file:
            file.write(response.content)
        print("File downloaded to {}".format(file_name))
        print("Total time taken to download {} is {} seconds".format(file_name, duration))
    #A function that calculates the time it takes to download a file.
    def download_file(url, file_name):
        start = time.time()
        response = requests.get(url)
        end = time.time()
        duration = end - start
        print("Downloading file from {} to {}".format(url, file_name))
        with open(file_name, "wb") as file:
            file.write(response.content)
        print("File downloaded to {}".format(file_name))
        print("Total time taken to download {} is {} seconds".format(file_name, duration))
    #A function that calculates the time it takes to download a file.
    def download_file(url, file_name):
        start = time.time()
        response = requests.get(url)
        end = time.time()
        duration = end - start
        print("Downloading file from {} to {}".format(url, file_name))
        with open(file_name, "wb") as file:
            file.write(response.content)
        print("File downloaded to {}".format(file_name))
        print("Total time taken to download {} is {} seconds".format(file_name, duration))
    #A function that calculates the time it takes to download a file.
    def download_file(url, file_name):
        start = time.time()
        response = requests.get(url)
        end = time.time()
        duration = end - start
        print("Downloading file from {} to {}".format(url, file_name))
        with open(file_name, "wb") as file:
            file.write(response.content)
        print("File downloaded to {}".format(file_name))
        print("Total time taken to download {} is {} seconds".format(file_name, duration))
    #A function that calculates the time it takes to download a file.
    def download_file(url, file_name):
        start = time.time()
        response = requests.get(url)
        end = time.time()
        duration = end - start
        print("Downloading file from {} to {}".format(url, file_name))
        with open(file_name, "wb") as file:
            file.write(response.content)
        print("File downloaded to {}".format(file_name))
        print("Total time taken to download {} is {} seconds".format(file_name, duration))
    #A function that calculates the time it takes to download a file.
    def download_file(url, file_name):
        start = time.time()
        response = requests.get(url)
        end = time.time()
        duration = end - start
        print("Downloading file from {} to {}".format(url, file_name))
        with open(file_name, "wb") as file:
            file.write(response.content)
        print("File downloaded to {}".format(file_name))
        print("Total time taken to download {} is {} seconds".format(file_name, duration))
    #A function that calculates the time it takes to download a file.
    def download_file(url, file_name):
        start = time.time()
        response = requests.get(url)
        end = time.time()
        duration = end - start
        print("Downloading file from {} to {}".format(url, file_name))
        with open(file_name, "wb") as file:
            file.write(response.content)
        print("File downloaded to {}".format(file_name))
        print("Total time taken to download {} is {} seconds".format(file_name, duration))
    #A function that calculates the time it takes to download a file.
    def download_file(url, file_name):
        start = time.time()
        response = requests.get(url)
        end = time.time()
        duration = end - start
        print("Downloading file from {} to {}".format(url, file_name))
        with open(file_name, "wb") as file:
            file.write(response.content)
        print("File downloaded to {}".format(file_name))
        print("Total time taken to download {} is {} seconds".format(file_name, duration))
    #A function that calculates the time it takes to download a file.
    def download_file(url, file_name):
        start = time.time()
        response = requests.get(url)
        end = time.time()
        duration = end - start
        print("Downloading file from {} to {}".format(url, file_name))
        with open(file_name, "wb") as file:
            file.write(response.content)
        print("File downloaded to {}".format(file_name))
        print("Total time taken to download {} is {} seconds".format(file_name, duration))
    #A function that calculates the time it takes to download a file.
    def download_file(url, file_name):
        start = time.time()
        response = requests.get(url)
        end = time.time()
        duration = end - start
        print("Downloading file from {} to {}".format(url, file_name))
        with open(file_name, "wb") as file:
            file.write(response.content)
        print("File downloaded to {}".format(file_name))
        print("Total time taken to download {} is {} seconds".format(file_name, duration))
    #A function that 

