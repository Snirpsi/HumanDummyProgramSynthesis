#!/usr/bin/python3
if __name__ == '__main__':
    #A function that converts user input.
    #It takes two arguments: a string and an integer as input.
    #It returns the integer converted to a string.
    #It also takes one argument: the name of the file to convert.
    convert(input("Enter a string: "))
    convert(input("Enter an integer: "))

