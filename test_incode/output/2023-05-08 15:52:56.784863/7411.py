#!/usr/bin/python3
if __name__ == '__main__':
    #A program that converts user input.
    #It will ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a number and then convert it to a number.
    #The program will then print out the number the user entered.
    #The program will then ask the user to enter a 

