#!/usr/bin/python3
if __name__ == '__main__':
    #A minimal webserver that adds user input.
    #It also prints out the user input.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML format.
    #It also prints out the user input in CSV format.
    #It also prints out the user input in JSON format.
    #It also prints out the user input in HTML format.
    #It also prints out the user input in XML 

