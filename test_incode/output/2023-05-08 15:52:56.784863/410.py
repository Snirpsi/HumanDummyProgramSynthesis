#!/usr/bin/python3
port = 8080
if __name__ == '__main__':
    #A function that enumerates a port.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available ports.
    ports = enumerate()
    for port in ports:
        print(port)
    #A function that prints out all available ports.
    #It will print out all available port

