#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that enumerates numbers.
    def enumerate_numbers(numbers):
        for number in numbers:
            yield number
    #A function that prints all the numbers from 1 to 10.
    def print_numbers(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again2(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again3(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again4(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again5(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again6(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again7(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again8(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again9(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again10(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again11(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again12(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again13(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again14(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again15(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again16(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again17(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again18(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again19(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again20(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again21(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again22(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again23(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again24(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again25(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again26(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints them again.
    def print_numbers_again27(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers from 1 to 10 and then prints 

