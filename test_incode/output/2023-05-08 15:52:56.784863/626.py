#!/usr/bin/python3
fruirs = ["apple","banana","orange"]
if __name__ == '__main__':
    #A minimal webserver that calculates fruits.
    #It serves up static files in the directory /fruits.
    #It also serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #It serves up static files in the directory /static
    #

