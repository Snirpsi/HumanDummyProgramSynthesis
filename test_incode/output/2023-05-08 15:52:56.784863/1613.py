#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that adds a list of numbers.
    def add(numbers):
        return sum(numbers)
    #A function that multiplies a list of numbers.
    def multiply(numbers):
        return numbers * 2
    #A function that divides a list of numbers.
    def divide(numbers):
        return numbers / 2
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #A function that returns the sum of a list of numbers.
    def sum(numbers):
        return reduce(lambda x, y: x + y, numbers)
    #A function that returns the difference of a list of numbers.
    def difference(numbers):
        return sum(numbers) - numbers[0]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #A function that returns the sum of a list of numbers.
    def sum(numbers):
        return reduce(lambda x, y: x + y, numbers)
    #A function that returns the difference of a list of numbers.
    def difference(numbers):
        return sum(numbers) - numbers[0]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #A function that returns the sum of a list of numbers.
    def sum(numbers):
        return reduce(lambda x, y: x + y, numbers)
    #A function that returns the difference of a list of numbers.
    def difference(numbers):
        return sum(numbers) - numbers[0]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #A function that returns the sum of a list of numbers.
    def sum(numbers):
        return reduce(lambda x, y: x + y, numbers)
    #A function that returns the difference of a list of numbers.
    def difference(numbers):
        return sum(numbers) - numbers[0]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #A function that returns the sum of a list of numbers.
    def sum(numbers):
        return reduce(lambda x, y: x + y, numbers)
    #A function that returns the difference of a list of numbers.
    def difference(numbers):
        return sum(numbers) - numbers[0]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #A function that returns the sum of a list of numbers.
    def sum(numbers):
        return reduce(lambda x, y: x + y, numbers)
    #A function that returns the difference of a list of numbers.
    def difference(numbers):
        return sum(numbers) - numbers[0]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #A function that returns the sum of a list of numbers.
    def sum(numbers):
        return reduce(lambda x, y: x + y, numbers)
    #A function that returns the difference of a list of numbers.
    def difference(numbers):
        return sum(numbers) - numbers[0]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #A function that returns the sum of a list of numbers.
    def sum(numbers):
        return reduce(lambda x, y: x + y, numbers)
    #A function that returns the difference of a list of numbers.
    def difference(numbers):
        return sum(numbers) - numbers[0]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #A function that returns the sum of a list of numbers.
    def sum(numbers):
        return reduce(lambda x, y: x + y, numbers)
    #A function that returns the difference of a list of numbers.
    def difference(numbers):
        return sum(numbers) - numbers[0]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #A function that returns the sum of a list of numbers.
    def sum(numbers):
        return reduce(lambda x, y: x + y, numbers)
    #A function that returns the difference of a list of numbers.
    def difference(numbers):
        return sum(numbers) - numbers[0]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #A function that returns the sum of a list of numbers.
    def sum(numbers):
        return reduce(lambda x, y: x + y, numbers)
    #A function that returns the difference of a list of numbers.
    def difference(numbers):
        return sum(numbers) - numbers[0]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #A function that returns the sum of a list of numbers.
    def sum(numbers):
        return reduce(lambda x, y: x + y, numbers)
    #A function that returns the difference of a list of numbers.
    def difference(numbers):
        return sum(numbers) - numbers[0]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #A function that returns the sum of a list of numbers.
    def sum(numbers):
        return reduce(lambda x, y: x + y, numbers)
    #A function that returns the difference of a list of numbers.
    def difference(numbers):
        return sum(numbers) - numbers[0]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #A function that returns the sum of a list of numbers.
    def sum(numbers):
        return reduce(lambda x, y: x + y, numbers)
    #A function that returns the difference of a list of numbers.
    def difference(numbers):
        return sum(numbers) - numbers[0]
    #A function that returns the product of a list of numbers.
    def product(numbers):
        return reduce(lambda x, y: x * y, numbers)
    #

