#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A endless loop that opens numbers.txt and reads line by line
    for line in open('numbers.txt'):
        print(line)
        #A while loop that reads line by line and prints line to screen
        while line:
            print(line)
            #A for loop that reads line by line and prints line to screen
            for number in numbers:
                print(number)
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press enter to exit')
            #A while loop that reads line by line and prints line to screen
            line = input('Press ent

