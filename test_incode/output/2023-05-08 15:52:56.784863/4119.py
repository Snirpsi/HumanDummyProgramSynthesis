#!/usr/bin/python3
port = 8080
if __name__ == '__main__':
    #A endless loop that converts a port.
    while True:
        port = port + 1
        
        #The port is now listening
        print("Listening on port " + str(port))
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being closed
        print("Port " + str(port) + " is now being closed")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port " + str(port) + " is now being used")
        
        #The port is now being used
        print("Port 

