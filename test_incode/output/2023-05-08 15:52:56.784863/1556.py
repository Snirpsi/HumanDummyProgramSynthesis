#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A program that converts ports.
    #It will convert ports 80 to 443 and port 8080 to 666
    #It will also convert ports 443 to 22
    #It will also convert ports 22 to 3
    #It will also convert ports 3 to 0
    #It will also convert ports 0 to 65535
    #It will also convert ports 65535 to 65536
    #It will also convert ports 65536 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 0 to 0
    #It will also convert ports 

