#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A program that stores a list of words.
    #The program will ask the user for input and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the screen.
    #The program will ask the user for input again and then store it in a list.
    #The program will then print out the list to the 

