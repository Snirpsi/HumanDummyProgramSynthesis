#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that adds words.
    def add(word):
        return word + word
    #A function that prints words.
    def print(word):
        print(word)
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds them together.
    def add(word):
        return word + word
    #A function that prints words and adds 

