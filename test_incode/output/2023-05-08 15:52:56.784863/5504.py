#!/usr/bin/python3
url = "http://snirps.ddns.net"
if __name__ == '__main__':
    #A function that converts a http request.
    #It takes the url as a parameter and returns the response.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is returned as a string.
    #The url is passed in as a parameter.
    #The response is 

