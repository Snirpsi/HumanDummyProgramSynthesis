#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that removes all ports.
    remove_ports()
    #A function that removes ports one by one.
    remove_ports(ports[0])
    #A function that removes ports one by one.
    remove_ports(ports[1])
    #A function that removes ports one by one.
    remove_ports(ports[2])
    #A function that removes ports one by one.
    remove_ports(ports[3])
    #A function that removes ports one by one.
    remove_ports(ports[4])
    #A function that removes ports one by one.
    remove_ports(ports[5])
    #A function that removes ports one by one.
    remove_ports(ports[6])
    #A function that removes ports one by one.
    remove_ports(ports[7])
    #A function that removes ports one by one.
    remove_ports(ports[8])
    #A function that removes ports one by one.
    remove_ports(ports[9])
    #A function that removes ports one by one.
    remove_ports(ports[10])
    #A function that removes ports one by one.
    remove_ports(ports[11])
    #A function that removes ports one by one.
    remove_ports(ports[12])
    #A function that removes ports one by one.
    remove_ports(ports[13])
    #A function that removes ports one by one.
    remove_ports(ports[14])
    #A function that removes ports one by one.
    remove_ports(ports[15])
    #A function that removes ports one by one.
    remove_ports(ports[16])
    #A function that removes ports one by one.
    remove_ports(ports[17])
    #A function that removes ports one by one.
    remove_ports(ports[18])
    #A function that removes ports one by one.
    remove_ports(ports[19])
    #A function that removes ports one by one.
    remove_ports(ports[20])
    #A function that removes ports one by one.
    remove_ports(ports[21])
    #A function that removes ports one by one.
    remove_ports(ports[22])
    #A function that removes ports one by one.
    remove_ports(ports[23])
    #A function that removes ports one by one.
    remove_ports(ports[24])
    #A function that removes ports one by one.
    remove_ports(ports[25])
    #A function that removes ports one by one.
    remove_ports(ports[26])
    #A function that removes ports one by one.
    remove_ports(ports[27])
    #A function that removes ports one by one.
    remove_ports(ports[28])
    #A function that removes ports one by one.
    remove_ports(ports[29])
    #A function that removes ports one by one.
    remove_ports(ports[30])
    #A function that removes ports one by one.
    remove_ports(ports[31])
    #A function that removes ports one by one.
    remove_ports(ports[32])
    #A function that removes ports one by one.
    remove_ports(ports[33])
    #A function that removes ports one by one.
    remove_ports(ports[34])
    #A function that removes ports one by one.
    remove_ports(ports[35])
    #A function that removes ports one by one.
    remove_ports(ports[36])
    #A function that removes ports one by one.
    remove_ports(ports[37])
    #A function that removes ports one by one.
    remove_ports(ports[38])
    #A function that removes ports one by one.
    remove_ports(ports[39])
    #A function that removes ports one by one.
    remove_ports(ports[40])
    #A function that removes ports one by one.
    remove_ports(ports[41])
    #A function that removes ports one by one.
    remove_ports(ports[42])
    #A function that removes ports one by one.
    remove_ports(ports[43])
    #A function that removes ports one by one.
    remove_ports(ports[44])
    #A function that removes ports one by one.
    remove_ports(ports[45])
    #A function that removes ports one by one.
    remove_ports(ports[46])
    #A function that removes ports one by one.
    remove_ports(ports[47])
    #A function that removes ports one by one.
    remove_ports(ports[48])
    #A function that removes ports one by one.
    remove_ports(ports[49])
    #A function that removes ports one by one.
    remove_ports(ports[50])
    #A function that removes ports one by one.


