#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that enumerates words.
    #The enumerate function takes an iterable and an optional start value.
    #The enumerate function returns an iterator that yields tuples of (word, index) 
    #where index is the position of the word in the iterable.
    #The enumerate function also accepts a stop value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a step value that defaults to 1.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a limit value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a start value that defaults to 0.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a count value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a step value that defaults to 1.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a limit value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a start value that defaults to 0.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a count value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a step value that defaults to 1.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a limit value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a start value that defaults to 0.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a count value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a step value that defaults to 1.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a limit value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a start value that defaults to 0.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a count value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a step value that defaults to 1.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a limit value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a start value that defaults to 0.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a count value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a step value that defaults to 1.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a limit value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a start value that defaults to 0.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a count value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a step value that defaults to 1.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a limit value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a start value that defaults to 0.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a count value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a step value that defaults to 1.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a limit value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a start value that defaults to 0.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a count value that defaults to None.
    #The enumerate function returns None when no more words can be enumerated.
    #The enumerate function also accepts a step value that defaults to 1.
    #The enumerate function returns None when no more 

