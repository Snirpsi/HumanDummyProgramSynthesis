#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):
        print(line)
    #A endless loop that opens numbers.txt and prints every line from that file
    for line in open('numbers.txt'):


