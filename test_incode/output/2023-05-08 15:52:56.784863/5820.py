#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A program that converts numbers.
    #It takes as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number in base 10.
    #It will also take as input a number as input
    #and prints the number in base 2.
    #It will also take as input a number as input
    #and prints the number 

