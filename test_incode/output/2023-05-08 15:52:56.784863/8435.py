#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A program that converts numbers.
    #The program will ask the user to input a number.
    #The program will then convert the number into 
    #a binary number in base 2.
    #The program will then print the binary number in 
    #base 10.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a decimal number in base 10.
    #The program will then print the decimal number in 
    #base 10.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a hexadecimal number in base 16.
    #The program will then print the hexadecimal number in 
    #base 16.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a octal number in base 8.
    #The program will then print the octal number in 
    #base 8.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a binary number in base 10.
    #The program will then print the binary number in 
    #base 10.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a decimal number in base 10.
    #The program will then print the decimal number in 
    #base 10.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a hexadecimal number in base 16.
    #The program will then print the hexadecimal number in 
    #base 16.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a octal number in base 8.
    #The program will then print the octal number in 
    #base 8.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a binary number in base 10.
    #The program will then print the binary number in 
    #base 10.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a decimal number in base 10.
    #The program will then print the decimal number in 
    #base 10.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a hexadecimal number in base 16.
    #The program will then print the hexadecimal number in 
    #base 16.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a octal number in base 8.
    #The program will then print the octal number in 
    #base 8.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a binary number in base 10.
    #The program will then print the binary number in 
    #base 10.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a decimal number in base 10.
    #The program will then print the decimal number in 
    #base 10.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a hexadecimal number in base 16.
    #The program will then print the hexadecimal number in 
    #base 16.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a octal number in base 8.
    #The program will then print the octal number in 
    #base 8.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a binary number in base 10.
    #The program will then print the binary number in 
    #base 10.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a decimal number in base 10.
    #The program will then print the decimal number in 
    #base 10.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a hexadecimal number in base 16.
    #The program will then print the hexadecimal number in 
    #base 16.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a octal number in base 8.
    #The program will then print the octal number in 
    #base 8.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a binary number in base 10.
    #The program will then print the binary number in 
    #base 10.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a decimal number in base 10.
    #The program will then print the decimal number in 
    #base 10.
    #The program will then ask the user to input a number.
    #The program will then convert the number into 
    #a hexadecimal number in base 16.
    #The program will then print the hexadecimal number in 
    #base 16.
    #

