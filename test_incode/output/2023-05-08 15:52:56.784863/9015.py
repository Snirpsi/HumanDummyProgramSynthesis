#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that requests all ports.
    ports = range(0,65535)
    
    #A function that prints all the ports.
    def print_ports():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports2():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports3():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports4():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports5():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports6():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports7():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports8():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports9():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports10():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports11():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports12():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports13():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports14():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports15():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports16():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports17():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports18():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports19():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports20():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports21():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports22():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports23():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports24():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports25():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports26():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports27():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports28():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports29():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports30():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports31():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports32():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports33():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports34():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports35():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports36():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports37():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def print_ports38():
        for 

