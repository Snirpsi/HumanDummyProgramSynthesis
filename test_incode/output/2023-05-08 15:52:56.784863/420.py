#!/usr/bin/python3
if __name__ == '__main__':
    #A function that converts user input.
    #It takes the user input as a string and converts it into a list.
    #The list contains each word in the string.
    #The function then prints the list.
    wordList = convertToList(userInput)
    print(wordList)

