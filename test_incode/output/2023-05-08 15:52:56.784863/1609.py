#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that converts a list of words.
    #It takes a list of words and returns a list of their anagrams.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.
    #The anagrams are returned as a list of strings.


