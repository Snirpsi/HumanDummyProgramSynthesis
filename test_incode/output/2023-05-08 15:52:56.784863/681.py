#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that requests a list of numbers.
    #The function should return a list of numbers.
    #The function should also print a message to the screen when called.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that is a list of
    #numbers to print.
    #The function should also accept a parameter called "numbers" that 

