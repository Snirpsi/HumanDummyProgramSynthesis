#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that stores numbers.
    #It should return a list of numbers.
    #The function should accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and store it in a list.
    #The function should also accept a parameter and 

