#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that enumerates numbers.
    #The function returns two values: the index and the value.
    #The function can be called with any number of arguments.
    #The function should return None if the enumeration does not proceed.
    #The function should return an iterable object if the enumeration proceeds.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a StopIteration exception if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The function should raise a ValueError if the enumeration does not proceed.
    #The function should raise a TypeError if the enumeration does not proceed.
    #The 

