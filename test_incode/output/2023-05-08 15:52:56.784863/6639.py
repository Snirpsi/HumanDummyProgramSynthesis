#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that stores all ports.
    ports = range(0,65535)
    
    #A function that prints all the ports.
    def printPorts():
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts2(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts3(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts4(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts5(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts6(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts7(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts8(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts9(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts10(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts11(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts12(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts13(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts14(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts15(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts16(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts17(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts18(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts19(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts20(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts21(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts22(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts23(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts24(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts25(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts26(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts27(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts28(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts29(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts30(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts31(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts32(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts33(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts34(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.
    def printPorts35(ports):
        for port in ports:
            print(port)
    
    #A function that prints all the ports.

