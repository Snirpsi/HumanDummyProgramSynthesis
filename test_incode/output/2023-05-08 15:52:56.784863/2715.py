#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all ports.
    #The program will run on port 0 only.
    #The program will run on port 65535 only.
    ports = range(0,65535)
    
    #A program that stores all port

