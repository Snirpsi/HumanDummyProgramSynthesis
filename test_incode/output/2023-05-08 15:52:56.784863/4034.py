#!/usr/bin/python3
if __name__ == '__main__':
    #A function that converts user input.
    #It takes the user input as a string.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a list.
    #It then converts it to a dictionary.
    #It then converts it to a 

