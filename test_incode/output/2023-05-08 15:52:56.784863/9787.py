#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that converts a list of words.
    def convert(words):
        converted = []
        for word in words:
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            converted.append(word.lower())
        return converted
    #A function that converts a string to a list of words.
    def convert(string):
        converted = []
        for word in string.split():
            

