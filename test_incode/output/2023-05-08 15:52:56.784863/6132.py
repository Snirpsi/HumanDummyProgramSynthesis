#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A minimal webserver that removes a list of numbers.
    #This script is intended to be run from the command line.
    import sys
    import os
    import webbrowser
    import argparse
    parser = argparse.ArgumentParser(description='A minimal webserver that removes a list of numbers.')
    parser.add_argument('numbers', metavar='numbers', type=str, nargs='+', help='The numbers to remove.')
    args = parser.parse_args()
    numbers = args.numbers
    numbers = list(map(int, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    numbers = numbers[:]
    numbers.extend(numbers)
    numbers = list(map(str, numbers))
    numbers.sort()
    

