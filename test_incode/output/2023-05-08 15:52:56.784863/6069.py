#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that adds a list of numbers.
    def add(numbers):
        return sum(numbers)
    #A function that multiplies a list of numbers.
    def multiply(numbers):
        return sum(numbers)
    #A function that subtracts a list of numbers.
    def subtract(numbers):
        return sum(numbers)
    #A function that divides a list of numbers.
    def divide(numbers):
        return sum(numbers)
    #A function that finds the greatest common divisor of two numbers.
    def gcd(numbers):
        return reduce(gcd, numbers)
    #A function that finds the least common divisor of two numbers.
    def lcm(numbers):
        return reduce(lcm, numbers)
    #A function that finds the factorial of a number.
    def factorial(number):
        if number == 0:
            return 1
        else:
            return number * factorial(number - 1)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):
        return reduce(multiply, numbers)
    #A function that finds the sum of a list of numbers.
    def sum(numbers):
        return reduce(add, numbers)
    #A function that finds the product of a list of numbers.
    def product(numbers):

