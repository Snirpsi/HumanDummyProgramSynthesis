#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that stores a list of numbers.
    #The function will take as input a list of
    #numbers and store them in a list.
    #The function will then print a list of
    #the numbers in ascending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in descending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in ascending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in descending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in ascending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in descending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in ascending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in descending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in ascending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in descending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in ascending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in descending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in ascending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in descending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in ascending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in descending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in ascending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in descending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in ascending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in descending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in ascending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in descending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in ascending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the list at the index specified by the user.
    #The function will then print the list
    #of numbers in descending order.
    #The function will then ask the user
    #to input a number and then store it
    #in the 

