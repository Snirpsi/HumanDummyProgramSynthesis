#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A minimal webserver that iterates over words.
    #The server will respond to GET requests with HTML.
    #The server will respond to POST requests with JSON.
    #The server will respond to PUT requests with JSON.
    #The server will respond to DELETE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to OPTIONS requests with JSON.
    #The server will respond to HEAD requests with JSON.
    #The server will respond to TRACE requests with JSON.
    #The server will respond to 

