#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A endless loop that opens a list of words.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that opens a list of words and prints them one by one.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that opens a list of words and prints them one by one.
    for word in words:
        print(word)
        
    #A while loop that open

