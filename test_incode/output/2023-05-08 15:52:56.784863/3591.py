#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A endless loop that enumerates a list of words.
    for index, word in enumerate(words):
        print(index, word)
        #This line is executed once for each word in words.
        #The word is printed and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed once for each word in words.
        #The word is replaced and the index is incremented.
        words[index] = word
        #This line is executed 

