#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A minimal webserver that converts a list of numbers.
    #The server will accept a GET request and convert it to a list of numbers.
    #The server will also accept a POST request and convert it to a list of numbers.
    #The server will also accept a PUT request and convert it to a list of numbers.
    #The server will also accept a DELETE request and convert it to a list of numbers.
    #The server will also accept a HEAD request and convert it to a list of numbers.
    #The server will also accept a OPTIONS request and convert it to a list of numbers.
    #The server will also accept a TRACE request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a PATCH request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT request and convert it to a list of numbers.
    #The server will also accept a CONNECT 

