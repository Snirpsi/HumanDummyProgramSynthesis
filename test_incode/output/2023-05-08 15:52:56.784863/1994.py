#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that iterates over a list of numbers.
    #It prints every number in the list to the screen.
    #The function takes an optional argument that determines how many
    #numbers to print to the screen.
    #The function returns None to indicate that no more numbers are to be
    #printed.
    #The function prints the numbers in the list to the screen one by one.
    #The function does not return anything.
    #The function does not print a newline character.
    #The function does not print a carriage return character.
    #The function does not print a newline character and does not print a
    #carriage return character.
    #The function does not print a newline character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #carriage return character and does not print a newline character and
    #does not print a carriage return character and does not print a
    #

