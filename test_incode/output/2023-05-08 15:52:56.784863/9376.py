#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that requests a list of numbers.
    #It will then print them out in reverse order.
    #The function will also ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their address.
    #The function will then ask the user for their phone number.
    #The function will then ask the user for their email address.
    #The function will then ask the user for their password.
    #The function will then ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their address.
    #The function will then ask the user for their phone number.
    #The function will then ask the user for their email address.
    #The function will then ask the user for their password.
    #The function will then ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their address.
    #The function will then ask the user for their phone number.
    #The function will then ask the user for their email address.
    #The function will then ask the user for their password.
    #The function will then ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their address.
    #The function will then ask the user for their phone number.
    #The function will then ask the user for their email address.
    #The function will then ask the user for their password.
    #The function will then ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their address.
    #The function will then ask the user for their phone number.
    #The function will then ask the user for their email address.
    #The function will then ask the user for their password.
    #The function will then ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their address.
    #The function will then ask the user for their phone number.
    #The function will then ask the user for their email address.
    #The function will then ask the user for their password.
    #The function will then ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their address.
    #The function will then ask the user for their phone number.
    #The function will then ask the user for their email address.
    #The function will then ask the user for their password.
    #The function will then ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their address.
    #The function will then ask the user for their phone number.
    #The function will then ask the user for their email address.
    #The function will then ask the user for their password.
    #The function will then ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their address.
    #The function will then ask the user for their phone number.
    #The function will then ask the user for their email address.
    #The function will then ask the user for their password.
    #The function will then ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their address.
    #The function will then ask the user for their phone number.
    #The function will then ask the user for their email address.
    #The function will then ask the user for their password.
    #The function will then ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their address.
    #The function will then ask the user for their phone number.
    #The function will then ask the user for their email address.
    #The function will then ask the user for their password.
    #The function will then ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their address.
    #The function will then ask the user for their phone number.
    #The function will then ask the user for their email address.
    #The function will then ask the user for their password.
    #The function will then ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their address.
    #The function will then ask the user for their phone number.
    #The function will then ask the user for their email address.
    #The function will then ask the user for their password.
    #The function will then ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their address.


