#!/usr/bin/python3
url = "http://snirps.ddns.net"
if __name__ == '__main__':
    #A function that enumerates a http request.
    def enumerate_request(request):
        request = urllib.request.Request(url)
        request.headers = {'User-Agent': 'Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/39.0.2171.95 Safari/537.36'}
        request = urllib.request.urlopen(request)
        html = request.read()
        return html
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.
    def parse_html(html):
        soup = BeautifulSoup(html, 'html.parser')
        return soup
    #A function that parses the html and returns the content of the page.


