#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that calculates a list of numbers.
    #It takes one argument, the list to work on.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The function then prints the list to the screen.
    #The 

