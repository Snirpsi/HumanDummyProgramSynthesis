#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A endless loop that stores a list of numbers.
    #The loop will run until the user enters 0.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.
    #The loop will then print the list to the screen.
    #The loop will then ask the user for another number.


