#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A minimal webserver that iterates over a list of words.
    #It will respond to GET requests with each word in the list.
    #The server will then print out the word in the GET request.
    #The server will then respond to POST requests with each word in the list.
    #The server will then print out the word in the POST request.
    #The server will then respond to PUT requests with each word in the list.
    #The server will then print out the word in the PUT request.
    #The server will then respond to DELETE requests with each word in the list.
    #The server will then print out the word in the DELETE request.
    #The server will then respond to OPTIONS requests with each word in the list.
    #The server will then print out the word in the OPTIONS request.
    #The server will then respond to HEAD requests with each word in the list.
    #The server will then print out the word in the HEAD request.
    #The server will then respond to TRACE requests with each word in the list.
    #The server will then print out the word in the TRACE request.
    #The server will then respond to OPTIONS requests with each word in the list.
    #The server will then print out the word in the OPTIONS request.
    #The server will then respond to HEAD requests with each word in the list.
    #The server will then print out the word in the HEAD request.
    #The server will then respond to TRACE requests with each word in the list.
    #The server will then print out the word in the TRACE request.
    #The server will then respond to OPTIONS requests with each word in the list.
    #The server will then print out the word in the OPTIONS request.
    #The server will then respond to HEAD requests with each word in the list.
    #The server will then print out the word in the HEAD request.
    #The server will then respond to TRACE requests with each word in the list.
    #The server will then print out the word in the TRACE request.
    #The server will then respond to OPTIONS requests with each word in the list.
    #The server will then print out the word in the OPTIONS request.
    #The server will then respond to HEAD requests with each word in the list.
    #The server will then print out the word in the HEAD request.
    #The server will then respond to TRACE requests with each word in the list.
    #The server will then print out the word in the TRACE request.
    #The server will then respond to OPTIONS requests with each word in the list.
    #The server will then print out the word in the OPTIONS request.
    #The server will then respond to HEAD requests with each word in the list.
    #The server will then print out the word in the HEAD request.
    #The server will then respond to TRACE requests with each word in the list.
    #The server will then print out the word in the TRACE request.
    #The server will then respond to OPTIONS requests with each word in the list.
    #The server will then print out the word in the OPTIONS request.
    #The server will then respond to HEAD requests with each word in the list.
    #The server will then print out the word in the HEAD request.
    #The server will then respond to TRACE requests with each word in the list.
    #The server will then print out the word in the TRACE request.
    #The server will then respond to OPTIONS requests with each word in the list.
    #The server will then print out the word in the OPTIONS request.
    #The server will then respond to HEAD requests with each word in the list.
    #The server will then print out the word in the HEAD request.
    #The server will then respond to TRACE requests with each word in the list.
    #The server will then print out the word in the TRACE request.
    #The server will then respond to OPTIONS requests with each word in the list.
    #The server will then print out the word in the OPTIONS request.
    #The server will then respond to HEAD requests with each word in the list.
    #The server will then print out the word in the HEAD request.
    #The server will then respond to TRACE requests with each word in the list.
    #The server will then print out the word in the TRACE request.
    #The server will then respond to OPTIONS requests with each word in the list.
    #The server will then print out the word in the OPTIONS request.
    #The server will then respond to HEAD requests with each word in the list.
    #The server will then print out the word in the HEAD request.
    #The server will then respond to TRACE requests with each word in the list.
    #The server will then print out the word in the TRACE request.
    #The server will then respond to OPTIONS requests with each word in the list.
    #The server will then print out the word in the OPTIONS request.
    #The server will then respond to HEAD requests with each word in the list.
    #The server will then print out the word in the HEAD request.
    #The server will then respond to TRACE requests with each word in the list.
    #The server will then print out the word in the TRACE request.
    #The server will then respond to OPTIONS requests with each word in the list.
    #The server will then print out the word in the OPTIONS request.
    #The server will then respond to HEAD requests with each word in the list.
    #The 

