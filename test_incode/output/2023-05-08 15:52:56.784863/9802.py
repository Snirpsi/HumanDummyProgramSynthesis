#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A function that opens ports.
    ports = openports(ports)
    
    #A function that closes ports.
    ports = closeports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints ports.
    ports = printports(ports)
    
    #A function that prints 

