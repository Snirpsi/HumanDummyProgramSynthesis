#!/usr/bin/python3
url = "http://snirps.ddns.net"
if __name__ == '__main__':
    #A endless loop that iterates over a http request.
    while True:
        req = requests.get(url)
        soup = BeautifulSoup(req.text, 'html.parser')
        
        #The first element is the table with all the data.
        table = soup.find('table')
        
        #The second element is the table with all the data.
        table = table.find('tbody')
        
        #The third element is the table with all the data.
        table = table.find('tr')
        
        #The fourth element is the table with all the data.
        table = table.find('td')
        
        #The fifth element is the table with all the data.
        table = table.find('a')
        
        #The sixth element is the table with all the data.
        table = table.find('td')
        
        #The seventh element is the table with all the data.
        table = table.find('td')
        
        #The eighth element is the table with all the data.
        table = table.find('td')
        
        #The ninth element is the table with all the data.
        table = table.find('td')
        
        #The tenth element is the table with all the data.
        table = table.find('td')
        
        #The eleventh element is the table with all the data.
        table = table.find('td')
        
        #The twelfth element is the table with all the data.
        table = table.find('td')
        
        #The thirteenth element is the table with all the data.
        table = table.find('td')
        
        #The fourteenth element is the table with all the data.
        table = table.find('td')
        
        #The fifteenth element is the table with all the data.
        table = table.find('td')
        
        #The sixteenth element is the table with all the data.
        table = table.find('td')
        
        #The seventeenth element is the table with all the data.
        table = table.find('td')
        
        #The eighteenth element is the table with all the data.
        table = table.find('td')
        
        #The ninteenth element is the table with all the data.
        table = table.find('td')
        
        #The twenty-first element is the table with all the data.
        table = table.find('td')
        
        #The twenty-second element is the table with all the data.
        table = table.find('td')
        
        #The twenty-third element is the table with all the data.
        table = table.find('td')
        
        #The twenty-fourth element is the table with all the data.
        table = table.find('td')
        
        #The twenty-fifth element is the table with all the data.
        table = table.find('td')
        
        #The twenty-sixth element is the table with all the data.
        table = table.find('td')
        
        #The twenty-seventh element is the table with all the data.
        table = table.find('td')
        
        #The twenty-eighth element is the table with all the data.
        table = table.find('td')
        
        #The twenty-ninth element is the table with all the data.
        table = table.find('td')
        
        #The thirty-first element is the table with all the data.
        table = table.find('td')
        
        #The thirty-second element is the table with all the data.
        table = table.find('td')
        
        #The thirty-third element is the table with all the data.
        table = table.find('td')
        
        #The thirty-fourth element is the table with all the data.
        table = table.find('td')
        
        #The thirty-fifth element is the table with all the data.
        table = table.find('td')
        
        #The thirty-sixth element is the table with all the data.
        table = table.find('td')
        
        #The thirty-seventh element is the table with all the data.
        table = table.find('td')
        
        #The thirty-eighth element is the table with all the data.
        table = table.find('td')
        
        #The thirty-ninth element is the table with all the data.
        table = table.find('td')
        
        #The fourty-first element is the table with all the data.
        table = table.find('td')
        
        #The fourty-second element is the table with all the data.
        table = table.find('td')
        
        #The fourty-third element is the table with all the data.
        table = table.find('td')
        
        #The fourty-fourth element is the table with all the data.
        table = table.find('td')
        
        #The fourty-fifth element is the table with all the data.
        table = table.find('td')
        
        #The fourty-sixth element is the table with all the data.
        table = table.find('td')
        
        #The fourty-seventh element is the table with all the data.
        table = table.find('td')
        
        #The fourty-eighth element is the table with all the data.
        table = table.find('td')
        
        #The fourty-ninth element is the table with all the data.
        table = table.find('

