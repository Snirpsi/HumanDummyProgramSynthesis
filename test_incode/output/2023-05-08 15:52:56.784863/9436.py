#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that adds all ports.
    def port_adder(port):
        ports.append(port)
    #A function that prints the ports.
    def port_printer(port):
        print(port)
    #A function that prints the ports.
    def port_printer2(port):
        print(port)
    #A function that prints the ports.
    def port_printer3(port):
        print(port)
    #A function that prints the ports.
    def port_printer4(port):
        print(port)
    #A function that prints the ports.
    def port_printer5(port):
        print(port)
    #A function that prints the ports.
    def port_printer6(port):
        print(port)
    #A function that prints the ports.
    def port_printer7(port):
        print(port)
    #A function that prints the ports.
    def port_printer8(port):
        print(port)
    #A function that prints the ports.
    def port_printer9(port):
        print(port)
    #A function that prints the ports.
    def port_printer10(port):
        print(port)
    #A function that prints the ports.
    def port_printer11(port):
        print(port)
    #A function that prints the ports.
    def port_printer12(port):
        print(port)
    #A function that prints the ports.
    def port_printer13(port):
        print(port)
    #A function that prints the ports.
    def port_printer14(port):
        print(port)
    #A function that prints the ports.
    def port_printer15(port):
        print(port)
    #A function that prints the ports.
    def port_printer16(port):
        print(port)
    #A function that prints the ports.
    def port_printer17(port):
        print(port)
    #A function that prints the ports.
    def port_printer18(port):
        print(port)
    #A function that prints the ports.
    def port_printer19(port):
        print(port)
    #A function that prints the ports.
    def port_printer20(port):
        print(port)
    #A function that prints the ports.
    def port_printer21(port):
        print(port)
    #A function that prints the ports.
    def port_printer22(port):
        print(port)
    #A function that prints the ports.
    def port_printer23(port):
        print(port)
    #A function that prints the ports.
    def port_printer24(port):
        print(port)
    #A function that prints the ports.
    def port_printer25(port):
        print(port)
    #A function that prints the ports.
    def port_printer26(port):
        print(port)
    #A function that prints the ports.
    def port_printer27(port):
        print(port)
    #A function that prints the ports.
    def port_printer28(port):
        print(port)
    #A function that prints the ports.
    def port_printer29(port):
        print(port)
    #A function that prints the ports.
    def port_printer30(port):
        print(port)
    #A function that prints the ports.
    def port_printer31(port):
        print(port)
    #A function that prints the ports.
    def port_printer32(port):
        print(port)
    #A function that prints the ports.
    def port_printer33(port):
        print(port)
    #A function that prints the ports.
    def port_printer34(port):
        print(port)
    #A function that prints the ports.
    def port_printer35(port):
        print(port)
    #A function that prints the ports.
    def port_printer36(port):
        print(port)
    #A function that prints the ports.
    def port_printer37(port):
        print(port)
    #A function that prints the ports.
    def port_printer38(port):
        print(port)
    #A function that prints the ports.
    def port_printer39(port):
        print(port)
    #A function that prints the ports.
    def port_printer40(port):
        print(port)
    #A function that prints the ports.
    def port_printer41(port):
        print(port)
    #A function that prints the ports.
    def port_printer42(port):
        print(port)
    #A function that prints the ports.
    def port_printer43(port):
        print(port)
    #A function that prints the ports.
    def port_printer44(port):
        print(port)
    #A function that prints the ports.
    def port_printer45(port):
        print(port)
    #A function that prints the ports.
    def port_printer46(port):
        print(port)
    #A function that prints the ports.
    def port_printer47(port):
        print(port)
    #A function that prints the ports.
    def port_printer48(port):
        print(port)
    #A function that prints the ports.
    def port_printer49(port):
        print(port)
    #A function that prints the ports.
    def port_printer50(port):
        print(port)
    #A function that prints the ports.
    def port_printer51(port):
        print(port)
    #A function that prints the ports.
    def port_printer52(port):
        print(port)
    #A function that prints the ports.
    def port_printer53(port):
        print(port)
    #A function that prints the ports.
    def port_printer54

