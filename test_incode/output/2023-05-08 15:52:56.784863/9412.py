#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A minimal webserver that removes a list of numbers.
    #The server will listen for HTTP requests and will respond with a JSON response
    #The client will send a list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #The server will then respond with a JSON response with the list of numbers removed
    #The client will then send another list of numbers to the server and it will add them to the list
    #The server will then respond with a JSON response with the list of numbers added
    #The client will then send another list of numbers to the server and it will remove them from the list
    #

