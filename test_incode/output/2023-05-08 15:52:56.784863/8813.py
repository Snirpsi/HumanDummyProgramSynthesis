#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that converts all ports.
    #The function will convert all ports to hexadecimal and print it.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.
    #The function will also print a message if port is invalid.


