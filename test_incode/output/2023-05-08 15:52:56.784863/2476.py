#!/usr/bin/python3
port = 8080
if __name__ == '__main__':
    #A function that converts a port.
    #port is a port number
    #port is converted to a string
    #port is converted to an integer
    #port is converted to a float
    #port is converted to a boolean
    port = 8080
    print(port)

