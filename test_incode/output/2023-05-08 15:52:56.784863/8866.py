#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that stores a list of numbers.
    #The function will store each number in the list in a separate line.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the main program.
    #The function will also print a newline character.
    #The function will also print the list to the screen.
    #The function will also return the list to the 

