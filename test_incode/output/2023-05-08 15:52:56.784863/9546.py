#!/usr/bin/python3
fruirs = ["apple","banana","orange"]
if __name__ == '__main__':
    #A function that requests fruits.
    #It will print them out in alphabetical order.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the user for their fruit.
    #The function will then print the fruit and the age.
    #The function will ask the user for their name.
    #The function will then ask the user for their age.
    #The function will then ask the 

