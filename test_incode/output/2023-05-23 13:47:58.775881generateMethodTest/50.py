#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A endless loop that prints a list of words.
    #The loop will stop when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty
    #The loop will also print a message when words is empty