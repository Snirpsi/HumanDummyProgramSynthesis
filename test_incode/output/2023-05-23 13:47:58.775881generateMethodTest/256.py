#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A function that calculates ports.
    def calc_ports(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_str(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_int(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_float(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_bool(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_list(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_dict(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_set(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_tuple(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_list(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_dict(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_set(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_tuple(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_list(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_dict(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_set(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_tuple(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_list(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_dict(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_set(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_tuple(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_list(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_dict(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_set(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_tuple(ports):
        ports_str = ""
        for port in ports:
            ports_str += str(port)
        return ports_str

    #A function that calculates ports.
    def calc_ports_list(ports):
        ports_str = ""
        for port in ports:
            ports_str += 