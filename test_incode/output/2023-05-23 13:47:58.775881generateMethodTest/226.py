#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A program that iterates over all ports.
    #It will print out all ports available on the system.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.
    #It will also print out all available ports.