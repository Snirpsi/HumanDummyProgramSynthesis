#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that stores a list of numbers.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(numbers):
        store = []
        for number in numbers:
            store.append(number)
        return store
    #A function that stores a list in a dictionary.
    def storeDict(numbers):
        store = {}
        for number in numbers:
            store[number] = number
        return store
    #A function that stores a list in a list.
    def storeList(