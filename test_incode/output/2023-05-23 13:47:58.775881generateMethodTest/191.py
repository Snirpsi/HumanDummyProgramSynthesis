#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that iterates over numbers.
    def iterate(numbers):
        for number in numbers:
            yield number
    #A function that prints all the numbers in numbers.
    def print_numbers(numbers):
        for number in numbers:
            print(number)
    #A function that prints all the numbers in numbers, except the last.
    def print_numbers_except_last(numbers):
        for number in numbers:
            if number != numbers[-1]:
                print(number)
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_2(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_3(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_4(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_5(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_6(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_7(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_8(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_9(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_10(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_11(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_12(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_13(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_14(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_15(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_16(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_17(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_18(numbers):
        for number in numbers:
            if number != numbers[-1]:
                yield number
    #A function that prints all the numbers in numbers, except the last, using an iterator.
    def print_numbers_except_last_iterate_generator_19(numbers):
        for number in numbers:
            if number 