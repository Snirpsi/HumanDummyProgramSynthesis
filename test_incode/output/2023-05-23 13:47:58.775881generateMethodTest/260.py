#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that returns a list of numbers.
    def numbers_generator():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_1():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_2():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_3():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_4():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_5():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_6():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_7():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_8():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_9():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_10():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_11():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_12():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_13():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_14():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_15():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_16():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_17():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_18():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_19():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of numbers from 1 to 10.
    def numbers_generator_20():
        numbers = []
        while numbers:
            number = int(input('Enter a number: '))
            if number not in numbers:
                numbers.append(number)
        return numbers
    #A function that returns a list of 