#!/usr/bin/python3
fruirs = ["apple","banana","orange"]
if __name__ == '__main__':
    #A function that enumerates fruits.
    def enumerateFruits(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list.
    def printFruits(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce2(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce3(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce4(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce5(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce6(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce7(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce8(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce9(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce10(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce11(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce12(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce13(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce14(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce15(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce16(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce17(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce18(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce19(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce20(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce21(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce22(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce23(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them once.
    def printFruitsOnce24(fruits):
        for fruit in fruits:
            print(fruit)
    #A function that prints all the fruits from the list but only prints them 