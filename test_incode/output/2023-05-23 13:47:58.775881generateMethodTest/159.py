#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that opens a list of numbers.
    def open_numbers(numbers):
        numbers_file = open(numbers, 'r')
        return numbers_file
    #A function that reads numbers from a file.
    def read_numbers(numbers_file):
        numbers = []
        for line in numbers_file:
            numbers.append(int(line))
        return numbers
    #A function that writes numbers to a file.
    def write_numbers(numbers, numbers_file):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that closes a file.
    def close_numbers(numbers_file):
        numbers_file.close()
    #A function that reads a file and writes numbers to a file.
    def merge_numbers(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_2(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_3(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_4(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_5(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_6(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_7(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_8(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_9(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_10(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_11(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_12(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_13(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_14(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_15(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_16(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_17(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_18(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_19(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_20(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_21(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_22(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_23(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_24(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_25(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_26(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a file.
    def merge_numbers_27(numbers_file, numbers):
        numbers_file.write('\n'.join(map(str, numbers)))
    #A function that reads a file and writes numbers to a 