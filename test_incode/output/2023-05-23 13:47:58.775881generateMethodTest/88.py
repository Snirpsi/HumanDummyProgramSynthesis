#!/usr/bin/python3
url = "http://snirps.ddns.net"
if __name__ == '__main__':
    #A program that iterates over a http request.
    #The request is made using urllib2.
    #The response is then converted to a string.
    #The response is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to a list.
    #The list is then converted to a string.
    #The string is then converted to a json object.
    #The json object is then converted to a dictionary.
    #The dictionary is then converted to 