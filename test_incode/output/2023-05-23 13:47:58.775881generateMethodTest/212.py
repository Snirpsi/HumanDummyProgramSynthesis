#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A program that removes a list of words.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program will ask the user for a list of words and then remove them
    #from the list. 
    #The program will then print the list to the screen.
    #The program 