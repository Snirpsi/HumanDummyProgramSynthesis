#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A endless loop that opens numbers.txt and reads its contents into a list.
    #The list will then be used as the argument to our program.
    #The program will then repeatedly ask the user for input until they enter 'q'.
    #The program will then print out the contents of the list.
    while(True):
        numbers = []
        file = open('numbers.txt','r')
        for line in file:
            numbers.append(int(line))
        file.close()
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q'):
                break
            else:
                numbers.append(int(number))
        
        #Print out the contents of the list.
        print("The contents of the list are:")
        print(numbers)
        
        #Ask the user for their input.
        while(True):
            number = input("Enter a number: ")
            if(number == 'q