#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A minimal webserver that prints a list of numbers.
    #The server will listen on port 8000, and will print a list of
    #numbers received from the client on port 8001.
    #The server will also print a message to the client whenever
    #the client sends a number greater than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number less than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number equal to the list size.
    #The server will also print a message to the client whenever
    #the client sends a number not in the list.
    #The server will also print a message to the client whenever
    #the client sends a number equal to the list size.
    #The server will also print a message to the client whenever
    #the client sends a number not in the list.
    #The server will also print a message to the client whenever
    #the client sends a number greater than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number less than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number equal to the list size.
    #The server will also print a message to the client whenever
    #the client sends a number not in the list.
    #The server will also print a message to the client whenever
    #the client sends a number greater than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number less than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number equal to the list size.
    #The server will also print a message to the client whenever
    #the client sends a number not in the list.
    #The server will also print a message to the client whenever
    #the client sends a number greater than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number less than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number equal to the list size.
    #The server will also print a message to the client whenever
    #the client sends a number not in the list.
    #The server will also print a message to the client whenever
    #the client sends a number greater than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number less than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number equal to the list size.
    #The server will also print a message to the client whenever
    #the client sends a number not in the list.
    #The server will also print a message to the client whenever
    #the client sends a number greater than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number less than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number equal to the list size.
    #The server will also print a message to the client whenever
    #the client sends a number not in the list.
    #The server will also print a message to the client whenever
    #the client sends a number greater than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number less than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number equal to the list size.
    #The server will also print a message to the client whenever
    #the client sends a number not in the list.
    #The server will also print a message to the client whenever
    #the client sends a number greater than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number less than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number equal to the list size.
    #The server will also print a message to the client whenever
    #the client sends a number not in the list.
    #The server will also print a message to the client whenever
    #the client sends a number greater than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number less than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number equal to the list size.
    #The server will also print a message to the client whenever
    #the client sends a number not in the list.
    #The server will also print a message to the client whenever
    #the client sends a number greater than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number less than the list size.
    #The server will also print a message to the client whenever
    #the client sends a number equal to the list size.
    #The server will also print a message to the client whenever
    #the client sends a number not in the list.
    #The server will also print a message to the client when