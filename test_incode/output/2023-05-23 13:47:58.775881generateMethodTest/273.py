#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A endless loop that prints a list of words.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        print(word)
        
    #A for loop that prints one word at a time.
    for word in words:
        print(word)
        
    #A while loop that prints one word at a time.
    while True:
        word = input("Enter a word: ")
        if word 