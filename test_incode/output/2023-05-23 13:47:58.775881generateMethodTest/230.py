#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A endless loop that enumerates numbers.
    while True:
        #A for loop that prints every number from 1 to 3.
        for number in numbers:
            print(number)
        #A while loop that prints every number from 1 to 3.
        while True:
            #A while loop that prints every number from 1 to 3.
            while numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(numbers.pop())
            #A while loop that prints every number from 1 to 3.
            while not numbers:
                print(