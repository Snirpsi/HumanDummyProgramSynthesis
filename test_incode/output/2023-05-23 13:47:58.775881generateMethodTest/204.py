#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that adds numbers.
    def add(numbers):
        total = 0
        for number in numbers:
            total = total + number
        return total
    #A function that multiplies numbers.
    def multiply(numbers):
        total = 1
        for number in numbers:
            total *= number
        return total
    #A function that subtracts numbers.
    def subtract(numbers):
        total = 1
        for number in numbers:
            total -= number
        return total
    #A function that divides numbers.
    def divide(numbers):
        total = 1
        for number in numbers:
            total /= number
        return total
    #A function that returns the product of numbers.
    def product(numbers):
        total = 1
        for number in numbers:
            total *= number
        return total
    #A function that returns the sum of numbers.
    def sum(numbers):
        total = 0
        for number in numbers:
            total += number
        return total
    #A function that returns the square of numbers.
    def square(numbers):
        total = 1
        for number in numbers:
            total *= number
        return total
    #A function that returns the reciprocal of numbers.
    def reciprocal(numbers):
        total = 1
        for number in numbers:
            total *= 1/number
        return total
    #A function that returns the absolute value of numbers.
    def abs(numbers):
        total = 0
        for number in numbers:
            total = np.absolute(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n_n_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n_n_n_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n_n_n_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n_n_n_n_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n_n_n_n_n_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n_n_n_n_n_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n_n_n_n_n_n_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n_n_n_n_n_n_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n_n_n_n_n_n_n_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n_n_n_n_n_n_n_n_n_n(numbers):
        total = 1
        for number in numbers:
            total = np.sqrt(number)
        return total
    #A function that returns the square root of numbers.
    def sqrt_n_n_n_n_n_