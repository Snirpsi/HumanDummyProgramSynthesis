#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A endless loop that opens a list of numbers.
    #The user can then choose to quit or continue to input numbers.
    while True:
        choice = input("Enter a number or q to quit: ")
        if choice == "q":
            break
        elif choice == "1":
            numbers.append(1)
        elif choice == "2":
            numbers.append(2)
        elif choice == "3":
            numbers.append(3)
        elif choice == "4":
            numbers.append(4)
        elif choice == "5":
            numbers.append(5)
        elif choice == "6":
            numbers.append(6)
        elif choice == "7":
            numbers.append(7)
        elif choice == "8":
            numbers.append(8)
        elif choice == "9":
            numbers.append(9)
        else:
            print("Invalid input. Please try again.")
    #A loop that prints the numbers in reverse order.
    #The user can then choose to quit or continue to input numbers.
    while True:
        choice = input("Enter a number or q to quit: ")
        if choice == "q":
            break
        elif choice == "1":
            numbers.reverse()
        elif choice == "2":
            numbers.reverse()
        elif choice == "3":
            numbers.reverse()
        elif choice == "4":
            numbers.reverse()
        elif choice == "5":
            numbers.reverse()
        elif choice == "6":
            numbers.reverse()
        elif choice == "7":
            numbers.reverse()
        elif choice == "8":
            numbers.reverse()
        elif choice == "9":
            numbers.reverse()
        else:
            print("Invalid input. Please try again.")
    #A loop that prints the numbers in ascending order.
    #The user can then choose to quit or continue to input numbers.
    while True:
        choice = input("Enter a number or q to quit: ")
        if choice == "q":
            break
        elif choice == "1":
            numbers.sort()
        elif choice == "2":
            numbers.sort()
        elif choice == "3":
            numbers.sort()
        elif choice == "4":
            numbers.sort()
        elif choice == "5":
            numbers.sort()
        elif choice == "6":
            numbers.sort()
        elif choice == "7":
            numbers.sort()
        elif choice == "8":
            numbers.sort()
        elif choice == "9":
            numbers.sort()
        else:
            print("Invalid input. Please try again.")
    #A loop that prints the numbers in ascending order.
    #The user can then choose to quit or continue to input numbers.
    while True:
        choice = input("Enter a number or q to quit: ")
        if choice == "q":
            break
        elif choice == "1":
            numbers.sort(reverse=True)
        elif choice == "2":
            numbers.sort(reverse=True)
        elif choice == "3":
            numbers.sort(reverse=True)
        elif choice == "4":
            numbers.sort(reverse=True)
        elif choice == "5":
            numbers.sort(reverse=True)
        elif choice == "6":
            numbers.sort(reverse=True)
        elif choice == "7":
            numbers.sort(reverse=True)
        elif choice == "8":
            numbers.sort(reverse=True)
        elif choice == "9":
            numbers.sort(reverse=True)
        else:
            print("Invalid input. Please try again.")
    #A loop that prints the numbers in ascending order.
    #The user can then choose to quit or continue to input numbers.
    while True:
        choice = input("Enter a number or q to quit: ")
        if choice == "q":
            break
        elif choice == "1":
            numbers.sort(reverse=True, key=int)
        elif choice == "2":
            numbers.sort(reverse=True, key=int)
        elif choice == "3":
            numbers.sort(reverse=True, key=int)
        elif choice == "4":
            numbers.sort(reverse=True, key=int)
        elif choice == "5":
            numbers.sort(reverse=True, key=int)
        elif choice == "6":
            numbers.sort(reverse=True, key=int)
        elif choice == "7":
            numbers.sort(reverse=True, key=int)
        elif choice == "8":
            numbers.sort(reverse=True, key=int)
        elif choice == "9":
            numbers.sort(reverse=True, key=int)
        else:
            print("Invalid input. Please try again.")
    #A loop that prints the numbers in ascending order.
    #The user can then choose to quit or continue to input numbers.
    while True:
        choice = input("Enter a number or q to quit: ")
        if choice == "q":
            break
        elif choice == "1":
            numbers.sort(reverse=True, key=float)
        elif choice == "2":
            numbers.sort(reverse=True, key=float)
        elif choice == "3":
            numbers.sort(reverse=True, key=float)
        elif choice == "4":
            numbers.sort(reverse=True, key=float)
        elif choice == "5":
            numbers.sort(reverse=True, key=float)
        elif choice == "6":
            numbers.sort(reverse=True, key=float)
        elif choice == "7":
            numbers.sort(reverse=True, key=float)
        elif choice == "8":
            numbers.sort(reverse=True, key=float)
        elif choice == "9":
            numbers.sort(reverse=True, key=float)
        else:
            print("Invalid input. Please try again.")
    #A loop that prints the numbers in ascending order.
    #The user can then choose to quit or continue to input numbers.
    while True:
        choice = input("Enter a number or 