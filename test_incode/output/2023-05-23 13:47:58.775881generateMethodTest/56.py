#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that enumerates a list of numbers.
    def enumerate(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def tuples(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of lists.
    def lists(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def tuples_list(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of lists.
    def lists_list(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def tuples_list_list(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def lists_list_list(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def tuples_list_list_list(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def lists_list_list_list(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def tuples_list_list_list_list(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def lists_list_list_list_list(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def tuples_list_list_list_list_list(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def lists_list_list_list_list_list(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def tuples_list_list_list_list_list_list(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def lists_list_list_list_list_list_list(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def tuples_list_list_list_list_list_list_list(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def lists_list_list_list_list_list_list_list(numbers):
        it = iter(numbers)
        count = 0
        try:
            for item in it:
                yield count, item
                count += 1
        except TypeError:
            for item in it:
                yield count, item
                count += 1
    
    #A function that takes a list of numbers and returns a list of tuples.
    def tuples_list_list_list_list_list_list_list_list(numbers):
        it = iter(numbers)
        count = 0
