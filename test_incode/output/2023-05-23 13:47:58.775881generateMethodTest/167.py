#!/usr/bin/python3
port = 8080
if __name__ == '__main__':
    #A minimal webserver that requests a port.
    #The port is then used to serve the client.
    #The client is then served at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening at port 8080.
    #The webserver will then be stopped at port 8080.
    #The webserver will then be started and listening 