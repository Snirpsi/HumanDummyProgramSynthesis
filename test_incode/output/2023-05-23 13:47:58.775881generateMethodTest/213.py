#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A endless loop that opens numbers.txt and reads its contents line by line
    while True:
        line = input('Enter a number: ')
        try:
            number = int(line)
        except ValueError:
            print('That is not a number!')
            continue
        else:
            break
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = numbers.readline()
        if not line:
            break
        print(line)
    #A while loop that prints the contents of the file numbers.txt one by one
    while True:
        line = 