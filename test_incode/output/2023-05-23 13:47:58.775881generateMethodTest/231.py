#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A program that requests ports.
    #This program will run on port 666 and port 8080
    #It will then request ports 80 and 443
    #It will then request ports 22 and 3
    #It will then print out all the ports requested.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
    #It will also print out which ports could not be reached.
