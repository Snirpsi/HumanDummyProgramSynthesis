#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A program that adds ports.
    ports_str = input("Enter the ports you want to add: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port not in ports:
            ports.append(port)
    print("The ports you have added are: ")
    print(ports)
    
    #A program that removes ports.
    ports_str = input("Enter the ports you want to remove: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            ports.remove(port)
    print("The ports you have removed are: ")
    print(ports)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")
    for port in ports_str:
        port = int(port)
        if port in ports:
            print(port)
    
    #A program that displays ports.
    ports_str = input("Enter the ports you want to display: ")
    ports_str = ports_str.split(",")