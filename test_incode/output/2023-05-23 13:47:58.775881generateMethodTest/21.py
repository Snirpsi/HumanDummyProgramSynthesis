#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A program that converts a list of words.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII code.
    #The program will ask the user to enter a word and then
    #it will print out the word and its corresponding 
    #ASCII 