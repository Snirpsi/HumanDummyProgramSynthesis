#!/usr/bin/python3
if __name__ == '__main__':
    #A minimal webserver that removes user input.
    #This example is intended to be run as a cron job
    #to remove user input from the terminal
    #It will also remove any files that may have been created
    #by this script
    #
    #To use this, simply run this script from the terminal
    #and it will remove any user input from the terminal
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py
    #
    #This script will remove any user input from the terminal
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py > remove_user_input.log
    #
    #This will create a file called remove_user_input.log
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py > remove_user_input.log 2>&1
    #
    #This will create a file called remove_user_input.log and redirect stderr to stdout
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py > remove_user_input.log 2>&1 &
    #
    #This will create a file called remove_user_input.log and redirect stderr to stdout and run the command in the background
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py > remove_user_input.log 2>&1 & 
    #
    #This will create a file called remove_user_input.log and redirect stderr to stdout and run the command in the background and redirect stderr to stdout
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py > remove_user_input.log 2>&1 & 
    #
    #This will create a file called remove_user_input.log and redirect stderr to stdout and run the command in the background and redirect stderr to stdout and run the command in the background
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py > remove_user_input.log 2>&1 & 
    #
    #This will create a file called remove_user_input.log and redirect stderr to stdout and run the command in the background and redirect stderr to stdout and run the command in the background
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py > remove_user_input.log 2>&1 & 
    #
    #This will create a file called remove_user_input.log and redirect stderr to stdout and run the command in the background and redirect stderr to stdout and run the command in the background
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py > remove_user_input.log 2>&1 & 
    #
    #This will create a file called remove_user_input.log and redirect stderr to stdout and run the command in the background and redirect stderr to stdout and run the command in the background
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py > remove_user_input.log 2>&1 & 
    #
    #This will create a file called remove_user_input.log and redirect stderr to stdout and run the command in the background and redirect stderr to stdout and run the command in the background
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py > remove_user_input.log 2>&1 & 
    #
    #This will create a file called remove_user_input.log and redirect stderr to stdout and run the command in the background and redirect stderr to stdout and run the command in the background
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py > remove_user_input.log 2>&1 & 
    #
    #This will create a file called remove_user_input.log and redirect stderr to stdout and run the command in the background and redirect stderr to stdout and run the command in the background
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py > remove_user_input.log 2>&1 & 
    #
    #This will create a file called remove_user_input.log and redirect stderr to stdout and run the command in the background and redirect stderr to stdout and run the command in the background
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py > remove_user_input.log 2>&1 & 
    #
    #This will create a file called remove_user_input.log and redirect stderr to stdout and run the command in the background and redirect stderr to stdout and run the command in the background
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input.py > remove_user_input.log 2>&1 & 
    #
    #This will create a file called remove_user_input.log and redirect stderr to stdout and run the command in the background and redirect stderr to stdout and run the command in the background
    #
    #To run this script from the command line, simply run
    #the following command:
    #
    #python3 remove_user_input