#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A endless loop that removes ports.
    while True:
        ports.remove()
        
        #A while loop that checks every 5 seconds for new ports.
        while True:
            for port in ports:
                if port not in range(0,65535):
                    ports.remove(port)
                    
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
            #A while loop that checks every 5 seconds for new ports.
            while True:
                for port in ports:
                    if port not in range(0,65535):
                        ports.remove(port)
                        
