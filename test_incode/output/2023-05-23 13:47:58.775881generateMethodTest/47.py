#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A function that converts a list of numbers.
    def convert(numbers):
        converted = []
        for number in numbers:
            converted.append(str(number))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(int(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(float(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(complex(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(str(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(float(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(complex(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(str(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(float(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(complex(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(str(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(float(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(complex(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(str(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(float(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(complex(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(str(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(float(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(complex(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(str(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(float(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(complex(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(str(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(float(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(complex(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(str(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(float(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(complex(character))
        return converted
    #A function that converts a string to a list of numbers.
    def convert(string):
        converted = []
        for character in string:
            converted.append(str(character))
        return converted
    #A function that converts a string to 