#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that requests all ports.
    def requestPorts():
        portList = []
        for port in ports:
            portList.append(port)
        return portList
    #A function that returns the port number of the requested port.
    def getPortNumber(port):
        for port in ports:
            if port == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in ports:
            if portName == port:
                return port
    #A function that returns the port number of the requested port.
    def getPortNumberByName(portName):
        for port in port