#!/usr/bin/python3
ports = range(0,65535)
if __name__ == '__main__':
    #A function that iterates over all ports.
    def port_iter():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter2():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter3():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter4():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter5():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter6():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter7():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter8():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter9():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter10():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter11():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter12():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter13():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter14():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter15():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter16():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter17():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter18():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter19():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter20():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter21():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter22():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter23():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter24():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter25():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter26():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter27():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter28():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter29():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter30():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter31():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter32():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter33():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter34():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter35():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter36():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter37():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter38():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter39():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter40():
        for port in ports:
            yield port

    #A function that iterates over all ports.
    def port_iter41():
        for port in ports:
            yield port

    #A function that iterates over all ports.