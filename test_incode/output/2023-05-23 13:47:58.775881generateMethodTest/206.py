#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that opens numbers.txt and reads numbers from it
    def open_numbers():
        numbers = []
        with open('numbers.txt', 'r') as file:
            for line in file:
                numbers.append(int(line))
        return numbers
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_2(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_3(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_4(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_5(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_6(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_7(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_8(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_9(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_10(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_11(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_12(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_13(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_14(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_15(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_16(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_17(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_18(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_19(numbers):
        with open('numbers.txt', 'w') as file:
            for number in numbers:
                file.write(str(number))
    #A function that prints numbers from numbers.txt and writes them to a new file
    def print_numbers_reversed_20(numbers):
        with open('numbers.txt', 'w') as file:
            for number in 