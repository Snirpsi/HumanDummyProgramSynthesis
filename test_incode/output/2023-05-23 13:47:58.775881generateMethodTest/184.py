#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that calculates a list of words.
    def wordlist(words):
        wordlist = []
        for word in words:
            wordlist.append(word)
        return wordlist
    #A function that prints a word list.
    def printwordlist(wordlist):
        for word in wordlist:
            print(word)
    #A function that prints a word list with spaces between words.
    def printwordlistwithspaces(wordlist):
        for word in wordlist:
            print(word,end=' ')
    #A function that prints a word list with spaces between words and a newline.
    def printwordlistwithspacesandnewline(wordlist):
        for word in wordlist:
            print(word,end=' ',flush=True)
    #A function that prints a word list with spaces between words and a newline and flush.
    def printwordlistwithspacesandnewlineandflush(wordlist):
        for word in wordlist:
            print(word,end=' ',flush=True)
    #A function that prints a word list with spaces between words and a newline and flush and a newline.
    def printwordlistwithspacesandnewlineandflushandnewline(wordlist):
        for word in wordlist:
            print(word,end=' ',flush=True)
    #A function that prints a word list with spaces between words and a newline and flush and a newline and flush.
    def printwordlistwithspacesandnewlineandflushandnewline(wordlist):
        for word in wordlist:
            print(word,end=' ',flush=True)
    #A function that prints a word list with spaces between words and a newline and flush and a newline and flush and a newline.
    def printwordlistwithspacesandnewlineandflushandnewlineandflush(wordlist):
        for word in wordlist:
            print(word,end=' ',flush=True)
    #A function that prints a word list with spaces between words and a newline and flush and a newline and flush and a newline and flush.
    def printwordlistwithspacesandnewlineandflushandnewlineandflushandnewline(wordlist):
        for word in wordlist:
            print(word,end=' ',flush=True)
    #A function that prints a word list with spaces between words and a newline and flush and a newline and flush and a newline and flush and a newline.
    def printwordlistwithspacesandnewlineandflushandnewlineandflushandnewline(wordlist):
        for word in wordlist:
            print(word,end=' ',flush=True)
    #A function that prints a word list with spaces between words and a newline and flush and a newline and flush and a newline and flush and a newline and flush.
    def printwordlistwithspacesandnewlineandflushandnewlineandflushandnewline(wordlist):
        for word in wordlist:
            print(word,end=' ',flush=True)
    #A function that prints a word list with spaces between words and a newline and flush and a newline and flush and a newline and flush and a newline and flush and a newline.
    def printwordlistwithspacesandnewlineandflushandnewlineandflushandnewlineandflushandnewline(wordlist):
        for word in wordlist:
            print(word,end=' ',flush=True)
    #A function that prints a word list with spaces between words and a newline and flush and a newline and flush and a newline and flush and a newline and flush and a newline.
    def printwordlistwithspacesandnewlineandflushandnewlineandflushandnewlineandflushandnewline(wordlist):
        for word in wordlist:
            print(word,end=' ',flush=True)
    #A function that prints a word list with spaces between words and a newline and flush and a newline and flush and a newline and flush and a newline and flush and a newline.
    def printwordlistwithspacesandnewlineandflushandnewlineandflushandnewlineandflushandnewline(wordlist):
        for word in wordlist:
            print(word,end=' ',flush=True)
    #A function that prints a word list with spaces between words and a newline and flush and a newline and flush and a newline and flush and a newline and flush and a newline.
    def printwordlistwithspacesandnewlineandflushandnewlineandflushandnewlineandflushandnewlineandflushandnewline(wordlist):
        for word in wordlist:
            print(word,end=' ',flush=True)
    #A function that prints a word list with spaces between words and a newline and flush and a newline and flush and a newline and flush and a newline and flush and a newline.
    def printwordlistwithspacesandnewlineandflushandnewlineandflushandnewlineandflushandnewlineandflushandnewline(wordlist):
        for word in wordlist:
            print(word,end=' ',flush=True)
    #A function that prints a word list with spaces between words and a newline and flush and a newline and flush and a newline and flush and a newline and flush and a newline.
    def printwordlistwithspacesandnewlineandflushandnewlineandflushandnewlineandflushandnewlineandflushandnewline(wordlist):
        for word in wordlist:
            print(word,end=' ',flush=True)
    #A function that prints a word list with spaces between words and a newline and flush and a newline and flush and a newline and flush and a newline and flush and a newline.
    def printwordlistwithspacesandnewlineandflushandnewlineandflushandnewlineandflushandnewlineandflush