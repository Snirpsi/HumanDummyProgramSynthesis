#!/usr/bin/python3
ports = [666,8080,80,443,22,3]
if __name__ == '__main__':
    #A program that removes ports.
    #It will listen for connections on ports specified in the ports list.
    #It will then remove those ports from the ports list.
    #It will then print a message to the screen indicating that the
    #ports were removed.
    #It will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message to the screen indicating that the
    #ports were removed.
    #The socket will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message to the screen indicating that the
    #ports were removed.
    #The socket will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message to the screen indicating that the
    #ports were removed.
    #The socket will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message to the screen indicating that the
    #ports were removed.
    #The socket will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message to the screen indicating that the
    #ports were removed.
    #The socket will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message to the screen indicating that the
    #ports were removed.
    #The socket will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message to the screen indicating that the
    #ports were removed.
    #The socket will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message to the screen indicating that the
    #ports were removed.
    #The socket will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message to the screen indicating that the
    #ports were removed.
    #The socket will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message to the screen indicating that the
    #ports were removed.
    #The socket will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message to the screen indicating that the
    #ports were removed.
    #The socket will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message to the screen indicating that the
    #ports were removed.
    #The socket will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message to the screen indicating that the
    #ports were removed.
    #The socket will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message to the screen indicating that the
    #ports were removed.
    #The socket will then exit the program.
    
    #Create a socket and bind to all specified ports.
    #The socket will then listen for connections on those ports.
    #The socket will then accept connections on those ports.
    #The socket will then print a message 