#!/usr/bin/python3
numbers = [1,2,3]
if __name__ == '__main__':
    #A function that returns numbers.
    def f():
        return numbers
    #A function that returns numbers and adds them up.
    def g():
        return numbers + numbers
    #A function that returns numbers and subtracts them from each other.
    def h():
        return numbers - numbers
    #A function that returns numbers and multiplies them together.
    def i():
        return numbers * numbers
    #A function that returns numbers and divides them by each other.
    def j():
        return numbers / numbers
    #A function that returns numbers and raises each number to the power of 2.
    def k():
        return numbers ** 2
    #A function that returns numbers and squares each number.
    def l():
        return numbers * numbers
    #A function that returns numbers and adds them to a list.
    def m():
        return numbers + [1,2,3]
    #A function that returns numbers and subtracts them from a list.
    def n():
        return numbers - [1,2,3]
    #A function that returns numbers and multiplies them to a list.
    def o():
        return numbers * [1,2,3]
    #A function that returns numbers and divides them to a list.
    def p():
        return numbers / [1,2,3]
    #A function that returns numbers and raises each number to the power of 2.
    def q():
        return numbers ** 2
    #A function that returns numbers and squares each number.
    def r():
        return numbers * [1,2,3]
    #A function that returns numbers and adds them to a set.
    def s():
        return numbers + set([1,2,3])
    #A function that returns numbers and subtracts them from a set.
    def t():
        return numbers - set([1,2,3])
    #A function that returns numbers and multiplies them to a set.
    def u():
        return numbers * set([1,2,3])
    #A function that returns numbers and divides them to a set.
    def v():
        return numbers / set([1,2,3])
    #A function that returns numbers and raises each number to the power of 2.
    def w():
        return numbers ** 2
    #A function that returns numbers and squares each number.
    def x():
        return numbers * [1,2,3]
    #A function that returns numbers and adds them to a list.
    def y():
        return numbers + [1,2,3]
    #A function that returns numbers and subtracts them from a list.
    def z():
        return numbers - [1,2,3]
    #A function that returns numbers and multiplies them to a list.
    def A():
        return numbers * [1,2,3]
    #A function that returns numbers and divides them to a list.
    def B():
        return numbers / [1,2,3]
    #A function that returns numbers and raises each number to the power of 2.
    def C():
        return numbers ** 2
    #A function that returns numbers and squares each number.
    def D():
        return numbers * [1,2,3]
    #A function that returns numbers and adds them to a set.
    def E():
        return numbers + set([1,2,3])
    #A function that returns numbers and subtracts them from a set.
    def F():
        return numbers - set([1,2,3])
    #A function that returns numbers and multiplies them to a set.
    def G():
        return numbers * set([1,2,3])
    #A function that returns numbers and divides them to a set.
    def H():
        return numbers / set([1,2,3])
    #A function that returns numbers and raises each number to the power of 2.
    def I():
        return numbers ** 2
    #A function that returns numbers and squares each number.
    def J():
        return numbers * [1,2,3]
    #A function that returns numbers and adds them to a list.
    def K():
        return numbers + [1,2,3]
    #A function that returns numbers and subtracts them from a list.
    def L():
        return numbers - [1,2,3]
    #A function that returns numbers and multiplies them to a list.
    def M():
        return numbers * [1,2,3]
    #A function that returns numbers and divides them to a list.
    def N():
        return numbers / [1,2,3]
    #A function that returns numbers and raises each number to the power of 2.
    def O():
        return numbers ** 2
    #A function that returns numbers and squares each number.
    def P():
        return numbers * [1,2,3]
    #A function that returns numbers and adds them to a list.
    def Q():
        return numbers + [1,2,3]
    #A function that returns numbers and subtracts them from a list.
    def R():
        return numbers - [1,2,3]
    #A function that returns numbers and multiplies them to a list.
    def S():
        return numbers * [1,2,3]
    #A function that returns numbers and divides them to a list.
    def T():
        return numbers / [1,2,3]
    #A function that returns numbers and raises each number to the power of 2.
    def U():
        return numbers ** 2
    #A function that returns numbers and squares each number.
    def V():
        return numbers * [1,2,3]
    #A function that returns numbers and adds them to a list.
    def W():
        return numbers + [1,2,3]
    #A function that returns numbers and subtracts them from a list.
    def X():
        return numbers - [1,2,3]
    #A function that returns numbers and multiplies them to a list.
    def Y():
        return numbers * [1,2,3]
    #A function that returns numbers and 