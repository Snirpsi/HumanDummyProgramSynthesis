if __name__ == '__main__': 
    """ A array that calculates a list of numbers. """    
    numbers = range(10)
    
    numbers_sum = sum(numbers)
    
    numbers_average = sum(numbers)/len(numbers)
    
    numbers_sum = sum(numbers)
    
    numbers_average = sum(numbers)/len(numbers)
    
    numbers_sum = sum(numbers)
    
    numbers_average = sum(numbers)/len(numbers)
    
    numbers_sum = sum(numbers)
    
    numbers_average = sum(numbers)/len(numbers)
    
    numbers_sum = sum(numbers)
    
    numbers_average = sum(numbers)/
<|/ file |>