if __name__ == '__main__': 
    
    import sys
    
    numbers = sys.argv[1:]
    
    numbers = map(int, numbers)
    
    numbers = map(str, numbers)
    
    numbers = map(float, numbers)
    
    numbers = map(tuple, numbers)
    
    numbers = map(list, numbers)
    
    numbers = map(set, numbers)
    
    numbers = map(frozenset, numbers)
    
    numbers = map(list, numbers)
    
    numbers = map(set, numbers)
    
    numbers = map(frozenset, numbers)
    
    numbers = map(list, numbers)
    

    """ A array that opens a list of numbers. """    
    
    numbers = map(int, numbers)
    
    numbers = map(str, numbers)
    
    numbers = map(float, numbers)
    
    numbers = map(tuple, numbers)
    
    numbers = map(list, numbers)
    
    numbers = map(set, numbers)
    
    numbers = map(frozenset, numbers)
    
    numbers = map(list, numbers)
    
    numbers = map(set, numbers)
    
    numbers = map(frozenset, numbers)
    
    numbers = map(list, numbers)
    
    numbers = map(set, numbers)
    
    numbers = 
<|/ file |>