#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A function that removes words.
    def clean(words):
        cleaned = []
        for word in words:
            cleaned.append(word.lower())
        return cleaned
    #A function that prints out the cleaned words.
    def printCleaned(words):
        for word in words:
            print(word)
    #A function that prints out the cleaned words.
    def printCleaned2(words):
        for word in words:
            print(word.lower())
    #A function that prints out the cleaned words.
    def printCleaned3(words):
        for word in words:
            print(word.upper())
    #A function that prints out the cleaned words.
    def printCleaned4(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned5(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned6(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned7(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned8(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned9(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned10(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned11(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned12(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned13(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned14(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned15(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned16(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned17(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned18(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned19(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned20(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned21(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned22(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned23(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned24(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned25(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned26(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned27(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned28(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned29(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned30(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned31(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned32(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned33(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned34(words):
        for word in words:
            print(word.title())
    #A function that prints out the cleaned words.
    def printCleaned35(words):
        for word in words:
            print(word.capitalize())
    #A function that prints out the cleaned words.
    def printCleaned36(words):
        for word in words:
            print(word.title())
    #