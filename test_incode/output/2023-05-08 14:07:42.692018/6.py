#!/usr/bin/python3
if __name__ == '__main__':
    #A function that converts user input.
    #It will take in user input from the command line
    #and convert it to a float.
    #It will also print a message to the screen saying the
    #user input was a float.
    #The function will also accept a float as an argument.
    #If the user inputs a string it will convert it to a float.
    #If the user inputs a number it will convert it to a float.
    #If the user inputs a boolean it will convert it to a float.
    #If the user inputs a list it will convert it to a float.
    #If the user inputs a dictionary it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
    #If the user inputs a tuple it will convert it to a float.
    #If the user inputs a set it will convert it to a float.
    #If the user inputs a frozenset it will convert it to a float.
