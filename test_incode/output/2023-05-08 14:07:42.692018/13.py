#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A program that requests a list of words.
    #The program will then print out each word in the list.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list to the screen.
    #The program will ask the user for input each time the program is run.
    #The program will then print the list 