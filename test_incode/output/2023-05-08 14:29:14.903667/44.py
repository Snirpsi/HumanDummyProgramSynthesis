#!/usr/bin/python3
url = "http://snirps.ddns.net"
if __name__ == '__main__':
    #A function that iterates over a http request.
    #The response is returned as a string.
    #The response is then parsed into a JSON object.
    #The JSON object is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a Python list.
    #The list is then converted to a Python dictionary.
    #The dictionary is then converted to a 

