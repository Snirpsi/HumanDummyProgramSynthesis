#!/usr/bin/python3
numbers = [1,2,3,4,9]
if __name__ == '__main__':
    #A minimal webserver that requests a list of numbers.
    #The server will respond with an HTML page that lists the numbers.
    #The client will then request the list of numbers and display them.
    #The client will also receive a request to stop the server.
    #The server will then exit the program.
    
    
    
    

