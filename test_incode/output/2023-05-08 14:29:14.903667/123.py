#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A minimal webserver that adds a list of words.
    #It will also print out the words in the list.
    #The server will also accept a GET request to display the list.
    #The server will also accept a POST request to add a new word.
    #The server will also accept a DELETE request to remove a word.
    #The server will also accept a PUT request to update a word.
    #The server will also accept a HEAD request to display the list of words.
    #The server will also accept a OPTIONS request to display the list of words.
    #The server will also accept a TRACE request to display the list of words.
    #The server will also accept a DELETE request to remove a word.
    #The server will also accept a GET request to display the list of words.
    #The server will also accept a POST request to add a new word.
    #The server will also accept a PUT request to update a word.
    #The server will also accept a HEAD request to display the list of words.
    #The server will also accept a OPTIONS request to display the list of words.
    #The server will also accept a TRACE request to display the list of words.
    #The server will also accept a DELETE request to remove a word.
    #The server will also accept a GET request to display the list of words.
    #The server will also accept a POST request to add a new word.
    #The server will also accept a PUT request to update a word.
    #The server will also accept a HEAD request to display the list of words.
    #The server will also accept a OPTIONS request to display the list of words.
    #The server will also accept a TRACE request to display the list of words.
    #The server will also accept a DELETE request to remove a word.
    #The server will also accept a GET request to display the list of words.
    #The server will also accept a POST request to add a new word.
    #The server will also accept a PUT request to update a word.
    #The server will also accept a HEAD request to display the list of words.
    #The server will also accept a OPTIONS request to display the list of words.
    #The server will also accept a TRACE request to display the list of words.
    #The server will also accept a DELETE request to remove a word.
    #The server will also accept a GET request to display the list of words.
    #The server will also accept a POST request to add a new word.
    #The server will also accept a PUT request to update a word.
    #The server will also accept a HEAD request to display the list of words.
    #The server will also accept a OPTIONS request to display the list of words.
    #The server will also accept a TRACE request to display the list of words.
    #The server will also accept a DELETE request to remove a word.
    #The server will also accept a GET request to display the list of words.
    #The server will also accept a POST request to add a new word.
    #The server will also accept a PUT request to update a word.
    #The server will also accept a HEAD request to display the list of words.
    #The server will also accept a OPTIONS request to display the list of words.
    #The server will also accept a TRACE request to display the list of words.
    #The server will also accept a DELETE request to remove a word.
    #The server will also accept a GET request to display the list of words.
    #The server will also accept a POST request to add a new word.
    #The server will also accept a PUT request to update a word.
    #The server will also accept a HEAD request to display the list of words.
    #The server will also accept a OPTIONS request to display the list of words.
    #The server will also accept a TRACE request to display the list of words.
    #The server will also accept a DELETE request to remove a word.
    #The server will also accept a GET request to display the list of words.
    #The server will also accept a POST request to add a new word.
    #The server will also accept a PUT request to update a word.
    #The server will also accept a HEAD request to display the list of words.
    #The server will also accept a OPTIONS request to display the list of words.
    #The server will also accept a TRACE request to display the list of words.
    #The server will also accept a DELETE request to remove a word.
    #The server will also accept a GET request to display the list of words.
    #The server will also accept a POST request to add a new word.
    #The server will also accept a PUT request to update a word.
    #The server will also accept a HEAD request to display the list of words.
    #The server will also accept a OPTIONS request to display the list of words.
    #The server will also accept a TRACE request to display the list of words.
    #The server will also accept a DELETE request to remove a word.
    #The server will also accept a GET request to display the list of words.
    #The server will also accept a POST request to add a new word.
    #The server will also accept a PUT request to update a word.
    #The server will also accept a HEAD request to display the list of words.
    #The server will also accept a OPTIONS request to display the list of words.
    #The server will also accept a TRACE request to display the list of words.
    #The server will also accept a DELETE request to remove a word.


