#!/usr/bin/python3
words = ["hello","world","!"]
if __name__ == '__main__':
    #A program that stores a list of words.
    #The program will ask the user for a word and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.
    #The program will then ask the user for a number between 1 and 5
    #and store it in a list.


