#!/usr/bin/python3
port = 8080
if __name__ == '__main__':
    #A function that removes a port.
    #It will remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also remove a port from the list of ports.
    #It will also 

