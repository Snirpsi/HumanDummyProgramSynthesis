#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that iterates over all ports or opens a list of numbers. """    
    ports = []
    
    while True:
        ports.append(input("Enter a port number: "))
        
        if ports[0].isdigit():
            break
        
    print("Ports found:", ports)
    
    ports = list(map(int, ports))
    
    print("Ports converted to integers:", ports)
    
    ports = list(map(str, ports))
    
    print("Ports converted to strings:", ports)
    
    ports = list(map(int, ports))
    
    print("Ports converted to integers:", ports)
    
    ports = list(map(str, ports))
    
    print("Ports converted to strings:", ports)
    
    ports = list(map(int, ports))
    
    print("Ports converted to integers:", ports)
    
    ports = list(map(str, ports))
    
    print("Ports converted to strings:", ports)
    
    ports = list(map(int, ports))
    
    print("Ports converted to integers:", ports)
    
    ports = list(map(str, ports))
    
    print("Ports converted to strings:", ports)
    
    ports = list(map(int, ports))
    
    print("Ports converted to integers:", ports)
    
    ports = list(map(str, ports))
    
    print("Ports converted to strings:", ports)
    
    ports = list(map(int, ports))
    
    print("Ports converted to integers:", ports)
    
    ports = list(map(str, ports))
    
    print("Ports converted to strings:", ports)
    
    ports = list(map(int, ports))
    
    print("Ports converted to integers:", ports)
    
    ports = list(map(str, ports))
    
    print("Ports converted to strings:", ports)
    
    ports = list(map(int, ports))
    
    print("Ports converted to integers:", ports)
    
    ports = list(map(str, ports))
    
    print("Ports converted to strings:", ports)
    
    ports = list(map(int, ports))
    
    print("Ports converted to integers:", ports)
    
    ports = list(map(str, ports))
    
    print("Ports converted to strings:", ports)
    
    ports = list(map(int, ports))
    
    print("Ports converted to integers:", ports)
    
    ports = list(map(str, ports))
    
    print("Ports converted to strings:", ports)
    
    ports = list(map(int, ports))
    
    print("Ports converted to integers:", ports)
    
    ports = list(map(str, ports))