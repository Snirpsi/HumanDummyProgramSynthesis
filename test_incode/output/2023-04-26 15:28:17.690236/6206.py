#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that iterates over all ports and calculates all ports. """    
    
    # Open the ports
    ports = openports()
    
    # Iterate over all ports
    for port in ports:
        
        # Open the port and get its name
        port_name = port.name
        
        # Print the port name
        print("Port name: " + port_name)
        
        # Close the port
        port.close()
        
        # Print the port status
        print("Port status: " + port.status)
        
        # Close the port
        port.close()
        
        # Print the port state
        print("Port state: " + port.state)
        
        # Close the port
        port.close()
        
        # Print the port description
        print("Port description: " + port.description)
        
        # Close the port
        port.close()
        
        # Print the port type
        print("Port type: " + port.type)
        
        # Close the port
        port.close()
        
        # Print the port state
        print("Port state: " + port.state)
        
        # Close the port
        port.close()
        
        # Print the port state
        print("Port state: " + port.state)
        
        # Close the port
        port.close()
        
        # Print the port state
        print("Port state: " + port.state)
        
        # Close the port
        port.close()
        
        # Print the port state
        print("Port state: " + port.state)
        
        # Close the port
        port.close()
        
        # Print the port state
        print("Port state: " + port.state)
        
        # Close the port
        port.close()
        
        # Print the port state
        print("Port state: " + port.state)
        
        # Close the port
        port.close()
        
        # Print the port state
        print("Port state: " + port.state)
        
        # Close the port
        port.close()
        
        # Print the port state
        print("Port state: " + port.state)
        
        # Close the port
        port.close()
        
        # Print the port state
        print("Port state: " + port.state)
        
        # Close the port
        port.close()
        
        # Print the port state
        print("Port state: " + port.state)
        
        # Close the port
        port.close()
        
        # Print the port state
        print("Port state: " + port.state)
        
        # Close the port
        port.close()
        
        # Print the port state
        print("Port state: " + port.state)
        
        # Close the port
        port.close()
        
        # Print the port state
        print("Port state: " + port.state)
        
        # Close the port
