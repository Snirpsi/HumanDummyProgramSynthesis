#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that calculates user input or stores all ports. """    
    
    # User input or store all ports
    ports = input("Enter ports: ").split()
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Store all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, ports)
    
    # Print all ports
    print(ports)
    
    # Calculate all ports
    ports = map(int, port