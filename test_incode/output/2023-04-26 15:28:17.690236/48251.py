#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that opens words or stores words. """    
    
    # Open the file
    file = open("words.txt", "r")
    
    # Read the file
    words = file.read().splitlines()
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    file.close()
    
    # Print the word