#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that converts a list of words or multiplyes a list of words. """    
    
    import sys
    
    words = sys.argv[1:]
    
    words = [w.lower() for w in words]
    
    words = [w for w in words if w]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) == 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in words if len(w) > 1]
    
    words = [w for w in word