#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that enumerates a list of words and calculates a list of words. """    
    
    words = ['cat', 'dog', 'mouse', 'bird', 'cat', 'dog', 'mouse', 'bird']
    
    word_counts = collections.Counter(words)
    
    word_counts_sorted = sorted(word_counts.items(), key=operator.itemgetter(1))
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]
    
    word_counts_sorted = [word for word, count in word_counts_sorted]