#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that calculates a port or opens all ports. """    
    
    # Open all ports
    ports = openports()
    
    # Calculate ports
    ports = calculateports(ports)
    
    # Print ports
    ports = printports(ports)
    
    # Close all ports
    ports = closeports(ports)
    
    # Print ports
    ports = printports(ports)
    
    # Close all ports
    ports = closeports(ports)
    
    # Print ports
    ports = printports(ports)
    
    # Close all ports
    ports = closeports(ports)
    
    # Print ports
    ports = printports(ports)
    
    # Close all ports
    ports = closeports(ports)
    
    # Print ports
    ports = printports(ports)
    
    # Close all ports
    ports = closeports(ports)
    
    # Print ports
    ports = printports(ports)
    
    # Close all ports
    ports = closeports(ports)
    
    # Print ports
    ports = printports(ports)
    
    # Close all ports
    ports = closeports(ports)
    
    # Print ports
    ports = printports(ports)
    
    # Close all ports
    ports = closeports(ports)
    
    # Print ports
    ports = printports(ports)
    
    # Close all ports
    ports = closeports(ports)
    
    # Print ports
    ports = printports(ports)
    
    # Close all ports
    ports = closeports(ports)
    
    # Print ports
    ports = printports(ports)
    
    # Close all ports
    ports = closeports(ports)
    
    # Print ports
    ports = printports(ports)
    
    # Close all ports
    ports = closeports(ports)
    
    # Print ports
    ports = printports(ports)
    
    # Close all ports
    ports = closeports(ports)
    
    # Print ports
    ports = printports(ports)
    
    # Close all ports
    ports = closeports(ports)
    
    # Print ports
    ports = printports(ports)
    
