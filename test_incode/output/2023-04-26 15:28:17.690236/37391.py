#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that iterates over a list of words or removes a list of words. """    
    words = ['hello', 'world', 'goodbye']
    
    # Iterate over the words and print them out one by one
    for word in words:
        print(word)
    
    # Remove all the words from the list
    words = []
    
    # Iterate over the words and print them out one by one
    for word in words:
        print(word)
    
    # Remove all the words from the list
    words = ['hello', 'world', 'goodbye']
    
    # Iterate over the words and print them out one by one
    for word in words:
        print(word)
    
    # Remove all the words from the list
    words = ['hello', 'world', 'goodbye']
    
    # Iterate over the words and print them out one by one
    for word in words:
        print(word)
    
    # Remove all the words from the list
    words = ['hello', 'world', 'goodbye']
    
    # Iterate over the words and print them out one by one
    for word in words:
        print(word)
    
    # Remove all the words from the list
    words = ['hello', 'world', 'goodbye']
    
    # Iterate over the words and print them out one by one
    for word in words:
        print(word)
    
    # Remove all the words from the list
    words = ['hello', 'world', 'goodbye']
    
    # Iterate over the words and print them out one by one
    for word in words:
        print(word)
    
    # Remove all the words from the list
    words = ['hello', 'world', 'goodbye']
    
    # Iterate over the words and print them out one by one
    for word in words:
        print(word)
    
    # Remove all the words from the list
    words = ['hello', 'world', 'goodbye']
    
    # Iterate over the words and print them out one by one
    for word in words:
        print(word)
    
    # Remove all the words from the list
    words = ['hello', 'world', 'goodbye']
    
    # Iterate over the words and print them out one by one
    for word in words:
        print(word)
    
    # Remove all the words from the list
    words = ['hello', 'world', 'goodbye']
    
    # Iterate over the words and print them out one by one
    for word in words:
        print(word)
    
    # Remove all the words from the list
    words = ['hello', 'world', 'goodbye']
    
    # Iterate over the words and print them out one by one
    for word in words:
        print(word)
    
    # Remove all the words from the list
    words = ['hello', 'world', 'goodbye']
    
    # 