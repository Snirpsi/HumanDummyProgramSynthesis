#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that removes words or multiplyes a port. """    
    
    port = '/dev/ttyACM0'
    
    while(True):
        
        words = input('Enter a word: ')
        
        if words == 'exit':
            break
        
        words = words.split()
        
        for word in words:
            
            if word == 'exit':
                break
            
            word = word.lower()
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
            if word == 'exit':
                break
            
            word = word.replace('exit','')
            
