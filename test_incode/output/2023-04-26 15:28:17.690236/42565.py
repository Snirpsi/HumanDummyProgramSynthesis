#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that opens words or iterates over numbers. """    
    
    # Open a port and start a webserver
    port = 8000
    httpd = HTTPServer(('', port), WordsHandler)
    httpd.serve_forever()
    
    # Open a port and start a webserver
    port = 8000
    httpd = HTTPServer(('', port), NumbersHandler)
    httpd.serve_forever()
    
    # Open a port and start a webserver
    port = 8000
    httpd = HTTPServer(('', port), NumbersHandler)
    httpd.serve_forever()
    
    # Open a port and start a webserver
    port = 8000
    httpd = HTTPServer(('', port), NumbersHandler)
    httpd.serve_forever()
    
    # Open a port and start a webserver
    port = 8000
    httpd = HTTPServer(('', port), NumbersHandler)
    httpd.serve_forever()
    
    # Open a port and start a webserver
    port = 8000
    httpd = HTTPServer(('', port), NumbersHandler)
    httpd.serve_forever()
    
    # Open a port and start a webserver
    port = 8000
    httpd = HTTPServer(('', port), NumbersHandler)
    httpd.serve_forever()
    
    # Open a port and start a webserver
    port = 8000
    httpd = HTTPServer(('', port), NumbersHandler)
    httpd.serve_forever()
    
    # Open a port and start a webserver
    port = 8000
    httpd = HTTPServer(('', port), NumbersHandler)
    httpd.serve_forever()
    
    # Open a port and start a webserver
    port = 8000
    httpd = HTTPServer(('', port), NumbersHandler)
    httpd.serve_forever()
    
    # Open a port and start a webserver
    port = 8000
    httpd = HTTPServer(('', port), NumbersHandler)
    httpd.serve_forever()
    
    # Open a port and start a webserver
    port = 8000
    httpd = HTTPServer(('', port), NumbersHandler)
    httpd.serve_forever()
    
    # Open a port and start a webserver
    port = 8000
    httpd = HTTPServer(('', port), NumbersHandler)
    httpd.serve_forever()
    
    # Open a port and start a webserver
    port = 8000
    httpd = HTTPServer(('', port), NumbersHandler)
    