#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that multiplyes a port and calculates words. """    
    while True:
        port = int(input('Enter a port number: '))
        words = int(input('Enter the number of words: '))
        words_per_line = words / 2
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = words_per_line if words_per_line > 0 else 1
        
        words_per_line = 