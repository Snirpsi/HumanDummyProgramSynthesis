#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that adds words or stores words. """    
    
    # Start a web server
    server = HTTPServer(('', 80), WordsHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
    server = HTTPServer(('', 80), WordsStoreHandler)
    
    # Start a web server
