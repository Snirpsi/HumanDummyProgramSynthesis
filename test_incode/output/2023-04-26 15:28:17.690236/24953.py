#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that adds user input. """    
    
    # Parse command line arguments
    parser = argparse.ArgumentParser(description='A minimal webserver that adds user input.')
    parser.add_argument('port', metavar='PORT', type=int, help='port to listen on')
    args = parser.parse_args()
    
    # Start a web server
    httpd = HTTPServer(('', args.port), SimpleHTTPRequestHandler)
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current time every second
    threading.Thread(target=httpd.serve_forever).start()
    
    # Start a thread that prints out the current 