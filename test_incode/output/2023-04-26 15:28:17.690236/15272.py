#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates words or converts user input. """    
    
    # Get the user input
    user_input = input("Enter a word: ")
    
    # Convert user input to lowercase
    user_input = user_input.lower()
    
    # Print the result
    print("The word is", user_input)
    
    # Calculate the length of the word
    word_length = len(user_input)
    
    # Print the length
    print("The length of the word is", word_length)
    
    # Calculate the average length of the word
    word_average = word_length / len(user_input)
    
    # Print the average length
    print("The average length of the word is", word_average)
    
    # Calculate the average length of the word
    word_average = word_length / len(user_input)
    
    # Print the average length
    print("The average length of the word is", word_average)
    
    # Calculate the average length of the word
    word_average = word_length / len(user_input)
    
    # Print the average length
    print("The average length of the word is", word_average)
    
    # Calculate the average length of the word
    word_average = word_length / len(user_input)
    
    # Print the average length
    print("The average length of the word is", word_average)
    
    # Calculate the average length of the word
    word_average = word_length / len(user_input)
    
    # Print the average length
    print("The average length of the word is", word_average)
    
    # Calculate the average length of the word
    word_average = word_length / len(user_input)
    
    # Print the average length
    print("The average length of the word is", word_average)
    
    # Calculate the average length of the word
    word_average = word_length / len(user_input)
    
    # Print the average length
    print("The average length of the word is", word_average)
    
    # Calculate the average length of the word
    word_average = word_length / len(user_input)
    
    # Print the average length
    print("The average length of the word is", word_average)
    
    # Calculate the average length of the word
    word_average = word_length / len(user_input)
    
    # Print the average length
    print("The average length of the word is", word_average)
    
    # Calculate the average length of the word
    word_average = word_length / len(user_input)
    
    # Print the average length
    print("The average length of the word is", word_average)
    
    # Calculate the average length of the word
    word_average = word_length / len(user_input)
    
    # Print the average length
    print("The average length of the word is", word_average)
    
    # Calculate the average length of the word
    word_average = word_length / len(user_input)
    