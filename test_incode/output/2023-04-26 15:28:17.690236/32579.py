#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that converts all ports or iterates over a list of words. """    
    ports = ports.split(',')
    words = []
    for port in ports:
        words.extend(iter_words(port))
    
    words = list(set(words))
    
    word_count = len(words)
    
    word_list = list(words)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    word_list.sort(key=len)
    
    word_count = len(word_list)
    
    word_list = list(word_list)
    
    