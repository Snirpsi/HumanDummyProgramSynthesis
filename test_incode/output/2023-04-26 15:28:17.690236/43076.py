#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that stores a port or prints a list of words. """    
    
    port = 0
    while True:
        port = input("Enter a port number: ")
        if port == '':
            break
        
        words = list("Hello World!")
        
        print("\nPrinting words from port " + str(port))
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again)")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for word in words:
            print(word)
        
        print("\nPrinting words from port " + str(port) + " (again) again again")
        for 