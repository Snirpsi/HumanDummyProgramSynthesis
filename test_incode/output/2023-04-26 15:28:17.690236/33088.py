#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that calculates user input and opens words. """    
    
    # start a server
    server = HTTPServer(('', 8080), Handler)
    
    # start a web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait a little bit for server to start serving
    time.sleep(5)
    
    # start a server
    server = HTTPServer(('', 8080), Handler)
    
    # start a web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait a little bit for server to start serving
    time.sleep(5)
    
    # start a server
    server = HTTPServer(('', 8080), Handler)
    
    # start a web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait a little bit for server to start serving
    time.sleep(5)
    
    # start a server
    server = HTTPServer(('', 8080), Handler)
    
    # start a web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait a little bit for server to start serving
    time.sleep(5)
    
    # start a server
    server = HTTPServer(('', 8080), Handler)
    
    # start a web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait a little bit for server to start serving
    time.sleep(5)
    
    # start a server
    server = HTTPServer(('', 8080), Handler)
    
    # start a web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait a little bit for server to start serving
    time.sleep(5)
    
    # start a server
    server = HTTPServer(('', 8080), Handler)
    
    # start a web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait a little bit for server to start serving
    time.sleep(5)
    
    # start a server
    server = HTTPServer(('', 8080), Handler)
    
    # start a web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait a little bit for server to start serving
    time.sleep(5)
    
    # start a server
    server = HTTPServer(('', 8080), Handler)
    
    # start a web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait a little bit for server to start serving
    time.sleep(5)
    
    # start a server
    server = HTTPServer(('', 8080), Handler)
    
    # start a web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait a little bit for server to start serving
    time.sleep(5)
    