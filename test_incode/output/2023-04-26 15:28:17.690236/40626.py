#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds user input or opens a port. """    
    
    # Open a port if user input is not given
    if len(sys.argv) > 1:
        port = int(sys.argv[1])
    else:
        port = 5000
    
    # Open a port if user input is not given
    if len(sys.argv) > 2:
        port = int(sys.argv[2])
    else:
        port = 5001
    
    # Open a port if user input is not given
    if len(sys.argv) > 3:
        port = int(sys.argv[3])
    else:
        port = 5002
    
    # Open a port if user input is not given
    if len(sys.argv) > 4:
        port = int(sys.argv[4])
    else:
        port = 5003
    
    # Open a port if user input is not given
    if len(sys.argv) > 5:
        port = int(sys.argv[5])
    else:
        port = 5004
    
    # Open a port if user input is not given
    if len(sys.argv) > 6:
        port = int(sys.argv[6])
    else:
        port = 5005
    
    # Open a port if user input is not given
    if len(sys.argv) > 7:
        port = int(sys.argv[7])
    else:
        port = 5006
    
    # Open a port if user input is not given
    if len(sys.argv) > 8:
        port = int(sys.argv[8])
    else:
        port = 5007
    
    # Open a port if user input is not given
    if len(sys.argv) > 9:
        port = int(sys.argv[9])
    else:
        port = 5008
    
    # Open a port if user input is not given
    if len(sys.argv) > 10:
        port = int(sys.argv[10])
    else:
        port = 5009
    
    # Open a port if user input is not given
    if len(sys.argv) > 11:
        port = int(sys.argv[11])
    else:
        port = 5010
    
    # Open a port if user input is not given
    if len(sys.argv) > 12:
        port = int(sys.argv[12])
    else:
        port = 5011
    
    # Open a port if user input is not given
    if len(sys.argv) > 13:
        port = int(sys.argv[13])
    else:
        port = 5012
    
    # Open a port if user input is not given
    if len(sys.argv) > 14:
        port = int(sys.argv[14])
    else:
        port = 5013
    
    # Open a port if user input is not given
    if len(sys.argv) > 15:
        port = int(sys.argv[15])
    else:
        port = 5014
    
    # Open a port if user input is not given
    if len(sys.argv) > 16:
        port = int(sys.argv[16])
    else:
        port = 5015
    
    # Open a port if user input is not given
    if len(sys.argv) > 