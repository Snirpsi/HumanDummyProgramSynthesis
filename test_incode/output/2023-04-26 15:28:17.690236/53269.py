#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts a port and removes words. """    
    import sys
    port = int(sys.argv[1])
    words = sys.argv[2:]
    
    words = [w for w in words if w != '']
    
    port = int(port)
    
    ports = [port for port in range(0,65536)]
    
    ports = [port for port in ports if port not in ports]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in ports]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in words]
    
    ports = [port for port in ports if port not in 