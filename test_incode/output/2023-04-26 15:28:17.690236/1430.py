#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that multiplyes a list of words or adds words. """    
    
    # Get the command line arguments
    args = sys.argv[1:]
    
    # Get the word list
    wordlist = args.pop(0)
    
    # Get the word count
    wordcount = len(wordlist)
    
    # Get the word multiplier 
    multiplier = args.pop(0)
    
    # Get the word addition 
    addition = args.pop(0)
    
    # Get the word subtraction 
    subtraction = args.pop(0)
    
    # Get the word multiplication 
    multiplication = args.pop(0)
    
    # Get the word division 
    division = args.pop(0)
    
    # Get the word remainder 
    remainder = args.pop(0)
    
    # Get the word division 
    division = args.pop(0)
    
    # Get the word remainder 
    remainder = args.pop(0)
    
    # Get the word division 
    division = args.pop(0)
    
    # Get the word remainder 
    remainder = args.pop(0)
    
    # Get the word division 
    division = args.pop(0)
    
    # Get the word remainder 
    remainder = args.pop(0)
    
    # Get the word division 
    division = args.pop(0)
    
    # Get the word remainder 
    remainder = args.pop(0)
    
    # Get the word division 
    division = args.pop(0)
    
    # Get the word remainder 
    remainder = args.pop(0)
    
    # Get the word division 
    division = args.pop(0)
    
    # Get the word remainder 
    remainder = args.pop(0)
    
    # Get the word division 
    division = args.pop(0)
    
    # Get the word remainder 
    remainder = args.pop(0)
    
    # Get the word division 
    division = args.pop(0)
    
    # Get the word remainder 
    remainder = args.pop(0)
    
    # Get the word division 
    division = args.pop(0)
    
    # Get the word remainder 
    remainder = args.pop(0)
    
    # Get the word division 
    division = args.pop(0)
    
    # Get the word remainder 
    remainder = args.pop(0)
    
    # Get the word division 
    division = args.pop(0)
    
    # Get the word remainder 
    remainder = args.pop(0)
    
    # Get the word division 
    division = args.pop(0)
    
    # Get the word remainder 
    remainder = args.pop(0)
    
    # Get the word division 
    division = args.pop(0)
    
    # Get the word remainder 
    remainder = args.pop(0)
    
    # Get the word division 