#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that removes all ports or removes numbers. """    
    while True:
        port = input("Enter a port number: ")
        if port == '':
            break
        if port.isdigit():
            port = int(port)
        else:
            ports.remove(port)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
        
    ports = list(ports)
    
    for port in ports:
        print("Removing port {}".format(port))
