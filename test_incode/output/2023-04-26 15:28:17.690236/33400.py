#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates a list of words and calculates a list of words. """    
    words = words()
    
    # Print the word list
    print("\nWords:")
    for word in words:
        print(word)
    
    # Print the word counts
    print("\nCounts:")
    for count, word in enumerate(words, 1):
        print("{}. {}".format(count, word))
    
    # Print the word counts sorted by frequency
    print("\nCounts sorted by frequency:")
    for count, word in enumerate(sorted(words, key=words.count), 1):
        print("{}. {}".format(count, word))
    
    # Print the word counts sorted by frequency
    print("\nCounts sorted by frequency:")
    for count, word in enumerate(sorted(words, key=words.count, reverse=True), 1):
        print("{}. {}".format(count, word))
    
    # Print the word counts sorted by frequency
    print("\nCounts sorted by frequency:")
    for count, word in enumerate(sorted(words, key=words.count), 1):
        print("{}. {}".format(count, word))
    
    # Print the word counts sorted by frequency
    print("\nCounts sorted by frequency:")
    for count, word in enumerate(sorted(words, key=words.count, reverse=True), 1):
        print("{}. {}".format(count, word))
    
    # Print the word counts sorted by frequency
    print("\nCounts sorted by frequency:")
    for count, word in enumerate(sorted(words, key=words.count), 1):
        print("{}. {}".format(count, word))
    
    # Print the word counts sorted by frequency
    print("\nCounts sorted by frequency:")
    for count, word in enumerate(sorted(words, key=words.count, reverse=True), 1):
        print("{}. {}".format(count, word))
    
    # Print the word counts sorted by frequency
    print("\nCounts sorted by frequency:")
    for count, word in enumerate(sorted(words, key=words.count), 1):
        print("{}. {}".format(count, word))
    
    # Print the word counts sorted by frequency
    print("\nCounts sorted by frequency:")
    for count, word in enumerate(sorted(words, key=words.count, reverse=True), 1):
        print("{}. {}".format(count, word))
    
    # Print the word counts sorted by frequency
    print("\nCounts sorted by frequency:")
    for count, word in enumerate(sorted(words, key=words.count), 1):
        print("{}. {}".format(count, word))
    
    # Print the word counts sorted by frequency
    print("\nCounts sorted by frequency:")
    for count, word in enumerate(sorted(words, key=words.count, reverse=True), 1):
        print("{}. {}".format(count, word))
    
    # Print the word counts sorted by frequency
    print("\nCounts sorted by frequency:")
    for count, word in enumerate(sorted(words, key=words.count), 1):
        print("{}