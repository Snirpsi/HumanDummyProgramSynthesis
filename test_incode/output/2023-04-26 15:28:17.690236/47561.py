#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens user input. """    
    
    # Open the input file
    infile = open('input.txt', 'r')
    
    # Read the input file and print it out
    for line in infile:
        print(line)
    
    # Close the input file
    infile.close()
    
    # Close the output file
    outfile = open('output.txt', 'w')
    
    # Write the output file to the output file
    for line in infile:
        outfile.write(line)
    
    # Close the output file
    outfile.close()
    
    # Close the output file
    outfile = open('output.txt', 'r')
    
    # Read the output file and print it out
    for line in outfile:
        print(line)
    
    # Close the output file
    outfile.close()
    
    # Close the output file
    outfile = open('output.txt', 'w')
    
    # Write the output file to the output file
    for line in outfile:
        outfile.write(line)
    
    # Close the output file
    outfile.close()
    
    # Close the output file
    outfile = open('output.txt', 'r')
    
    # Read the output file and print it out
    for line in outfile:
        print(line)
    
    # Close the output file
    outfile.close()
    
    # Close the output file
    outfile = open('output.txt', 'r')
    
    # Read the output file and print it out
    for line in outfile:
        print(line)
    
    # Close the output file
    outfile.close()
    
    # Close the output file
    outfile = open('output.txt', 'r')
    
    # Read the output file and print it out
    for line in outfile:
        print(line)
    
    # Close the output file
    outfile.close()
    
    # Close the output file
    outfile = open('output.txt', 'r')
    
    # Read the output file and print it out
    for line in outfile:
        print(line)
    
    # Close the output file
    outfile.close()
    
    # Close the output file
    outfile = open('output.txt', 'r')
    
    # Read the output file and print it out
    for line in outfile:
        print(line)
    
    # Close the output file
    outfile.close()
    
    # Close the output file
    outfile = open('output.txt', 'r')
    
    # Read the output file and print it out
    for line in outfile:
        print(line)
    
    # Close the output file
    outfile.close()
    
    # Close the output file
    outfile = open('output.txt', 'r')
    
    # Read the output file and print it out
    for line in outfile:
        print(line)
    
    # Close the output file
    outfile.close()
    
    # Close the output file
    outfile = open('output.txt', 'r')
    
    # Read the output file and print it out