#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that opens user input. """    
    
    # Open user input file
    file = open("input.txt", "r")
    
    # Read user input from file
    line = file.readline()
    
    # Print user input for debugging
    print(line)
    
    # Close file
    file.close()
    
    # Close program
    exit(0)
    
    # Print error if file can't be opened
    else:
        print("File can't be opened")
    
    # Print error if file can't be read
    else:
        print("File can't be read")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")
    
    # Print error if file can't be written
    else:
        print("File can't be written")