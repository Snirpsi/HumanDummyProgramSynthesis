#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that iterates over a list of numbers and adds all ports. """    
    
    ports = []
    for port in ports:
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in ports:
        
        ports.append(port)
    
    ports = list(set(ports))
    
    for port in 