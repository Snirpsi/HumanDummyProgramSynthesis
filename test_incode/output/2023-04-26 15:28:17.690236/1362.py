#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that returns a list of words. """    
    words = []
    
    # Read a file and append each word to our list
    with open('words.txt', 'r') as f:
        for line in f:
            words.append(line.strip())
            
    # Print the list of words
    print(words)
    
    # Print the length of the list
    print(len(words))
    
    # Print the length of the first word
    print(len(words[0]))
    
    # Print the length of the last word
    print(len(words[-1]))
    
    # Print the length of the first and last word
    print(len(words[0]) + len(words[-1]))
    
    # Print the length of the first and last word plus the length of the first word
    print(len(words[0]) + len(words[0]) + len(words[0]))
    
    # Print the length of the first and last word plus the length of the first and last word
    print(len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]))
    
    # Print the length of the first and last word plus the length of the first and last word
    print(len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]))
    
    # Print the length of the first and last word plus the length of the first and last word
    print(len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]))
    
    # Print the length of the first and last word plus the length of the first and last word
    print(len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]))
    
    # Print the length of the first and last word plus the length of the first and last word
    print(len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]))
    
    # Print the length of the first and last word plus the length of the first and last word
    print(len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]))
    
    # Print the length of the first and last word plus the length of the first and last word
    print(len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]))
    
    # Print the length of the first and last word plus the length of the first and last word
    print(len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]))
    
    # Print the length of the first and last word plus the length of the first and last word
    print(len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]) + len(words[0]))
    
    # Print the length of the first and last word plus the length of the first and last word