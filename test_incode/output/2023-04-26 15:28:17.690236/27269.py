#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that enumerates all ports. """    
    ports = []
    
    # enumerate all available ports
    for port in ports.__iter__():
        print(port)
    
    # enumerate all available ports
    for port in ports.__iter__(True):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(False):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        
    # enumerate all available ports
    for port in ports.__iter__(None):
        print(port)
        