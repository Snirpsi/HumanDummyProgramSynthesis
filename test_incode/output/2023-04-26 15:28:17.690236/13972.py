#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that iterates over a port. """    
    
    # Open a port and read its content
    port = serial.Serial('/dev/ttyACM0', 115200)
    port.readline()
    port.close()
    
    # Print the port content
    print(port.read())
    
    # Close the port
    port.close()
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # Close the serial port
    serial.Serial('/dev/ttyACM0', 115200, timeout=1)
    
    # 