#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds fruits or calculates a list of words. """    
    
    # Read command line arguments
    args = sys.argv[1:]
    
    # Print help if user does not provide any arguments
    if len(args) < 1:
        print("Usage: python3 add_fruit.py <fruit>")
        exit()
    
    # Check if user provided a fruit
    fruit = args[0]
    
    # Check if fruit already exists
    if fruit not in fruit.keys():
        print("Fruit does not exist")
        exit()
    
    # Add fruit to dictionary
    fruit[fruit] = True
    
    # Print fruit dictionary
    for key, value in fruit.items():
        print(key + ": " + str(value))
        
    # Print list of fruits
    for key, value in fruit.items():
        print(key + ": " + str(value))
        
    # Print all fruits in the dictionary
    for key, value in fruit.items():
        print(key + ": " + str(value))
        
    # Print all fruits in the dictionary in reverse order
    for key, value in fruit.items():
        print(key + ": " + str(value))
        
    # Print all fruits in the dictionary in reverse order and print their counts
    for key, value in fruit.items():
        print(key + ": " + str(value))
        
    # Print all fruits in the dictionary in reverse order and print their counts
    for key, value in fruit.items():
        print(key + ": " + str(value))
        
    # Print all fruits in the dictionary in reverse order and print their counts
    for key, value in fruit.items():
        print(key + ": " + str(value))
        
    # Print all fruits in the dictionary in reverse order and print their counts
    for key, value in fruit.items():
        print(key + ": " + str(value))
        
    # Print all fruits in the dictionary in reverse order and print their counts
    for key, value in fruit.items():
        print(key + ": " + str(value))
        
    # Print all fruits in the dictionary in reverse order and print their counts
    for key, value in fruit.items():
        print(key + ": " + str(value))
        
    # Print all fruits in the dictionary in reverse order and print their counts
    for key, value in fruit.items():
        print(key + ": " + str(value))
        
    # Print all fruits in the dictionary in reverse order and print their counts
    for key, value in fruit.items():
        print(key + ": " + str(value))
        
    # Print all fruits in the dictionary in reverse order and print their counts
    for key, value in fruit.items():
        print(key + ": " + str(value))
        
    # Print all fruits in the dictionary in reverse order and print their counts
    for key, value in fruit.items():
        print(key + ": " + str(value))
        
    # Print all fruits in the dictionary in reverse order and 