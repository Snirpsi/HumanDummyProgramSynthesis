#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that enumerates a port or converts a list of words. """    
    ports = ports.split(',')
    ports = list(map(lambda port: port.strip(), ports))
    ports = list(filter(lambda port: len(port) > 0, ports))
    ports = list(map(lambda port: int(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), ports))
    ports = list(map(lambda port: str(port), 