#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that calculates all ports. """    
    
    ports = []
    
    for port in range(1, 65535):
        ports.append(port)
    
    ports.sort()
    
    ports_str = '\n'.join(map(str, ports))
    
    print('\n'.join(['Port ' + str(port) for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is open!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports]))
    print('\n'.join(['Port ' + str(port) + ' is closed!' for port in ports