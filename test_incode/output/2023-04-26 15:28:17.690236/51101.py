#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that adds all ports or converts numbers. """    
    ports = [int(p) for p in sys.argv[1:]]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p]
    ports = [str(p) for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports.reverse()
    ports = [str(p) for p in ports]
    ports = [p for p in ports if p > 0]