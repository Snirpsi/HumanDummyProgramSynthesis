#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that opens a list of numbers or iterates over fruits. """    
    
    fruits = ['apple', 'banana', 'cherry', 'orange', 'pear']
    
    # Open the browser
    browser = Browser()
    
    # Open the list
    browser.open_list_of(fruits)
    
    # Open the fruit
    browser.open_list_of(fruits)
    
    # Iterate over the fruit
    for fruit in fruits:
        browser.open_list_of(fruits)
        
    # Close the browser
    browser.close_list_of()
    
    # Close the server
    server.close_server()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close the terminal
    terminal.close_terminal()
    
    # Close 