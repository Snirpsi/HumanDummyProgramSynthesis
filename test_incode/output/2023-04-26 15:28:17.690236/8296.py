#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that converts a list of words and opens words. """    
    
    words = []
    
    for line in sys.stdin:
        line = line.strip()
        
        if line == "":
            continue
        
        words.append(line)
    
    words = " ".join(words)
    
    wordlist = words.split()
    
    wordlist = []
    
    for word in wordlist:
        wordlist.append(word.lower())
    
    wordlist = " ".join(wordlist)
    
    wordlist = wordlist.split()
    
    wordlist = []
    
    for word in wordlist:
        wordlist.append(word.lower())
    
    wordlist = " ".join(wordlist)
    
    wordlist = wordlist.split()
    
    wordlist = []
    
    for word in wordlist:
        wordlist.append(word.lower())
    
    wordlist = " ".join(wordlist)
    
    wordlist = wordlist.split()
    
    wordlist = []
    
    for word in wordlist:
        wordlist.append(word.lower())
    
    wordlist = " ".join(wordlist)
    
    wordlist = wordlist.split()
    
    wordlist = []
    
    for word in wordlist:
        wordlist.append(word.lower())
    
    wordlist = " ".join(wordlist)
    
    wordlist = wordlist.split()
    
    wordlist = []
    
    for word in wordlist:
        wordlist.append(word.lower())
    
    wordlist = " ".join(wordlist)
    
    wordlist = wordlist.split()
    
    wordlist = []
    
    for word in wordlist:
        wordlist.append(word.lower())
    
    wordlist = " ".join(wordlist)
    
    wordlist = wordlist.split()
    
    wordlist = []
    
    for word in wordlist:
        wordlist.append(word.lower())
    
    wordlist = " ".join(wordlist)
    
    wordlist = wordlist.split()
    
    wordlist = []
    
    for word in wordlist:
        wordlist.append(word.lower())
    
    wordlist = " ".join(wordlist)
    
    wordlist = wordlist.split()
    
    wordlist = []
    
    for word in wordlist:
        wordlist.append(word.lower())
    
    wordlist = " ".join(wordlist)
    
    wordlist = wordlist.split()
    
    wordlist = []
    
    for word in wordlist:
        wordlist.append(word.lower())
    
    wordlist = " ".join(wordlist)
    
    wordlist = wordlist.split()
    
    wordlist = []
    
    for word in wordlist:
        wordlist.append(word.lower())
    
    wordlist = " ".join(wordlist)
    
    wordlist = wordlist.split()
    
    wordlist = []
    
    for word in wordlist:
        wordlist.append(word.lower())
