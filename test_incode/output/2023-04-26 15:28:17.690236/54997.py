#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that enumerates a port or converts a port. """    
    
    port = '/dev/ttyUSB0'
    
    if port == '/dev/ttyUSB0':
        port = '/dev/ttyUSB1'
    
    ports = [port]
    
    ports_iter = enumerate(ports)
    
    for port, port_info in ports_iter:
        print('Port %s is %s' % (port, port_info))
        
    ports_iter = enumerate(ports, 1)
    
    for port, port_info in ports_iter:
        print('Port %s is %s' % (port, port_info))
        
    ports_iter = enumerate(ports, 2)
    
    for port, port_info in ports_iter:
        print('Port %s is %s' % (port, port_info))
        
    ports_iter = enumerate(ports, 3)
    
    for port, port_info in ports_iter:
        print('Port %s is %s' % (port, port_info))
        
    ports_iter = enumerate(ports, 4)
    
    for port, port_info in ports_iter:
        print('Port %s is %s' % (port, port_info))
        
    ports_iter = enumerate(ports, 5)
    
    for port, port_info in ports_iter:
        print('Port %s is %s' % (port, port_info))
        
    ports_iter = enumerate(ports, 6)
    
    for port, port_info in ports_iter:
        print('Port %s is %s' % (port, port_info))
        
    ports_iter = enumerate(ports, 7)
    
    for port, port_info in ports_iter:
        print('Port %s is %s' % (port, port_info))
        
    ports_iter = enumerate(ports, 8)
    
    for port, port_info in ports_iter:
        print('Port %s is %s' % (port, port_info))
        
    ports_iter = enumerate(ports, 9)
    
    for port, port_info in ports_iter:
        print('Port %s is %s' % (port, port_info))
        
    ports_iter = enumerate(ports, 10)
    
    for port, port_info in ports_iter:
        print('Port %s is %s' % (port, port_info))
        
    ports_iter = enumerate(ports, 11)
    
    for port, port_info in ports_iter:
        print('Port %s is %s' % (port, port_info))
        
    ports_iter = enumerate(ports, 12)
    
    for port, port_info in ports_iter:
        print('Port %s is %s' % (port, port_info))
        
    ports_iter = enumerate(ports, 13)
    
    for port, port_info in ports_iter:
        print('Port %s is %s' % (port, port_info))
        
    ports_iter = enumerate(ports, 14)
