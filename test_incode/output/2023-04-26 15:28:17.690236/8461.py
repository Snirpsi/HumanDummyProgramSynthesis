#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that calculates a list of words or returns all ports. """    
    
    ports = []
    
    # Open a socket to listen for connections
    server_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    server_socket.bind(('', 0))
    server_socket.listen(1)
    
    # Accept the connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then close the socket
    (client_socket, address) = server_socket.accept()
    
    # Accept a connection and then 