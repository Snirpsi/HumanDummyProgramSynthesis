#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that adds user input. """    
    
    # Create a list to store user input
    user_input = []
    
    # Get the user input
    while True:
        user_input.append(input("Enter a number: "))
    
    # Print the user input
    print("The user input is:", user_input)
    
    # Print the result
    print("The sum of the user input is:", sum(user_input))
    
    # Print the result
    print("The product of the user input is:", product(user_input))
    
    # Print the result
    print("The quotient of the user input is:", quotient(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print the result
    print("The remainder of the user input is:", remainder(user_input))
    
    # Print 