#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that iterates over user input and iterates over a port. """    
    
    # Initialize a port scanner object
    scanner = PortScanner()
    
    # Iterate over ports
    for port in scanner.ports:
        # Print the port
        print(port)
    
    # Close the scanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # Close the portscanner
    scanner.close()
    
    # 