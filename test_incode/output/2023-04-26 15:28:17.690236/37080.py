#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that enumerates a list of numbers. """    
    
    # Create a list with 10 numbers, from 1 to 10
    numbers = [x for x in range(1, 11)]
    
    # Print the list with enumerate and print a line for each number
    for number, index in enumerate(numbers, 1):
        print("Number {}: {}".format(number, index))
    
    # Print the list with enumerate and print a line for each number starting at 1
    for number, index in enumerate(numbers, 1):
        print("Number {}: {}".format(number, index))
    
    # Print the list with enumerate and print a line for each number starting at 2
    for number, index in enumerate(numbers, 2):
        print("Number {}: {}".format(number, index))
    
    # Print the list with enumerate and print a line for each number starting at 3
    for number, index in enumerate(numbers, 3):
        print("Number {}: {}".format(number, index))
    
    # Print the list with enumerate and print a line for each number starting at 4
    for number, index in enumerate(numbers, 4):
        print("Number {}: {}".format(number, index))
    
    # Print the list with enumerate and print a line for each number starting at 5
    for number, index in enumerate(numbers, 5):
        print("Number {}: {}".format(number, index))
    
    # Print the list with enumerate and print a line for each number starting at 6
    for number, index in enumerate(numbers, 6):
        print("Number {}: {}".format(number, index))
    
    # Print the list with enumerate and print a line for each number starting at 7
    for number, index in enumerate(numbers, 7):
        print("Number {}: {}".format(number, index))
    
    # Print the list with enumerate and print a line for each number starting at 8
    for number, index in enumerate(numbers, 8):
        print("Number {}: {}".format(number, index))
    
    # Print the list with enumerate and print a line for each number starting at 9
    for number, index in enumerate(numbers, 9):
        print("Number {}: {}".format(number, index))
    
    # Print the list with enumerate and print a line for each number starting at 10
    for number, index in enumerate(numbers, 10):
        print("Number {}: {}".format(number, index))
    
    # Print the list with enumerate and print a line for each number starting at 11
    for number, index in enumerate(numbers, 11):
        print("Number {}: {}".format(number, index))
    
    # Print the list with enumerate and print a line for each number starting at 12
    for number, index in enumerate(numbers, 12):
        print("Number {}: {}".format(number, index))
    
    # Print the list with enumerate and print a line for each number starting at 13
    for number, index in enumerate(numbers, 13):
        print("Number {}: {}".format(number, index))
    
    # Print the list with enumerate 