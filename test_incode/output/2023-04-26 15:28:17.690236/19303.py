#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that adds a list of numbers and enumerates words. """    
    words = []
    numbers = []
    while True:
        number = int(input("Enter a number: "))
        words.append(number)
        numbers.append(number)
        print("The numbers are:", numbers)
        print("The words are:", words)
        print("The total is:", sum(numbers))
        print("The average is:", sum(numbers) / len(numbers))
        print("The worst case is:", max(numbers))
        print("The worst case is:", min(numbers))
        print("The worst case is:", sum(numbers) ** 0.5)
        print("The worst case is:", sum(numbers) ** len(numbers))
        print("The worst case is:", sum(numbers) ** len(numbers) / len(numbers))
        print("The worst case is:", sum(numbers) ** len(numbers) / len(numbers) / len(numbers))
        print("The worst case is:", sum(numbers) ** len(numbers) / len(numbers) / len(numbers) / len(numbers))
        print("The worst case is:", sum(numbers) ** len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers))
        print("The worst case is:", sum(numbers) ** len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers))
        print("The worst case is:", sum(numbers) ** len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers))
        print("The worst case is:", sum(numbers) ** len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers))
        print("The worst case is:", sum(numbers) ** len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers))
        print("The worst case is:", sum(numbers) ** len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers))
        print("The worst case is:", sum(numbers) ** len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers))
        print("The worst case is:", sum(numbers) ** len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers))
        print("The worst case is:", sum(numbers) ** len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers))
        print("The worst case is:", sum(numbers) ** len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers))
        print("The worst case is:", sum(numbers) ** len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers) / len(numbers))