#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that multiplyes a port or stores user input. """    
    
    port = int(input("Enter a port: "))
    
    if port > 0 and port < 65535:
        
        port = int(input("Enter a port between 1 and 65535: "))
        
        if port > 0 and port < 65535:
            
            port = int(input("Enter a port between 1 and 65535: "))
            
            if port > 0 and port < 65535:
                
                port = int(input("Enter a port between 1 and 65535: "))
                
                if port > 0 and port < 65535:
                    
                    port = int(input("Enter a port between 1 and 65535: "))
                    
                    if port > 0 and port < 65535:
                        
                        port = int(input("Enter a port between 1 and 65535: "))
                        
                        if port > 0 and port < 65535:
                            
                            port = int(input("Enter a port between 1 and 65535: "))
                            
                            if port > 0 and port < 65535:
                                
                                port = int(input("Enter a port between 1 and 65535: "))
                                
                                if port > 0 and port < 65535:
                                    
                                    port = int(input("Enter a port between 1 and 65535: "))
                                    
                                    if port > 0 and port < 65535:
                                        
                                        port = int(input("Enter a port between 1 and 65535: "))
                                        
                                        if port > 0 and port < 65535:
                                            
                                            port = int(input("Enter a port between 1 and 65535: "))
                                            
                                            if port > 0 and port < 65535:
                                                
                                                port = int(input("Enter a port between 1 and 65535: "))
                                                
                                                if port > 0 and port < 65535:
                                                    
                                                    port = int(input("Enter a port between 1 and 65535: "))
                                                    
                                                    if port > 0 and port < 65535:
                                                        
                                                        port = int(input("Enter a port between 1 and 65535: "))
                                                        
                                                        if port > 0 and port < 65535:
                                                            
                                                            port = int(input("Enter a port between 1 and 65535: "))
                                                            
                                                            if port > 0 and port < 65535:
                                                                
                                                                port = int(input("Enter a port between 1 and 65535: "))
                                                                
                                                                if port > 0 and port < 65535:
                                                                    
                                                                    port = int(input("Enter a port between 1 and 65535: "))
                                                                    
                                                                    if port > 0 and port < 65535:
                                                                        
                                                                        port = int(input("Enter a port between 1 and 65535: "))
                                                                        
                                                                        if port > 