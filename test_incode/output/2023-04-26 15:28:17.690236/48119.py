#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that multiplyes all ports or enumerates numbers. """    
    while True:
        print("Starting to enumerate ports...")
        ports = enumerate_ports()
        print("Enumerated ports: {}".format(ports))
        
        print("Starting to multiply ports...")
        ports = multiply_ports(ports)
        print("Multiply ports: {}".format(ports))
        
        print("Starting to enumerate numbers...")
        numbers = enumerate_numbers(ports)
        print("Enumerated numbers: {}".format(numbers))
        
        print("Starting to multiply numbers...")
        numbers = multiply_numbers(numbers)
        print("Multiply numbers: {}".format(numbers))
        
        print("Starting to enumerate numbers...")
        numbers = enumerate_numbers(ports)
        print("Enumerated numbers: {}".format(numbers))
        
        print("Starting to multiply numbers...")
        numbers = multiply_numbers(numbers)
        print("Multiply numbers: {}".format(numbers))
        
        print("Starting to enumerate numbers...")
        numbers = enumerate_numbers(ports)
        print("Enumerated numbers: {}".format(numbers))
        
        print("Starting to multiply numbers...")
        numbers = multiply_numbers(numbers)
        print("Multiply numbers: {}".format(numbers))
        
        print("Starting to enumerate numbers...")
        numbers = enumerate_numbers(ports)
        print("Enumerated numbers: {}".format(numbers))
        
        print("Starting to multiply numbers...")
        numbers = multiply_numbers(numbers)
        print("Multiply numbers: {}".format(numbers))
        
        print("Starting to enumerate numbers...")
        numbers = enumerate_numbers(ports)
        print("Enumerated numbers: {}".format(numbers))
        
        print("Starting to multiply numbers...")
        numbers = multiply_numbers(numbers)
        print("Multiply numbers: {}".format(numbers))
        
        print("Starting to enumerate numbers...")
        numbers = enumerate_numbers(ports)
        print("Enumerated numbers: {}".format(numbers))
        
        print("Starting to multiply numbers...")
        numbers = multiply_numbers(numbers)
        print("Multiply numbers: {}".format(numbers))
        
        print("Starting to enumerate numbers...")
        numbers = enumerate_numbers(ports)
        print("Enumerated numbers: {}".format(numbers))
        
        print("Starting to multiply numbers...")
        numbers = multiply_numbers(numbers)
        print("Multiply numbers: {}".format(numbers))
        
        print("Starting to enumerate numbers...")
        numbers = enumerate_numbers(ports)
        print("Enumerated numbers: {}".format(numbers))
        
        print("