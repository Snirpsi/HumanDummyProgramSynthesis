#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that returns a list of words and iterates over a list of numbers. """    
    words = []
    numbers = []
    
    # Iterate over all numbers and add them to the list
    for number in range(1, 101):
        numbers.append(number)
        
    # Iterate over all words and add them to the list
    for word in ["cat", "dog", "mouse"]:
        words.append(word)
        
    # Print the list
    print("The numbers are:")
    print(numbers)
    print("The words are:")
    print(words)
    
    # Iterate over all numbers and add them to the list
    for number in numbers:
        print("The number is", number)
        
    # Iterate over all words and add them to the list
    for word in words:
        print("The word is", word)
        
    # Iterate over all numbers and add them to the list
    for number in numbers:
        print("The number is", number)
        
    # Iterate over all words and add them to the list
    for word in words:
        print("The word is", word)
        
    # Iterate over all numbers and add them to the list
    for number in numbers:
        print("The number is", number)
        
    # Iterate over all words and add them to the list
    for word in words:
        print("The word is", word)
        
    # Iterate over all numbers and add them to the list
    for number in numbers:
        print("The number is", number)
        
    # Iterate over all words and add them to the list
    for word in words:
        print("The word is", word)
        
    # Iterate over all numbers and add them to the list
    for number in numbers:
        print("The number is", number)
        
    # Iterate over all words and add them to the list
    for word in words:
        print("The word is", word)
        
    # Iterate over all numbers and add them to the list
    for number in numbers:
        print("The number is", number)
        
    # Iterate over all words and add them to the list
    for word in words:
        print("The word is", word)
        
    # Iterate over all numbers and add them to the list
    for number in numbers:
        print("The number is", number)
        
    # Iterate over all words and add them to the list
    for word in words:
        print("The word is", word)
        
    # Iterate over all numbers and add them to the list
    for number in numbers:
        print("The number is", number)
        
    # Iterate over all words and add them to the list
    for word in words:
        print("The word is", word)
        
    # Iterate over all numbers and add them to the list
    for number in numbers:
        print("The number is", 