#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that opens a list of numbers. """    
    
    numbers = range(1,10)
    
    while True:
        
        # Print the numbers to the screen
        for number in numbers:
            print(number)
            
        # Wait for a keypress
        pressed_key = input('Press any key to continue: ')
        
        # Check if the user pressed a key
        if pressed_key == 'q':
            break
        
        # Wait for a keypress and then print it to the screen
        pressed_key = input('Press any key to continue: ')
        
        # Check if the user pressed a key
        if pressed_key == 'q':
            break
        
        # Wait for a keypress and then print it to the screen
        pressed_key = input('Press any key to continue: ')
        
        # Check if the user pressed a key
        if pressed_key == 'q':
            break
        
        # Wait for a keypress and then print it to the screen
        pressed_key = input('Press any key to continue: ')
        
        # Check if the user pressed a key
        if pressed_key == 'q':
            break
        
        # Wait for a keypress and then print it to the screen
        pressed_key = input('Press any key to continue: ')
        
        # Check if the user pressed a key
        if pressed_key == 'q':
            break
        
        # Wait for a keypress and then print it to the screen
        pressed_key = input('Press any key to continue: ')
        
        # Check if the user pressed a key
        if pressed_key == 'q':
            break
        
        # Wait for a keypress and then print it to the screen
        pressed_key = input('Press any key to continue: ')
        
        # Check if the user pressed a key
        if pressed_key == 'q':
            break
        
        # Wait for a keypress and then print it to the screen
        pressed_key = input('Press any key to continue: ')
        
        # Check if the user pressed a key
        if pressed_key == 'q':
            break
        
        # Wait for a keypress and then print it to the screen
        pressed_key = input('Press any key to continue: ')
        
        # Check if the user pressed a key
        if pressed_key == 'q':
            break
        
        # Wait for a keypress and then print it to the screen
        pressed_key = input('Press any key to continue: ')
        
        # Check if the user pressed a key
        if pressed_key == 'q':
            break
        
        # Wait for a keypress and then print it to the screen
        pressed_key = input('Press any key to continue: ')
        
        # Check if the user pressed a key
        if pressed_key == 'q':
            break
        
        # Wait for a keypress and then print it to the screen