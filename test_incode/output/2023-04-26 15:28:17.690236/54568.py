#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates a list of words and returns words. """    
    words = []
    
    # Read words from file
    with open('words.txt', 'r') as f:
        words = f.read().splitlines()
    
    # Calculate list of words
    words = list(set(words))
    
    # Print list of words
    print(words)
    
    # Print number of words in list
    print(len(words))
    
    # Print number of unique words
    print(len(set(words)))
    
    # Print number of words with no duplicates
    print(len(words))
    
    # Print number of words with duplicates
    print(len(words))
    
    # Print number of words with duplicates and no words
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicates and no words and no punctuation
    print(len(words))
    
    # Print number of words with duplicate