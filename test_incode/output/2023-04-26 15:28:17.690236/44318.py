#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that returns user input and converts all ports. """    
    
    # Read user input
    ports = input("Enter ports separated by commas: ").split(',')
    
    # Convert ports to integers
    ports = [int(p) for p in ports]
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to strings
    ports = [str(p) for p in ports]
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to booleans
    ports = [True if p == 'True' else False for p in ports]
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to floats
    ports = [float(p) for p in ports]
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to list
    ports = [int(p) for p in ports]
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to tuple
    ports = [int(p) for p in ports]
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to dict
    ports = {int(p): p for p in ports}
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to set
    ports = {int(p): p for p in ports}
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to frozenset
    ports = {int(p): p for p in ports}
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to list
    ports = [int(p) for p in ports]
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to set
    ports = {int(p): p for p in ports}
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to frozenset
    ports = {int(p): p for p in ports}
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to list
    ports = [int(p) for p in ports]
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to set
    ports = {int(p): p for p in ports}
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to frozenset
    ports = {int(p): p for p in ports}
    
    # Print results
    print("Ports:", ports)
    
    # Convert ports to list
    ports = [int(p) for p in port