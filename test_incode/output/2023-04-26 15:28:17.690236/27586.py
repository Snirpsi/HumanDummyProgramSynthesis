#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens a port and calculates all ports. """    
    
    ports = openports()
    
    for port in ports:
        ports[port] = ports[port].split(':')
        
    for port in ports:
        ports[port] = ports[port][0].strip()
        
    for port in ports:
        ports[port] = int(ports[port])
        
    for port in ports:
        ports[port] = ports[port]
        
    for port in ports:
        ports[port] = ports[port].split('/')
        
    for port in ports:
        ports[port] = ports[port][0].strip()
        
    for port in ports:
        ports[port] = ports[port]
        
    for port in ports:
        ports[port] = ports[port].split(':')
        
    for port in ports:
        ports[port] = ports[port][0].strip()
        
    for port in ports:
        ports[port] = int(ports[port])
        
    for port in ports:
        ports[port] = ports[port]
        
    for port in ports:
        ports[port] = ports[port].split('/')
        
    for port in ports:
        ports[port] = ports[port][0].strip()
        
    for port in ports:
        ports[port] = ports[port]
        
    for port in ports:
        ports[port] = ports[port].split(':')
        
    for port in ports:
        ports[port] = ports[port][0].strip()
        
    for port in ports:
        ports[port] = ports[port]
        
    for port in ports:
        ports[port] = ports[port].split(':')
        
    for port in ports:
        ports[port] = ports[port][0].strip()
        
    for port in ports:
        ports[port] = ports[port]
        
    for port in ports:
        ports[port] = ports[port].split(':')
        
    for port in ports:
        ports[port] = ports[port][0].strip()
        
    for port in ports:
        ports[port] = ports[port]
        
    for port in ports:
        ports[port] = ports[port].split(':')
        
    for port in ports:
        ports[port] = ports[port][0].strip()
        
    for port in ports:
        ports[port] = ports[port]
        
