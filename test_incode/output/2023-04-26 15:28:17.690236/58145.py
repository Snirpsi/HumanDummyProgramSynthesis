#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts all ports and returns numbers. """    
    ports = [int(i) for i in input("Enter ports: ").split()]
    numbers = [int(i) for i in input("Enter numbers: ").split()]
    
    numbers = numbers[:len(ports)]
    ports = ports[:len(numbers)]
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    ports = ports + ports
    numbers = numbers + numbers
    
    port