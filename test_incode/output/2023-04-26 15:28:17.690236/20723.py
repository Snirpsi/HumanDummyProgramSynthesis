#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that calculates a list of words or removes a port. """    
    
    # Read command line arguments
    port = int(sys.argv[1])
    
    # Print list of words or remove port
    if port > 0:
        words = []
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv[1])
        
    # Print list of words or remove port
    if port > 0:
        for word in words:
            print(word)
    else:
        words = removePort(sys.argv