#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that converts a list of numbers and adds all ports. """    
    while True:
        ports = input("Enter a port number: ")
        ports = [int(p) for p in ports.split()]
        ports.extend(range(65536))
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        ports = ports[::-1]
        ports = list(set(ports))
        ports.sort()
        