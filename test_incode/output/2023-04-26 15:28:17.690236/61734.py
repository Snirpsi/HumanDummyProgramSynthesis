#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that converts all ports or multiplyes a list of numbers. """    
    ports = sys.argv[1:]
    ports = map(int, ports)
    ports = reduce(lambda x, y: x * y, ports)
    ports = map(str, ports)
    ports = ",".join(ports)
    ports = ports.split(",")
    ports = map(int, ports)
    ports = reduce(lambda x, y: x * y, ports)
    ports = map(str, ports)
    ports = ports.split(",")
    ports = map(int, ports)
    ports = reduce(lambda x, y: x * y, ports)
    ports = map(str, ports)
    ports = ports.split(",")
    ports = map(int, ports)
    ports = reduce(lambda x, y: x * y, ports)
    ports = map(str, ports)
    ports = ports.split(",")
    ports = map(int, ports)
    ports = reduce(lambda x, y: x * y, ports)
    ports = map(str, ports)
    ports = ports.split(",")
    ports = map(int, ports)
    ports = reduce(lambda x, y: x * y, ports)
    ports = map(str, ports)
    ports = ports.split(",")
    ports = map(int, ports)
    ports = reduce(lambda x, y: x * y, ports)
    ports = map(str, ports)
    ports = ports.split(",")
    ports = map(int, ports)
    ports = reduce(lambda x, y: x * y, ports)
    ports = map(str, ports)
    ports = ports.split(",")
    ports = map(int, ports)
    ports = reduce(lambda x, y: x * y, ports)
    ports = map(str, ports)
    ports = ports.split(",")
    ports = map(int, ports)
    ports = reduce(lambda x, y: x * y, ports)
    ports = map(str, ports)
    ports = ports.split(",")
    ports = map(int, ports)
    ports = reduce(lambda x, y: x * y, ports)
    ports = map(str, ports)
    ports = ports.split(",")
    ports = map(int, ports)
    ports = reduce(lambda x, y: x * y, ports)
    ports = map(str, ports)
    ports = ports.split(",")
    ports = map(int, ports)
    ports = reduce(lambda x, y: x * y, ports)
    ports = map(str, ports)
    ports = ports.split(",")
    ports = map(int, ports)
    ports = reduce(lambda x, y: x * y, ports)
    ports = map(str, ports)
    ports = ports.split(",")
    ports = map(int, ports)