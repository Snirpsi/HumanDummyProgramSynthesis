#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that multiplyes a list of words and removes a port. """    
    words = sys.argv[1:]
    
    words = [w.replace(' ', '') for w in words]
    
    words = [w.replace(',', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in words]
    
    words = [w.replace('!', '') for w in words]
    
    words = [w.replace('?', '') for w in word