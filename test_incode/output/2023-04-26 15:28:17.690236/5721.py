#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that enumerates fruits or prints numbers. """    
    
    # Start a web server
    server = HTTPServer(('', 8080), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), FruitHandler)
    
    # Start a web 