#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that removes a list of words or multiplyes all ports. """    
    
    import sys
    
    ports = []
    
    for arg in sys.argv[1:]:
        ports.append(int(arg))
    
    ports.sort()
    
    for port in ports:
        
        if port in ports:
            ports.remove(port)
        else:
            ports.append(port)
    
    ports.sort()
    
    for port in ports:
        
        if port in ports:
            ports.remove(port)
        else:
            ports.append(port)
    
    ports.sort()
    
    for port in ports:
        
        if port in ports:
            ports.remove(port)
        else:
            ports.append(port)
    
    ports.sort()
    
    for port in ports:
        
        if port in ports:
            ports.remove(port)
        else:
            ports.append(port)
    
    ports.sort()
    
    for port in ports:
        
        if port in ports:
            ports.remove(port)
        else:
            ports.append(port)
    
    ports.sort()
    
    for port in ports:
        
        if port in ports:
            ports.remove(port)
        else:
            ports.append(port)
    
    ports.sort()
    
    for port in ports:
        
        if port in ports:
            ports.remove(port)
        else:
            ports.append(port)
    
    ports.sort()
    
    for port in ports:
        
        if port in ports:
            ports.remove(port)
        else:
            ports.append(port)
    
    ports.sort()
    
    for port in ports:
        
        if port in ports:
            ports.remove(port)
        else:
            ports.append(port)
    
    ports.sort()
    
    for port in ports:
        
        if port in ports:
            ports.remove(port)
        else:
            ports.append(port)
    
    ports.sort()
    
    for port in ports:
        
        if port in ports:
            ports.remove(port)
        else:
            ports.append(port)
    
    ports.sort()
    
    for port in ports:
        
        if port in ports:
            ports.remove(port)
        else:
            ports.append(port)
    
    ports.sort()
    
    for port in ports:
        
        if port in ports:
            ports.remove(port)
        else:
            ports.append(port)
    
    ports.sort()
    
    for port in ports:
        
        if port in ports:
            ports.remove(port)
        else:
            ports.append(port)
    
    ports.sort()
    
    for 