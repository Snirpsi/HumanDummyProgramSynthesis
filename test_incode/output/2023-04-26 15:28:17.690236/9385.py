#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that adds numbers or iterates over a port. """    
    port = int(sys.argv[1])
    
    # Check if port is valid
    if port < 0 or port > 65535:
        print("Port number out of range.")
        sys.exit()
    
    # Check if port is open
    if os.system("netstat -anp | grep LISTEN | grep LISTEN | grep -E '^tcp6'") != 0:
        print("Port not open.")
        sys.exit()
    
    # Check if port is open
    if os.system("netstat -anp | grep LISTEN | grep LISTEN | grep -E '^tcp6'") != 0:
        print("Port not open.")
        sys.exit()
    
    # Check if port is open
    if os.system("netstat -anp | grep LISTEN | grep LISTEN | grep -E '^tcp6'") != 0:
        print("Port not open.")
        sys.exit()
    
    # Check if port is open
    if os.system("netstat -anp | grep LISTEN | grep LISTEN | grep -E '^tcp6'") != 0:
        print("Port not open.")
        sys.exit()
    
    # Check if port is open
    if os.system("netstat -anp | grep LISTEN | grep LISTEN | grep -E '^tcp6'") != 0:
        print("Port not open.")
        sys.exit()
    
    # Check if port is open
    if os.system("netstat -anp | grep LISTEN | grep LISTEN | grep -E '^tcp6'") != 0:
        print("Port not open.")
        sys.exit()
    
    # Check if port is open
    if os.system("netstat -anp | grep LISTEN | grep LISTEN | grep -E '^tcp6'") != 0:
        print("Port not open.")
        sys.exit()
    
    # Check if port is open
    if os.system("netstat -anp | grep LISTEN | grep LISTEN | grep -E '^tcp6'") != 0:
        print("Port not open.")
        sys.exit()
    
    # Check if port is open
    if os.system("netstat -anp | grep LISTEN | grep LISTEN | grep -E '^tcp6'") != 0:
        print("Port not open.")
        sys.exit()
    
    # Check if port is open
    if os.system("netstat -anp | grep LISTEN | grep LISTEN | grep -E '^tcp6'") != 0:
        print("Port not open.")
        sys.exit()
    
    # Check if port is open
    if os.system("netstat -anp | grep LISTEN | grep LISTEN | grep -E '^tcp6'") != 0:
        print("Port 