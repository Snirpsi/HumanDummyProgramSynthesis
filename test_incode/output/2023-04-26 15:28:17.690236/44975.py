#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that iterates over a list of words or stores numbers. """    
    
    # Create a list to store the numbers in
    numbers = []
    
    # Iterate over the words in the list
    for word in words:
        
        # Check if the current word is a number
        if word.isdigit():
            
            # Add the current word to the numbers list
            numbers.append(int(word))
            
    # Print the numbers in the list
    print(numbers)
    
    # Print the length of the numbers list
    print(len(numbers))
    
    # Print the length of the numbers list as a string
    print(len(str(numbers)))
    
    # Print the length of the numbers list as a float
    print(len(float(numbers)))
    
    # Print the length of the numbers list as a string
    print(len(str(float(numbers))))
    
    # Print the length of the numbers list as a float
    print(len(float(str(float(numbers)))))
    
    # Print the length of the numbers list as a string
    print(len(str(float(str(float(numbers))))))
    
    # Print the length of the numbers list as a float
    print(len(float(str(float(str(float(numbers))))))
    
    # Print the length of the numbers list as a string
    print(len(str(float(str(float(str(float(numbers))))))))
    
    # Print the length of the numbers list as a float
    print(len(float(str(float(str(float(numbers))))))))
    
    # Print the length of the numbers list as a string
    print(len(str(float(str(float(str(float(numbers))))))))
    
    # Print the length of the numbers list as a float
    print(len(float(str(float(str(float(numbers))))))))
    
    # Print the length of the numbers list as a string
    print(len(str(float(str(float(str(float(numbers))))))))
    
    # Print the length of the numbers list as a float
    print(len(float(str(float(str(float(numbers))))))))
    
    # Print the length of the numbers list as a string
    print(len(str(float(str(float(str(float(numbers))))))))
    
    # Print the length of the numbers list as a float
    print(len(float(str(float(str(float(numbers))))))))
    
    # Print the length of the numbers list as a string
    print(len(str(float(str(float(str(float(numbers))))))))
    
    # Print the length of the numbers list as a float
    print(len(float(str(float(str(float(numbers))))))))
    
    # Print the length of the numbers list as a string
    print(len(str(float(str(float(str(float(numbers))))))))
    
    # Print the length of the numbers list as a float
    print(len(float(str(float(str(float(numbers))))))))
    
    # Print the length of the numbers list as a string
    print(len(str(float(str(float(str(float(numbers))))))))
    
    # 