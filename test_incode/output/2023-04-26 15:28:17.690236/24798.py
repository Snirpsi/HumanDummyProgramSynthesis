#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that removes numbers. """    
    import sys
    
    # Remove all numbers from sys.stdin
    for line in sys.stdin:
        line = line.strip()
        if line:
            print(line)
        else:
            break
    
    # Remove all numbers from sys.stdout and sys.stdout
    for line in sys.stdout + sys.stdin:
        line = line.strip()
        if line:
            print(line)
        else:
            break
    
    # Remove all numbers from sys.stdout and sys.stdin
    for line in sys.stdout + sys.stdin:
        line = line.strip()
        if line:
            print(line)
        else:
            break
    
    # Remove all numbers from sys.stdout and sys.stdin
    for line in sys.stdout + sys.stdin:
        line = line.strip()
        if line:
            print(line)
        else:
            break
    
    # Remove all numbers from sys.stdout and sys.stdin
    for line in sys.stdout + sys.stdin:
        line = line.strip()
        if line:
            print(line)
        else:
            break
    
    # Remove all numbers from sys.stdout and sys.stdin
    for line in sys.stdout + sys.stdin:
        line = line.strip()
        if line:
            print(line)
        else:
            break
    
    # Remove all numbers from sys.stdout and sys.stdin
    for line in sys.stdout + sys.stdin:
        line = line.strip()
        if line:
            print(line)
        else:
            break
    
    # Remove all numbers from sys.stdout and sys.stdin
    for line in sys.stdout + sys.stdin:
        line = line.strip()
        if line:
            print(line)
        else:
            break
    
    # Remove all numbers from sys.stdout and sys.stdin
    for line in sys.stdout + sys.stdin:
        line = line.strip()
        if line:
            print(line)
        else:
            break
    
    # Remove all numbers from sys.stdout and sys.stdin
    for line in sys.stdout + sys.stdin:
        line = line.strip()
        if line:
            print(line)
        else:
            break
    
    # Remove all numbers from sys.stdout and sys.stdin
    for line in sys.stdout + sys.stdin:
        line = line.strip()
        if line:
            print(line)
        else:
            break
    
    # Remove all numbers from sys.stdout and sys.stdin
    for line in sys.stdout + sys.stdin:
        line = line.strip()
        if line:
            print(line)
        else:
            break
    
    # Remove all numbers from sys.stdout and sys.stdin
    for line in sys.stdout + sys.stdin:
        line = line.strip()
        if line:
            print(line)
        else:
            break
    
    # Remove all numbers from sys.stdout and sys.stdin
    for line in sys.stdout + sys.stdin:
        line = line.strip()
        if line:
            print(line)
        else:
            break
    
    # Remove all numbers from sys.stdout and sys.stdin
    for line in sys.stdout + sys.stdin:
        line = line.strip()
