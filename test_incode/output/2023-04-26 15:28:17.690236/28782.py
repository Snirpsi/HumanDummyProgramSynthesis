#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that calculates a list of words. """    
    
    port = int(sys.argv[1])
    
    wordlist = list(open('wordlist.txt'))
    
    wordlist.sort(key=lambda word: word.lower())
    
    wordlist.sort(key=lambda word: word.upper())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize())
    
    wordlist.sort(key=lambda word: word.title())
    
    wordlist.sort(key=lambda word: word.capitalize