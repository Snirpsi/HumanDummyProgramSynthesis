#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that returns all ports. """    
    ports = []
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    
    # Get the port number of the server from stdin
    port = int(input('Enter the port number: '))
    