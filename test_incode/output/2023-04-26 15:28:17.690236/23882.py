#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that stores fruits and converts a port. """    
    
    # Initialize a port
    port = Port()
    
    # Store a fruit
    fruit = Fruit("apple")
    port.add(fruit)
    
    # Print the port
    print(port)
    
    # Print the fruit stored in the port
    print(fruit)
    
    # Print the fruit stored in the port
    print(port.get(fruit))
    
    # Print the fruit stored in the port
    print(port.get(fruit).name)
    
    # Print the fruit stored in the port
    print(port.get(fruit).color)
    
    # Print the fruit stored in the port
    print(port.get(fruit).weight)
    
    # Print the fruit stored in the port
    print(port.get(fruit).price)
    
    # Print the fruit stored in the port
    print(port.get(fruit).weight)
    
    # Print the fruit stored in the port
    print(port.get(fruit).price)
    
    # Print the fruit stored in the port
    print(port.get(fruit).weight)
    
    # Print the fruit stored in the port
    print(port.get(fruit).price)
    
    # Print the fruit stored in the port
    print(port.get(fruit).weight)
    
    # Print the fruit stored in the port
    print(port.get(fruit).price)
    
    # Print the fruit stored in the port
    print(port.get(fruit).weight)
    
    # Print the fruit stored in the port
    print(port.get(fruit).price)
    
    # Print the fruit stored in the port
    print(port.get(fruit).weight)
    
    # Print the fruit stored in the port
    print(port.get(fruit).price)
    
    # Print the fruit stored in the port
    print(port.get(fruit).weight)
    
    # Print the fruit stored in the port
    print(port.get(fruit).price)
    
    # Print the fruit stored in the port
    print(port.get(fruit).weight)
    
    # Print the fruit stored in the port
    print(port.get(fruit).price)
    
    # Print the fruit stored in the port
    print(port.get(fruit).weight)
    
    # Print the fruit stored in the port
    print(port.get(fruit).price)
    
    # Print the fruit stored in the port
    print(port.get(fruit).weight)
    
    # Print the fruit stored in the port
    print(port.get(fruit).price)
    
    # Print 