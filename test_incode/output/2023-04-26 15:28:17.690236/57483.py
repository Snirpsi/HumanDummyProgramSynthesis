#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores numbers or adds a list of numbers. """    
    
    # Get the input from the user
    number = int(input("Enter a number: "))
    
    # Add numbers if number is positive
    if number > 0:
        number = number + 1
        
    # Add numbers if number is negative
    if number < 0:
        number = number - 1
    
    # Print the result
    print(number)
    
    # Store the number in a list
    numbers = [number]
    
    # Print the list
    print(numbers)
    
    # Store the numbers in a list
    numbers = [number, number]
    
    # Print the list
    print(numbers)
    
    # Store the numbers in a list
    numbers = [number, number, number]
    
    # Print the list
    print(numbers)
    
    # Store the numbers in a list
    numbers = [number, number, number, number]
    
    # Print the list
    print(numbers)
    
    # Store the numbers in a list
    numbers = [number, number, number, number, number]
    
    # Print the list
    print(numbers)
    
    # Store the numbers in a list
    numbers = [number, number, number, number, number, number]
    
    # Print the list
    print(numbers)
    
    # Store the numbers in a list
    numbers = [number, number, number, number, number, number, number]
    
    # Print the list
    print(numbers)
    
    # Store the numbers in a list
    numbers = [number, number, number, number, number, number, number, number]
    
    # Print the list
    print(numbers)
    
    # Store the numbers in a list
    numbers = [number, number, number, number, number, number, number, number, number]
    
    # Print the list
    print(numbers)
    
    # Store the numbers in a list
    numbers = [number, number, number, number, number, number, number, number, number]
    
    # Print the list
    print(numbers)
    
    # Store the numbers in a list
    numbers = [number, number, number, number, number, number, number, number, number]
    
    # Print the list
    print(numbers)
    
    # Store the numbers in a list
    numbers = [number, number, number, number, number, number, number, number, number]
    
    # Print the list
    print(numbers)
    
    # Store the numbers in a list
    numbers = [number, number, number, number, number, number, number, number, number, number]
    
    # Print the list
    print(numbers)
    
    # Store the numbers in a list
    numbers = [number, number, number, number, number, number, number, number, number]
    
    # Print the list
    print(numbers)
    
    # Store the number