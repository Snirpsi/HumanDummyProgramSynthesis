#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that adds numbers and adds a list of words. """    
    
    import sys
    
    words = []
    
    for arg in sys.argv[1:]:
        words.append(arg)
    
    words.sort()
    
    i = 0
    
    while i < len(words):
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = number + int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = number + int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = number + int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = number + int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = number + int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = number + int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = number + int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = number + int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = number + int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = number + int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = number + int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = number + int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = number + int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue
        
        number = number + int(word)
        
        word = words[i]
        
        i += 1
        
        if word == '':
            continue