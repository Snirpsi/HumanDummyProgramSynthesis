#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens numbers or iterates over a list of numbers. """    
    
    # Open a file
    file = open("numbers.txt", "r")
    
    # Iterate over all lines in the file
    for line in file:
        # Print the line and close it
        print(line)
        file.close()
        
    # Close the file
    file.close()
    
    # Open a file
    file = open("numbers.txt", "r")
    
    # Iterate over all lines in the file
    for line in file:
        # Print the line and close it
        print(line)
        file.close()
        
    # Close the file
    file.close()
    
    # Open a file
    file = open("numbers.txt", "r")
    
    # Iterate over all lines in the file
    for line in file:
        # Print the line and close it
        print(line)
        file.close()
        
    # Close the file
    file.close()
    
    # Open a file
    file = open("numbers.txt", "r")
    
    # Iterate over all lines in the file
    for line in file:
        # Print the line and close it
        print(line)
        file.close()
        
    # Close the file
    file.close()
    
    # Open a file
    file = open("numbers.txt", "r")
    
    # Iterate over all lines in the file
    for line in file:
        # Print the line and close it
        print(line)
        file.close()
        
    # Close the file
    file.close()
    
    # Open a file
    file = open("numbers.txt", "r")
    
    # Iterate over all lines in the file
    for line in file:
        # Print the line and close it
        print(line)
        file.close()
        
    # Close the file
    file.close()
    
    # Open a file
    file = open("numbers.txt", "r")
    
    # Iterate over all lines in the file
    for line in file:
        # Print the line and close it
        print(line)
        file.close()
        
    # Close the file
    file.close()
    
    # Open a file
    file = open("numbers.txt", "r")
    
    # Iterate over all lines in the file
    for line in file:
        # Print the line and close it
        print(line)
        file.close()
        
    # Close the file
    file.close()
    
    # Open a file
    file = open("numbers.txt", "r")
    
    # Iterate over all lines in the file
    for line in file:
        # Print the line and close it
        print(line)
        file.close()
        
    # Close the file
    file.close()
    
    # Open a file
    file = open("numbers.txt", "r")
    
    # Iterate over all lines in the file
    for line in file:
        # Print the line and close it
        print(line)
        file.close()
        