#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that removes user input or converts all ports. """    
    while True:
        ports = input("Enter ports (separated by space): ")
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports.sort()
        ports = list(set(ports))
        ports.sort()
        ports = [str(p) for p in ports]
        ports.sort()
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        ports = ports + ["*"]
        port