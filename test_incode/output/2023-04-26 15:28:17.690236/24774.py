#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that iterates over a port or removes a list of numbers. """    
    
    port = '/dev/ttyUSB0'
    
    ports = port
    
    ports = ports.split(',')
    
    ports = [int(p) for p in ports]
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(