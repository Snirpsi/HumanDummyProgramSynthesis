#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds all ports and opens user input. """    
    ports = input("Enter the ports separated by comma: ")
    ports = ports.split(",")
    ports = map(int, ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = map(lambda x: str(x), ports)
    ports = 