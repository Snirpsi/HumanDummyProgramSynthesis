#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that opens all ports or prints user input. """    
    while True:
        ports = open('/proc/net/tcp', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/udp', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/unix', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/tcp6', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/udp6', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/unix6', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/tcp4', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/udp4', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/unix4', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/tcp3', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/udp3', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/unix3', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/tcp2', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/udp2', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/unix2', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/tcp1', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/udp1', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/unix1', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/tcp', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/udp', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/unix', 'r')
        ports.close()
        print("Ports open")
        ports = open('/proc/net/tcp6', 'r')
