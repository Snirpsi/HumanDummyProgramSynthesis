#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that prints user input or converts words. """    
    
    # Get the user input
    words = get_words()
    
    # Print the results
    print_results(words)
    
    # Convert the words to lowercase
    words = [word.lower() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to uppercase
    words = [word.upper() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to titlecase
    words = [word.title() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to titlecase
    words = [word.title() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to titlecase
    words = [word.title() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to titlecase
    words = [word.title() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to titlecase
    words = [word.title() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to titlecase
    words = [word.title() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to titlecase
    words = [word.title() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to titlecase
    words = [word.title() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to titlecase
    words = [word.title() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to titlecase
    words = [word.title() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to titlecase
    words = [word.title() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to titlecase
    words = [word.title() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to titlecase
    words = [word.title() for word in words]
    
    # Print the results
    print_results(words)
    
    # Convert the words to titlecase
    words = [word.title() for word in words]
    
    # Print 