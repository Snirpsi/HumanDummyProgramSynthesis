#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that prints a list of words or enumerates a port. """    
    import sys
    import time
    
    port = sys.argv[1]
    
    while True:
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        
        if port > 65535:
            port = port - 1
            
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        
        if port > 65535:
            port = port - 1
            
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        
        if port > 65535:
            port = port - 1
            
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        
        if port > 65535:
            port = port - 1
            
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        
        if port > 65535:
            port = port - 1
            
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        
        if port > 65535:
            port = port - 1
            
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        
        if port > 65535:
            port = port - 1
            
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        
        if port > 65535:
            port = port - 1
            
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        
        if port > 65535:
            port = port - 1
            
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        
        if port > 65535:
            port = port - 1
            
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        
        if port > 65535:
            port = port - 1
            
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        
        if port > 65535:
            port = port - 1
            
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        
        if port > 65535:
            port = port - 1
            
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        
        if port > 65535:
            port = port - 1
            
        print("\nPort: " + port)
        
        time.sleep(1)
        
        port = port + 1
        