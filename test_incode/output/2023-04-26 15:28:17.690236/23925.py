#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that enumerates fruits or converts a list of words. """    
    
    # Iterate over all words in input file
    for word in inputFile.read().split():
        # Convert word to lower case and print it
        print(word.lower())
        
    # End the loop
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close the file
    inputFile.close()
    
    # Close 