#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that adds user input and calculates all ports. """    
    
    while True:
        
        ports = input("Enter ports separated by comma (empty to quit): ")
        
        if ports == "":
            break
        
        ports = ports.split(",")
        
        for port in ports:
            
            port = int(port)
            
            if port < 0 or port > 65535:
                print("Invalid port")
                continue
            
            ports.append(port)
        
        print("Ports added:", ports)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_sum = sum(ports)
        
        print("Total ports added:", ports_sum)
        
        ports_