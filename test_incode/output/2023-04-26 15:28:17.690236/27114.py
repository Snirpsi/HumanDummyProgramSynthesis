#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that iterates over a list of words or enumerates a port. """    
    
    words = ['cat', 'dog', 'horse', 'monkey', 'rabbit', 'sheep', 'tiger', 'zebra']
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over words and print them out
    for word in words:
        print(word)
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over words and print them out
    for word in words:
        print(word)
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over words and print them out
    for word in words:
        print(word)
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over words and print them out
    for word in words:
        print(word)
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over words and print them out
    for word in words:
        print(word)
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over words and print them out
    for word in words:
        print(word)
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over words and print them out
    for word in words:
        print(word)
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over words and print them out
    for word in words:
        print(word)
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over words and print them out
    for word in words:
        print(word)
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over words and print them out
    for word in words:
        print(word)
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over words and print them out
    for word in words:
        print(word)
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over words and print them out
    for word in words:
        print(word)
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over words and print them out
    for word in words:
        print(word)
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over words and print them out
    for word in words:
        print(word)
    
    # start webserver
    webserver.serve_forever()
    
    # iterate over word