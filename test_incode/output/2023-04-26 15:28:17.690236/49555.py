#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that opens all ports and stores fruits. """    
    
    ports = Ports()
    fruits = fruits.fruits()
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)
    
    for fruit in fruits:
        ports.close_port(fruit)
    
    for fruit in fruits:
        ports.open_port(fruit)