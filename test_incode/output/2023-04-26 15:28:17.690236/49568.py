#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that iterates over fruits or adds all ports. """    
    fruit = input("Enter the fruit name: ")
    ports = input("Enter the ports: ")
    ports = ports.split(",")
    ports = map(int, ports)
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]
    ports = list(ports)
    ports.sort()
    ports = ports[::-1]