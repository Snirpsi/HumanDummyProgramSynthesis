#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that stores all ports and calculates a list of numbers. """    
    while True:
        ports = listPorts()
        ports.sort()
        ports.reverse()
        ports.insert(0, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(ports)-1, 0)
        ports.insert(len(port