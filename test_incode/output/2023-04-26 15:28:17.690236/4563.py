#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens a list of numbers or calculates numbers. """    
    
    # Open the file with the numbers or calculates numbers
    file = open('numbers.txt', 'r')
    
    # Read the numbers or calculates numbers
    numbers = file.readlines()
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the numbers or calculates numbers
    print(numbers)