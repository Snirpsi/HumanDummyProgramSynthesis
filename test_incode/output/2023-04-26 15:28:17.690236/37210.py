#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens a list of words or prints all ports. """    
    
    # Open the ports list
    ports = open('/etc/openvswitch/ovs.bridge.ovs-vsctl')
    
    # Print the ports list
    for line in ports:
        print(line)
    
    # Close the ports list
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close 