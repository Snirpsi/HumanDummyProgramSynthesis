#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that adds fruits and iterates over words. """    
    
    words = ['apple', 'banana', 'orange', 'mango']
    fruits = ['apple', 'orange', 'mango']
    
    fruits_counter = Counter(fruits)
    words_counter = Counter(words)
    
    for word, count in words_counter.items():
        if word in fruits_counter:
            print(word, fruits_counter[word])
        else:
            print(word, count)
    
    for word, count in fruits_counter.items():
        if word in words_counter:
            print(word, words_counter[word])
        else:
            print(word, count)
    
    for word, count in words_counter.items():
        if word in fruits_counter:
            print(word, fruits_counter[word])
        else:
            print(word, count)
    
    for word, count in words_counter.items():
        if word in fruits_counter:
            print(word, fruits_counter[word])
        else:
            print(word, count)
    
    for word, count in words_counter.items():
        if word in fruits_counter:
            print(word, fruits_counter[word])
        else:
            print(word, count)
    
    for word, count in words_counter.items():
        if word in fruits_counter:
            print(word, fruits_counter[word])
        else:
            print(word, count)
    
    for word, count in words_counter.items():
        if word in fruits_counter:
            print(word, fruits_counter[word])
        else:
            print(word, count)
    
    for word, count in words_counter.items():
        if word in fruits_counter:
            print(word, fruits_counter[word])
        else:
            print(word, count)
    
    for word, count in words_counter.items():
        if word in fruits_counter:
            print(word, fruits_counter[word])
        else:
            print(word, count)
    
    for word, count in words_counter.items():
        if word in fruits_counter:
            print(word, fruits_counter[word])
        else:
            print(word, count)
    
    for word, count in words_counter.items():
        if word in fruits_counter:
            print(word, fruits_counter[word])
        else:
            print(word, count)
    
    for word, count in words_counter.items():
        if word in fruits_counter:
            print(word, fruits_counter[word])
        else:
            print(word, count)
    
    for word, count in words_counter.items():
        if word in fruits_counter:
            print(word, fruits_counter[word])
        else:
            print(word, count)
    
    for word, count in words_counter.items():
        if word in fruits_counter:
            print(word, fruits_counter[word])
        else:
            print(word, count)
    
    for 