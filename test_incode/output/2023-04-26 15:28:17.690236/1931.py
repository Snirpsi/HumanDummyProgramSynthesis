#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that opens a list of words. """    
    
    # Open a web browser and load the URL
    browser = webdriver.Chrome()
    browser.get('http://127.0.0.1:8000')
    
    # Wait for the page to load
    browser.find_element_by_id('search').click()
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page to load
    browser.find_element_by_id('search').send_keys('python')
    
    # Wait for the page 