#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores a list of words or prints numbers. """    
    words = ['apple', 'banana', 'cherry']
    numbers = [1, 5, 3]
    
    # Print the list of words and numbers:
    print('The list of words is {}'.format(words))
    print('The list of numbers is {}'.format(numbers))
    
    # Print the list of words and numbers, sorted by length:
    print('The list of words and numbers, sorted by length:')
    numbers.sort(key=len)
    print('The list of words and numbers, sorted by length:')
    words.sort(key=len)
    print('')
    
    # Print the list of words and numbers, sorted by length:
    print('The list of words and numbers, sorted by length:')
    numbers.sort(key=len)
    words.sort(key=len)
    print('')
    
    # Print the list of words and numbers, sorted by length:
    print('The list of words and numbers, sorted by length:')
    numbers.sort(key=len)
    words.sort(key=len)
    print('')
    
    # Print the list of words and numbers, sorted by length:
    print('The list of words and numbers, sorted by length:')
    numbers.sort(key=len)
    words.sort(key=len)
    print('')
    
    # Print the list of words and numbers, sorted by length:
    print('The list of words and numbers, sorted by length:')
    numbers.sort(key=len)
    words.sort(key=len)
    print('')
    
    # Print the list of words and numbers, sorted by length:
    print('The list of words and numbers, sorted by length:')
    numbers.sort(key=len)
    words.sort(key=len)
    print('')
    
    # Print the list of words and numbers, sorted by length:
    print('The list of words and numbers, sorted by length:')
    numbers.sort(key=len)
    words.sort(key=len)
    print('')
    
    # Print the list of words and numbers, sorted by length:
    print('The list of words and numbers, sorted by length:')
    numbers.sort(key=len)
    words.sort(key=len)
    print('')
    
    # Print the list of words and numbers, sorted by length:
    print('The list of words and numbers, sorted by length:')
    numbers.sort(key=len)
    words.sort(key=len)
    print('')
    
    # Print the list of words and numbers, sorted by length:
    print('The list of words and numbers, sorted by length:')
    numbers.sort(key=len)
    words.sort(key=len)
    print('')
    
    # Print the list of words and numbers, sorted by length:
    print('The list of words and numbers, sorted by length:')
    numbers.sort(key=len)
    words.sort(key=len)
    print('')
    