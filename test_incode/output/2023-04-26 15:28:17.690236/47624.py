#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens a port or adds a list of numbers. """    
    ports = [int(i) for i in sys.argv[1:]]
    
    if len(ports) == 1:
        ports.append(0)
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = ports[0] + 1
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 0
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 1
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 0
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 1
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 0
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 1
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 0
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 1
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 0
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 1
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 0
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 1
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 0
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 1
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 0
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 1
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 0
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 1
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 0
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 1
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 0
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 1
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports[0] = 0
    
    ports = list(set(port