#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that iterates over a list of numbers or enumerates a port. """    
    
    # Get the port number from the command line
    port = int(sys.argv[1])
    
    # Iterate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
    # Enumerate over the list of ports
    for port in range(port):
        print(port)
        
