#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts all ports or calculates user input. """    
    
    ports = input("Enter ports separated by comma:\n").split(',')
    
    ports = [int(p) for p in ports]
    
    ports_sum = sum(ports)
    
    ports_mean = ports_sum / len(ports)
    
    ports_median = ports[int(len(ports) / 2)]
    
    ports_mode = ports[int(len(ports) / 2)]
    
    ports_min = ports[int(len(ports) / 2)]
    
    ports_max = ports[int(len(ports) / 2)]
    
    ports_range = ports_max - ports_min
    
    ports_range = ports_range / ports_mean
    
    ports_range = ports_range * 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range / 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range * 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range / 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range * 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range / 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range * 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range / 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range * 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range / 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range * 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range / 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range * 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range / 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range * 100
    
    ports_range = round(ports_range, 2)
    
    ports_range = ports_range / 100
