#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that multiplyes a list of words and multiplyes numbers. """    
    words = ['one', 'two', 'three', 'four', 'five', 'six', 'seven', 'eight', 'nine']
    numbers = [1, 2, 3, 4, 5, 6, 7, 8]
    
    # Multiply each word with its corresponding number
    product = map(lambda word: word*number, words)
    
    # Print the product as a list
    print(product)
    
    # Multiply each number with its corresponding word
    product = map(lambda number: number*word, numbers)
    
    # Print the product as a list
    print(product)
    
    # Multiply each word with its corresponding number
    product = map(lambda word: word*number, words)
    
    # Print the product as a list
    print(product)
    
    # Multiply each number with its corresponding word
    product = map(lambda number: number*word, numbers)
    
    # Print the product as a list
    print(product)
    
    # Multiply each word with its corresponding number
    product = map(lambda word: word*number, words)
    
    # Print the product as a list
    print(product)
    
    # Multiply each number with its corresponding word
    product = map(lambda number: number*word, numbers)
    
    # Print the product as a list
    print(product)
    
    # Multiply each word with its corresponding number
    product = map(lambda word: word*number, words)
    
    # Print the product as a list
    print(product)
    
    # Multiply each number with its corresponding word
    product = map(lambda number: number*word, numbers)
    
    # Print the product as a list
    print(product)
    
    # Multiply each word with its corresponding number
    product = map(lambda word: word*number, words)
    
    # Print the product as a list
    print(product)
    
    # Multiply each number with its corresponding word
    product = map(lambda number: number*word, numbers)
    
    # Print the product as a list
    print(product)
    
    # Multiply each word with its corresponding number
    product = map(lambda word: word*number, words)
    
    # Print the product as a list
    print(product)
    
    # Multiply each number with its corresponding word
    product = map(lambda number: number*word, numbers)
    
    # Print the product as a list
    print(product)
    
    # Multiply each word with its corresponding number
    product = map(lambda word: word*number, words)
    
    # Print the product as a list
    print(product)
    
    # 