#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that enumerates numbers or iterates over a list of words. """    
    
    # Read command line arguments
    wordlist = sys.argv[1]
    
    # Iterate over wordlist
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for i in range(0, len(wordlist)):
        print(wordlist[i])
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in wordlist:
        print(word)
        
    # Iterate over wordlist using range
    for word in word