#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that opens a port or calculates a list of numbers. """    
    
    # Start a web server on the default port (8080)
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), SimpleHTTPRequestHandler)
    
    # Start a web server on the port 8080
    httpd = HTTPServer(('localhost', 8080), Simple