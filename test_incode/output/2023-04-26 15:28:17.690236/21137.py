#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that adds user input and enumerates all ports. """    
    
    ports = []
    
    while True:
        port = input('Enter a port number: ')
        
        if port == 'quit':
            break
        
        ports.append(port)
        
    print('\n'.join(ports))
    
    print('\n'.join(['{:<2}'.format(port) for port in ports]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(ports, key=int, reverse=True)]))
    
    print('\n'.join(['{:<2}'.format(port) for port in sorted(port