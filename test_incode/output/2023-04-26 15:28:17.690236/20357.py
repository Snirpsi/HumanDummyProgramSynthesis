#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that stores a list of numbers. """    
    numbers = []
    
    while True:
        number = int(input('Enter a number: '))
        if number == -1:
            break
        numbers.append(number)
    
    print('The numbers entered are:')
    for number in numbers:
        print(number)
    
    print('The average of the numbers entered is:', sum(numbers) / len(numbers))
    
    print('The largest number entered is:', max(numbers))
    
    print('The smallest number entered is:', min(numbers))
    
    print('The median number entered is:', median(numbers))
    
    print('The standard deviation of the numbers entered is:', std(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
    
    print('The mode of the numbers entered is:', mode(numbers))
