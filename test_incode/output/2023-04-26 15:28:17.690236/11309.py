#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that calculates numbers or multiplyes a list of words. """    
    
    import sys
    
    if len(sys.argv) < 2:
        print("Usage: %s <words>" % sys.argv[0])
        sys.exit()
    
    words = sys.argv[1:]
    
    numbers = map(int, words)
    
    numbers = map(lambda number: number * 2, numbers)
    
    numbers = map(lambda number: str(number), numbers)
    
    numbers = map(lambda number: number.replace(',', '.'), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace(',', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace('.', ''), numbers)
    
    numbers = map(lambda number: number.replace