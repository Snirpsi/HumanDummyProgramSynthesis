#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that converts a list of numbers. """    
    import sys
    
    numbers = list(map(int, sys.stdin.read().split()))
    
    numbers = map(lambda x: x if x > 0 else 0, numbers)
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))
    
    numbers = list(map(lambda x: x if x < 0 else x, numbers))
    
    numbers = list(map(lambda x: x if x > 0 else 0, numbers))