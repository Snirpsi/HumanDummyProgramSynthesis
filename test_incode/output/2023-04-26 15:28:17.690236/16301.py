#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that removes all ports and adds user input. """    
    
    ports = input("Enter ports separated by comma: ").split(',')
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(port)
    
    ports = list(set(ports))
    
    for port in ports:
        ports.remove(