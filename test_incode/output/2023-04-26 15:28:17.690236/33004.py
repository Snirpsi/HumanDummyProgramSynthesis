#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts numbers. """    
    import sys
    
    if len(sys.argv) != 2:
        print("Usage: %s number" % sys.argv[0])
        sys.exit()
    
    number = int(sys.argv[1])
    
    if number < 0:
        print("Error: negative number")
        sys.exit()
    
    number = str(number)
    
    if len(number) != 2:
        print("Error: number must be 2 digits")
        sys.exit()
    
    number = number[::-1]
    
    if number != sys.argv[1]:
        print("Error: number does not match")
        sys.exit()
    
    number = int(number)
    
    if number < 0:
        print("Error: negative number")
        sys.exit()
    
    number = str(number)
    
    if len(number) != 2:
        print("Error: number must be 2 digits")
        sys.exit()
    
    number = number[::-1]
    
    if number != sys.argv[1]:
        print("Error: number does not match")
        sys.exit()
    
    number = int(number)
    
    if number < 0:
        print("Error: negative number")
        sys.exit()
    
    number = str(number)
    
    if len(number) != 2:
        print("Error: number must be 2 digits")
        sys.exit()
    
    number = number[::-1]
    
    if number != sys.argv[1]:
        print("Error: number does not match")
        sys.exit()
    
    number = int(number)
    
    if number < 0:
        print("Error: negative number")
        sys.exit()
    
    number = str(number)
    
    if len(number) != 2:
        print("Error: number must be 2 digits")
        sys.exit()
    
    number = number[::-1]
    
    if number != sys.argv[1]:
        print("Error: number does not match")
        sys.exit()
    
    number = int(number)
    
    if number < 0:
        print("Error: negative number")
        sys.exit()
    
    number = str(number)
    
    if len(number) != 2:
        print("Error: number must be 2 digits")
        sys.exit()
    
    number = number[::-1]
    
    if number != sys.argv[1]:
        print("Error: number does not match")
        sys.exit()
    
    number = int(number)
    
    if number < 0:
        print("Error: negative number")
        sys.exit()
    
    number = str(number)
    
    if len(number) != 2:
        print("Error: number must be 2 digits")
        sys.exit()
    
    number = number[::-1]
    
    if number != sys.argv[1]:
        print("Error: number does not match")
        sys.exit()
    
    number = int(number)
    
    if number < 0:
        print("Error: negative number")
        sys.exit()
    
    number = str(number)
    
    if len(number) != 2:
        print("Error: number must be 2 digits")
        sys.exit()
    
    number = number[::-1]
    
    if 