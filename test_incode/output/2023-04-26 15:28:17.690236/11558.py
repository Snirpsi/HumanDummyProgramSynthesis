#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that calculates a list of words or stores all ports. """    
    ports = []
    
    # start a web server on port 80
    httpd = HTTPServer(('', 80), WordsHandler)
    
    # start a web server on port 8080
    httpd = HTTPServer(('', 8080), WordsHandler)
    
    # start a web server on port 8081
    httpd = HTTPServer(('', 8081), WordsHandler)
    
    # start a web server on port 8082
    httpd = HTTPServer(('', 8082), WordsHandler)
    
    # start a web server on port 8083
    httpd = HTTPServer(('', 8083), WordsHandler)
    
    # start a web server on port 8084
    httpd = HTTPServer(('', 8084), WordsHandler)
    
    # start a web server on port 8085
    httpd = HTTPServer(('', 8085), WordsHandler)
    
    # start a web server on port 8086
    httpd = HTTPServer(('', 8086), WordsHandler)
    
    # start a web server on port 8087
    httpd = HTTPServer(('', 8087), WordsHandler)
    
    # start a web server on port 8088
    httpd = HTTPServer(('', 8088), WordsHandler)
    
    # start a web server on port 8089
    httpd = HTTPServer(('', 8089), WordsHandler)
    
    # start a web server on port 8090
    httpd = HTTPServer(('', 8090), WordsHandler)
    
    # start a web server on port 8091
    httpd = HTTPServer(('', 8091), WordsHandler)
    
    # start a web server on port 8092
    httpd = HTTPServer(('', 8092), WordsHandler)
    
    # start a web server on port 8093
    httpd = HTTPServer(('', 8093), WordsHandler)
    
    # start a web server on port 8094
    httpd = HTTPServer(('', 8094), WordsHandler)
    
    # start a web server on port 8095
    httpd = HTTPServer(('', 8095), WordsHandler)
    
    # start a web server on port 8096
    httpd = HTTPServer(('', 8096), WordsHandler)
    
    # start a web server on port 8097
    httpd = HTTPServer(('', 8097), WordsHandler)
    
    # start a web server on port 8098
    httpd = HTTPServer(('', 8098), 