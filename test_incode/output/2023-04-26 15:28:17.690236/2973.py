#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that calculates a port or iterates over a list of numbers. """    
    
    # Define a port generator that generates numbers between 1 and 10
    portgen = PortGenerator(range(1,11))
    
    # Print the port generator to stdout
    print(portgen)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
    for port in portgen:
        print(port)
    
    # Print the port generator to stdout
