#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds words or adds all ports. """    
    ports = input("Enter ports: ")
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    port