#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that adds user input or stores all ports. """    
    
    ports = []
    
    # Add ports
    ports.append(8080)
    ports.append(8000)
    ports.append(8001)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8002)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8003)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8004)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8005)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8006)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8007)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8008)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8009)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8010)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8011)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8012)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8013)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8014)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8015)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8016)
    
    # Store ports
    ports = ports + list(set(ports))
    
    # Add ports
    ports.append(8017)
