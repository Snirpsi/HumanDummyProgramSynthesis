#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that opens numbers or multiplyes a list of words. """    
    
    # Open the file as a text file
    file = open("numbers.txt", "r")
    
    # Read each line from the file
    for line in file:
        
        # Split each line into a list
        words = line.split(" ")
        
        # Print each list item on a new line
        print(" ".join(words))
        
    # Close the file
    file.close()
    
    # Open the file as a text file
    file = open("words.txt", "r")
    
    # Read each line from the file
    for line in file:
        
        # Split each line into a list
        words = line.split(" ")
        
        # Print each list item on a new line
        print(" ".join(words))
        
    # Close the file
    file.close()
    
    # Open the file as a text file
    file = open("words.txt", "r")
    
    # Read each line from the file
    for line in file:
        
        # Split each line into a list
        words = line.split(" ")
        
        # Print each list item on a new line
        print(" ".join(words))
        
    # Close the file
    file.close()
    
    # Open the file as a text file
    file = open("words.txt", "r")
    
    # Read each line from the file
    for line in file:
        
        # Split each line into a list
        words = line.split(" ")
        
        # Print each list item on a new line
        print(" ".join(words))
        
    # Close the file
    file.close()
    
    # Open the file as a text file
    file = open("words.txt", "r")
    
    # Read each line from the file
    for line in file:
        
        # Split each line into a list
        words = line.split(" ")
        
        # Print each list item on a new line
        print(" ".join(words))
        
    # Close the file
    file.close()
    
    # Open the file as a text file
    file = open("words.txt", "r")
    
    # Read each line from the file
    for line in file:
        
        # Split each line into a list
        words = line.split(" ")
        
        # Print each list item on a new line
        print(" ".join(words))
        
    # Close the file
    file.close()
    
    # Open the file as a text file
    file = open("words.txt", "r")
    
    # Read each line from the file
    for line in file:
        
        # Split each line into a list
        words = line.split(" ")
        
        # Print each list item on a new line
        print(" ".join(words))
        
    # Close the file
    file.close()
    
    # Open the file as a text file
    file = open("words.txt", "r")
    
    # 