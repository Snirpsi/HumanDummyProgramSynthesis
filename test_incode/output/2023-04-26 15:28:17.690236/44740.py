#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that stores all ports or multiplyes words. """    
    while True:
        ports = input("Enter a port number or word: ").split()
        ports = [int(p) for p in ports]
        ports = list(set(ports))
        ports.sort()
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in ports]
        ports = [str(p) for p in port