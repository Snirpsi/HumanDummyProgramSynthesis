#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that enumerates all ports and opens all ports. """    
    
    # Open all ports
    ports = enumerate_ports()
    
    # Print all ports
    for port in ports:
        print(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # Close all ports
    for port in ports:
        close_port(port)
        
    # 