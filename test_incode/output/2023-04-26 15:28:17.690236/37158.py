#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that iterates over a list of words and iterates over a list of words. """    
    words = ['apple', 'banana', 'cherry']
    
    # Iterate over the words and print them out in reverse order.
    for word in reversed(words):
        print(word)
        
    # Iterate over the words and print them out in alphabetical order.
    for word in words:
        print(word)
        
    # Iterate over the words and print them out in alphabetical order.
    for word in sorted(words):
        print(word)
        
    # Iterate over the words and print them out in alphabetical order.
    for word in sorted(words, reverse=True):
        print(word)
        
    # Iterate over the words and print them out in alphabetical order.
    for word in sorted(words, reverse=True, key=len):
        print(word)
        
    # Iterate over the words and print them out in alphabetical order.
    for word in sorted(words, reverse=True, key=len, reverse=True):
        print(word)
        
    # Iterate over the words and print them out in alphabetical order.
    for word in sorted(words, reverse=True, key=len, reverse=True, key=len):
        print(word)
        
    # Iterate over the words and print them out in alphabetical order.
    for word in sorted(words, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len):
        print(word)
        
    # Iterate over the words and print them out in alphabetical order.
    for word in sorted(words, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len):
        print(word)
        
    # Iterate over the words and print them out in alphabetical order.
    for word in sorted(words, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse=True, key=len, reverse