#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that prints user input or multiplyes all ports. """    
    ports = input("Enter ports to multiply: ")
    ports = ports.split()
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
    ports = ports[0]
    ports = ports[1:]
    ports = ports.split(",")
