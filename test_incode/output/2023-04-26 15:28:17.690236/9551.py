#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that stores user input. """    
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 8000), MyHandler)
    
    # Start a web server
    httpd = 