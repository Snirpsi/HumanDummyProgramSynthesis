#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that prints words and converts numbers. """    
    
    # Convert numbers to words
    words = [str(num) for num in range(1, 1001)]
    
    # Print the words and convert the numbers to words
    print('\n'.join(words))
    
    # Convert numbers to words
    words = [str(num) for num in range(1, 1001)]
    
    # Print the words and convert the numbers to words
    print('\n'.join(words))
    
    # Convert numbers to words
    words = [str(num) for num in range(1, 1001)]
    
    # Print the words and convert the numbers to words
    print('\n'.join(words))
    
    # Convert numbers to words
    words = [str(num) for num in range(1, 1001)]
    
    # Print the words and convert the numbers to words
    print('\n'.join(words))
    
    # Convert numbers to words
    words = [str(num) for num in range(1, 1001)]
    
    # Print the words and convert the numbers to words
    print('\n'.join(words))
    
    # Convert numbers to words
    words = [str(num) for num in range(1, 1001)]
    
    # Print the words and convert the numbers to words
    print('\n'.join(words))
    
    # Convert numbers to words
    words = [str(num) for num in range(1, 1001)]
    
    # Print the words and convert the numbers to words
    print('\n'.join(words))
    
    # Convert numbers to words
    words = [str(num) for num in range(1, 1001)]
    
    # Print the words and convert the numbers to words
    print('\n'.join(words))
    
    # Convert numbers to words
    words = [str(num) for num in range(1, 1001)]
    
    # Print the words and convert the numbers to words
    print('\n'.join(words))
    
    # Convert numbers to words
    words = [str(num) for num in range(1, 1001)]
    
    # Print the words and convert the numbers to words
    print('\n'.join(words))
    
    # Convert numbers to words
    words = [str(num) for num in range(1, 1001)]
    
    # Print the words and convert the numbers to words
    print('\n'.join(words))
    
    # Convert numbers to words
    words = [str(num) for num in range(1, 1001)]
    
    # Print the words and convert the numbers to words
    print('\n'.join(words))
    
    # Convert numbers to words
    words = [str(num) for num in range(1, 1001)]
    
    # Print the words and convert the numbers to words
    print('\n'.join(words))
    
    # Convert numbers to words
    words = [str(num) for num in range(1, 1001)]
    
    # Print the words and convert the numbers to 