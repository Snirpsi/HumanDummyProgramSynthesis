#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that multiplyes a port and enumerates words. """    
    port = 8080
    words = []
    
    # Create a server to receive HTTP requests and send responses
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # Start a thread to handle requests
    httpd.serve_forever()
    
    # Print the list of words to the user
    for word in words:
        print(word)
    
    # Shut down the server
    httpd.shutdown()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.socket.close()
    
    # Close the server
    httpd.server_close()
    
    # Close the socket
    httpd.