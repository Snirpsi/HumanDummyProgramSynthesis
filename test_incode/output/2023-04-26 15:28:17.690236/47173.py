#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores all ports or enumerates a list of numbers. """    
    ports = []
    
    # enumerate all ports
    for port in range(1, 65535):
        ports.append(port)
        
    # store all ports
    ports_store = ports
    
    # enumerate all ports
    for port in range(1, 65535):
        ports.append(port)
        
    # store all ports
    ports_store = ports
    
    # enumerate all ports
    for port in range(1, 65535):
        ports.append(port)
        
    # store all ports
    ports_store = ports
    
    # enumerate all ports
    for port in range(1, 65535):
        ports.append(port)
        
    # store all ports
    ports_store = ports
    
    # enumerate all ports
    for port in range(1, 65535):
        ports.append(port)
        
    # store all ports
    ports_store = ports
    
    # enumerate all ports
    for port in range(1, 65535):
        ports.append(port)
        
    # store all ports
    ports_store = ports
    
    # enumerate all ports
    for port in range(1, 65535):
        ports.append(port)
        
    # store all ports
    ports_store = ports
    
    # enumerate all ports
    for port in range(1, 65535):
        ports.append(port)
        
    # store all ports
    ports_store = ports
    
    # enumerate all ports
    for port in range(1, 65535):
        ports.append(port)
        
    # store all ports
    ports_store = ports
    
    # enumerate all ports
    for port in range(1, 65535):
        ports.append(port)
        
    # store all ports
    ports_store = ports
    
    # enumerate all ports
    for port in range(1, 65535):
        ports.append(port)
        
    # store all ports
    ports_store = ports
    
    # enumerate all ports
    for port in range(1, 65535):
        ports.append(port)
        
    # store all ports
    ports_store = ports
    
    # enumerate all ports
    for port in range(1, 65535):
        ports.append(port)
        
    # store all ports
    ports_store = ports
    
    # enumerate all ports
    for port in range(1, 