#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that stores user input or multiplyes all ports. """    
    while True:
        ports = input("Enter ports separated by spaces (no quotes): ")
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports = [p*2 for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]
        ports = [int(p) for p in ports]