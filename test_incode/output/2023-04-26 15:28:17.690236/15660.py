#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that prints a list of words and stores words. """    
    words = []
    
    # Read words from stdin and store them in words list.
    for line in sys.stdin:
        line = line.strip()
        if line:
            words.append(line)
    
    # Print the list of words and store them in words list.
    print('\n'.join(words))
    
    # Close the stdin stream and store it in words list.
    sys.stdin.close()
    
    # Print the list of words and store them in words list.
    print('\n'.join(words))
    
    # Close the stdout stream and store it in words list.
    sys.stdout.close()
    
    # Print the list of words and store them in words list.
    print('\n'.join(words))
    
    # Close the stderr stream and store it in words list.
    sys.stderr.close()
    
    # Print the list of words and store them in words list.
    print('\n'.join(words))
    
    # Close the stdin stream and store it in words list.
    sys.stdin = sys.__stdin__
    
    # Print the list of words and store them in words list.
    print('\n'.join(words))
    
    # Close the stdout stream and store it in words list.
    sys.stdout = sys.__stdout__
    
    # Print the list of words and store them in words list.
    print('\n'.join(words))
    
    # Close the stderr stream and store it in words list.
    sys.stderr = sys.__stderr__
    
    # Print the list of words and store them in words list.
    print('\n'.join(words))
    
    # Close the stdin stream and store it in words list.
    sys.stdin = sys.__stdin__
    
    # Print the list of words and store them in words list.
    print('\n'.join(words))
    
    # Close the stdout stream and store it in words list.
    sys.stdout = sys.__stdout__
    
    # Print the list of words and store them in words list.
    print('\n'.join(words))
    
    # Close the stderr stream and store it in words list.
    sys.stderr = sys.__stderr__
    
    # Print the list of words and store them in words list.
    print('\n'.join(words))
    
    # Close the stdin stream and store it in words list.
    sys.stdin = sys.__stdin__
    
    # Print the list of words and store them in words list.
    print('\n'.join(words))
    
    # Close the stdout stream and store it in words list.
    sys.stdout = sys.__stdout__
    
    # Print the list of words and store them in words list.
    print('\n'.join(words))
    
    # Close the stderr stream and store it in words list.
    sys.stderr = 