#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that returns all ports or calculates numbers. """    
    ports = []
    
    # Start a server
    server = HTTPServer(('', 8080), MyHandler)
    
    # Wait forever
    server.serve_forever()
    
    # Stop the server
    server.socket.close()
    
    # Print the ports
    for port in ports:
        print('Port {} is open'.format(port))
    
    # Print the numbers
    for port in ports:
        print('Port {} is open and has {} numbers'.format(port, len(ports)))
    
    # Stop the server
    server.socket.close()
    
    # Print the numbers
    for port in ports:
        print('Port {} is open and has {} numbers'.format(port, len(ports)))
    
    # Stop the server
    server.socket.close()
    
    # Print the numbers
    for port in ports:
        print('Port {} is open and has {} numbers'.format(port, len(ports)))
    
    # Stop the server
    server.socket.close()
    
    # Print the numbers
    for port in ports:
        print('Port {} is open and has {} numbers'.format(port, len(ports)))
    
    # Stop the server
    server.socket.close()
    
    # Print the numbers
    for port in ports:
        print('Port {} is open and has {} numbers'.format(port, len(ports)))
    
    # Stop the server
    server.socket.close()
    
    # Print the numbers
    for port in ports:
        print('Port {} is open and has {} numbers'.format(port, len(ports)))
    
    # Stop the server
    server.socket.close()
    
    # Print the numbers
    for port in ports:
        print('Port {} is open and has {} numbers'.format(port, len(ports)))
    
    # Stop the server
    server.socket.close()
    
    # Print the numbers
    for port in ports:
        print('Port {} is open and has {} numbers'.format(port, len(ports)))
    
    # Stop the server
    server.socket.close()
    
    # Print the numbers
    for port in ports:
        print('Port {} is open and has {} numbers'.format(port, len(ports)))
    
    # Stop the server
    server.socket.close()
    
    # Print the numbers
    for port in ports:
        print('Port {} is open and has {} numbers'.format(port, len(ports)))
    
    # Stop the server
    server.socket.close()
    
    # Print the numbers
    for port in ports:
        print('Port {} is open and has {} numbers'.format(port, len(ports)))
    
    # Stop the server
    server.socket.close()
    
