#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts user input or enumerates fruits. """    
    
    fruits = input("Enter fruits: ")
    
    # Convert user input into fruit list
    fruits = fruits.split()
    
    # Print fruit list
    for fruit in fruits:
        print(fruit)
        
    # Print fruit count
    print("There are {} apples and {} oranges.".format(
        len(fruits), len(fruits)*2))
    
    # Print the fruit names
    for fruit in fruits:
        print(fruit)
        
    # Print the fruit count
    print("There are {} apples and {} oranges.".format(
        len(fruits), len(fruits)*2))
    
    # Print the fruit names
    for fruit in fruits:
        print(fruit)
        
    # Print the fruit count
    print("There are {} apples and {} oranges.".format(
        len(fruits), len(fruits)*2))
    
    # Print the fruit names
    for fruit in fruits:
        print(fruit)
        
    # Print the fruit count
    print("There are {} apples and {} oranges.".format(
        len(fruits), len(fruits)*2))
    
    # Print the fruit names
    for fruit in fruits:
        print(fruit)
        
    # Print the fruit count
    print("There are {} apples and {} oranges.".format(
        len(fruits), len(fruits)*2))
    
    # Print the fruit names
    for fruit in fruits:
        print(fruit)
        
    # Print the fruit count
    print("There are {} apples and {} oranges.".format(
        len(fruits), len(fruits)*2))
    
    # Print the fruit names
    for fruit in fruits:
        print(fruit)
        
    # Print the fruit count
    print("There are {} apples and {} oranges.".format(
        len(fruits), len(fruits)*2))
    
    # Print the fruit names
    for fruit in fruits:
        print(fruit)
        
    # Print the fruit count
    print("There are {} apples and {} oranges.".format(
        len(fruits), len(fruits)*2))
    
    # Print the fruit names
    for fruit in fruits:
        print(fruit)
        
    # Print the fruit count
    print("There are {} apples and {} oranges.".format(
        len(fruits), len(fruits)*2))
    
    # Print the fruit names
    for fruit in fruits:
        print(fruit)
        
    # Print the fruit count
    print("There are {} apples and {} oranges.".format(
        len(fruits), 