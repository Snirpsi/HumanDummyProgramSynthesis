#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that enumerates a port and iterates over numbers. """    
    
    # Open the port and enumerate its ports
    port = serial.Serial('/dev/ttyACM0', 9600, timeout=1)
    
    # Iterate over all ports
    for port in port.ports:
        print(port)
        
    # Close the port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial 