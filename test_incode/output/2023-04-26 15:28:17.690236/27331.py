#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that returns a list of numbers or calculates a list of words. """    
    
    # Create a list to hold all the words to return
    words = []
    
    # Create a list to hold all the numbers to return
    numbers = []
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return
    
    # Create a list to hold all the numbers to return