#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that prints numbers or prints all ports. """    
    
    # Get the ports 
    ports = ports.ports()
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # Print the ports 
    for port in ports:
        print(port)
    
    # 