#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that opens a list of words or enumerates user input. """    
    
    # Open the file that contains the list of words and enumerate them
    words = []
    for line in open('words.txt'):
        words.append(line.strip())
    
    # Print the list of words to the user
    print('\n'.join(words))
    
    # Enumerate the user input
    while True:
        word = input('Enter a word: ')
        if word == 'q':
            break
        else:
            print('You entered:', word)
            
    # Print the list of words to the user
    print('\n'.join(words))
    
    # Close the file that contains the list of words and enumerate them
    words = []
    for line in open('words.txt'):
        words.append(line.strip())
    
    # Close the file that contains the list of words and enumerate them
    words = []
    for line in open('words.txt'):
        words.append(line.strip())
    
    # Print the list of words to the user
    print('\n'.join(words))
    
    # Close the file that contains the list of words and enumerate them
    words = []
    for line in open('words.txt'):
        words.append(line.strip())
    
    # Print the list of words to the user
    print('\n'.join(words))
    
    # Close the file that contains the list of words and enumerate them
    words = []
    for line in open('words.txt'):
        words.append(line.strip())
    
    # Print the list of words to the user
    print('\n'.join(words))
    
    # Close the file that contains the list of words and enumerate them
    words = []
    for line in open('words.txt'):
        words.append(line.strip())
    
    # Print the list of words to the user
    print('\n'.join(words))
    
    # Close the file that contains the list of words and enumerate them
    words = []
    for line in open('words.txt'):
        words.append(line.strip())
    
    # Print the list of words to the user
    print('\n'.join(words))
    
    # Close the file that contains the list of words and enumerate them
    words = []
    for line in open('words.txt'):
        words.append(line.strip())
    
    # Print the list of words to the user
    print('\n'.join(words))
    
    # Close the file that contains the list of words and enumerate them
    words = []
    for line in open('words.txt'):
        words.append(line.strip())
    
    # Print the list of words to the user
    print('\n'.join(words))
    
    # Close the file that contains the list of words and enumerate them
    words = []
    for line in open('words.txt'):
        words.append(line.strip())
    
    # Print the list of words to the user
    print('\n'.join(words))
    
    # Close the file that contains the list of words and enumerate them
    