#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that adds words or converts a list of numbers. """    
    
    import sys
    
    if len(sys.argv) > 1:
        wordlist = sys.argv[1]
    else:
        wordlist = input("Enter a comma-separated list of numbers: ")
    
    wordlist = wordlist.split(",")
    
    wordlist = list(map(int, wordlist))
    
    wordlist = list(set(wordlist))
    
    wordlist = sorted(wordlist)
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(wordlist)
    
    wordlist = list(set(wordlist))
    
    wordlist = list(