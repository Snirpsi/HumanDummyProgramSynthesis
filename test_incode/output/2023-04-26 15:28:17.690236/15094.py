#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that adds a list of words or multiplyes a list of numbers. """    
    
    import sys
    
    if len(sys.argv) != 2:
        print("Usage: %s <words>" % sys.argv[0])
        sys.exit(1)
    
    words = sys.argv[1].split()
    
    if len(words) == 0:
        print("Usage: %s <words>" % sys.argv[0])
        sys.exit(1)
    
    numbers = map(int, sys.argv[2:])
    
    numbers = map(lambda x: x * 10, numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(