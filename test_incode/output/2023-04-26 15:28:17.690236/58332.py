#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that multiplyes all ports or iterates over words. """    
    ports = ['COM1', 'COM2', 'COM3']
    words = ['word1', 'word2', 'word3']
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for port in ports:
        for word in words:
            print(port + ' ' + word)
    
    for 