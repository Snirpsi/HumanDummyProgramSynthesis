#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens a port or adds a list of numbers. """    
    ports = [int(i) for i in input("Enter the ports separated by comma: ").split(",")]
    ports = list(set(ports))
    ports.sort()
    
    ports_open = input("Enter the ports you want to open: ")
    
    ports_open = list(set(ports_open))
    ports_open = ports_open.sort()
    
    ports_open = ports_open + ports_open
    
    ports_open = list(set(ports_open))
    ports_open = ports_open.sort()
    
    ports_open = ports_open + ports_open
    
    ports_open = list(set(ports_open))
    ports_open = ports_open.sort()
    
    ports_open = ports_open + ports_open
    
    ports_open = list(set(ports_open))
    ports_open = ports_open.sort()
    
    ports_open = ports_open + ports_open
    
    ports_open = list(set(ports_open))
    ports_open = ports_open.sort()
    
    ports_open = ports_open + ports_open
    
    ports_open = list(set(ports_open))
    ports_open = ports_open.sort()
    
    ports_open = ports_open + ports_open
    
    ports_open = list(set(ports_open))
    ports_open = ports_open.sort()
    
    ports_open = ports_open + ports_open
    
    ports_open = list(set(ports_open))
    ports_open = ports_open.sort()
    
    ports_open = ports_open + ports_open
    
    ports_open = list(set(ports_open))
    ports_open = ports_open.sort()
    
    ports_open = ports_open + ports_open
    
    ports_open = list(set(ports_open))
    ports_open = ports_open.sort()
    
    ports_open = ports_open + ports_open
    
    ports_open = list(set(ports_open))
    ports_open = ports_open.sort()
    
    ports_open = ports_open + ports_open
    
    ports_open = list(set(ports_open))
    ports_open = ports_open.sort()
    
    ports_open = ports_open + ports_open
    
    ports_open = list(set(port