#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that enumerates numbers and converts a list of numbers. """    
    numbers = range(10)
    numbers_converted = []
    
    while True:
        numbers_converted.append(numbers.pop())
        
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if numbers:
            numbers_converted.append(numbers.pop())
            
        if number