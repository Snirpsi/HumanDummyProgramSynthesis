#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that enumerates a list of words and opens a list of words. """    
    words = ['hello', 'world', '!', '!', '!']
    
    # enumerate words and open them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and close them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and open them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and close them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and open them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and close them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and open them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and close them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and open them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and close them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and open them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and close them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and open them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and close them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and open them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and close them in a new window
    for index, word in enumerate(words):
        subprocess.call(['notepad', word])
        
    # enumerate words and open them in a new window
    for index, word in enumerate(