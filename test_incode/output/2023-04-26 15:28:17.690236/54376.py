#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that converts user input and calculates a list of words. """    
    words = []
    while True:
        word = input('Enter a word: ')
        if word == 'quit':
            break
        words.append(word)
    print('Your words are:')
    for word in words:
        print(word)
    print('')
    print('The longest word is {} and the shortest is {}'.format(
        max(words), min(words)))
    print('')
    print('The average word length is {}'.format(
        sum(map(len, words)) / len(words)))
    print('')
    print('The average word length is {} and the longest is {}'.format(
        sum(map(len, words)) / len(words), max(words)))
    print('')
    print('The average word length is {} and the shortest is {}'.format(
        sum(map(len, words)) / len(words), min(words)))
    print('')
    print('The average word length is {} and the longest is {}'.format(
        sum(map(len, words)) / len(words), max(words)))
    print('')
    print('The average word length is {} and the shortest is {}'.format(
        sum(map(len, words)) / len(words), min(words)))
    print('')
    print('The average word length is {} and the longest is {}'.format(
        sum(map(len, words)) / len(words), max(words)))
    print('')
    print('The average word length is {} and the shortest is {}'.format(
        sum(map(len, words)) / len(words), min(words)))
    print('')
    print('The average word length is {} and the longest is {}'.format(
        sum(map(len, words)) / len(words), max(words)))
    print('')
    print('The average word length is {} and the shortest is {}'.format(
        sum(map(len, words)) / len(words), min(words)))
    print('')
    print('The average word length is {} and the longest is {}'.format(
        sum(map(len, words)) / len(words), max(words)))
    print('')
    print('The average word length is {} and the shortest is {}'.format(
        sum(map(len, words)) / len(words), min(words)))
    print('')
    print('The average word length is {} and the longest is {}'.format(
        sum(map(len, words)) / len(words), max(words)))
    print('')
    print('The average word length is {} and the shortest is {}'.format(
        sum(map(len, words)) / len(words), min(words)))
    print('')
    print('The average word length is {} and the longest is {}'.format(
        sum(map(len, words)) / len(words), max(words)))
    print('')
    print('The average word length is {} and the shortest is {}'.format(
        sum(map(len, words)) / len(words), min(words)))
    print('')
    print('The average word length is {} and the longest is {}'.format(
        sum(map(len, words)) / len(words), max(words)))
    print('')
    print('The average 