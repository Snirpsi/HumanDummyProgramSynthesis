#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that opens numbers or removes fruits. """    
    
    # Open a file
    file = open('fruits.txt', 'r')
    
    # Read all lines in file
    lines = file.readlines()
    
    # Remove all fruits
    lines = filter(lambda x: x != '', lines)
    
    # Write all lines to file
    file.writelines(lines)
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fruits
    print('\n'.join(' '.join(line) for line in lines))
    
    # Close file
    file.close()
    
    # Print all fru