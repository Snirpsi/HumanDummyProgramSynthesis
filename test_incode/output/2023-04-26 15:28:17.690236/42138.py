#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that removes words and opens numbers. """    
    
    words = input("Enter the words to remove: ")
    
    words = words.split()
    
    numbers = input("Enter the numbers to remove: ")
    
    numbers = numbers.split()
    
    numbers = filter(lambda x: x not in words, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in numbers, numbers)
    
    numbers = filter(lambda x: x not in number