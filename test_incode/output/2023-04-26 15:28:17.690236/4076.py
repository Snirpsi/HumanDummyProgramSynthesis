#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that multiplyes a list of numbers and converts a list of words. """    
    import sys
    
    words = []
    numbers = []
    
    for arg in sys.argv[1:]:
        words.append(arg)
        
    for arg in sys.argv[2:]:
        numbers.append(arg)
    
    numbers = map(int, numbers)
    words = map(str, words)
    
    numbers = map(lambda x: x*10, numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(str, numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(lambda x: str(x)+'x', numbers)
    words = map(lambda x: str(x)+'x', words)
    
    numbers = map(