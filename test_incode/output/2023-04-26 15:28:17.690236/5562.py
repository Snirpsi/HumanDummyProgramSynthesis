#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that returns a port or opens numbers. """    
    ports = [int(i) for i in sys.argv[1:]]
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.append(ports[0])
    
    ports = list(set(