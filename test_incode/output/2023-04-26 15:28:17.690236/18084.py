#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that prints a list of words. """    
    words = ['cat', 'dog', 'mouse', 'bird', 'elephant']
    
    while True:
        
        # Print the words in the list.
        for word in words:
            print(word)
            
        # Wait for a key press.
        pressed = input('Press any key to exit: ')
        
        # Exit the loop if the user pressed 'q'.
        if pressed == 'q':
            break
        
        # Wait for a second for the user to press a key.
        pressed = input('Press any key to continue: ')
        
        # Continue the loop if the user pressed 'c'.
        if pressed == 'c':
            continue
        
        # Wait for a second for the user to press a key.
        pressed = input('Press any key to continue: ')
        
        # Continue the loop if the user pressed 'q'.
        if pressed == 'q':
            break
        
        # Wait for a second for the user to press a key.
        pressed = input('Press any key to continue: ')
        
        # Continue the loop if the user pressed 'c'.
        if pressed == 'c':
            continue
        
        # Wait for a second for the user to press a key.
        pressed = input('Press any key to continue: ')
        
        # Continue the loop if the user pressed 'q'.
        if pressed == 'q':
            break
        
        # Wait for a second for the user to press a key.
        pressed = input('Press any key to continue: ')
        
        # Continue the loop if the user pressed 'c'.
        if pressed == 'c':
            continue
        
        # Wait for a second for the user to press a key.
        pressed = input('Press any key to continue: ')
        
        # Continue the loop if the user pressed 'q'.
        if pressed == 'q':
            break
        
        # Wait for a second for the user to press a key.
        pressed = input('Press any key to continue: ')
        
        # Continue the loop if the user pressed 'c'.
        if pressed == 'c':
            continue
        
        # Wait for a second for the user to press a key.
        pressed = input('Press any key to continue: ')
        
        # Continue the loop if the user pressed 'q'.
        if pressed == 'q':
            break
        
        # Wait for a second for the user to press a key.
        pressed = input('Press any key to continue: ')
        
        # Continue the loop if the user pressed 'c'.
        if pressed == 'c':
            continue
        
        # Wait for a second for the user to pres