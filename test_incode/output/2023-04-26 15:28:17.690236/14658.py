#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that removes words. """    
    
    import sys
    
    words = sys.argv[1:]
    
    for word in words:
        
        word = word.strip()
        
        if word == '':
            continue
        
        if word.endswith('s'):
            word = word[:-1]
        
        if word.endswith('e'):
            word = word[:-1]
        
        if word.endswith('x'):
            word = word[:-1]
        
        if word.endswith('o'):
            word = word[:-1]
        
        if word.endswith('e'):
            word = word[:-1]
        
        if word.endswith('x'):
            word = word[:-1]
        
        if word.endswith('o'):
            word = word[:-1]
        
        if word.endswith('e'):
            word = word[:-1]
        
        if word.endswith('x'):
            word = word[:-1]
        
        if word.endswith('o'):
            word = word[:-1]
        
        if word.endswith('e'):
            word = word[:-1]
        
        if word.endswith('x'):
            word = word[:-1]
        
        if word.endswith('o'):
            word = word[:-1]
        
        if word.endswith('e'):
            word = word[:-1]
        
        if word.endswith('x'):
            word = word[:-1]
        
        if word.endswith('o'):
            word = word[:-1]
        
        if word.endswith('e'):
            word = word[:-1]
        
        if word.endswith('x'):
            word = word[:-1]
        
        if word.endswith('o'):
            word = word[:-1]
        
        if word.endswith('e'):
            word = word[:-1]
        
        if word.endswith('x'):
            word = word[:-1]
        
        if word.endswith('o'):
            word = word[:-1]
        
        if word.endswith('e'):
            word = word[:-1]
        
        if word.endswith('x'):
            word = word[:-1]
        
        if word.endswith('o'):
            word = word[:-1]
        
        if word.endswith('e'):
            word = word[:-1]
        
        if word.endswith('x'):
            word = word[:-1]
        
        if word.endswith('o'):
            word = word[:-1]
        
        if word.endswith('e'):
            word = word[:-1]
        
        if word.endswith('x'):
            word = word[:-1]
        
        if word.endswith('o'):
            word = word[:-1]
        
        if word.endswith('e'):
            word = word[:-1]
        
        if word.endswith('x'):
            word = word[:-1]
        
        if word.endswith('o'):
            word = word[:-1]
        
        if word.endswith('e'):
            word = word[:-1]
        
        if word.endswith('x'):
            word = word[:-1]
        
        if word.endswith('o'):
            word = 