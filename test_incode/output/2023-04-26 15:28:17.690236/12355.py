#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that removes a port and converts numbers. """    
    port = int(sys.argv[1])
    
    if port < 0 or port > 65535:
        print("Port must be between 0 and 65535.")
        exit(1)
    
    port_to_remove = port
    
    ports_to_remove = []
    
    # Remove all ports except the one we want
    for port in range(port, 65535):
        if port not in ports_to_remove:
            ports_to_remove.append(port)
            
    # Remove all ports except the one we want
    for port in ports_to_remove:
        
        # Remove all ports except the one we want
        if port not in ports_to_remove:
            
            # Remove all ports except the one we want
            if port not in ports_to_remove:
                
                # Remove all ports except the one we want
                if port not in ports_to_remove:
                    
                    # Remove all ports except the one we want
                    if port not in ports_to_remove:
                        
                        # Remove all ports except the one we want
                        if port not in ports_to_remove:
                            
                            # Remove all ports except the one we want
                            if port not in ports_to_remove:
                                
                                # Remove all ports except the one we want
                                if port not in ports_to_remove:
                                    
                                    # Remove all ports except the one we want
                                    if port not in ports_to_remove:
                                        
                                        # Remove all ports except the one we want
                                        if port not in ports_to_remove:
                                            
                                            # Remove all ports except the one we want
                                            if port not in ports_to_remove:
                                                
                                                # Remove all ports except the one we want
                                                if port not in ports_to_remove:
                                                    
                                                    # Remove all ports except the one we want
                                                    if port not in ports_to_remove:
                                                        
                                                        # Remove all ports except the one we want
                                                        if port not in ports_to_remove:
                                                            
                                                            # Remove all ports except the one we want
                                                            if port not in ports_to_remove:
                                                                
                                                                # Remove all ports except the one we want
                                                                if port not in ports_to_remove:
                                                                    
                                                                    # Remove all ports except the one we want
                                                                    if port not in ports_to_remove:
                                                                        
                                                                        # Remove all ports except the one we want
                                                                        if port not in ports_to_remove:
                                                                
                                                                            # Remove all ports except the one we want
                                                                            if port not in ports_to_remove:
                                                                