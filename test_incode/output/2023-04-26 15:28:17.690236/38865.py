#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that adds numbers and calculates all ports. """    
    
    import sys
    
    ports = []
    
    if len(sys.argv) > 1:
        ports = sys.argv[1:]
    
    ports = map(int, ports)
    
    ports = list(set(ports))
    
    ports.sort()
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    ports = list(map(str, ports))
    
    