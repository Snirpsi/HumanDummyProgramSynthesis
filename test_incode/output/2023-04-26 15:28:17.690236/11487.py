#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that multiplyes a port or stores numbers. """    
    
    # Read the numbers from the command line
    numbers = sys.argv[1:]
    
    # Multiply each number in the list
    for number in numbers:
        number = int(number)
        
        # Store the result in the list
        numbers.append(number * number)
        
    # Print the list
    print(numbers)
    
    # Print the result
    print(sum(numbers))
    
    # Print the average
    print(sum(numbers) / len(numbers))
    
    # Print the standard deviation
    print(math.sqrt((sum(numbers)-len(numbers))*len(numbers)/len(numbers)))
    
    # Print the minimum
    print(min(numbers))
    
    # Print the maximum
    print(max(numbers))
    
    # Print the median
    print(sorted(numbers)[int(len(numbers) / 2)])
    
    # Print the mode
    print(max(numbers))
    
    # Print the standard deviation of a list
    print(math.sqrt((sum(numbers)-len(numbers))*len(numbers)/len(numbers)))
    
    # Print the standard deviation of a list
    print(math.sqrt((sum(numbers)-len(numbers))*len(numbers)/len(numbers)))
    
    # Print the standard deviation of a list
    print(math.sqrt((sum(numbers)-len(numbers))*len(numbers)/len(numbers)))
    
    # Print the standard deviation of a list
    print(math.sqrt((sum(numbers)-len(numbers))*len(numbers)/len(numbers)))
    
    # Print the standard deviation of a list
    print(math.sqrt((sum(numbers)-len(numbers))*len(numbers)/len(numbers)))
    
    # Print the standard deviation of a list
    print(math.sqrt((sum(numbers)-len(numbers))*len(numbers)/len(numbers)))
    
    # Print the standard deviation of a list
    print(math.sqrt((sum(numbers)-len(numbers))*len(numbers)/len(numbers)))
    
    # Print the standard deviation of a list
    print(math.sqrt((sum(numbers)-len(numbers))*len(numbers)/len(numbers)))
    
    # Print the standard deviation of a list
    print(math.sqrt((sum(numbers)-len(numbers))*len(numbers)/len(numbers)))
    
    # Print the standard deviation of a list
    print(math.sqrt((sum(numbers)-len(numbers))*len(numbers)/len(numbers)))
    
    # Print the standard deviation of a list
    print(math.sqrt((sum(numbers)-len(numbers))*len(numbers)/len(numbers)))
    
    # Print the standard deviation of a list
    print(math.sqrt((sum(numbers)-len(numbers))*