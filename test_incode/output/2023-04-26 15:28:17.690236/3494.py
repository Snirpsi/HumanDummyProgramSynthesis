#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that converts all ports or iterates over fruits. """    
    
    while True:
        
        # convert all ports to integers
        ports = [int(port) for port in input().split()]
        
        # iterate over all fruits and convert each port
        for fruit in fruits:
            
            # convert each port to integer
            ports = [int(port) for port in input().split()]
            
            # iterate over all ports and convert each fruit
            for port in ports:
                
                # convert each fruit to integer
                fruits = [int(fruit) for fruit in input().split()]
                
                # convert each port to integer
                ports = [int(port) for port in input().split()]
                
                # convert each fruit to integer
                fruits = [int(fruit) for fruit in input().split()]
                
                # convert each port to integer
                ports = [int(port) for port in input().split()]
                
                # convert each fruit to integer
                fruits = [int(fruit) for fruit in input().split()]
                
                # convert each port to integer
                ports = [int(port) for port in input().split()]
                
                # convert each fruit to integer
                fruits = [int(fruit) for fruit in input().split()]
                
                # convert each port to integer
                ports = [int(port) for port in input().split()]
                
                # convert each fruit to integer
                fruits = [int(fruit) for fruit in input().split()]
                
                # convert each port to integer
                ports = [int(port) for port in input().split()]
                
                # convert each fruit to integer
                fruits = [int(fruit) for fruit in input().split()]
                
                # convert each port to integer
                ports = [int(port) for port in input().split()]
                
                # convert each fruit to integer
                fruits = [int(fruit) for fruit in input().split()]
                
                # convert each port to integer
                ports = [int(port) for port in input().split()]
                
                # convert each fruit to integer
                fruits = [int(fruit) for fruit in input().split()]
                
                # convert each port to integer
                ports = [int(port) for port in input().split()]
                
                # convert each fruit to integer
                fruits = [int(fruit) for fruit in input().split()]
                
                # convert each port to integer
                ports = [int(port) for port in input().split()]
                
                # convert each fruit to integer
                fruits = [