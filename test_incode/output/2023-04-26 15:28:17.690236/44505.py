#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that adds a list of words or returns words. """    
    while True:
        words = input('Enter a word: ')
        words = words.split()
        words.extend(words)
        print(words)
        
        if words == ['quit']:
            break
        
        if words == ['exit']:
            break
        
        if words == ['quit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit', 'exit']:
            break
        
        if words == ['quit', 'exit', 'exit', '