#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that stores all ports or removes a list of numbers. """    
    ports = sys.argv[1:]
    ports = ports.split(',')
    ports = [int(port) for port in ports]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    ports = list(set(ports))
    ports.sort()
    ports = ports[::-1]
    