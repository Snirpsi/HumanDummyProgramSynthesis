#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens a list of words and opens all ports. """    
    
    words = []
    ports = []
    
    for line in sys.stdin:
        line = line.strip()
        
        if line == '':
            continue
        
        words.append(line)
        
    for line in sys.stdin:
        line = line.strip()
        
        if line == '':
            continue
        
        ports.append(line)
        
    ports = ports.split(',')
    
    words = list(set(words))
    
    ports = list(set(ports))
    
    words.sort()
    
    ports.sort()
    
    ports_map = {}
    
    for port in ports:
        ports_map[port] = port
    
    ports_map = {}
    
    for word in words:
        ports_map[word] = word
    
    ports_map = {}
    
    for port in ports:
        ports_map[port] = port
    
    ports_map = {}
    
    for word in words:
        ports_map[word] = word
    
    ports_map = {}
    
    for port in ports:
        ports_map[port] = port
    
    ports_map = {}
    
    for word in words:
        ports_map[word] = word
    
    ports_map = {}
    
    for port in ports:
        ports_map[port] = port
    
    ports_map = {}
    
    for word in words:
        ports_map[word] = word
    
    ports_map = {}
    
    for port in ports:
        ports_map[port] = port
    
    ports_map = {}
    
    for word in words:
        ports_map[word] = word
    
    ports_map = {}
    
    for port in ports:
        ports_map[port] = port
    
    ports_map = {}
    
    for word in words:
        ports_map[word] = word
    
    ports_map = {}
    
    for port in ports:
        ports_map[port] = port
    
    ports_map = {}
    
    for word in words:
        ports_map[word] = word
    
    ports_map = {}
    
    for port in ports:
        ports_map[port] = port
    
    ports_map = {}
    
    for word in words:
        ports_map[word] = word
    
    ports_map = {}
    
    for port in ports:
        ports_map[port] = port
    
    ports_map = {}
    
    for word in words:
        ports_map[word] = word
    
    ports_map = {}
    
    for port in 