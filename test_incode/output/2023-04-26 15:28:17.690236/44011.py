#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that converts all ports or calculates numbers. """    
    while True:
        ports = input("Enter ports: ")
        ports = ports.split()
        ports = map(int, ports)
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)
        ports.sort()
        ports = list(ports)