#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that multiplyes words. """    
    
    import sys
    
    words = [w.strip() for w in sys.stdin.read().split()]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if w]
    
    words = [w for w in words if 