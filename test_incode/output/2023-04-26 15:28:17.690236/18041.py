#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds all ports and converts user input. """    
    ports = input("Enter ports: ").split()
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["-" + str(i) for i in range(len(ports))]
    ports = ports + ["