#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that calculates a list of words or enumerates words. """    
    
    words = []
    
    # Add words to the list
    for word in sys.argv[1:]:
        words.append(word)
    
    # Print the list
    for word in words:
        print(word)
    
    # Enumerate the list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))
    
    # Print the enumerated list
    for index, word in enumerate(words):
        print('{}. {}'.format(index+1, word))