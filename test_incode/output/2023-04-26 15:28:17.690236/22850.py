#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that iterates over user input and opens all ports. """    
    
    ports = []
    
    while True:
        ports.append(input("Enter a port number: "))
        
        if ports[-1] == 'q':
            break
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port in ports:
        
        if port == 'q':
            break
        
        ports.append(port)
    
    for port 