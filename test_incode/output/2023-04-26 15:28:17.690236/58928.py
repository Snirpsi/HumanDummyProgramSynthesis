#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that converts a list of words and calculates a list of numbers. """    
    words = ['apple', 'banana', 'cherry']
    numbers = [1, 2, 3]
    
    word_count = len(words)
    number_count = len(numbers)
    
    words_per_number = word_count / number_count
    
    numbers_per_word = number_count / word_count
    
    average = words_per_number * numbers_per_word
    
    print('Average word count per number:', average)
    
    average = numbers_per_word * word_count
    
    print('Average number count per word:', average)
    
    average = words_per_word * number_count
    
    print('Average word count per number per word:', average)
    
    average = numbers_per_word * word_count * number_count
    
    print('Average number count per number per word:', average)
    
    average = words_per_word * number_count * word_count
    
    print('Average word count per number per number per word:', average)
    
    average = numbers_per_word * word_count * number_count * word_count
    
    print('Average number count per number per number per word:', average)
    
    average = words_per_word * number_count * word_count * number_count
    
    print('Average word count per number per number per number per word:', average)
    
    average = numbers_per_word * word_count * number_count * word_count * number_count
    
    print('Average number count per number per number per number per word:', average)
    
    average = words_per_word * number_count * word_count * number_count * word_count * number_count
    
    print('Average word count per number per number per number per number per word:', average)
    
    average = numbers_per_word * word_count * number_count * word_count * number_count * word_count
    
    print('Average number count per number per number per number per number per word:', average)
    
    average = words_per_word * number_count * word_count * number_count * word_count * number_count * word_count
    
    print('Average word count per number per number per number per number per number per word:', average)
    
    average = numbers_per_word * word_count * number_count * word_count * number_count * word_count * number_count * word_count
    
    print('Average number count per number per number per number per number per number per word:', average)
    
    average = words_per_word * number_count * word_count * number_count * word_count * number_