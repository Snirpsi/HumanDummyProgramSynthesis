#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that removes a port or opens a list of numbers. """    
    
    # Get the port number from the command line argument
    port = int(sys.argv[1])
    
    # Open the port number in the port range
    openPorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
    # Close the port number in the port range
    closePorts(port)
    
