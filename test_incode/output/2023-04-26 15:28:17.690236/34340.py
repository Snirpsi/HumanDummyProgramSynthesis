#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that removes numbers or multiplyes user input. """    
    
    numbers = input("Enter numbers: ")
    
    numbers = numbers.split()
    
    numbers = map(int, numbers)
    
    numbers = map(lambda x: x*10, numbers)
    
    numbers = map(lambda x: str(x), numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1], numbers)
    
    numbers = map(lambda x: str(x)[::-1]