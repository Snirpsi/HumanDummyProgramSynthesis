#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that calculates all ports or calculates user input. """    
    
    # Read user input
    ports = input('Enter the ports separated by comma: ')
    
    # Split ports
    ports = ports.split(',')
    
    # Calculate all ports
    ports = calc_ports(ports)
    
    # Print results
    print('Ports:', ports)
    
    # Calculate user input
    ports = input('Enter the ports separated by comma: ')
    
    # Split ports
    ports = ports.split(',')
    
    # Calculate all ports
    ports = calc_ports(ports)
    
    # Print results
    print('Ports:', ports)
    
    # Calculate user input
    ports = input('Enter the ports separated by comma: ')
    
    # Split ports
    ports = ports.split(',')
    
    # Calculate all ports
    ports = calc_ports(ports)
    
    # Print results
    print('Ports:', ports)
    
    # Calculate user input
    ports = input('Enter the ports separated by comma: ')
    
    # Split ports
    ports = ports.split(',')
    
    # Calculate all ports
    ports = calc_ports(ports)
    
    # Print results
    print('Ports:', ports)
    
    # Calculate user input
    ports = input('Enter the ports separated by comma: ')
    
    # Split ports
    ports = ports.split(',')
    
    # Calculate all ports
    ports = calc_ports(ports)
    
    # Print results
    print('Ports:', ports)
    
    # Calculate user input
    ports = input('Enter the ports separated by comma: ')
    
    # Split ports
    ports = ports.split(',')
    
    # Calculate all ports
    ports = calc_ports(ports)
    
    # Print results
    print('Ports:', ports)
    
    # Calculate user input
    ports = input('Enter the ports separated by comma: ')
    
    # Split ports
    ports = ports.split(',')
    
    # Calculate all ports
    ports = calc_ports(ports)
    
    # Print results
    print('Ports:', ports)
    
    # Calculate user input
    ports = input('Enter the ports separated by comma: ')
    
    # Split ports
    ports = ports.split(',')
    
    # Calculate all ports
    ports = calc_ports(ports)
    
    # Print 