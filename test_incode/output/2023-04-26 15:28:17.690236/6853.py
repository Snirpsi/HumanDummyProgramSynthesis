#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that iterates over a list of words or stores user input. """    
    
    # Read user input from stdin
    words = []
    for line in sys.stdin:
        words.append(line.strip())
    
    # Iterate over each word
    for word in words:
        print(word)
    
    # Print the final word
    print(words[-1])
    
    # Print the final word with spaces
    print(" ".join(words))
    
    # Print the final word with spaces and a newline
    print(" ".join(words), end="\n")
    
    # Print the final word with spaces and a newline and an extra space
    print(" ".join(words), end="")
    
    # Print the final word with spaces and a newline and an extra space and an extra newline
    print(" ".join(words), end="")
    
    # Print the final word with spaces and a newline and an extra space and an extra newline and an extra space
    print(" ".join(words), end="")
    
    # Print the final word with spaces and a newline and an extra space and an extra newline and an extra space
    print(" ".join(words), end="")
    
    # Print the final word with spaces and a newline and an extra space and an extra newline and an extra space
    print(" ".join(words), end="")
    
    # Print the final word with spaces and a newline and an extra space and an extra newline and an extra space and an extra newline
    print(" ".join(words), end="")
    
    # Print the final word with spaces and a newline and an extra space and an extra newline and an extra space and an extra newline and an extra space
    print(" ".join(words), end="")
    
    # Print the final word with spaces and a newline and an extra space and an extra newline and an extra space and an extra newline and an extra space
    print(" ".join(words), end="")
    
    # Print the final word with spaces and a newline and an extra space and an extra newline and an extra space and an extra newline and an extra space
    print(" ".join(words), end="")
    
    # Print the final word with spaces and a newline and an extra space and an extra newline and an extra space and an extra newline and an extra space
    print(" ".join(words), end="")
    
    # Print the final word with spaces and a newline and an extra space and an extra newline and an extra space and an extra newline and an extra space and an extra newline
    print(" ".join(words), end="")
    
    # Print the final word with spaces and a newline and an extra space and an extra newline and an extra space and an extra newline and an extra space and an extra 