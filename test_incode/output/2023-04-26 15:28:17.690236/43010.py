#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that removes words. """    
    
    words = input("Enter a word: ").split()
    
    # Remove all words that aren't in the list
    words = [w for w in words if w not in words]
    
    # Print the remaining words
    print("Remaining words: ", words)
    
    # Remove all words that aren't in the list
    words = [w for w in words if w not in words]
    
    # Print the remaining words
    print("Remaining words: ", words)
    
    # Remove all words that aren't in the list
    words = [w for w in words if w not in words]
    
    # Print the remaining words
    print("Remaining words: ", words)
    
    # Remove all words that aren't in the list
    words = [w for w in words if w not in words]
    
    # Print the remaining words
    print("Remaining words: ", words)
    
    # Remove all words that aren't in the list
    words = [w for w in words if w not in words]
    
    # Print the remaining words
    print("Remaining words: ", words)
    
    # Remove all words that aren't in the list
    words = [w for w in words if w not in words]
    
    # Print the remaining words
    print("Remaining words: ", words)
    
    # Remove all words that aren't in the list
    words = [w for w in words if w not in words]
    
    # Print the remaining words
    print("Remaining words: ", words)
    
    # Remove all words that aren't in the list
    words = [w for w in words if w not in words]
    
    # Print the remaining words
    print("Remaining words: ", words)
    
    # Remove all words that aren't in the list
    words = [w for w in words if w not in words]
    
    # Print the remaining words
    print("Remaining words: ", words)
    
    # Remove all words that aren't in the list
    words = [w for w in words if w not in words]
    
    # Print the remaining words
    print("Remaining words: ", words)
    
    # Remove all words that aren't in the list
    words = [w for w in words if w not in words]
    
    # Print the remaining words
    print("Remaining words: ", words)
    
    # Remove all words that aren't in the list
    words = [w for w in words if w not in words]
    
    # Print the remaining words
    print("Remaining words: ", words)
    
    # Remove all words that aren't in the list
    words = [w for w in words if 