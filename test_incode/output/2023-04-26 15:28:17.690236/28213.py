#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores all ports or multiplyes numbers. """    
    ports = input("Enter the ports: ")
    ports = ports.split()
    ports = map(int, ports)
    ports = list(ports)
    ports = map(mul, ports)
    ports = list(ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    ports = map(str, ports)
    