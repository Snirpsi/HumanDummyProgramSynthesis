#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates words and iterates over fruits. """    
    fruits = ['apple', 'orange', 'pear', 'banana']
    
    # iterate over fruits
    for fruit in fruits:
        # calculate word length
        word_length = calc_word_length(fruit)
        
        # print word length
        print('Length of {}: {}'.format(fruit, word_length))
        
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
        
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
            
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
            
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
            
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
            
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
            
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
            
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
            
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
            
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
            
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
            
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
            
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
            
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
            
        # iterate over words of length
        for word in calc_words(word_length):
            # print word
            print('{}: {}'.format(word, calc_word(word)))
            