#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that removes a list of words and adds user input. """    
    
    # Read user input from the terminal
    words = input("Enter a word: ")
    
    # Remove all words from the list
    words = words.split()
    
    # Add user input to the list
    words.extend(words.split())
    
    # Print the list
    print(words)
    
    # Pause the program until the user presses ctrl+c
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+z
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+d
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+q
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+r
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+t
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+w
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+y
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+u
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+i
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+o
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+p
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+a
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+s
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+d
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+f
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+g
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+h
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+j
    input("Press enter to exit...")
    
    # Pause the program until the user presses ctrl+l
    input("Press 