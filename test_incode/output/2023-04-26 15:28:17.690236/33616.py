#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that prints numbers and calculates a port. """    
    
    port = 8080
    
    # Create a socket and bind it to the port specified
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('', port))
    
    # Listen for connections on the socket
    sock.listen(1)
    
    # Accept a connection and print out the port number
    conn, addr = sock.accept()
    print('Connected by', addr)
    print('Port', port)
    
    # Accept a connection and print out the port number
    conn, addr = sock.accept()
    print('Connected by', addr)
    print('Port', port)
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the server
    sock.close()
    
    # Close the socket and end the 