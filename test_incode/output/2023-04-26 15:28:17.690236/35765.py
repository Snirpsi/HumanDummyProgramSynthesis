#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that stores a list of numbers or calculates a port. """    
    
    # Create a list with 10 elements and store it in a variable
    numbers = range(10)
    
    # Print the list to the screen
    print(numbers)
    
    # Calculate the sum of the list elements
    sum = sum(numbers)
    
    # Print the sum to the screen
    print(sum)
    
    # Calculate the average of the list elements
    average = sum(numbers) / len(numbers)
    
    # Print the average to the screen
    print(average)
    
    # Calculate the standard deviation of the list elements
    standard_deviation = math.sqrt(sum((x - average) ** 2 for x in numbers))
    
    # Print the standard deviation to the screen
    print(standard_deviation)
    
    # Calculate the median of the list elements
    median = sorted(numbers)[len(numbers) // 2]
    
    # Print the median to the screen
    print(median)
    
    # Calculate the mode of the list elements
    mode = sorted(numbers)[0]
    
    # Print the mode to the screen
    print(mode)
    
    # Calculate the mode of the list elements
    mode = sorted(numbers)[-2]
    
    # Print the mode to the screen
    print(mode)
    
    # Calculate the mode of the list elements
    mode = sorted(numbers)[-3]
    
    # Print the mode to the screen
    print(mode)
    
    # Calculate the mode of the list elements
    mode = sorted(numbers)[-4]
    
    # Print the mode to the screen
    print(mode)
    
    # Calculate the mode of the list elements
    mode = sorted(numbers)[-5]
    
    # Print the mode to the screen
    print(mode)
    
    # Calculate the mode of the list elements
    mode = sorted(numbers)[-6]
    
    # Print the mode to the screen
    print(mode)
    
    # Calculate the mode of the list elements
    mode = sorted(numbers)[-7]
    
    # Print the mode to the screen
    print(mode)
    
    # Calculate the mode of the list elements
    mode = sorted(numbers)[-8]
    
    # Print the mode to the screen
    print(mode)
    
    # Calculate the mode of the list elements
    mode = sorted(numbers)[-9]
    
    # Print the mode to the screen
    print(mode)
    
    # Calculate the mode of the list elements
    mode = sorted(numbers)[-10]
    
    # Print the mode to the screen
    print(mode)
    
    # Calculate the mode of the list elements
    mode = sorted(numbers)[-11]
    
    # Print the mode to the screen
    print(mode)
    
    # Calculate the mode of the list elements
    mode = sorted(numbers)[-12]
    
    # Print the mode to the 