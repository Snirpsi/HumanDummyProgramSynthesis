#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that prints user input and enumerates all ports. """    
    ports = []
    
    while True:
        ports.append(input("Enter a port number: "))
        
        if len(ports) == 2:
            break
    
    print("\nPorts:")
    for port in ports:
        print(port)
    
    print("\nEnumerating all ports...")
    
    ports = []
    
    while True:
        ports.append(input("Enter a port number: "))
        
        if len(ports) == 2:
            break
    
    ports.sort()
    
    print("\nPorts:")
    for port in ports:
        print(port)
    
    print("\nEnumerating all ports...")
    
    ports = []
    
    while True:
        ports.append(input("Enter a port number: "))
        
        if len(ports) == 2:
            break
    
    ports.sort()
    
    print("\nPorts:")
    for port in ports:
        print(port)
    
    print("\nEnumerating all ports...")
    
    ports = []
    
    while True:
        ports.append(input("Enter a port number: "))
        
        if len(ports) == 2:
            break
    
    ports.sort()
    
    print("\nPorts:")
    for port in ports:
        print(port)
    
    print("\nEnumerating all ports...")
    
    ports = []
    
    while True:
        ports.append(input("Enter a port number: "))
        
        if len(ports) == 2:
            break
    
    ports.sort()
    
    print("\nPorts:")
    for port in ports:
        print(port)
    
    print("\nEnumerating all ports...")
    
    ports = []
    
    while True:
        ports.append(input("Enter a port number: "))
        
        if len(ports) == 2:
            break
    
    ports.sort()
    
    print("\nPorts:")
    for port in ports:
        print(port)
    
    print("\nEnumerating all ports...")
    
    ports = []
    
    while True:
        ports.append(input("Enter a port number: "))
        
        if len(ports) == 2:
            break
    
    ports.sort()
    
    print("\nPorts:")
    for port in ports:
        print(port)
    
    print("\nEnumerating all ports...")
    
    ports = []
    
    while True:
        ports.append(input("Enter a port number: "))
        
        if len(ports) == 2:
            break
    
    ports.sort()
    
    print("\nPorts:")
    for port in ports:
        print(port)
    
    print("\nEnumerating all ports...")
    
    ports = []
    