#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds a port or stores a list of numbers. """    
    ports = []
    
    # Add ports
    ports.append(8080)
    ports.append(8081)
    ports.append(8082)
    ports.append(8085)
    ports.append(8086)
    
    # Store list of numbers
    ports = ports + list(range(8088, 8089))
    
    # Print list of numbers
    for port in ports:
        print(port)
    
    # Print list of ports
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    
    # Print list of ports with port
    for port in ports:
        print(port)
    