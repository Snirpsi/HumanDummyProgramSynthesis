#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that removes a port and converts a list of numbers. """    
    ports = input("Enter a list of ports: ")
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    ports = [p for p in ports if p > 0]
    ports.sort()
    ports = ports[len(ports)-1:]
    port