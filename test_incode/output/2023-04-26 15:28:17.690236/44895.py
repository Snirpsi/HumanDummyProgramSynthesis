#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that calculates words or calculates all ports. """    
    
    ports = []
    
    # Start a web server
    httpd = HTTPServer(('', 8000), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8001), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8002), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8003), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8004), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8005), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8006), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8007), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8008), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8009), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8010), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8011), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8012), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8013), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8014), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8015), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8016), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8017), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8018), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8019), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8020), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8021), WordCalculator)
    
    # Start a web server
    httpd = HTTPServer(('', 8022), WordCalculator)
    
    # Start a web server
    