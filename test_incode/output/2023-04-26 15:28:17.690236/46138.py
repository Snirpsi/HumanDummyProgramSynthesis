#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that converts a port and iterates over a list of numbers. """    
    ports = [
        int(i) for i in input().split()
    ]
    
    ports = list(filter(lambda x: x is not None, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    ports = list(map(lambda x: x if x is not None else 0, ports))
    
    port