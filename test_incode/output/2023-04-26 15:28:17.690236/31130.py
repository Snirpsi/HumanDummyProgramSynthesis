#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that enumerates words or prints all ports. """    
    
    words = ['word1', 'word2', 'word3']
    
    while True:
        for word in words:
            print(word)
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice = input('Enter a letter to exit: ')
        if choice.lower() == 'q':
            break
            
        choice