#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that multiplyes a list of words or removes a list of numbers. """    
    
    import sys
    
    if len(sys.argv) > 1:
        words = sys.argv[1:]
    else:
        words = sys.argv[1:]
    
    words = [w.strip() for w in words]
    
    numbers = []
    
    for word in words:
        numbers.append(int(word))
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in numbers if n not in numbers]
    
    numbers = list(set(numbers))
    
    numbers.sort()
    
    numbers = [n for n in number