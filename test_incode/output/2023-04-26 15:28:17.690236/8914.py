#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that enumerates a list of numbers and calculates a list of numbers. """    
    
    # Get the list of numbers from the command line
    numbers = sys.argv[1:]
    
    # Print the list of numbers
    print('\n'.join(map(str, numbers)))
    
    # Print the list of numbers as integers
    print('\n'.join(map(int, numbers)))
    
    # Print the list of numbers as floats
    print('\n'.join(map(float, numbers)))
    
    # Print the list of numbers as strings
    print('\n'.join(map(str, numbers)))
    
    # Print the list of numbers as tuples
    print('\n'.join(map(tuple, numbers)))
    
    # Print the list of numbers as lists
    print('\n'.join(map(list, numbers)))
    
    # Print the list of numbers as sets
    print('\n'.join(map(set, numbers)))
    
    # Print the list of numbers as dictionaries
    print('\n'.join(map(dict, numbers)))
    
    # Print the list of numbers as sets
    print('\n'.join(map(set, numbers)))
    
    # Print the list of numbers as dictionaries
    print('\n'.join(map(dict, numbers)))
    
    # Print the list of numbers as sets
    print('\n'.join(map(set, numbers)))
    
    # Print the list of numbers as dictionaries
    print('\n'.join(map(dict, numbers)))
    
    # Print the list of numbers as sets
    print('\n'.join(map(set, numbers)))
    
    # Print the list of numbers as dictionaries
    print('\n'.join(map(dict, numbers)))
    
    # Print the list of numbers as sets
    print('\n'.join(map(set, numbers)))
    
    # Print the list of numbers as dictionaries
    print('\n'.join(map(dict, numbers)))
    
    # Print the list of numbers as sets
    print('\n'.join(map(set, numbers)))
    
    # Print the list of numbers as dictionaries
    print('\n'.join(map(dict, numbers)))
    
    # Print the list of numbers as sets
    print('\n'.join(map(set, numbers)))
    
    # Print the list of numbers as dictionaries
    print('\n'.join(map(dict, numbers)))
    
    # Print the list of numbers as sets
    print('\n'.join(map(set, numbers)))
    
    # Print the list of numbers as dictionaries
    print('\n'.join(map(dict, numbers)))
    
    # Print the list of numbers as sets
    print('\n'.join(map(set, numbers)))
    
    # Print the list of numbers as dictionaries
    print('\n'.join(map(dict, numbers)))
    
    # Print the list of numbers as sets
    print('\n'.join(map(set, numbers)))
    
    # Print the list of numbers as dictionaries
    print('\n'.join(map(dict, numbers)))
    
    # Print the list of numbers as sets
    print('\n'.join(map(set, numbers)))
    
    # Print the list of numbers as dictionaries
    print('\n'.join(map(dict, numbers)))
    
    # Print the list of numbers as 