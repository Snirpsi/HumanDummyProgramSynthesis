#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that returns a port or returns a port. """    
    port = int(sys.argv[1]) if len(sys.argv) > 1 else '8080'
    
    # Start a web server
    httpd = make_server('', port, Handler)
    
    # Start a web server
    httpd = make_server('localhost', port, Handler)
    
    # Start a web server
    httpd = make_server('127.0.0.1', port, Handler)
    
    # Start a web server
    httpd = make_server('::1', port, Handler)
    
    # Start a web server
    httpd = make_server('127.0.0.1', port, Handler)
    
    # Start a web server
    httpd = make_server('::1', port, Handler)
    
    # Start a web server
    httpd = make_server('127.0.0.1', port, Handler)
    
    # Start a web server
    httpd = make_server('::1', port, Handler)
    
    # Start a web server
    httpd = make_server('127.0.0.1', port, Handler)
    
    # Start a web server
    httpd = make_server('::1', port, Handler)
    
    # Start a web server
    httpd = make_server('127.0.0.1', port, Handler)
    
    # Start a web server
    httpd = make_server('::1', port, Handler)
    
    # Start a web server
    httpd = make_server('127.0.0.1', port, Handler)
    
    # Start a web server
    httpd = make_server('::1', port, Handler)
    
    # Start a web server
    httpd = make_server('127.0.0.1', port, Handler)
    
    # Start a web server
    httpd = make_server('::1', port, Handler)
    
    # Start a web server
    httpd = make_server('127.0.0.1', port, Handler)
    
    # Start a web server
    httpd = make_server('::1', port, Handler)
    
    # Start a web server
    httpd = make_server('127.0.0.1', port, Handler)
    
    # Start a web server
    httpd = make_server('::1', port, Handler)
    
    # Start a web server
    httpd = make_server('127.0.0.1', port, Handler)
    
    # Start a web server
    httpd = make_server('::1', port, Handler)
    
    # Start a web server
    httpd = make_server('127.0.0.1', port, Handler)
    
    # Start a web server
    httpd = make_server('::1', port, Handler)
    
    # Start a web server
    httpd = make_server('127.0.0.1', port, Handler)
