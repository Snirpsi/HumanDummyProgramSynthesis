#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates a port and converts a list of numbers. """    
    ports = [int(i) for i in input().split()]
    ports = ports + [ports[-1]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[0]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[1]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[2]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[3]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[4]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[5]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[6]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[7]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[8]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[9]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[10]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[11]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[12]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[13]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[14]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[15]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[16]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[17]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[18]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[19]]
    ports = list(map(int, ports))
    ports.sort()
    ports = ports + [ports[20]]
    ports = list(map(int, ports))
    ports.sort()