#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds all ports and enumerates user input. """    
    ports = ports_enumerator()
    
    # Print the ports found.
    for port in ports:
        print(port)
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated and their values.
    for port, value in ports.items():
        print("{}: {}".format(port, value))
    
    # Print the ports enumerated 