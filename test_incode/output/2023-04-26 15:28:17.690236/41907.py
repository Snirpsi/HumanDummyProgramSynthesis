#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that calculates words and iterates over user input. """    
    words = []
    
    while True:
        word = input('Enter a word: ')
        
        if word == 'q':
            break
        
        words.append(word)
        
    print('The words are:')
    for word in words:
        print(word)
    
    print('The word count is:', len(words))
    
    print('The longest word is:', max(words))
    
    print('The shortest word is:', min(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The longest word is:', max(words))
    
    print('The shortest word is:', min(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The longest word is:', max(words))
    
    print('The shortest word is:', min(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The longest word is:', max(words))
    
    print('The shortest word is:', min(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The longest word is:', max(words))
    
    print('The shortest word is:', min(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The longest word is:', max(words))
    
    print('The shortest word is:', min(words))
    
    print('The average word length is:', sum(map(len, words)) / len(words))
    
    print('The average word 