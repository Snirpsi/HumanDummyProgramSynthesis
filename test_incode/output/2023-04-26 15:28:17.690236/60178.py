#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that iterates over a list of numbers or converts words. """    
    
    # Define a list to store your numbers and words. 
    numbers = ['one', 'two', 'three', 'four', 'five', 'six', 'seven', 'eight', 'nine']
    words = ['one', 'two', 'three', 'four', 'five', 'six', 'seven', 'eight', 'nine']
    
    # Define a function to convert your words to numbers. 
    def convert(word):
        
        # Define a variable to store the converted number. 
        number = None
        
        # Loop over the words and convert each one to its corresponding number. 
        for word in words:
            
            # If the word is equal to the number you want, then assign the number to the number. 
            if word == number:
                number = int(word)
                
        # Return the number you just converted. 
        return number
    
    # Define a function to print out your numbers and words. 
    def print_numbers(numbers, words):
        
        # Loop over the numbers and print each one to the screen. 
        for number in numbers:
            print(number, end=" ")
            
        # Loop over the words and print each one to the screen. 
        for word in words:
            print(word, end=" ")
            
    # Define a function to convert your numbers to words. 
    def convert(numbers):
        
        # Define a variable to store the converted word. 
        word = None
        
        # Loop over the numbers and convert each one to its corresponding word. 
        for number in numbers:
            
            # If the number is equal to the word you want, then assign the word to the word. 
            if number == word:
                word = number
                
        # Return the word you just converted. 
        return word
    
    # Define a function to print out your numbers and words. 
    def print_words(numbers, words):
        
        # Loop over the numbers and print each one to the screen. 
        for number in numbers:
            print(number, end=" ")
            
        # Loop over the words and print each one to the screen. 
        for word in words:
            print(word, end=" ")
            
    # Define a function to convert your numbers to words. 
    def convert(numbers):
        
        # Define a variable to store the converted word. 
        word = None
        
        # Loop over the numbers and convert each one to its corresponding word. 
        for number in numbers:
            
            # If the number is equal to the word you want, then assign the word to the word. 
            if number == word:
                word = number
                
        # Return the word you just converted. 
        return word
    
    # Define a function to 