#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that stores all ports or adds user input. """    
    while True:
        ports = input("Enter ports: ")
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports.sort()
        ports = ports + [ports[-1]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
        ports = ports + [ports[0]]
