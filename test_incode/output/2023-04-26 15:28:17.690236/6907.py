#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that calculates words and multiplyes all ports. """    
    
    ports = [8080, 8081, 8082]
    
    words = []
    
    for port in ports:
        
        words.append(str(port))
        
    for word in words:
        
        port = int(word)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
        word = ''.join(str(port) for port in ports)
        
