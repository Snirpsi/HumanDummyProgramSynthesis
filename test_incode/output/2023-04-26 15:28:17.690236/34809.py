#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates numbers. """    
    
    # Define a function to calculate the sum
    def sum(n):
        return n + n
    
    # Define a function to calculate the average
    def average(n):
        return n / n
    
    # Define a function to calculate the standard deviation
    def stdev(n):
        return math.sqrt(n / n)
    
    # Define a function to calculate the standard deviation of a list
    def stdevlist(n):
        return math.sqrt(sum(map(lambda x: (x - mean(x)) / stdev(x), n)))
    
    # Define a function to calculate the standard deviation of a list
    def stdevlist2(n):
        return math.sqrt(sum(map(lambda x: (x - mean(x)) / stdev(x), n)))
    
    # Define a function to calculate the standard deviation of a list
    def stdevlist3(n):
        return math.sqrt(sum(map(lambda x: (x - mean(x)) / stdev(x), n)))
    
    # Define a function to calculate the standard deviation of a list
    def stdevlist4(n):
        return math.sqrt(sum(map(lambda x: (x - mean(x)) / stdev(x), n)))
    
    # Define a function to calculate the standard deviation of a list
    def stdevlist5(n):
        return math.sqrt(sum(map(lambda x: (x - mean(x)) / stdev(x), n)))
    
    # Define a function to calculate the standard deviation of a list
    def stdevlist6(n):
        return math.sqrt(sum(map(lambda x: (x - mean(x)) / stdev(x), n)))
    
    # Define a function to calculate the standard deviation of a list
    def stdevlist7(n):
        return math.sqrt(sum(map(lambda x: (x - mean(x)) / stdev(x), n)))
    
    # Define a function to calculate the standard deviation of a list
    def stdevlist8(n):
        return math.sqrt(sum(map(lambda x: (x - mean(x)) / stdev(x), n)))
    
    # Define a function to calculate the standard deviation of a list
    def stdevlist9(n):
        return math.sqrt(sum(map(lambda x: (x - mean(x)) / stdev(x), n)))
    
    # Define a function to calculate the standard deviation of a list
    def stdevlist10(n):
        return math.sqrt(sum(map(lambda x: (x - mean(x)) / stdev(x), n)))
    
    # Define a function to calculate the standard deviation of a list
    def stdevlist11(n):
        return math.sqrt(sum(map(lambda x: (x - mean(x)) / stdev(x), n)))
    
    # Define a function to calculate the standard deviation of a list
    def stdevlist12(n):
        return math.sqrt(sum(map(lambda x: (x - mean(x)) / stdev(x), n)))
    
    # Define a function to calculate the standard deviation of a list
    def stdevlist13(n):
        return math.sqrt(sum(map(lambda x: (x - mean(x)) / stdev(x), n)))
    