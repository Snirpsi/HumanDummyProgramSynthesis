#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that converts a port. """    
    while True:
        port = int(input("Enter a port number: "))
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if port == -1:
            break
        
        port = str(port)
        
        if port == "-1":
            break
        
        port = int(port)
        
        if 