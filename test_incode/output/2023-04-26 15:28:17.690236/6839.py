#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts all ports and stores user input. """    
    ports = input("Enter the ports you want to convert: ")
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports = list(set(ports))
    ports.sort()
    ports = [str(p) for p in ports]
    ports = " ".join(ports)
    ports = ports.strip()
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports = list(set(ports))
    ports.sort()
    ports = [str(p) for p in ports]
    ports = ports.strip()
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports = list(set(ports))
    ports.sort()
    ports = [str(p) for p in ports]
    ports = ports.strip()
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports = list(set(ports))
    ports.sort()
    ports = [str(p) for p in ports]
    ports = ports.strip()
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports = list(set(ports))
    ports.sort()
    ports = [str(p) for p in ports]
    ports = ports.strip()
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports = list(set(ports))
    ports.sort()
    ports = [str(p) for p in ports]
    ports = ports.strip()
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports = list(set(ports))
    ports.sort()
    ports = [str(p) for p in ports]
    ports = ports.strip()
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports = list(set(ports))
    ports.sort()
    ports = [str(p) for p in ports]
    ports = ports.strip()
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports = list(set(ports))
    ports.sort()
    ports = [str(p) for p in ports]
    ports = ports.strip()
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports = list(set(ports))
    ports.sort()
    ports = [str(p) for p in ports]
    ports = ports.strip()
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports = list(set(ports))
    ports.sort()
    ports = [str(p) for p in ports]
    ports = ports.strip()
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports = list(