#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that converts all ports or removes a list of numbers. """    
    ports = sys.argv[1:]
    ports = [int(p) for p in ports]
    ports = [p for p in ports if p > 0]
    ports = [p for p in ports if p <= 65535]
    ports = [p for p in ports if p > 65535]
    ports = [p for p in ports if p > 0]
    ports = [p for p in ports if p <= 65535]
    ports = [p for p in ports if p > 65535]
    ports = [p for p in ports if p > 0]
    ports = [p for p in ports if p <= 65535]
    ports = [p for p in ports if p > 65535]
    ports = [p for p in ports if p > 0]
    ports = [p for p in ports if p <= 65535]
    ports = [p for p in ports if p > 65535]
    ports = [p for p in ports if p > 0]
    ports = [p for p in ports if p <= 65535]
    ports = [p for p in ports if p > 65535]
    ports = [p for p in ports if p > 0]
    ports = [p for p in ports if p <= 65535]
    ports = [p for p in ports if p > 65535]
    ports = [p for p in ports if p > 0]
    ports = [p for p in ports if p <= 65535]
    ports = [p for p in ports if p > 65535]
    ports = [p for p in ports if p > 0]
    ports = [p for p in ports if p <= 65535]
    ports = [p for p in ports if p > 65535]
    ports = [p for p in ports if p > 0]
    ports = [p for p in ports if p <= 65535]
    ports = [p for p in ports if p > 65535]
    ports = [p for p in ports if p > 0]
    ports = [p for p in ports if p <= 65535]
    ports = [p for p in ports if p > 65535]
    ports = [p for p in ports if p > 0]
    ports = [p for p in ports if p <= 65535]
    ports = [p for p in ports if p > 65535]
    ports = [p for p in ports if p > 0]
    ports = [p for p in ports if p <= 65535]
    ports = [p for p in ports if p > 65535]
    ports = [p for p in ports if p > 0]
    ports = [p for p in ports if p <= 65535]
    ports = [p for p in ports if p > 65535]
    ports = [p for p in ports if p > 0]
    ports = [p for 