#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that multiplyes all ports or converts a list of words. """    
    while True:
        ports = input("Enter a list of ports: ")
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = ports[:]
        ports.sort()
        ports = [str(p) for p in ports]
        ports = 