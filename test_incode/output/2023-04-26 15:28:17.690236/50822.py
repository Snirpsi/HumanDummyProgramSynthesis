#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that converts a port or enumerates words. """    
    import sys
    port = int(sys.argv[1])
    words = []
    
    # Enumerate words from port #
    for i in range(port):
        words.append(i)
    
    # Convert port #
    words = list(map(str, words))
    
    # Print the converted words
    for word in words:
        print(word)
    
    # Print the port #
    print(port)
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(len(words))
    
    # Print the port #
    print(