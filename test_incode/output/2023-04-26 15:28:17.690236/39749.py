#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that converts a list of numbers. """    
    import sys
    port = int(sys.argv[1])
    
    numbers = []
    for line in sys.stdin:
        numbers.append(int(line))
    
    numbers = sorted(numbers)
    
    numbers = numbers[::-1]
    
    numbers = numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
    numbers = numbers + numbers[:port]
    
