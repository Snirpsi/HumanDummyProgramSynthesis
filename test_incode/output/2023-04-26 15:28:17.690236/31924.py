#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that multiplyes user input or converts all ports. """    
    ports = input("Enter ports separated by commas: ")
    ports = ports.split(',')
    ports = [int(p) for p in ports]
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    port