#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds a port or prints numbers. """    
    
    # Print number of ports 
    print(len(sys.argv))
    
    # Add port to list
    ports = sys.argv[1:]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port)]
    
    # Add port to list
    ports += ['--port', str(port