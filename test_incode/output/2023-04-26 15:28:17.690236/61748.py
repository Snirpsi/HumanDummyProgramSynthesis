#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that converts all ports or multiplyes all ports. """    
    import sys
    ports = sys.argv[1:]
    ports = [int(p) for p in ports]
    ports = reduce(lambda x, y: x * y, ports)
    ports = ports % 65536
    ports = ports // 65536
    ports = [str(p) for p in ports]
    ports = ','.join(ports)
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace('\n', ' ')
    ports = ports.replace('\r', ' ')
    ports = ports.replace('\t', ' ')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace('\r', ' ')
    ports = ports.replace('\t', ' ')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace('\r', ' ')
    ports = ports.replace('\t', ' ')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace('\r', ' ')
    ports = ports.replace('\t', ' ')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace('\r', ' ')
    ports = ports.replace('\t', ' ')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace('\r', ' ')
    ports = ports.replace('\t', ' ')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace('\r', ' ')
    ports = ports.replace('\t', ' ')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace('\r', ' ')
    ports = ports.replace('\t', ' ')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace('\r', ' ')