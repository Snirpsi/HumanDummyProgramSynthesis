#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that enumerates a list of words or calculates user input. """    
    
    import sys
    
    if len(sys.argv) < 2:
        print("Usage: %s <words>" % sys.argv[0])
        sys.exit(1)
    
    words = sys.argv[1:]
    
    words = list(set(words))
    
    words.sort()
    
    word_count = len(words)
    
    word_count_per_line = word_count // 20
    
    word_count_per_page = int(word_count_per_line * 0.8)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
    
    word_count_per_page = int(word_count_per_page)
