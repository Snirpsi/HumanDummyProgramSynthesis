#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that returns a list of numbers and removes all ports. """    
    ports = [int(p) for p in sys.argv[1:]]
    ports = list(filter(lambda p: p > 0, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p < 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p > 65536, ports))
    ports.sort()
    ports = list(filter(lambda p: p 