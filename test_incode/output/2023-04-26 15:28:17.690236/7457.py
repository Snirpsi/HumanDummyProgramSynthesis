#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts a list of numbers and removes words. """    
    
    numbers = []
    
    for number in sys.argv[1:]:
        numbers.append(int(number))
    
    numbers = list(filter(lambda x: x is not None, numbers))
    
    numbers = list(map(lambda x: str(x), numbers))
    
    numbers = list(map(lambda x: x.lower(), numbers))
    
    numbers = list(map(lambda x: x.upper(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize(), numbers))
    
    numbers = list(map(lambda x: x.title(), numbers))
    
    numbers = list(map(lambda x: x.capitalize