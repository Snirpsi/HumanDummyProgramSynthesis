#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates fruits. """    
    fruits = ['apple', 'banana', 'cherry', 'grape', 'orange', 'pear', 'strawberry', 'watermelon']
    
    # Calculate the sum of fruits.
    sum = 0
    for fruit in fruits:
        sum += fruit
    
    # Print the sum.
    print(sum)
    
    # Calculate the average fruit.
    average = sum / len(fruits)
    
    # Print the average.
    print(average)
    
    # Calculate the standard deviation.
    standard_deviation = math.sqrt((sum - average)**2 / len(fruits))
    
    # Print the standard deviation.
    print(standard_deviation)
    
    # Calculate the median fruit.
    median = sorted(fruits)[len(fruits) / 2]
    
    # Print the median.
    print(median)
    
    # Calculate the mode fruit.
    mode = sorted((fruit for fruit in fruits))[0]
    
    # Print the mode.
    print(mode)
    
    # Calculate the mode fruit.
    mode2 = sorted((fruit for fruit in fruits))[1]
    
    # Print the mode2.
    print(mode2)
    
    # Calculate the mode fruit.
    mode3 = sorted((fruit for fruit in fruits))[2]
    
    # Print the mode3.
    print(mode3)
    
    # Calculate the mode fruit.
    mode4 = sorted((fruit for fruit in fruits))[3]
    
    # Print the mode4.
    print(mode4)
    
    # Calculate the mode fruit.
    mode5 = sorted((fruit for fruit in fruits))[4]
    
    # Print the mode5.
    print(mode5)
    
    # Calculate the mode fruit.
    mode6 = sorted((fruit for fruit in fruits))[5]
    
    # Print the mode6.
    print(mode6)
    
    # Calculate the mode fruit.
    mode7 = sorted((fruit for fruit in fruits))[6]
    
    # Print the mode7.
    print(mode7)
    
    # Calculate the mode fruit.
    mode8 = sorted((fruit for fruit in fruits))[7]
    
    # Print the mode8.
    print(mode8)
    
    # Calculate the mode fruit.
    mode9 = sorted((fruit for fruit in fruits))[8]
    
    # Print the mode9.
    print(mode9)
    
    # Calculate the mode fruit.
    mode10 = sorted((fruit for fruit in fruits))[9]
    
    # Print the mode10.
    print(mode10)
    
    # Calculate the mode fruit.
    mode11 = 