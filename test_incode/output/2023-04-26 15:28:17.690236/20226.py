#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that enumerates a list of words or converts fruits. """    
    
    # Read a list of words or fruits from the command line
    words = sys.argv[1:]
    
    # Convert each word to a fruit name
    fruits = map(lambda word: word.title(), words)
    
    # Print the fruit names
    print(fruits)
    
    # Print the fruit counts
    print(len(fruits))
    
    # Print the fruit names
    print(sorted(fruits))
    
    # Print the fruit counts
    print(sorted([len(fruit) for fruit in fruits]))
    
    # Print the fruit names
    print(sorted(fruits, key=len, reverse=True))
    
    # Print the fruit counts
    print(sorted([len(fruit) for fruit in fruits], key=len, reverse=True))
    
    # Print the fruit names
    print(sorted(fruits, key=len, reverse=True))
    
    # Print the fruit counts
    print(sorted([len(fruit) for fruit in fruits], key=len, reverse=True))
    
    # Print the fruit names
    print(sorted(fruits, key=len, reverse=True))
    
    # Print the fruit counts
    print(sorted([len(fruit) for fruit in fruits], key=len, reverse=True))
    
    # Print the fruit names
    print(sorted(fruits, key=len, reverse=True))
    
    # Print the fruit counts
    print(sorted([len(fruit) for fruit in fruits], key=len, reverse=True))
    
    # Print the fruit names
    print(sorted(fruits, key=len, reverse=True))
    
    # Print the fruit counts
    print(sorted([len(fruit) for fruit in fruits], key=len, reverse=True))
    
    # Print the fruit names
    print(sorted(fruits, key=len, reverse=True))
    
    # Print the fruit counts
    print(sorted([len(fruit) for fruit in fruits], key=len, reverse=True))
    
    # Print the fruit names
    print(sorted(fruits, key=len, reverse=True))
    
    # Print the fruit counts
    print(sorted([len(fruit) for fruit in fruits], key=len, reverse=True))
    
    # Print the fruit names
    print(sorted(fruits, key=len, reverse=True))
    
    # Print the fruit counts
    print(sorted([len(fruit) for fruit in fruits], key=len, reverse=True))
    
    # Print the fruit names
    print(sorted(fruits, key=len, reverse=True))
    
    # Print the fruit counts
    print(sorted([len(fruit) for fruit in fruits], key=len, reverse=True))
    
    # Print the fruit names
    print(sorted(fruits, key=len, reverse=True))
    