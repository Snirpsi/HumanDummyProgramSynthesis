#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that returns fruits. """    
    fruits = ['apple', 'orange', 'banana', 'cherry']
    
    # Print all fruits in the list
    for fruit in fruits:
        print(fruit)
        
    # Print the length of each fruit
    for fruit in fruits:
        print(fruit.__len__())
        
    # Print the length of each fruit using len()
    for fruit in fruits:
        print(len(fruit))
        
    # Print the length of each fruit using len(fruit)
    for fruit in fruits:
        print(len(fruit))
        
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the length of each fruit using len(fruit)
    print(len(fruits))
    
    # Print the 