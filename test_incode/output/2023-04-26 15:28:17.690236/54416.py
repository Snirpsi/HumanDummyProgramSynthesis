#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that returns all ports and converts user input. """    
    
    ports = []
    
    # start a server
    server = HTTPServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer(("", 0), Handler)
    
    # start a web server
    webserver = WebServer