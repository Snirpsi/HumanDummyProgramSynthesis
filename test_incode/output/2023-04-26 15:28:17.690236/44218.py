#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that enumerates all ports. """    
    while True:
        ports = os.listdir('/sys/class/net/')
        for port in ports:
            if  port == 'lo':
                continue
            if  port == 'eth0':
                continue
            if  port == 'eth1':
                continue
            if  port == 'eth2':
                continue
            if  port == 'eth3':
                continue
            if  port == 'eth4':
                continue
            if  port == 'eth5':
                continue
            if  port == 'eth6':
                continue
            if  port == 'eth7':
                continue
            if  port == 'eth8':
                continue
            if  port == 'eth9':
                continue
            if  port == 'eth10':
                continue
            if  port == 'eth11':
                continue
            if  port == 'eth12':
                continue
            if  port == 'eth13':
                continue
            if  port == 'eth14':
                continue
            if  port == 'eth15':
                continue
            if  port == 'eth16':
                continue
            if  port == 'eth17':
                continue
            if  port == 'eth18':
                continue
            if  port == 'eth19':
                continue
            if  port == 'eth20':
                continue
            if  port == 'eth21':
                continue
            if  port == 'eth22':
                continue
            if  port == 'eth23':
                continue
            if  port == 'eth24':
                continue
            if  port == 'eth25':
                continue
            if  port == 'eth26':
                continue
            if  port == 'eth27':
                continue
            if  port == 'eth28':
                continue
            if  port == 'eth29':
                continue
            if  port == 'eth30':
                continue
            if  port == 'eth31':
                continue
            if  port == 'eth32':
                continue
            if  port == 'eth33':
                continue
            if  port == 'eth34':
                continue
            if  port == 'eth35':
                continue
            if  port == 'eth36':
                continue
            if  port == 'eth37':
                continue
            if  port == 'eth38':
                continue
            if  port == 'eth39':
                continue
            if  port == 'eth40':
                continue
            if  port == 'eth41':
                continue
            if  port == 'eth42':
                continue
            if  port == 'eth43':
                continue