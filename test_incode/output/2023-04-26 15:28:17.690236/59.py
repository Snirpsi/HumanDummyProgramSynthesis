#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that multiplyes user input and calculates all ports. """    
    
    # Program to multiply user input and calculate all ports
    ports_input = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all ports
    ports_output = int(input('Enter the ports you want to multiply: '))
    
    # Program to calculate all 