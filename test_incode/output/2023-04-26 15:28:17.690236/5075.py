#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that adds a port or adds user input. """    
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '))
    
    # Add user
    user = input('Enter the user: ')
    
    # Add port
    port = int(input('Enter the port you want to add: '