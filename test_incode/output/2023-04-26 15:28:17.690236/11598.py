#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that opens user input and converts a list of numbers. """    
    import sys
    
    numbers = sys.argv[1:]
    
    if len(numbers) == 0:
        print("Usage: python webserver.py [numbers]")
        sys.exit()
    
    numbers = map(int, numbers)
    
    numbers = map(str, numbers)
    
    numbers = map(str, numbers)
    
    numbers = map(str, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
    
    numbers = map(int, numbers)
