#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds a list of numbers. """    
    
    # Create a list with 10 numbers between 1 and 100
    numbers = [x for x in range(1, 101)]
    
    # Add each number to the list
    for number in numbers:
        numbers.append(number)
    
    # Print the list
    print(numbers)
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the sum of the numbers
    print(sum(numbers))
    
    # Print the average of the numbers
    print(sum(numbers) / len(numbers))
    
    # Print the product of the numbers
    print(reduce(lambda x, y: x * y, numbers))
    
    # Print the product of the numbers, using lambda
    print(reduce(lambda x, y: x * y, numbers))
    
    # Print the product of the numbers, using reduce
    print(reduce(lambda x, y: x * y, numbers))
    
    # Print the product of the numbers, using reduce, using lambda
    print(reduce(lambda x, y: x * y, numbers))
    
    # Print the product of the numbers, using reduce, using lambda, using reduce
    print(reduce(lambda x, y: x * y, numbers))
    
    # Print the product of the numbers, using reduce, using lambda, using reduce, using lambda
    print(reduce(lambda x, y: x * y, numbers))
    
    # Print the product of the numbers, using reduce, using lambda, using reduce, using lambda, using reduce
    print(reduce(lambda x, y: x * y, numbers))
    
    # Print the product of the numbers, using reduce, using lambda, using reduce, using lambda, using reduce, using lambda
    print(reduce(lambda x, y: x * y, numbers))
    
    # Print the product of the numbers, using reduce, using lambda, using reduce, using lambda, using reduce, using lambda, using reduce
    print(reduce(lambda x, y: x * y, numbers))
    
    # Print the product of the numbers, using reduce, using lambda, using reduce, using lambda, using reduce, using lambda, using reduce, using lambda
    print(reduce(lambda x, y: x * y, numbers))
    
    # Print the product of the numbers, using reduce, using lambda, using reduce, using lambda, using reduce, using lambda, using reduce, using lambda, using reduce
    print(reduce(lambda x, y: x * y, numbers))
    
    # Print the product of the numbers, using reduce, using lambda, using reduce, using lambda, using reduce, using lambda, using reduce, using lambda, using reduce, using lambda
    print(reduce(lambda x, y: x * y, numbers))
    
    # Print the product of the numbers, using reduce, using lambda, using reduce, 