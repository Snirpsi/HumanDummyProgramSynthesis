#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that removes words or opens fruits. """    
    
    import sys
    
    if len(sys.argv) < 2:
        print("Usage: python3 remove-words-from-fruits.py <fruits.txt>")
        sys.exit()
    
    with open(sys.argv[1], 'r') as file:
        words = file.read().splitlines()
        
    for word in words:
        word = word.strip()
        
        if word == '':
            continue
        
        if word.startswith('#'):
            continue
        
        if word.startswith('-'):
            continue
        
        if word.startswith('!'):
            continue
        
        if word.startswith('?'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('`'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"'):
            continue
        
        if word.startswith('"