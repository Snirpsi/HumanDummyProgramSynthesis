#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that multiplyes all ports or converts numbers. """    
    
    ports = [int(port) for port in sys.argv[1:]]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
    ports_str = [str(port) for port in ports]
    
