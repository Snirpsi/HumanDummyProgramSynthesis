#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that enumerates a port and stores user input. """    
    
    # Open the port and get the IP address
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.bind(('127.0.0.1', 0))
    sock.listen(1)
    
    # Accept a connection
    client, addr = sock.accept()
    
    # Print the IP address and port number
    print('Client connected from', addr)
    print('Port number:', client.getpeername()[1])
    
    # Print the IP address and port number
    print('Client connected from', addr)
    print('Port number:', client.getpeername()[1])
    
    # Close the connection
    client.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket
    sock.close()
    
    # Close the port and socket