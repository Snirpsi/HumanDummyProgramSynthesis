#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts all ports. """    
    
    # Load the ports dictionary
    ports = load_ports()
    
    # Iterate over all ports
    for port in ports:
        
        # Convert the port to a string
        port_string = ports[port]
        
        # Print the string
        print(port_string)
        
        # Print the port name
        print(port)
        
        # Print the port number 
        print(ports[port])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port type 
        print(ports[port]['type'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port]['description'])
        
        # Print the port description
        print(ports[port