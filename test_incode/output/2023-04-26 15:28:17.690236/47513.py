#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that iterates over all ports or removes a list of words. """    
    ports = sys.argv[1:]
    
    for port in ports:
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        port = str(port)
        
        if port == '':
            port = '*'
        
        