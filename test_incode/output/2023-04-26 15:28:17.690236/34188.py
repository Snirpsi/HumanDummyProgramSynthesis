#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that returns all ports and removes fruits. """    
    
    ports = []
    
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
        ports.remove(port)
        
    for port in ports:
        
