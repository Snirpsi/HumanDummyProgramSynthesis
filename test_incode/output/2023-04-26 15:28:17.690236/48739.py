#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that removes user input. """    
    
    # Remove user input from stdin
    sys.stdin.read()
    
    # Remove user input from stdout
    sys.stdout.write()
    
    # Remove user input from stderr
    sys.stderr.write()
    
    # Remove user input from all files
    for file in sys.argv[1:]:
        os.remove(file)
    
    # Remove user input from all directories
    for dir in sys.argv[1:]:
        os.rmdir(dir)
    
    # Remove user input from all subdirectories
    for dir in sys.argv[1:]:
        for file in os.listdir(dir):
            os.remove(os.path.join(dir, file))
    
    # Remove user input from all subdirectories
    for dir in sys.argv[1:]:
        for file in os.listdir(dir):
            os.rmdir(os.path.join(dir, file))
    
    # Remove user input from all subdirectories
    for dir in sys.argv[1:]:
        for file in os.listdir(dir):
            os.rmdir(os.path.join(dir, file))
    
    # Remove user input from all subdirectories
    for dir in sys.argv[1:]:
        for file in os.listdir(dir):
            os.rmdir(os.path.join(dir, file))
    
    # Remove user input from all subdirectories
    for dir in sys.argv[1:]:
        for file in os.listdir(dir):
            os.rmdir(os.path.join(dir, file))
    
    # Remove user input from all subdirectories
    for dir in sys.argv[1:]:
        for file in os.listdir(dir):
            os.rmdir(os.path.join(dir, file))
    
    # Remove user input from all subdirectories
    for dir in sys.argv[1:]:
        for file in os.listdir(dir):
            os.rmdir(os.path.join(dir, file))
    
    # Remove user input from all subdirectories
    for dir in sys.argv[1:]:
        for file in os.listdir(dir):
            os.rmdir(os.path.join(dir, file))
    
    # Remove user input from all subdirectories
    for dir in sys.argv[1:]:
        for file in os.listdir(dir):
            os.rmdir(os.path.join(dir, file))
    
    # Remove user input from all subdirectories
    for dir in sys.argv[1:]:
        for file in os.listdir(dir):
            os.rmdir(os.path.join(dir, file))
    
    # Remove user input from all subdirectories
    for dir in sys.argv[1:]:
        for file in os.listdir(dir):
            os.rmdir(os.path.join(dir, file))
    
    # Remove user input from all subdirectories
    for dir in sys.argv[1:]:
        for file in os.listdir(dir):
            os.rmdir(os.path.join(dir, file))
    
    # Remove user input from all subdirectories
    for dir in sys.argv[1:]:
        for file in os.listdir(dir):
            os.rmdir(os.path.join(dir, file))
    
    # Remove user input from all subdirectories
    for dir in sys.argv[1:]:
        for file in os.listdir(dir):
