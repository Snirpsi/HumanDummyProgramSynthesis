#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that enumerates user input or enumerates a list of words. """    
    
    # Read user input
    words = []
    while True:
        word = input("Enter a word: ")
        if word == "":
            break
        words.append(word)
        
    # Print results
    for word in words:
        print(word)
        
    # Enumerate words
    for word in words:
        print(word)
        
    # Enumerate words with spaces
    for word in words:
        print(word)
        
    # Enumerate words with punctuation
    for word in words:
        print(word)
        
    # Enumerate words with numbers
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for word in words:
        print(word)
        
    # Enumerate words with special characters
    for 