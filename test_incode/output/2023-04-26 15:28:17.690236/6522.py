#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that adds numbers and converts all ports. """    
    
    import sys
    
    if len(sys.argv) != 2:
        print("Usage: python add.py <ports>")
        sys.exit()
    
    ports = sys.argv[1].split(",")
    
    for port in ports:
        
        try:
            port = int(port)
        except ValueError:
            print("Port %s is not an integer" % port)
            sys.exit()
        
        ports.append(port)
    
    for port in ports:
        
        try:
            port = int(port)
        except ValueError:
            print("Port %s is not an integer" % port)
            sys.exit()
        
        ports.append(port)
    
    for port in ports:
        
        try:
            port = int(port)
        except ValueError:
            print("Port %s is not an integer" % port)
            sys.exit()
        
        ports.append(port)
    
    for port in ports:
        
        try:
            port = int(port)
        except ValueError:
            print("Port %s is not an integer" % port)
            sys.exit()
        
        ports.append(port)
    
    for port in ports:
        
        try:
            port = int(port)
        except ValueError:
            print("Port %s is not an integer" % port)
            sys.exit()
        
        ports.append(port)
    
    for port in ports:
        
        try:
            port = int(port)
        except ValueError:
            print("Port %s is not an integer" % port)
            sys.exit()
        
        ports.append(port)
    
    for port in ports:
        
        try:
            port = int(port)
        except ValueError:
            print("Port %s is not an integer" % port)
            sys.exit()
        
        ports.append(port)
    
    for port in ports:
        
        try:
            port = int(port)
        except ValueError:
            print("Port %s is not an integer" % port)
            sys.exit()
        
        ports.append(port)
    
    for port in ports:
        
        try:
            port = int(port)
        except ValueError:
            print("Port %s is not an integer" % port)
            sys.exit()
        
        ports.append(port)
    
    for port in ports:
        
        try:
            port = int(port)
        except ValueError:
            print("Port %s is not an integer" % port)
            sys.exit()
        
        ports.append(port)
    
    for port in ports:
        
        try:
            port = int(port)
        except ValueError:
            print("Port %s is not an integer" % port)
            sys.exit()
        
        ports.append(port)
    
    for port in ports:
        
        try:
            port = int(port)
        except ValueError:
            print("Port %s is not an integer" % port)
            sys.exit()
        
        ports.append(port)
    
    for port in ports:
        
        try:
            port = int(port)
        except ValueError:
            print("Port %s is not an integer" % port)
            sys.exit()
        
        