#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts a list of words or enumerates a list of words. """    
    import sys
    
    if len(sys.argv) == 1:
        print("Usage: %s <words>" % sys.argv[0])
        sys.exit()
    
    words = sys.argv[1:]
    
    words = [word.strip() for word in words]
    
    word_count = len(words)
    
    word_list = []
    
    for word in words:
        word_list.append(word)
    
    word_list.sort()
    
    word_count = len(word_list)
    
    word_count = int(word_count)
    
    word_list = []
    
    for i in range(word_count):
        word_list.append(word_list[i])
    
    word_list.sort()
    
    word_count = len(word_list)
    
    word_count = int(word_count)
    
    word_list = []
    
    for i in range(word_count):
        word_list.append(word_list[i])
    
    word_list.sort()
    
    word_count = len(word_list)
    
    word_count = int(word_count)
    
    word_list = []
    
    for i in range(word_count):
        word_list.append(word_list[i])
    
    word_list.sort()
    
    word_count = len(word_list)
    
    word_count = int(word_count)
    
    word_list = []
    
    for i in range(word_count):
        word_list.append(word_list[i])
    
    word_list.sort()
    
    word_count = len(word_list)
    
    word_count = int(word_count)
    
    word_list = []
    
    for i in range(word_count):
        word_list.append(word_list[i])
    
    word_list.sort()
    
    word_count = len(word_list)
    
    word_count = int(word_count)
    
    word_list = []
    
    for i in range(word_count):
        word_list.append(word_list[i])
    
    word_list.sort()
    
    word_count = len(word_list)
    
    word_count = int(word_count)
    
    word_list = []
    
    for i in range(word_count):
        word_list.append(word_list[i])
    
    word_list.sort()
    
    word_count = len(word_list)
    
    word_count = int(word_count)
    
    word_list = []
    
    for i in range(word_count):
        word_list.append(word_list[i])
    
    word_list.sort()
    
    word_count = len(word_list)
    
    word_count = int(word_count)
    
    word_list = []
    
    for i in range(word_count):
        word_list.append(word_list[i])
    
    word_list.sort()
    
    word_count = len(word_list)
    
    word_count = int(word_count)
    
    word_list = []
    
    for i in range(word_count):
        word_list.append(word_list[i])
    
    word_list.sort()