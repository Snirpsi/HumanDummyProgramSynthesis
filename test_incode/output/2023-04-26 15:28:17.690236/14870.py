#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens fruits or stores fruits. """    
    
    # Open the fruits file
    fruitsFile = open('fruits.txt', 'r')
    
    # Read fruits from the file
    fruits = fruitsFile.readlines()
    fruitsFile.close()
    
    # Print the fruits from the file
    print('Fruits:')
    for fruit in fruits:
        print(fruit)
    
    # Print the fruits from the file
    print('Fruits:')
    for fruit in fruits:
        print(fruit)
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
    fruitsFile.close()
    
    # Close the fruits file
