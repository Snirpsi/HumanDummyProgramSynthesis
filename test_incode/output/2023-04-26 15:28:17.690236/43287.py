#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds words or returns a list of words. """    
    
    words = add_words()
    
    # Print the list of words
    print(words)
    
    # Print the list of words with a space
    print(" ".join(words))
    
    # Print the list of words with no space
    print(" ".join(words.split()))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space
    print(" ".join(words.split(", ")))
    
    # Print the list of words with no space