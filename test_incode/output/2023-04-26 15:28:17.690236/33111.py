#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that iterates over all ports or opens a list of words. """    
    
    import sys
    
    if len(sys.argv) > 1:
        ports = sys.argv[1]
    else:
        ports = input("Enter ports separated by comma: ")
    
    ports = ports.split(",")
    
    for port in ports:
        
        port = int(port)
        
        ports = ports.split(",")
        
        for port in ports:
            
            port = int(port)
            
            ports = ports.split(",")
            
            for port in ports:
                
                port = int(port)
                
                ports = ports.split(",")
                
                for port in ports:
                    
                    port = int(port)
                    
                    ports = ports.split(",")
                    
                    for port in ports:
                        
                        port = int(port)
                        
                        ports = ports.split(",")
                        
                        for port in ports:
                            
                            port = int(port)
                            
                            ports = ports.split(",")
                            
                            for port in ports:
                                
                                port = int(port)
                                
                                ports = ports.split(",")
                                
                                for port in ports:
                                    
                                    port = int(port)
                                    
                                    ports = ports.split(",")
                                    
                                    for port in ports:
                                        
                                        port = int(port)
                                        
                                        ports = ports.split(",")
                                        
                                        for port in ports:
                                            
                                            port = int(port)
                                            
                                            ports = ports.split(",")
                                            
                                            for port in ports:
                                                
                                                port = int(port)
                                                
                                                ports = ports.split(",")
                                                
                                                for port in ports:
                                                    
                                                    port = int(port)
                                                    
                                                    ports = ports.split(",")
                                                    
                                                    for port in ports:
                                                        
                                                        port = int(port)
                                                        
                                                        ports = ports.split(",")
                                                        
                                                        for port in ports:
                                                            
                                                            port = int(port)
                                                            
                                                            ports = ports.split(",")
                                                            
                                                            for port in ports:
                                                                
                                                                port = int(port)
                                                                
                                                                ports = ports.split(",")
                                                                
                                                                for port in ports:
                                                                    
                                                                    port = int(port)
                                                                    
                                                                    ports = ports.split(",")
                                                                    
                                                                    for port in ports:
                                                                        
                                                                        port = int(port)
                                                                        
                                                                        ports = ports.split(",")
                                                                        
                                                                        for port in ports:
                                                                            
                                                                            port = int(port)
                                                                            
                                                                            ports = ports.split(",")
                                                                            
                                                                            for port in ports:
                                                                                
