#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds numbers. """    
    
    numbers = map(int, input().split())
    
    numbers = list(map(int, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, numbers))
    
    numbers = list(map(lambda x: x - 1, numbers))
    
    numbers = list(map(lambda x: x + 1, number