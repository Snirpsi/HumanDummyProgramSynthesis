#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that opens a port or stores a list of numbers. """    
    while True:
        port = int(input("Enter a port number: "))
        if port == -1:
            break
        ports.append(port)
        print("The port number is", port)
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the ports
    for port in ports:
        sock.close()
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("The list of ports is", ports)
    
    # Close the socket
    sock.close()
    
    # Print the list of ports
    print("