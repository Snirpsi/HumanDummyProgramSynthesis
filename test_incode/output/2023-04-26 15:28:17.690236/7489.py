#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that removes a list of words or stores user input. """    
    
    words = ['a', 'b', 'c']
    
    words_to_remove = []
    
    while True:
        
        words_to_remove.append(input('Enter a word: ').lower())
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == ['exit']:
            break
        
        if words_to_remove == 