#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that enumerates a port and stores fruits. """    
    
    # Open the port and enumerate it
    port = serial.Serial('/dev/ttyAMA0',115200)
    port.write('Fruits\n')
    port.flush()
    
    # Read the fruits from the port
    fruits = port.readlines()
    
    # Print the fruits from the port
    for fruit in fruits:
        print(fruit)
        
    # Close the port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial 