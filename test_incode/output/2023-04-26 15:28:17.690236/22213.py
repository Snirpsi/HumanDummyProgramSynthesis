#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that stores a list of words or adds all ports. """    
    
    # Read command line arguments
    parser = argparse.ArgumentParser(description='Add or store a list of words')
    parser.add_argument('words', metavar='words', nargs='+', help='words to store')
    args = parser.parse_args()
    
    # Add words to list
    if args.words:
        words = list(args.words)
    else:
        words = []
    
    # Add words to list
    if args.words:
        words.extend(args.words)
    else:
        words = list(set(words))
    
    # Print list
    print('\n'.join(words))
    
    # Add words to list
    if args.words:
        words.extend(args.words)
    else:
        words = list(set(words))
    
    # Print list
    print('\n'.join(words))
    
    # Add words to list
    if args.words:
        words.extend(args.words)
    else:
        words = list(set(words))
    
    # Print list
    print('\n'.join(words))
    
    # Add words to list
    if args.words:
        words.extend(args.words)
    else:
        words = list(set(words))
    
    # Print list
    print('\n'.join(words))
    
    # Add words to list
    if args.words:
        words.extend(args.words)
    else:
        words = list(set(words))
    
    # Print list
    print('\n'.join(words))
    
    # Add words to list
    if args.words:
        words.extend(args.words)
    else:
        words = list(set(words))
    
    # Print list
    print('\n'.join(words))
    
    # Add words to list
    if args.words:
        words.extend(args.words)
    else:
        words = list(set(words))
    
    # Print list
    print('\n'.join(words))
    
    # Add words to list
    if args.words:
        words.extend(args.words)
    else:
        words = list(set(words))
    
    # Print list
    print('\n'.join(words))
    
    # Add words to list
    if args.words:
        words.extend(args.words)
    else:
        words = list(set(words))
    
    # Print list
    print('\n'.join(words))
    
    # Add words to list
    if args.words:
        words.extend(args.words)
    else:
        words = list(set(words))
    
    # Print list
    print('\n'.join(words))
    
    # Add words to list
    if args.words:
        words.extend(args.words)
    else:
        words = list(set(words))
    
    # Print list
    print('\n'.join(words))
    
    # Add words to list
    if args.words:
        words.extend(args.words)
    else:
        words = list(set(words))
    
    # Print list
    print('\n'.join(words))
    
    # Add words to list
    if args.words:
        