#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens a list of numbers. """    
    
    # Open the file
    file = open('numbers.txt', 'r')
    
    # Read all numbers in the file
    numbers = file.readlines()
    
    # Close the file
    file.close()
    
    # Print all numbers in the file
    print(numbers)
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total amount of numbers in the file
    print(len(numbers))
    
    # Close the file
    file.close()
    
    # Print the total 