#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that calculates a port and converts a list of numbers. """    
    port = int(sys.argv[1])
    numbers = [int(n) for n in sys.argv[2:]]
    
    ports = [port + i for i in range(1, len(numbers))]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_str = [str(n) for n in numbers]
    
    numbers_