#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that stores words and multiplyes fruits. """    
    
    # Create a dictionary to store words and their frequencies.
    words = {}
    
    # Open the file that contains the words and frequencies.
    with open('words.txt', 'r') as words_file:
        # Read each line of the file into a list.
        for line in words_file:
            # Split the line into words.
            words = line.split()
    
    # Open the file that contains the fruits.
    with open('fruits.txt', 'r') as fruits_file:
        # Read each line of the file into a list.
        for line in fruits_file:
            # Split the line into words.
            fruits = line.split()
    
    # Create a dictionary to store words and their frequencies.
    fruits_dictionary = {}
    
    # Open the file that contains the words and frequencies.
    with open('fruits.txt', 'r') as fruits_file:
        # Read each line of the file into a list.
        for line in fruits_file:
            # Split the line into words.
            fruits = line.split()
    
    # Create a dictionary to store words and their frequencies.
    fruits_dictionary = {}
    
    # Open the file that contains the words and frequencies.
    with open('fruits.txt', 'r') as fruits_file:
        # Read each line of the file into a list.
        for line in fruits_file:
            # Split the line into words.
            fruits = line.split()
    
    # Create a dictionary to store words and their frequencies.
    fruits_dictionary = {}
    
    # Open the file that contains the words and frequencies.
    with open('fruits.txt', 'r') as fruits_file:
        # Read each line of the file into a list.
        for line in fruits_file:
            # Split the line into words.
            fruits = line.split()
    
    # Create a dictionary to store words and their frequencies.
    fruits_dictionary = {}
    
    # Open the file that contains the words and frequencies.
    with open('fruits.txt', 'r') as fruits_file:
        # Read each line of the file into a list.
        for line in fruits_file:
            # Split the line into words.
            fruits = line.split()
    
    # Create a dictionary to store words and their frequencies.
    fruits_dictionary = {}
    
    # Open the file that contains the words and frequencies.
    with open('fruits.txt', 'r') as fruits_file:
        # Read each line of the file into a list.
        for line in fruits_file:
            # Split the line into words.
            fruits = line.split()
    
    # Create a dictionary to store words and their frequencies.
    fruits_dictionary = {}
    
    # Open the file that contains the words and frequencies.
    with open('fruits.txt', 'r') as fruits_file:
        # Read each line of the file into a list.
        for line in fruits_file:
            # Split the line into words.