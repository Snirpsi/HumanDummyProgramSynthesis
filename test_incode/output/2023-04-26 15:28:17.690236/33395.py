#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that returns user input and removes words. """    
    
    while(True):
        line = input('Enter a word: ')
        
        if line == 'quit':
            break
        
        line = line.split()
        
        for word in line:
            if word not in stopwords:
                print(word)
                
        line = input('Enter another word: ')
        
        if line == 'quit':
            break
        
        line = line.split()
        
        for word in line:
            if word not in stopwords:
                print(word)
                
        line = input('Enter another word: ')
        
        if line == 'quit':
            break
        
        line = line.split()
        
        for word in line:
            if word not in stopwords:
                print(word)
                
        line = input('Enter another word: ')
        
        if line == 'quit':
            break
        
        line = line.split()
        
        for word in line:
            if word not in stopwords:
                print(word)
                
        line = input('Enter another word: ')
        
        if line == 'quit':
            break
        
        line = line.split()
        
        for word in line:
            if word not in stopwords:
                print(word)
                
        line = input('Enter another word: ')
        
        if line == 'quit':
            break
        
        line = line.split()
        
        for word in line:
            if word not in stopwords:
                print(word)
                
        line = input('Enter another word: ')
        
        if line == 'quit':
            break
        
        line = line.split()
        
        for word in line:
            if word not in stopwords:
                print(word)
                
        line = input('Enter another word: ')
        
        if line == 'quit':
            break
        
        line = line.split()
        
        for word in line:
            if word not in stopwords:
                print(word)
                
        line = input('Enter another word: ')
        
        if line == 'quit':
            break
        
        line = line.split()
        
        for word in line:
            if word not in stopwords:
                print(word)
                
        line = input('Enter another word: ')
        
        if line == 'quit':
            break
        
        line = line.split()
        
        for word in line:
            if word not in stopwords:
                print(word)
                
        line = input('Enter another word: ')
        
        if line == 'quit':
            break
        
        line = line.split()
        
        for word in line:
            if word not in stopwords:
                print(word)
                
        line = input('Enter another word: ')
        
        if line == 'quit':
            break
        
        line = line.split()
        
        for word in line:
            if word not in stopwords:
                print(word)
                
        