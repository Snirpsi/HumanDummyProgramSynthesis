#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that returns words or removes numbers. """    
    import sys
    
    words = sys.argv[1:]
    
    if len(words) == 0:
        words = sys.argv[1:]
    
    words = [w for w in words if w != '']
    
    words = [w for w in words if not w.isdigit()]
    
    words = [w for w in words if not w.isalpha()]
    
    words = [w for w in words if not w.isspace()]
    
    words = [w for w in words if not w.isalnum()]
    
    words = [w for w in words if not w in '-_']
    
    words = [w for w in words if not w in '!@#$%^&*()_+{}[]|\:;"\'<>,./?']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']
    
    words = [w for w in words if not w in '`~`']