#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that removes all ports or adds all ports. """    
    
    # Remove all ports
    remove_all_ports()
    
    # Add all ports
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for ports to be added
    add_all_ports()
    
    # Wait for ports to be removed
    remove_all_ports()
    
    # Wait for 