#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds a port and adds words. """    
    
    # Create a port and add words to it.
    port = Port('tcp', 1234)
    port.add_word('word1')
    port.add_word('word2')
    
    # Print the port and words to it.
    print(port)
    print(port.words)
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.
    port.close()
    
    # Close the port.