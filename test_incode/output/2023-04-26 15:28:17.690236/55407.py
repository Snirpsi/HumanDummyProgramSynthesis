#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that iterates over a port or iterates over all ports. """    
    while True:
        
        # Read a line from stdin
        line = sys.stdin.readline()
        
        # Send a line to port 0
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 1
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 2
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 3
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 4
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 5
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 6
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 7
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 8
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 9
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 10
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 11
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 12
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 13
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 14
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 15
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 16
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 17
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 18
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 19
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 20
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 21
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 22
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 23
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 24
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 25
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 26
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 27
        sys.stdout.write("%s\n" % line)
        
        # Send a line to port 28
        sys.stdout.write("%s\n" % line)
