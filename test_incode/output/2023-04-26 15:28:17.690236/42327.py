#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds user input or iterates over a port. """    
    port = int(input('Enter a port number: '))
    
    # If the port number is greater than zero, iterate over the port number
    if port > 0:
        
        # Iterate over the port number
        for port in range(1, port + 1):
            
            # Print the port number
            print('Port number {} is {}'.format(port, port+1))
            
            # Print the port name
            print('Port name {} is {}'.format(port, port+1))
            
            # Print the port state
            print('Port state {} is {}'.format(port, port+1))
            
            # Print the port description
            print('Port description {} is {}'.format(port, port+1))
            
            # Print the port state
            print('Port state {} is {}'.format(port, port+1))
            
            # Print the port description
            print('Port description {} is {}'.format(port, port+1))
            
            # Print the port state
            print('Port state {} is {}'.format(port, port+1))
            
            # Print the port description
            print('Port description {} is {}'.format(port, port+1))
            
            # Print the port state
            print('Port state {} is {}'.format(port, port+1))
            
            # Print the port description
            print('Port description {} is {}'.format(port, port+1))
            
            # Print the port state
            print('Port state {} is {}'.format(port, port+1))
            
            # Print the port description
            print('Port description {} is {}'.format(port, port+1))
            
            # Print the port state
            print('Port state {} is {}'.format(port, port+1))
            
            # Print the port description
            print('Port description {} is {}'.format(port, port+1))
            
            # Print the port state
            print('Port state {} is {}'.format(port, port+1))
            
            # Print the port description
            print('Port description {} is {}'.format(port, port+1))
            
            # Print the port state
            print('Port state {} is {}'.format(port, port+1))
            
            # Print the port description
            print('Port description {} is {}'.format(port, port+1))
            
            # Print the port state
            print('Port state {} is {}'.format(port, port+1))
            
            # Print the port description
            print('Port description {} is {}'.format(port, port+1))
            
            # Print the port state
            print('Port state {} is {}'.format(port, port+1))
            
            # Print the port description
            print('Port description {} is {}'.format(port, port+1))
            
            # Print the port state
            print('Port state {} is {}'.format(port, port+1))
            
            # Print the port description
            print('Port description {} is {}'.format(port, port+1))
            
            # Print the port state
            print('Port 