#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that stores a port or calculates a list of numbers. """    
    
    # Read command line arguments
    port = int(sys.argv[1])
    
    # Calculate list of numbers
    numbers = [i for i in range(1,port+1)]
    
    # Print list of numbers
    print(numbers)
    
    # Print the sum of the numbers
    print(sum(numbers))
    
    # Print the average of the numbers
    print(sum(numbers)/len(numbers))
    
    # Print the median of the numbers
    print(sorted(numbers)[len(numbers)//2])
    
    # Print the minimum of the numbers
    print(min(numbers))
    
    # Print the maximum of the numbers
    print(max(numbers))
    
    # Print the average of the numbers between 1 and 10
    print(sum(numbers)/len(numbers)*10)
    
    # Print the average of the numbers between 1 and 10 and store in variable average
    average = sum(numbers)/len(numbers)*10
    print(average)
    
    # Print the average of the numbers between 1 and 10 and store in variable average
    average = sum(numbers)/len(numbers)*10
    print(average)
    
    # Print the average of the numbers between 1 and 10 and store in variable average
    average = sum(numbers)/len(numbers)*10
    print(average)
    
    # Print the average of the numbers between 1 and 10 and store in variable average
    average = sum(numbers)/len(numbers)*10
    print(average)
    
    # Print the average of the numbers between 1 and 10 and store in variable average
    average = sum(numbers)/len(numbers)*10
    print(average)
    
    # Print the average of the numbers between 1 and 10 and store in variable average
    average = sum(numbers)/len(numbers)*10
    print(average)
    
    # Print the average of the numbers between 1 and 10 and store in variable average
    average = sum(numbers)/len(numbers)*10
    print(average)
    
    # Print the average of the numbers between 1 and 10 and store in variable average
    average = sum(numbers)/len(numbers)*10
    print(average)
    
    # Print the average of the numbers between 1 and 10 and store in variable average
    average = sum(numbers)/len(numbers)*10
    print(average)
    
    # Print the average of the numbers between 1 and 10 and store in variable average
    average = sum(numbers)/len(numbers)*10
    print(average)
    
    # Print the average of the numbers between 1 and 10 and store in variable average
    average