#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that converts a port. """    
    while True:
        port = int(input('Enter a port to convert (1-65535): '))
        
        if port > 65535:
            print('Port must be less than 65535.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already converted.')
            continue
        
        port = int(port)
        
        if port == port:
            print('Port is already 