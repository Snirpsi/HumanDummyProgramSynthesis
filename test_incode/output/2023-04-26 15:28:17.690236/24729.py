#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that adds a list of words and opens user input. """    
    words = ['hello', 'world', 'python', 'is', 'fun']
    words.extend(['python', 'is', 'fun', 'of', 'python'])
    
    while True:
        
        # Read user input
        word = input('Enter a word: ')
        
        # Check if word is in list
        if word in words:
            print(word + ' is a word!')
        else:
            print(word + ' is not a word!')
            
        # Ask user to input again
        print('Would you like to try again? Y/N: ')
        
        # Check if user input is 'Y'
        if input('Enter Y/N: ').lower() == 'y':
            break
            
        # Ask user to input again
        print('Would you like to try again? Y/N: ')
        
        # Check if user input is 'Y'
        if input('Enter Y/N: ').lower() == 'y':
            break
            
        # Ask user to input again
        print('Would you like to try again? Y/N: ')
        
        # Check if user input is 'Y'
        if input('Enter Y/N: ').lower() == 'y':
            break
            
        # Ask user to input again
        print('Would you like to try again? Y/N: ')
        
        # Check if user input is 'Y'
        if input('Enter Y/N: ').lower() == 'y':
            break
            
        # Ask user to input again
        print('Would you like to try again? Y/N: ')
        
        # Check if user input is 'Y'
        if input('Enter Y/N: ').lower() == 'y':
            break
            
        # Ask user to input again
        print('Would you like to try again? Y/N: ')
        
        # Check if user input is 'Y'
        if input('Enter Y/N: ').lower() == 'y':
            break
            
        # Ask user to input again
        print('Would you like to try again? Y/N: ')
        
        # Check if user input is 'Y'
        if input('Enter Y/N: ').lower() == 'y':
            break
            
        # Ask user to input again
        print('Would you like to try again? Y/N: ')
        
        # Check if user input is 'Y'
        if input('Enter Y/N: ').lower() == 'y':
            break
            
        # Ask user to input again
        print('Would you like to try again? Y/N: ')
        
        # Check if user input is 'Y'
        if input('Enter Y/N: ').lower() == 'y':
            break
            
        # Ask user to input again
        print('