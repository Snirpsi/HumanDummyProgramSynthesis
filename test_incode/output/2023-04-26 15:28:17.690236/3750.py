#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that returns words and multiplyes numbers. """    
    words = []
    numbers = []
    
    for word in words:
        words.append(word.lower())
        
    for number in numbers:
        numbers.append(int(number))
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] += 1
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] /= len(words)
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] /= len(words)
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] /= len(words)
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] /= len(words)
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] /= len(words)
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] /= len(words)
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] /= len(words)
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] /= len(words)
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] /= len(words)
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] /= len(words)
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] /= len(words)
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] /= len(words)
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] /= len(words)
    
    numbers = sum(numbers)
    
    for word in words:
        if word in numbers:
            numbers[numbers.index(word)] /= len(words)
    
    numbers = sum(numbers)
    
    for word in words: