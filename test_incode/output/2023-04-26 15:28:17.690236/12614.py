#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts a list of words or converts fruits. """    
    
    import sys
    
    if len(sys.argv) != 2:
        print("Usage: python3 wordlist.py <wordlist>")
        sys.exit()
    
    wordlist = sys.argv[1]
    
    wordlist = wordlist.split()
    
    wordlist = list(set(wordlist))
    
    wordlist = list(filter(lambda w: w not in ['.', '!', '?', '...'], wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
    wordlist = list(filter(lambda w: w != '', wordlist))
    
