#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that removes a port or converts words. """    
    port = int(sys.argv[1])
    
    # Remove port from URL
    port = str(port)
    
    # Convert words to numbers
    words = sys.argv[2:]
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
    # Convert words to numbers
    words = map(int, words)
    
    # Convert words to numbers
    words = map(str, words)
    
