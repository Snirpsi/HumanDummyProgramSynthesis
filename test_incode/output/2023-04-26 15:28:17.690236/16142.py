#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates a list of words. """    
    words = []
    
    # Read in the file words.txt
    with open('words.txt', 'r') as file:
        for line in file:
            words.append(line.strip())
    
    # Print the list of words
    print(words)
    
    # Print the longest word
    print(max(words))
    
    # Print the shortest word
    print(min(words))
    
    # Print the average length of the words
    print(sum(words) / len(words))
    
    # Print the average length of each word in the list
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a string
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a float
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a int
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a long
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a float
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a long
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a float
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a long
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a float
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a long
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a float
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a long
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a float
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a long
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a float
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a long
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of each word in the list as a float
    print(sum(map(len, words)