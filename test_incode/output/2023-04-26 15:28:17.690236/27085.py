#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that removes a port and removes a port. """    
    
    # Get the port to remove
    port = sys.argv[1]
    
    # Remove the port
    subprocess.call(['iptables', '--flush', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])
    
    # Remove the port
    subprocess.call(['iptables', '--delete', port])