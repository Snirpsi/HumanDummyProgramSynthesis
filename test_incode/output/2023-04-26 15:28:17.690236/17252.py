#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that calculates a list of words. """    
    
    # Start a web server
    httpd = HTTPServer(('', 8080), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 443), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), WordsHandler)
    
    # Start a web server