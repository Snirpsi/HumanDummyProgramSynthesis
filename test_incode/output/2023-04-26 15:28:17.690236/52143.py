#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that prints all ports or iterates over a list of numbers. """    
    ports = []
    for port in range(1, 65535):
        ports.append(port)
    
    # Print all ports
    for port in ports:
        print(port)
    
    # Iterate over all ports
    for port in ports:
        print(port)
        
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
    
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
            
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
            
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
            
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
            
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
            
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
            
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
            
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
            
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
            
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
            
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
            
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
            
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
            
    # Iterate over all ports except for port 80
    for port in range(1, 65535):
        if port != 80:
            print(port)
            
    # Iterate over all ports except 