#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that calculates a list of numbers or converts a list of words. """    
    
    # Input the file name
    filename = input("Enter the file name: ")
    
    # Open the file
    with open(filename, "r") as file:
        
        # Read the file line by line
        for line in file:
            
            # Split the line into words
            words = line.split()
            
            # Calculate the sum
            sum = sum(words)
            
            # Print the sum
            print(sum)
            
            # Print the word
            print(words)
            
            # Print the line number
            print(line)
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.tell())
            
            # Print the line number
            print(file.