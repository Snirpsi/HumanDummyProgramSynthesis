#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates a port or removes words. """    
    
    # Read input from stdin
    lines = sys.stdin.readlines()
    
    # Remove words from each line
    for line in lines:
        words = line.split()
        
        # Remove words that start with '#' 
        if words[0] == '#':
            continue
        
        # Remove words that end with '#' 
        if words[-1] == '#':
            continue
        
        # Remove words that start with '#' 
        if words[0] == '#':
            continue
        
        # Remove words that end with '#' 
        if words[-1] == '#':
            continue
        
        # Remove words that start with '#' 
        if words[0] == '#':
            continue
        
        # Remove words that end with '#' 
        if words[-1] == '#':
            continue
        
        # Remove words that start with '#' 
        if words[0] == '#':
            continue
        
        # Remove words that end with '#' 
        if words[-1] == '#':
            continue
        
        # Remove words that start with '#' 
        if words[0] == '#':
            continue
        
        # Remove words that end with '#' 
        if words[-1] == '#':
            continue
        
        # Remove words that start with '#' 
        if words[0] == '#':
            continue
        
        # Remove words that end with '#' 
        if words[-1] == '#':
            continue
        
        # Remove words that start with '#' 
        if words[0] == '#':
            continue
        
        # Remove words that end with '#' 
        if words[-1] == '#':
            continue
        
        # Remove words that start with '#' 
        if words[0] == '#':
            continue
        
        # Remove words that end with '#' 
        if words[-1] == '#':
            continue
        
        # Remove words that start with '#' 
        if words[0] == '#':
            continue
        
        # Remove words that end with '#' 
        if words[-1] == '#':
            continue
        
        # Remove words that start with '#' 
        if words[0] == '#':
            continue
        
        # Remove words that end with '#' 
        if words[-1] == '#':
            continue
        
        # Remove words that start with '#' 
        if words[0] == '#':
            continue
        
        # Remove words that end with '#' 
        if words[-1] == '#':
            continue
        
        # Remove words that start with '#' 
        if words[0] == '#':
            continue
        
        # Remove words that end with '