#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens user input or adds user input. """    
    
    import sys
    
    if len(sys.argv) > 1:
        filename = sys.argv[1]
    else:
        filename = input("Enter the file name: ")
    
    file = open(filename, 'r')
    
    for line in file:
        line = line.strip()
        
        if line == '':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
        
        if line == '#':
            continue
