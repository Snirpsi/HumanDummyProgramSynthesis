#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores all ports and calculates all ports. """    
    ports = []
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
    
    for port in ports:
        ports.append(port)
