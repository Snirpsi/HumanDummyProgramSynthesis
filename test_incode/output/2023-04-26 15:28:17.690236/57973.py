#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts a port or adds fruits. """    
    
    # Parse command line arguments
    port = int(sys.argv[1])
    fruits = sys.argv[2:]
    
    # Convert port
    fruits_converted = convert_port(port)
    
    # Add fruits to port
    fruits_added = add_fruits(fruits, fruits_converted)
    
    # Print the result
    print("\n".join([str(fruit) for fruit in fruits_added]))
    
    # Print the port and fruits converted
    print("\n".join([str(port), str(fruits_converted)]))
    
    # Print the port and fruits added
    print("\n".join([str(port), str(fruits_added)]))
    
    # Print the port and fruits converted
    print("\n".join([str(port), str(fruits_converted)]))
    
    # Print the port and fruits added
    print("\n".join([str(port), str(fruits_added)]))
    
    # Print the port and fruits converted
    print("\n".join([str(port), str(fruits_converted)]))
    
    # Print the port and fruits added
    print("\n".join([str(port), str(fruits_added)]))
    
    # Print the port and fruits converted
    print("\n".join([str(port), str(fruits_converted)]))
    
    # Print the port and fruits added
    print("\n".join([str(port), str(fruits_added)]))
    
    # Print the port and fruits converted
    print("\n".join([str(port), str(fruits_converted)]))
    
    # Print the port and fruits added
    print("\n".join([str(port), str(fruits_added)]))
    
    # Print the port and fruits converted
    print("\n".join([str(port), str(fruits_converted)]))
    
    # Print the port and fruits added
    print("\n".join([str(port), str(fruits_added)]))
    
    # Print the port and fruits converted
    print("\n".join([str(port), str(fruits_converted)]))
    
    # Print the port and fruits added
    print("\n".join([str(port), str(fruits_added)]))
    
    # Print the port and fruits converted
    print("\n".join([str(port), str(fruits_converted)]))
    
    # Print the port and fruits added
    print("\n".join([str(port), str(fruits_added)]))
    
    # Print the port and fruits converted
    print("\n".join([str(port), str(fruits_converted)]))
    
    # Print the port and fruits added
    print("\n".join([str(port), str(fruits_added)]))
