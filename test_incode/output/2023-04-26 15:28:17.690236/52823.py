#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that calculates all ports or opens user input. """    
    while True:
        ports = input("Enter ports: ")
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports.sort()
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
        ports = ports[::-1]
        ports = [str(p) for p in ports]
