#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that iterates over a list of numbers or prints numbers. """    
    
    # iterates over the list of numbers and prints them
    for number in numbers:
        print(number)
    
    # iterates over the list of numbers and prints them with spaces
    for number in numbers:
        print(number, end=' ')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end='\n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines
    for number in numbers:
        print(number, end=' \n')
    
    # iterates over the list of numbers and prints them with new lines