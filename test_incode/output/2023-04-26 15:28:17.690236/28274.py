#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that enumerates a list of numbers or multiplyes all ports. """    
    ports = [int(port) for port in sys.argv[1:]]
    ports = list(filter(lambda port: port > 0, ports))
    ports = list(map(lambda port: port % 2, ports))
    ports = list(map(lambda port: port * 2, ports))
    ports = list(map(lambda port: port / 2, ports))
    ports = list(map(lambda port: port % 3, ports))
    ports = list(map(lambda port: port * 3, ports))
    ports = list(map(lambda port: port / 3, ports))
    ports = list(map(lambda port: port % 4, ports))
    ports = list(map(lambda port: port * 4, ports))
    ports = list(map(lambda port: port / 4, ports))
    ports = list(map(lambda port: port % 5, ports))
    ports = list(map(lambda port: port * 5, ports))
    ports = list(map(lambda port: port / 5, ports))
    ports = list(map(lambda port: port % 6, ports))
    ports = list(map(lambda port: port * 6, ports))
    ports = list(map(lambda port: port / 6, ports))
    ports = list(map(lambda port: port % 7, ports))
    ports = list(map(lambda port: port * 7, ports))
    ports = list(map(lambda port: port / 7, ports))
    ports = list(map(lambda port: port % 8, ports))
    ports = list(map(lambda port: port * 8, ports))
    ports = list(map(lambda port: port / 8, ports))
    ports = list(map(lambda port: port % 9, ports))
    ports = list(map(lambda port: port * 9, ports))
    ports = list(map(lambda port: port / 9, ports))
    ports = list(map(lambda port: port % 10, ports))
    ports = list(map(lambda port: port * 10, ports))
    ports = list(map(lambda port: port / 10, ports))
    ports = list(map(lambda port: port % 11, ports))
    ports = list(map(lambda port: port * 11, ports))
    ports = list(map(lambda port: port / 11, ports))
    ports = list(map(lambda port: port % 12, ports))
    ports = list(map(lambda port: port * 12, ports))
    ports = list(map(lambda port: port / 12, ports))
    ports = list(map(lambda port: port % 13, ports))
    ports = list(map(lambda port: port * 13, ports))
    ports = list(map(lambda port: port / 13, ports))
    ports = list(map(lambda port: port % 14, ports))
    ports = list(map(lambda port: port * 14, ports))
    ports = list(map(lambda port: port / 14, ports))
    ports = list(map(lambda port: port % 15, ports))
    ports = list(map(lambda port: port * 15, ports))
    ports = list(map(lambda port: port / 15, ports))
    ports = list(map(lambda port: port % 16, ports))
    ports = list(map(lambda port: port * 16, ports))
    ports = list(map(lambda 