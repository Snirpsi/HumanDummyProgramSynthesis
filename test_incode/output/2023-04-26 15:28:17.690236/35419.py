#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that converts all ports and removes a port. """    
    ports = sys.argv[1:]
    ports = [p for p in ports if p!='']
    
    for p in ports:
        ports.remove(p)
    
    for p in ports:
        ports.remove(p)
        
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    
    ports.sort()
    
    for p in ports:
        ports.remove(p)
    