#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that multiplyes words. """    
    
    import os
    
    webserver_address = os.environ['WEBSERVER_ADDRESS']
    webserver_port = os.environ['WEBSERVER_PORT']
    
    webserver_address = webserver_address.split(':')
    webserver_port = int(webserver_port)
    
    webserver_address = webserver_address[0] + ':' + str(webserver_port)
    
    webserver_address = 'http://' + webserver_address
    
    webserver_address = webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + webserver_address + ':8000'
    
    webserver_address = 'http://' + web