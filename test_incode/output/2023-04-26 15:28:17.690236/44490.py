#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that multiplyes user input or returns all ports. """    
    ports = []
    
    # Get the ports from the user
    while True:
        ports.append(input("Enter a port number: "))
        
        # Check if the user entered a port
        if len(ports) == 2:
            break
            
    # Check if the user entered a port
    if len(ports) == 1:
        ports.append(input("Enter a port number: "))
        
    # Check if the user entered a port
    if len(ports) == 0:
        print("No ports entered")
        
    # Multiply user input with ports
    ports = ports * len(ports)
    
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(