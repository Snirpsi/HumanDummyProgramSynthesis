#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens a list of numbers. """    
    
    # Open the file that contains the numbers to open
    with open('numbers.txt', 'r') as file:
        numbers = file.readlines()
    
    # Open the file that contains the numbers to open
    with open('numbers.txt', 'w') as file:
        
        # Loop over the numbers and write them to the file
        for line in numbers:
            
            # Split the line into two parts separated by spaces
            parts = line.split(' ')
            
            # Write the parts to the file
            file.write(' '.join(parts) + '\n')
            
            # Print the line to the screen
            print(line)
            
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open
    file.close()
    
    # Close the file that contains the numbers to open