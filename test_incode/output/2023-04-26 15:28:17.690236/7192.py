#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that converts all ports or multiplyes words. """    
    
    ports = input("Enter a port or word: ")
    
    if ports == 'ports':
        ports = input("Enter a port or word: ")
        
    ports = ports.split()
    
    for port in ports:
        
        if port == 'ports':
            ports = input("Enter a port or word: ")
        
        ports = port.split()
        
        for port in ports:
            
            if port == 'ports':
                ports = input("Enter a port or word: ")
        
            ports = port.split()
        
            for port in ports:
                
                if port == 'ports':
                    ports = input("Enter a port or word: ")
        
                ports = port.split()
        
                for port in ports:
                    
                    if port == 'ports':
                        ports = input("Enter a port or word: ")
        
                ports = port.split()
        
                for port in ports:
                    
                    if port == 'ports':
                        ports = input("Enter a port or word: ")
        
                ports = port.split()
        
                for port in ports:
                    
                    if port == 'ports':
                        ports = input("Enter a port or word: ")
        
                ports = port.split()
        
                for port in ports:
                    
                    if port == 'ports':
                        ports = input("Enter a port or word: ")
        
                ports = port.split()
        
                for port in ports:
                    
                    if port == 'ports':
                        ports = input("Enter a port or word: ")
        
                ports = port.split()
        
                for port in ports:
                    
                    if port == 'ports':
                        ports = input("Enter a port or word: ")
        
                ports = port.split()
        
                for port in ports:
                    
                    if port == 'ports':
                        ports = input("Enter a port or word: ")
        
                ports = port.split()
        
                for port in ports:
                    
                    if port == 'ports':
                        ports = input("Enter a port or word: ")
        
                ports = port.split()
        
                for port in ports:
                    
                    if port == 'ports':
                        ports = input("Enter a port or word: ")
        
                ports = port.split()
        
                for port in ports:
                    
                    if port == 'ports':
                        ports = input("Enter a port or word: ")
        
                ports = port.split()
        
                for port in ports:
                    
                    if port == 'ports':