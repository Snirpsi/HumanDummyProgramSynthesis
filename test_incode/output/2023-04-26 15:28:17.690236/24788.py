#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that converts numbers or prints numbers. """    
    
    # Convert numbers to string
    numbers = [int(x) for x in input("Enter numbers: ").split()]
    
    # Print numbers as string
    print("\n".join(str(x) for x in numbers))
    
    # Convert numbers to float
    numbers = [float(x) for x in input("Enter numbers: ").split()]
    
    # Print numbers as float
    print("\n".join(str(x) for x in numbers))
    
    # Convert numbers to int
    numbers = [int(x) for x in input("Enter numbers: ").split()]
    
    # Print numbers as int
    print("\n".join(str(x) for x in numbers))
    
    # Convert numbers to boolean
    numbers = [int(x) for x in input("Enter numbers: ").split()]
    
    # Print numbers as boolean
    print("\n".join(str(x) for x in numbers))
    
    # Convert numbers to list
    numbers = [int(x) for x in input("Enter numbers: ").split()]
    
    # Print numbers as list
    print("\n".join(str(x) for x in numbers))
    
    # Convert numbers to tuple
    numbers = [int(x) for x in input("Enter numbers: ").split()]
    
    # Print numbers as tuple
    print("\n".join(str(x) for x in numbers))
    
    # Convert numbers to set
    numbers = [int(x) for x in input("Enter numbers: ").split()]
    
    # Print numbers as set
    print("\n".join(str(x) for x in numbers))
    
    # Convert numbers to frozenset
    numbers = [int(x) for x in input("Enter numbers: ").split()]
    
    # Print numbers as frozenset
    print("\n".join(str(x) for x in numbers))
    
    # Convert numbers to frozenset
    numbers = [int(x) for x in input("Enter numbers: ").split()]
    
    # Print numbers as frozenset
    print("\n".join(str(x) for x in numbers))
    
    # Convert numbers to set
    numbers = [int(x) for x in input("Enter numbers: ").split()]
    
    # Print numbers as set
    print("\n".join(str(x) for x in numbers))
    
    # Convert numbers to set
    numbers = [int(x) for x in input("Enter numbers: ").split()]
    
    # Print numbers as set
    print("\n".join(str(x) for x in numbers))
    
    # Convert numbers to set
    numbers = [int(x) for x in input("Enter numbers: ").split()]
    
    # Print numbers as set
    print("\n".join(str(x) for x in numbers))
    
    # Convert numbers to set
    numbers = [int(x) for x in input("Enter numbers: ").split()]
    
    # Print numbers as set
    print("\n".join(str(x) for x in numbers))
    
    # Convert numbers to set
    numbers = [int(x) for x in input("Enter numbers: ").split()]
    
    # Print numbers as set
    print("\n".join(str(x) for x in numbers))
    
    # 