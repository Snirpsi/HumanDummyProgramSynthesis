#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores a port or removes a port. """    
    
    # Get the port number from the command line arguments
    if len(sys.argv) > 1:
        port = int(sys.argv[1])
    else:
        port = 80

    # If the port number is less than 0, then remove it
    if port < 0:
        port = 0

    # If the port number is greater than 65535, then remove it
    if port > 65535:
        port = 65535

    # If the port number is 0, then remove it
    if port == 0:
        port = 0

    # If the port number is greater than 65535, then remove it
    if port > 65535:
        port = 65535

    # If the port number is 0, then remove it
    if port == 0:
        port = 0

    # If the port number is greater than 65535, then remove it
    if port > 65535:
        port = 65535

    # If the port number is 0, then remove it
    if port == 0:
        port = 0

    # If the port number is greater than 65535, then remove it
    if port > 65535:
        port = 65535

    # If the port number is 0, then remove it
    if port == 0:
        port = 0

    # If the port number is greater than 65535, then remove it
    if port > 65535:
        port = 65535

    # If the port number is 0, then remove it
    if port == 0:
        port = 0

    # If the port number is greater than 65535, then remove it
    if port > 65535:
        port = 65535

    # If the port number is 0, then remove it
    if port == 0:
        port = 0

    # If the port number is greater than 65535, then remove it
    if port > 65535:
        port = 65535

    # If the port number is 0, then remove it
    if port == 0:
        port = 0

    # If the port number is greater than 65535, then remove it
    if port > 65535:
        port = 65535

    # If the port number is 0, then remove it
    if port == 0:
        port = 0

    # If the port number is greater than 65535, then remove it
    if port > 65535:
        port = 65535

    # If the port number is 0, then remove it
    if port == 0:
        port = 0

    # If the port number is greater than 65535, then remove it
    if port > 65535:
        port = 65535

    # If the port number is 0, then remove it
    if port == 0:
        port = 0

    # If the port number is greater than 65535, then remove it
    if port > 65535:
        port = 65535

    # If the port number is 0, then remove it
    if port == 0:
        port = 0

    # If the port number is greater than 65535, then remove it
    if port > 65535:
        port = 65535

    # If the port number is 