#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that iterates over a list of numbers or iterates over a list of words. """    
    
    # Iterate over list of numbers:
    for number in numbers:
        print(number)
    
    # Iterate over list of words:
    for word in words:
        print(word)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # Iterate over list of tuples:
    for tuple in tuples:
        print(tuple)
    
    # 