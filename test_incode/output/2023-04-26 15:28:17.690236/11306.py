#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that enumerates all ports and stores a list of numbers. """    
    ports = []
    
    # Enumerate all ports
    for port in range(1,65535):
        ports.append(port)
        
    # Print the list of ports
    print(ports)
    
    # Print the list of numbers in each port
    for port in ports:
        print(port)
        
    # Print the list of numbers in each port and store them in a list
    numbers = []
    for port in ports:
        numbers.append(port)
        
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of numbers in each port and store them in a list
    numbers = numbers + numbers
    
    # Print the list of number