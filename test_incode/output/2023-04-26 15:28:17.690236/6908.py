#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds words. """    
    words = sys.argv[1:]
    words += ['']
    
    words = list(filter(None, words))
    
    for word in words:
        word = word.strip()
        
        if word == '':
            continue
        
        if word == '.':
            continue
        
        if word == '?':
            continue
        
        if word == '!':
            continue
        
        if word == '':
            continue
        
        if word == '-':
            continue
        
        if word == '.':
            continue
        
        if word == '?':
            continue
        
        if word == '!':
            continue
        
        if word == '':
            continue
        
        if word == '-':
            continue
        
        if word == '.':
            continue
        
        if word == '?':
            continue
        
        if word == '!':
            continue
        
        if word == '':
            continue
        
        if word == '-':
            continue
        
        if word == '.':
            continue
        
        if word == '?':
            continue
        
        if word == '!':
            continue
        
        if word == '':
            continue
        
        if word == '-':
            continue
        
        if word == '.':
            continue
        
        if word == '?':
            continue
        
        if word == '!':
            continue
        
        if word == '':
            continue
        
        if word == '-':
            continue
        
        if word == '.':
            continue
        
        if word == '?':
            continue
        
        if word == '!':
            continue
        
        if word == '':
            continue
        
        if word == '-':
            continue
        
        if word == '.':
            continue
        
        if word == '?':
            continue
        
        if word == '!':
            continue
        
        if word == '':
            continue
        
        if word == '-':
            continue
        
        if word == '.':
            continue
        
        if word == '?':
            continue
        
        if word == '!':
            continue
        
        if word == '':
            continue
        
        if word == '-':
            continue
        
        if word == '.':
            continue
        
        if word == '?':
            continue
        
        if word == '!':
            continue
        
        if word == '':
            continue
        
        if word == '-':
            continue
        
        if word == '.':
            continue
        
        if word == '?':
            continue
        
        if word == '!':
            continue