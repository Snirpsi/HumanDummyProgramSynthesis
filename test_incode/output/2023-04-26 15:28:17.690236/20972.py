#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores numbers or multiplyes a port. """    
    
    # Get the port number from the command line
    port = int(sys.argv[1])
    
    # Store the port number in the file
    with open('ports.txt', 'w') as f:
        f.write(str(port))
        
    # Multiply the port number by 10
    port *= 10
    
    # Store the port number in the file
    with open('ports.txt', 'w') as f:
        f.write(str(port))
        
    # Multiply the port number by 100
    port *= 100
    
    # Store the port number in the file
    with open('ports.txt', 'w') as f:
        f.write(str(port))
        
    # Multiply the port number by 1000
    port *= 1000
    
    # Store the port number in the file
    with open('ports.txt', 'w') as f:
        f.write(str(port))
        
    # Multiply the port number by 10000
    port *= 10000
    
    # Store the port number in the file
    with open('ports.txt', 'w') as f:
        f.write(str(port))
        
    # Multiply the port number by 100000
    port *= 100000
    
    # Store the port number in the file
    with open('ports.txt', 'w') as f:
        f.write(str(port))
        
    # Multiply the port number by 1000000
    port *= 1000000
    
    # Store the port number in the file
    with open('ports.txt', 'w') as f:
        f.write(str(port))
        
    # Multiply the port number by 10000000
    port *= 10000000
    
    # Store the port number in the file
    with open('ports.txt', 'w') as f:
        f.write(str(port))
        
    # Multiply the port number by 100000000
    port *= 100000000
    
    # Store the port number in the file
    with open('ports.txt', 'w') as f:
        f.write(str(port))
        
    # Multiply the port number by 1000000000
    port *= 1000000000
    
    # Store the port number in the file
    with open('ports.txt', 'w') as f:
        f.write(str(port))
        
    # Multiply the port number by 10000000000
    port *= 10000000000
    
    # Store the port number in the file
    with open('ports.txt', 'w') as f:
        f.write(str(port))
        
    # Multiply the port number by 100000000000
    port *= 100000000000
    
    # Store the port number in the file
    with open('ports.txt', 'w') as f:
        f.write(str(port))
        
    # Multiply the port number by 1000000000000
    port *= 1000000000000
    
    # Store the port number in the file
    with open('ports.txt', 'w') as f:
        f.write(str(port))
        
    # Multiply the port number by 10000000000000
    