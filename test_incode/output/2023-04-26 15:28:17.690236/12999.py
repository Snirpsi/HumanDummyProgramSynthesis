#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that removes user input and adds a list of words. """    
    
    # Remove user input
    line = input()
    
    # Remove all spaces and new lines
    line = line.strip()
    
    # Remove all punctuation except for letters and numbers
    line = line.translate(str.maketrans('', '', string.punctuation))
    
    # Remove all non-alphanumeric characters
    line = line.translate(str.maketrans('', '', string.digits))
    
    # Remove all non-alphabetic characters
    line = line.translate(str.maketrans('', '', string.ascii_letters))
    
    # Remove all non-numeric characters
    line = line.translate(str.maketrans('', '', string.punctuation))
    
    # Remove all non-alphabetic characters
    line = line.translate(str.maketrans('', '', string.digits))
    
    # Remove all non-numeric characters
    line = line.translate(str.maketrans('', '', string.punctuation))
    
    # Remove all non-alphabetic characters
    line = line.translate(str.maketrans('', '', string.ascii_letters))
    
    # Remove all non-numeric characters
    line = line.translate(str.maketrans('', '', string.punctuation))
    
    # Remove all non-alphabetic characters
    line = line.translate(str.maketrans('', '', string.digits))
    
    # Remove all non-numeric characters
    line = line.translate(str.maketrans('', '', string.punctuation))
    
    # Remove all non-alphabetic characters
    line = line.translate(str.maketrans('', '', string.ascii_letters))
    
    # Remove all non-numeric characters
    line = line.translate(str.maketrans('', '', string.punctuation))
    
    # Remove all non-alphabetic characters
    line = line.translate(str.maketrans('', '', string.digits))
    
    # Remove all non-numeric characters
    line = line.translate(str.maketrans('', '', string.punctuation))
    
    # Remove all non-alphabetic characters
    line = line.translate(str.maketrans('', '', string.ascii_letters))
    
    # Remove all non-numeric characters
    line = line.translate(str.maketrans('', '', string.punctuation))
    
    # Remove all non-alphabetic characters
    line = line.translate(str.maketrans('', '', string.digits))
    
    # Remove all non-numeric characters
    line = line.translate(str.maketrans('', '', string.punctuation))
    
    # Remove all non-alphabetic characters
    line = line.translate(str.maketrans('', '', string.ascii_letters))
    
    # Remove all non-numeric characters
    line = line.translate(str.maketrans('', '', string.p