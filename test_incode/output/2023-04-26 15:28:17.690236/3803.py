#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that opens fruits or prints all ports. """    
    
    # Open the fruit ports
    ports = openfruit.open_ports()
    
    # Print the ports
    for port in ports:
        print(port)
        
    # Close the fruit ports
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    ports.close()
    
    # Close the ports file
    port