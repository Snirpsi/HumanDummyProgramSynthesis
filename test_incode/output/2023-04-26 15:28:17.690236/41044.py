#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that iterates over a list of words or iterates over a port. """    
    
    port = '/dev/ttyUSB0'
    
    # Open the serial port and wait for it to open
    ser = serial.Serial(port, 9600, timeout=1)
    
    # Read the first word from the serial port
    word = ser.readline()
    
    # Close the serial port
    ser.close()
    
    # Print the word we received
    print(word)
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # Close the serial port
    ser.close()
    
    # 