#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that iterates over a list of numbers and enumerates a list of numbers. """    
    
    import sys
    
    # Parse command line arguments
    numbers = sys.argv[1:]
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    # Iterate over numbers and print their indices
    for index, number in enumerate(numbers):
