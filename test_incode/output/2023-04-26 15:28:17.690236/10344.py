#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores a list of words. """    
    words = []
    
    # Read a file and store its contents in an array
    with open('words.txt', 'r') as f:
        for line in f:
            words.append(line)
    
    # Print the contents of the array
    print(words)
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))
    
    # Print the length of the array
    print(len(words))