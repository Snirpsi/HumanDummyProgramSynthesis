#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that stores fruits or adds words. """    
    fruit = input("Enter a fruit name: ")
    
    # Check if fruit already exists
    if fruit in Fruits:
        print("Fruit already exists")
    else:
        # Check if fruit already exists
        if fruit in Fruits:
            print("Fruit already exists")
        else:
            # Add fruit to Fruits
            Fruits[fruit] = input("Enter a word for " + fruit + ": ")
            
            # Print fruit list
            print("Fruit list: ")
            for fruit in Fruits:
                print(fruit + ": " + Fruits[fruit])
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # Print fruit list
            print("\nPress Enter to exit")
            
            # Wait for user input
            
            # 