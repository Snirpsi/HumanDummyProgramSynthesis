#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds all ports and stores user input. """    
    ports = input("Enter the ports separated by comma: ")
    ports = ports.split(",")
    ports = ports[:-1]
    ports = ports.split(":")
    ports = ports[:-1]
    ports = ports.split("-")
    ports = ports[:-1]
    ports = ports.split("+")
    ports = ports[:-1]
    ports = ports.split(":")
    ports = ports[:-1]
    ports = ports.split("-")
    ports = ports[:-1]
    ports = ports.split("+")
    ports = ports[:-1]
    ports = ports.split(":")
    ports = ports[:-1]
    ports = ports.split("-")
    ports = ports[:-1]
    ports = ports.split("+")
    ports = ports[:-1]
    ports = ports.split(":")
    ports = ports[:-1]
    ports = ports.split("-")
    ports = ports[:-1]
    ports = ports.split("+")
    ports = ports[:-1]
    ports = ports.split(":")
    ports = ports[:-1]
    ports = ports.split("-")
    ports = ports[:-1]
    ports = ports.split("+")
    ports = ports[:-1]
    ports = ports.split(":")
    ports = ports[:-1]
    ports = ports.split("-")
    ports = ports[:-1]
    ports = ports.split("+")
    ports = ports[:-1]
    ports = ports.split(":")
    ports = ports[:-1]
    ports = ports.split("-")
    ports = ports[:-1]
    ports = ports.split("+")
    ports = ports[:-1]
    ports = ports.split(":")
    ports = ports[:-1]
    ports = ports.split("-")
    ports = ports[:-1]
    ports = ports.split("+")
    ports = ports[:-1]
    ports = ports.split(":")
    ports = ports[:-1]
    ports = ports.split("-")
    ports = ports[:-1]
    ports = ports.split("+")
    ports = ports[:-1]
    ports = ports.split(":")
    ports = ports[:-1]
    ports = ports.split("-")
    ports = ports[:-1]
    ports = ports.split("+")
    ports = ports[:-1]
    ports = ports.split(":")
    ports = ports[:-1]
    ports = ports.split("-")
    ports = ports[:-1]
    ports = ports.split("+")
    ports = ports[:-1]
    ports = ports.split(":")
    ports = ports[:-1]
    ports = ports.split("-")
    