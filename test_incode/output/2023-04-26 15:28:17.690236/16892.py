#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that enumerates a port or stores a list of words. """    
    
    # Open the port and enumerate its contents
    port = serial.Serial('/dev/ttyACM0', 9600)
    port.flushInput()
    port.flushOutput()
    port.close()
    
    # Print the port contents
    words = port.readlines()
    
    # Print the port contents as a list
    print(words)
    
    # Close the port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.close()
    
    # Close the serial port
    port.