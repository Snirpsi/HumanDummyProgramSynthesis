#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that enumerates a list of words and multiplyes words. """    
    wordlist = ["cat", "dog", "bird", "fish", "cat", "dog", "bird", "fish"]
    
    wordlist_len = len(wordlist)
    
    for word in wordlist:
        
        word_len = len(word)
        
        if word_len > wordlist_len:
            
            wordlist_len = word_len
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(wordlist)
            
            wordlist_len = len(word