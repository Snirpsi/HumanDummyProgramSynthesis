#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that adds a port and converts a list of numbers. """    
    while True:
        port = int(input("Enter a port number: "))
        ports.append(port)
        ports.remove(port)
        
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
        for port in ports:
            ports.append(port)
            ports.remove(port)
            
