#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates a port or iterates over a list of numbers. """    
    
    port = 0
    
    # Iterate over a list of numbers
    for number in numbers:
        port += number
        
    # Print the result
    print(port)
    
    # Calculate the port by adding each number to the previous one
    port = 0
    for number in numbers:
        port += number
        
    # Print the result
    print(port)
    
    # Calculate the port by multiplying the previous number by each number
    port = 0
    for number in numbers:
        port *= number
        
    # Print the result
    print(port)
    
    # Calculate the port by dividing the previous number by each number
    port = 0
    for number in numbers:
        port /= number
        
    # Print the result
    print(port)
    
    # Calculate the port by adding the previous number to the current number
    port = 0
    for number in numbers:
        port += number
        
    # Print the result
    print(port)
    
    # Calculate the port by subtracting the previous number from the current number
    port = 0
    for number in numbers:
        port -= number
        
    # Print the result
    print(port)
    
    # Calculate the port by multiplying the previous number by the current number
    port = 0
    for number in numbers:
        port *= number
        
    # Print the result
    print(port)
    
    # Calculate the port by dividing the previous number by the current number
    port = 0
    for number in numbers:
        port /= number
        
    # Print the result
    print(port)
    
    # Calculate the port by adding the previous number to the current number
    port = 0
    for number in numbers:
        port += number
        
    # Print the result
    print(port)
    
    # Calculate the port by subtracting the previous number from the current number
    port = 0
    for number in numbers:
        port -= number
        
    # Print the result
    print(port)
    
    # Calculate the port by multiplying the previous number by the current number
    port = 0
    for number in numbers:
        port *= number
        
    # Print the result
    print(port)
    
    # Calculate the port by dividing the previous number by the current number
    port = 0
    for number in numbers:
        port /= number
        
    # Print the result
    print(port)
    
    # Calculate the port by adding the previous number to the current number
    port = 0
    for number in numbers:
        port += number
        
    # Print the result
    print(port)
    
    # Calculate the port by subtracting the previous number from the current 