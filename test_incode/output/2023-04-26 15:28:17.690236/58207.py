#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates all ports. """    
    
    # Get the ports 
    ports = find_ports()
    
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print the ports 
    for port in ports:
        print(port)
        
    # Print 