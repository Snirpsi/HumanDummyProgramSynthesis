#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens all ports and multiplyes user input. """    
    
    ports = open("/dev/ttyACM0","r").read().splitlines()
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in ports]
    
    ports = [p*2 for p in ports]
    
    ports = [int(p) for p in port