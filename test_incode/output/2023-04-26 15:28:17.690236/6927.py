#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that calculates fruits or calculates a port. """    
    
    port = int(sys.argv[1])
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalculator)
    
    # start a web server
    httpd = HTTPServer(('', port), FruitCalcul