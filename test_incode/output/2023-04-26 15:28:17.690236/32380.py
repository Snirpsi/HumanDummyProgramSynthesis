#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates a list of words or converts user input. """    
    
    words = []
    
    while True:
        word = input('Enter a word: ')
        
        if word == '':
            break
        
        words.append(word)
    
    print('The words are:')
    for word in words:
        print(word)
    
    print('The average word length is {}'.format(
        sum(map(len, words)) / float(len(words))))
    
    print('The longest word is {}'.format(
        max(map(len, words))))
    
    print('The shortest word is {}'.format(
        min(map(len, words))))
    
    print('The average word length is {}'.format(
        sum(map(len, words)) / float(len(words))))
    
    print('The longest word is {}'.format(
        max(map(len, words))))
    
    print('The shortest word is {}'.format(
        min(map(len, words))))
    
    print('The average word length is {}'.format(
        sum(map(len, words)) / float(len(words))))
    
    print('The longest word is {}'.format(
        max(map(len, words))))
    
    print('The shortest word is {}'.format(
        min(map(len, words))))
    
    print('The average word length is {}'.format(
        sum(map(len, words)) / float(len(words))))
    
    print('The longest word is {}'.format(
        max(map(len, words))))
    
    print('The shortest word is {}'.format(
        min(map(len, words))))
    
    print('The average word length is {}'.format(
        sum(map(len, words)) / float(len(words))))
    
    print('The longest word is {}'.format(
        max(map(len, words))))
    
    print('The shortest word is {}'.format(
        min(map(len, words))))
    
    print('The average word length is {}'.format(
        sum(map(len, words)) / float(len(words))))
    
    print('The longest word is {}'.format(
        max(map(len, words))))
    
    print('The shortest word is {}'.format(
        min(map(len, words))))
    
    print('The average word length is {}'.format(
        sum(map(len, words)) / float(len(words))))
    
    print('The longest word is {}'.format(
        max(map(len, words))))
    
    print('The shortest word is {}'.format(
        min(map(len, words))))
    
    print('The average word length is {}'.format(
        sum(map(len, words)) / float(len(words))))
    
    print('The longest word is {}'.format(
        max(map(len, words))))
    
    print('The shortest word is {}'.format(
        min(map(len, words))))
    
    print('The average 