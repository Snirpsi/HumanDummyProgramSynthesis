#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds fruits and returns user input. """    
    fruits = add_fruits()
    
    # Print the fruits that have been added.
    print("The fruits that have been added are:")
    for fruit in fruits:
        print(fruit)
    
    # Get the user input from the user
    choice = input("Do you want to add another fruit? y/n: ")
    
    # If the user inputs "y", then add another fruit.
    if choice == "y":
        fruits = add_fruits()
    
    # Print the fruits that have been added.
    print("The fruits that have been added are:")
    for fruit in fruits:
        print(fruit)
    
    # Get the user input from the user
    choice = input("Do you want to add another fruit? y/n: ")
    
    # If the user inputs "y", then add another fruit.
    if choice == "y":
        fruits = add_fruits()
    
    # Print the fruits that have been added.
    print("The fruits that have been added are:")
    for fruit in fruits:
        print(fruit)
    
    # Get the user input from the user
    choice = input("Do you want to add another fruit? y/n: ")
    
    # If the user inputs "y", then add another fruit.
    if choice == "y":
        fruits = add_fruits()
    
    # Print the fruits that have been added.
    print("The fruits that have been added are:")
    for fruit in fruits:
        print(fruit)
    
    # Get the user input from the user
    choice = input("Do you want to add another fruit? y/n: ")
    
    # If the user inputs "y", then add another fruit.
    if choice == "y":
        fruits = add_fruits()
    
    # Print the fruits that have been added.
    print("The fruits that have been added are:")
    for fruit in fruits:
        print(fruit)
    
    # Get the user input from the user
    choice = input("Do you want to add another fruit? y/n: ")
    
    # If the user inputs "y", then add another fruit.
    if choice == "y":
        fruits = add_fruits()
    
    # Print the fruits that have been added.
    print("The fruits that have been added are:")
    for fruit in fruits:
        print(fruit)
    
    # Get the user input from the user
    choice = input("Do you want to add another fruit? y/n: ")
    
    # If the user inputs "y", then add another fruit.
    if choice == "y":
        fruits = add_fruits()
    
    # Print the fruits that have been added.
    print("The fruits that have been added are:")
    for fruit in fruits:
        print(fruit)
