#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that iterates over all ports or prints a list of words. """    
    import sys
    
    # iterate over all ports
    for port in sys.argv[1:]:
        print("Port " + port)
    
    # iterate over all words and print them
    for word in sys.argv[1:]:
        print(word)
    
    # iterate over all words and print them with indentation
    for word in sys.argv[1:]:
        print(word + "\t")
    
    # iterate over all words and print them with indentation
    for word in sys.argv[1:]:
        print(word + "\t" + word + "\t")
    
    # iterate over all words and print them with indentation
    for word in sys.argv[1:]:
        print(word + "\t" + word + "\t" + word + "\t")
    
    # iterate over all words and print them with indentation
    for word in sys.argv[1:]:
        print(word + "\t" + word + "\t" + word + "\t" + word + "\t")
    
    # iterate over all words and print them with indentation
    for word in sys.argv[1:]:
        print(word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t")
    
    # iterate over all words and print them with indentation
    for word in sys.argv[1:]:
        print(word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t")
    
    # iterate over all words and print them with indentation
    for word in sys.argv[1:]:
        print(word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t")
    
    # iterate over all words and print them with indentation
    for word in sys.argv[1:]:
        print(word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t")
    
    # iterate over all words and print them with indentation
    for word in sys.argv[1:]:
        print(word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t")
    
    # iterate over all words and print them with indentation
    for word in sys.argv[1:]:
        print(word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + word + "\t" + 