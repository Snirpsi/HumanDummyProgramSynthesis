#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts user input and converts all ports. """    
    ports = input("Enter the ports separated by comma: ").split(',')
    ports = [int(p) for p in ports]
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(ports))
    ports.sort()
    ports = map(lambda x: x if x >= 0 and x <= 65535 else 65535, ports)
    ports = list(set(port