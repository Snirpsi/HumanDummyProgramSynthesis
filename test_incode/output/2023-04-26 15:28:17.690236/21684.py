#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that multiplyes a list of words or converts all ports. """    
    ports = ['COM1', 'COM2', 'COM3']
    
    ports_str = ''
    for port in ports:
        ports_str += str(port) + ','
    
    ports_str = ports_str[:-1]
    
    ports_str = ports_str.strip()
    
    ports_str = ports_str.split(',')
    
    ports_str = [int(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in ports_str]
    
    ports_str = [str(port) for port in 