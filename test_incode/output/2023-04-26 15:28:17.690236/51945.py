#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates numbers or opens a list of numbers. """    
    
    # Read input from stdin
    numbers = sys.stdin.read().split()
    
    # Calculate numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
    
    # Open numbers
    numbers = [int(n) for n in numbers]
    
    # Print numbers
    print(numbers)
