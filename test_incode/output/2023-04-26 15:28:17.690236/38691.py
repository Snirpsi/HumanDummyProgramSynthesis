#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts a list of numbers or prints a list of numbers. """    
    
    # Read input from stdin
    import sys
    
    numbers = []
    for line in sys.stdin:
        numbers.append(int(line))
    
    # Print the list
    print(numbers)
    
    # Write output to stdout
    sys.stdout.write('\n'.join(map(str, numbers)))
    
    # Close stdin
    sys.stdin.close()
    
    # Close stdout
    sys.stdout.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close the file
    file.close()
    
    # Close 