#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that adds all ports or multiplyes fruits. """    
    ports = [8080, 8081, 8082, 8083, 8084, 8085]
    ports = [int(port) for port in ports]
    ports = ports * 2
    ports = [str(port) for port in ports]
    ports = ','.join(ports)
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',', ':')
    ports = ports.replace(':', ':')
    ports = ports.replace(' ', ':')
    ports = ports.replace(',