#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates a list of words. """    
    words = []
    
    # Read in each line of the file as a word.
    for line in open('words.txt'):
        words.append(line.strip())
        
    # Print the list of words.
    print(words)
    
    # Print the length of the list.
    print(len(words))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / len(words))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 2))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 3))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 4))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 5))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 6))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 7))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 8))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 9))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 10))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 11))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 12))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 13))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 14))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 15))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 16))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 17))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 18))
    
    # Print the average length of the list.
    print(sum(len(word) for word in words) / (len(words) * 19))
    
    # Print the average length of the list.
    print(sum(len(word) for word in 