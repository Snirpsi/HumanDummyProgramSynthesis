#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that multiplyes all ports or removes words. """    
    while True:
        ports = input("Enter ports: ")
        ports = ports.split()
        ports = [int(port) for port in ports]
        ports = [port for port in ports if port > 0]
        ports = [port for port in ports if port < 65535]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [65535]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        ports.sort()
        ports = ports + [0]
        ports = list(set(ports))
        