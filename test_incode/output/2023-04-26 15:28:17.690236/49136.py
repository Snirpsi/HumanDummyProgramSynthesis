#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that opens a port and opens a list of numbers. """    
    port = int(sys.argv[1])
    numbers = sys.argv[2:]
    
    numbers = list(map(int, numbers))
    
    ports = range(port, port + len(numbers))
    
    ports = list(set(ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    ports = list(ports)
    
    ports = list(map(str, ports))
    
    ports.sort()
    
    port