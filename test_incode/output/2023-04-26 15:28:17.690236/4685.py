#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that opens numbers and opens a list of numbers. """    
    numbers = range(1,10)
    numbers_open = numbers.copy()
    
    while True:
        numbers_open = numbers.copy()
        
        for number in numbers_open:
            print(number)
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()
        
        if numbers_open == numbers:
            break
            
        numbers_open = numbers.copy()