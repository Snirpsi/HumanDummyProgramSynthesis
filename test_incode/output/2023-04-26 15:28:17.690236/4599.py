#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that multiplyes a list of words or calculates numbers. """    
    
    import sys
    
    if len(sys.argv) != 2:
        print("Usage: %s <words>" % sys.argv[0])
        sys.exit()
    
    words = sys.argv[1].split()
    
    if len(words) == 0:
        print("Usage: %s <words>" % sys.argv[0])
        sys.exit()
    
    numbers = map(int, words)
    
    numbers = list(numbers)
    
    numbers = map(lambda n: n * 10, numbers)
    
    numbers = list(map(str, numbers))
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    numbers = map(lambda n: str(n), numbers)
    
    number