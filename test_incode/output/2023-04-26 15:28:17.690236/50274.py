#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that multiplyes words and iterates over numbers. """    
    import sys
    words = []
    for line in sys.stdin:
        line = line.strip()
        if line:
            words.append(line)
    
    words = list(set(words))
    
    numbers = []
    for line in sys.stdin:
        line = line.strip()
        if line:
            numbers.append(line)
    
    numbers = list(set(numbers))
    
    numbers_set = set(numbers)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = numbers_set_len - 1
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_set_len = len(numbers_set)
    
    numbers_