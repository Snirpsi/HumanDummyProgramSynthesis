#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that removes all ports and stores numbers. """    
    port = int(sys.argv[1])
    ports = [port for port in range(10000, 65535)]
    ports.remove(port)
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(65536, 65536 + len(ports)))
    ports.sort()
    ports.reverse()
    ports.insert(0, port)
    ports = ports + list(range(