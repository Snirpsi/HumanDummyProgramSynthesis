#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that enumerates all ports. """    
    
    ports = []
    
    # Enumerate all available ports
    for port in ports.__iter__():
        print(port)
    
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
    # Enumerate all available ports except for port 0
    for port in ports.__iter__():
        if port != 0: 
            print(port)
            
