#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens a list of words. """    
    words = []
    
    # Open the wordlist
    with open('wordlist.txt', 'r') as f:
        words = f.read().splitlines()
        
    # Print the wordlist
    print('\n'.join(words))
    
    # Print the wordlist length
    print(len(words))
    
    # Print the wordlist length in characters
    print(len(words[0]))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in words
    print(len(words))
    
    # Print the wordlist length in 