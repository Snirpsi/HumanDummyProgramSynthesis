#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that removes numbers. """    
    
    # Remove all numbers from stdin
    numbers = map(int, sys.stdin.readlines())
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    numbers = filter(lambda x: x is not None, numbers)
    
    # Remove all numbers from stdin
    number