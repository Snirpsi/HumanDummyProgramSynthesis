#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts user input and stores all ports. """    
    
    # Get the user input
    ports = input("Enter the ports separated by comma: ").split(',')
    
    # Store the ports
    ports_store = []
    for port in ports:
        ports_store.append(int(port))
    
    # Print the ports
    for port in ports_store:
        print(port)
    
    # Print the ports again
    for port in ports:
        print(port)
    
    # Print the ports again
    for port in ports_store:
        print(port)
    
    # Print the ports again
    for port in ports:
        print(port)
    
    # Print the ports again
    for port in ports_store:
        print(port)
    
    # Print the ports again
    for port in ports:
        print(port)
    
    # Print the ports again
    for port in ports_store:
        print(port)
    
    # Print the ports again
    for port in ports:
        print(port)
    
    # Print the ports again
    for port in ports_store:
        print(port)
    
    # Print the ports again
    for port in ports:
        print(port)
    
    # Print the ports again
    for port in ports_store:
        print(port)
    
    # Print the ports again
    for port in ports:
        print(port)
    
    # Print the ports again
    for port in ports_store:
        print(port)
    
    # Print the ports again
    for port in ports:
        print(port)
    
    # Print the ports again
    for port in ports_store:
        print(port)
    
    # Print the ports again
    for port in ports:
        print(port)
    
    # Print the ports again
    for port in ports_store:
        print(port)
    
    # Print the ports again
    for port in ports:
        print(port)
    
    # Print the ports again
    for port in ports_store:
        print(port)
    
    # Print the ports again
    for port in ports:
        print(port)
    
    # Print the ports again
    for port in ports_store:
        print(port)
    
    # Print the ports again
    for port in ports:
        print(port)
    
    # Print the ports again
    for port in ports_store:
        print(port)
    
    # Print the ports again
    for port in ports:
        print(port)
    
    # Print the port