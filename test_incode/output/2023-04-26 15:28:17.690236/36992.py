#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores a list of words. """    
    words = []
    
    # Read words from file
    with open('words.txt', 'r') as f:
        for line in f:
            line = line.strip()
            words.append(line)
    
    # Print the words in order
    print('\n'.join(words))
    
    # Print the length of the longest word
    print(len(max(words, key=len)))
    
    # Print the length of the shortest word
    print(len(min(words, key=len)))
    
    # Print the length of the average word
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(len, words)) / len(words))
    
    # Print the average word length
    print(sum(map(