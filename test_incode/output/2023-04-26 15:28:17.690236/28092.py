#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that removes a list of words and adds a port. """    
    while True:
        words = []
        ports = []
        
        words = input("Enter a list of words: ").split()
        
        ports = input("Enter a list of ports: ").split()
        
        ports = list(set(ports))
        
        ports.sort()
        
        for port in ports:
            if port not in words:
                words.append(port)
        
        print("Words: ", words)
        print("Ports: ", ports)
        
        words = input("Enter a list of words: ").split()
        
        ports = input("Enter a list of ports: ").split()
        
        ports = list(set(ports))
        
        ports.sort()
        
        for port in ports:
            if port not in words:
                words.append(port)
        
        print("Words: ", words)
        print("Ports: ", ports)
        
        words = input("Enter a list of words: ").split()
        
        ports = input("Enter a list of ports: ").split()
        
        ports = list(set(ports))
        
        ports.sort()
        
        for port in ports:
            if port not in words:
                words.append(port)
        
        print("Words: ", words)
        print("Ports: ", ports)
        
        words = input("Enter a list of words: ").split()
        
        ports = input("Enter a list of ports: ").split()
        
        ports = list(set(ports))
        
        ports.sort()
        
        for port in ports:
            if port not in words:
                words.append(port)
        
        print("Words: ", words)
        print("Ports: ", ports)
        
        words = input("Enter a list of words: ").split()
        
        ports = input("Enter a list of ports: ").split()
        
        ports = list(set(ports))
        
        ports.sort()
        
        for port in ports:
            if port not in words:
                words.append(port)
        
        print("Words: ", words)
        print("Ports: ", ports)
        
        words = input("Enter a list of words: ").split()
        
        ports = input("Enter a list of ports: ").split()
        
        ports = list(set(ports))
        
        ports.sort()
        
        for port in ports:
            if port not in words:
                words.append(port)
        
        print("Words: ", words)
        print("Ports: ", ports)
        
        words = input("Enter a list of words: ").split()
        
        ports = input("Enter a list of ports: ").split()
        
        ports = list(set(ports))
        
        ports.sort()
        
        for port in ports:
            if port not in words:
                words.append(port)
        
        print("Words: 