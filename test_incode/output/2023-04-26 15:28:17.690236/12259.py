#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that returns a port or stores a list of words. """    
    port = []
    words = []
    
    # Read a line from stdin
    line = sys.stdin.readline()
    
    # Split it into words
    words = line.split()
    
    # Add each word to a port
    for word in words:
        port.append(word)
    
    # Print the port
    for word in port:
        print(word)
        
    # Print the words
    for word in words:
        print(word)
        
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()
    
    # Close the port
    words.close()
    
    # Close the port
    port.close()