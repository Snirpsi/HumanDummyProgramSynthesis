#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores a port or iterates over all ports. """    
    
    ports = []
    
    # Iterate over all ports
    for port in ports:
        
        # Store the port
        port.store()
        
        # Print the port
        print(port)
        
        # Stop the port
        port.stop()
        
    # Stop the program
    ports[0].stop()
    
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # Print the ports
    for port in ports:
        print(port)
        
    # 