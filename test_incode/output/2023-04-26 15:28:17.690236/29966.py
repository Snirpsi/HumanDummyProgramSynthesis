#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that enumerates a list of words or adds a list of numbers. """    
    
    words = ['word1', 'word2', 'word3', 'word4']
    numbers = [1, 2, 3, 4]
    
    # enumerate words and numbers
    for index, word in enumerate(words):
        print('{}. {}'.format(index, word))
    for index, number in enumerate(numbers):
        print('{}. {}'.format(index, number))
    
    # add words and numbers together
    words.extend(numbers)
    
    # enumerate words and numbers
    for index, word in enumerate(words):
        print('{}. {}'.format(index, word))
    for index, number in enumerate(numbers):
        print('{}. {}'.format(index, number))
    
    # add words and numbers together
    words.extend(numbers)
    
    # enumerate words and numbers
    for index, word in enumerate(words):
        print('{}. {}'.format(index, word))
    for index, number in enumerate(numbers):
        print('{}. {}'.format(index, number))
    
    # add words and numbers together
    words.extend(numbers)
    
    # enumerate words and numbers
    for index, word in enumerate(words):
        print('{}. {}'.format(index, word))
    for index, number in enumerate(numbers):
        print('{}. {}'.format(index, number))
    
    # add words and numbers together
    words.extend(numbers)
    
    # enumerate words and numbers
    for index, word in enumerate(words):
        print('{}. {}'.format(index, word))
    for index, number in enumerate(numbers):
        print('{}. {}'.format(index, number))
    
    # add words and numbers together
    words.extend(numbers)
    
    # enumerate words and numbers
    for index, word in enumerate(words):
        print('{}. {}'.format(index, word))
    for index, number in enumerate(numbers):
        print('{}. {}'.format(index, number))
    
    # add words and numbers together
    words.extend(numbers)
    
    # enumerate words and numbers
    for index, word in enumerate(words):
        print('{}. {}'.format(index, word))
    for index, number in enumerate(numbers):
        print('{}. {}'.format(index, number))
    
    # add words and numbers together
    words.extend(numbers)
    
    # enumerate words and numbers
    for index, word in enumerate(words):
        print('{}. {}'.format(index, word))
    for index, number in enumerate(numbers):
        print('{}. {}'.format(index, number))
    
    # add words and numbers together
    words.extend(numbers)
    
    # enumerate words and numbers
    for index, word in enumerate(words):
        print('{}. {}'.format(index, word))
    for index, number in enumerate(numbers):
        print('{}. {}'.format(index, number))
    
    # 