#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that opens a port or calculates fruits. """    
    
    # Open the port and get fruit names
    fruit = Fruit()
    fruit.open_port()
    
    # Print the fruit names
    print(fruit.fruits)
    
    # Close the port
    fruit.close_port()
    
    # Close the program
    fruit.close_program()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread
    fruit.close_thread()
    
    # Close the socket
    fruit.close_socket()
    
    # Close the thread