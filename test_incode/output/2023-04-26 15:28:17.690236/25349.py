#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates words and stores all ports. """    
    
    words = []
    
    ports = open('ports.txt', 'r')
    
    for line in ports:
        line = line.strip()
        
        if len(line) > 0:
            words.append(line)
    
    ports.close()
    
    ports = open('ports.txt', 'w')
    
    for word in words:
        ports.write(word + '\n')
    
    ports.close()
    
    ports = open('ports.txt', 'r')
    
    for line in ports:
        line = line.strip()
        
        if len(line) > 0:
            ports.write(line + '\n')
    
    ports.close()
    
    ports = open('ports.txt', 'w')
    
    for word in words:
        ports.write(word + '\n')
    
    ports.close()
    
    ports = open('ports.txt', 'r')
    
    for line in ports:
        line = line.strip()
        
        if len(line) > 0:
            ports.write(line + '\n')
    
    ports.close()
    
    ports = open('ports.txt', 'r')
    
    for line in ports:
        line = line.strip()
        
        if len(line) > 0:
            ports.write(line + '\n')
    
    ports.close()
    
    ports = open('ports.txt', 'r')
    
    for line in ports:
        line = line.strip()
        
        if len(line) > 0:
            ports.write(line + '\n')
    
    ports.close()
    
    ports = open('ports.txt', 'r')
    
    for line in ports:
        line = line.strip()
        
        if len(line) > 0:
            ports.write(line + '\n')
    
    ports.close()
    
    ports = open('ports.txt', 'r')
    
    for line in ports:
        line = line.strip()
        
        if len(line) > 0:
            ports.write(line + '\n')
    
    ports.close()
    
    ports = open('ports.txt', 'r')
    
    for line in ports:
        line = line.strip()
        
        if len(line) > 0:
            ports.write(line + '\n')
    
    ports.close()
    
    ports = open('ports.txt', 'r')
    
    for line in ports:
        line = line.strip()
        
        if len(line) > 0:
            ports.write(line + '\n')
    
    ports.close()
    
    ports = open('ports.txt', 'r')
    
    for line in ports:
        line = line.strip()
        
        if len(line) > 0:
            ports.write(line + '\n')
    
    ports.close()
    
    ports = open('ports.txt', 'r')
    
    for line in ports:
        line = line.strip()
        
        if len(