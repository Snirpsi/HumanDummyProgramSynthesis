#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts words or iterates over user input. """    
    
    import sys
    
    if len(sys.argv) > 1:
        word = sys.argv[1]
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        word = word.lower()
        
        if word == 'quit':
            sys.exit(0)
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word in ['help', 'quit']:
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word == 'quit':
            sys.exit(0)
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word == 'quit':
            sys.exit(0)
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word == 'quit':
            sys.exit(0)
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word == 'quit':
            sys.exit(0)
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word == 'quit':
            sys.exit(0)
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word == 'quit':
            sys.exit(0)
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word == 'quit':
            sys.exit(0)
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word == 'quit':
            sys.exit(0)
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word == 'quit':
            sys.exit(0)
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word == 'quit':
            sys.exit(0)
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word == 'quit':
            sys.exit(0)
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word == 'quit':
            sys.exit(0)
        
        if word == 'help':
            print('Usage: %s [word]' % sys.argv[0])
            sys.exit(0)
        
        if word == 'quit':
