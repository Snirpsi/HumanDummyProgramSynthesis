#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that converts all ports or converts a list of numbers. """    
    ports = [int(port) for port in sys.argv[1:]]
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in {0, 65535} else 65535, ports))
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in {0, 65535} else 65535, ports))
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in {0, 65535} else 65535, ports))
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in {0, 65535} else 65535, ports))
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in {0, 65535} else 65535, ports))
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in {0, 65535} else 65535, ports))
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in {0, 65535} else 65535, ports))
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in {0, 65535} else 65535, ports))
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in {0, 65535} else 65535, ports))
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in {0, 65535} else 65535, ports))
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in {0, 65535} else 65535, ports))
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in {0, 65535} else 65535, ports))
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in {0, 65535} else 65535, ports))
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in {0, 65535} else 65535, ports))
    
    if len(ports) == 1:
        ports = ports[0]
    
    ports = list(map(lambda x: x if x not in 