#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens a list of words. """    
    
    # Open the file
    with open('words.txt', 'r') as f:
        words = f.read().splitlines()
    
    # Print the words to the screen
    for word in words:
        print(word)
    
    # Close the file
    f.close()
    
    # Print the total number of words
    print('Total number of words: {}'.format(len(words)))
    
    # Close the file
    f.close()
    
    # Print the total number of lines
    print('Total number of lines: {}'.format(len(lines)))
    
    # Close the file
    f.close()
    
    # Print the total number of words in each line
    for count, line in enumerate(lines):
        print('Line {}: {}'.format(count, line))
    
    # Close the file
    f.close()
    
    # Print the total number of words in each line
    for count, line in enumerate(lines):
        print('Line {}: {}'.format(count, line))
    
    # Close the file
    f.close()
    
    # Print the total number of words in each line
    for count, line in enumerate(lines):
        print('Line {}: {}'.format(count, line))
    
    # Close the file
    f.close()
    
    # Print the total number of words in each line
    for count, line in enumerate(lines):
        print('Line {}: {}'.format(count, line))
    
    # Close the file
    f.close()
    
    # Print the total number of words in each line
    for count, line in enumerate(lines):
        print('Line {}: {}'.format(count, line))
    
    # Close the file
    f.close()
    
    # Print the total number of words in each line
    for count, line in enumerate(lines):
        print('Line {}: {}'.format(count, line))
    
    # Close the file
    f.close()
    
    # Print the total number of words in each line
    for count, line in enumerate(lines):
        print('Line {}: {}'.format(count, line))
    
    # Close the file
    f.close()
    
    # Print the total number of words in each line
    for count, line in enumerate(lines):
        print('Line {}: {}'.format(count, line))
    
    # Close the file
    f.close()
    
    # Print the total number of words in each line
    for count, line in enumerate(lines):
        print('Line {}: {}'.format(count, line))
    
    # Close the file
    f.close()
    
    # Print the total number of words in each line
    for count, line in enumerate(lines):
        print('Line {}: {}'.format(count, line))
    
    # Close the file
    f.close()
    
    # Print the total number of words in each line
    for count, line in enumerate(lines):
        print('Line {}: {}'.format(count, line))
    
    # Close the file
    f.close()
    
    # Print the total number of words in each line
    for count, line in enumerate(lines):
        print('Line {}: {}'.format(count, line))
    
    # Close the file
    f.close()
    