#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that removes user input and stores all ports. """    
    ports = remove_user_ports()
    
    # Store the ports back into the file
    ports_file = open('ports.txt', 'w')
    ports_file.write(ports)
    ports_file.close()
    
    # Print the ports back out
    print('\nPorts saved to ports.txt')
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
    # Close the ports file
    ports_file.close()
    
