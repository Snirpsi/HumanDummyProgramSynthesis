#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that opens a list of words. """    
    words = []
    
    # Open the file containing the words and read it line by line.
    for line in open('words.txt'):
        line = line.strip()
        words.append(line)
    
    # Print the words in the file to the screen.
    print('\n'.join(words))
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    
    # Close the file.
    file.close()
    