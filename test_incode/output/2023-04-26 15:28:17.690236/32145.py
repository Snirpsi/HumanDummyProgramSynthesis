#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that opens a list of numbers or adds a list of words. """    
    
    # Open the port number from the command line
    port = int(sys.argv[1])
    
    # Open the port number from the environment variables
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(os.environ.get('PORT', 8080))
    
    # Open the port number from the configuration file
    port = int(