#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that prints a port or iterates over a list of words. """    
    port = '/dev/ttyUSB0'
    words = ['word1', 'word2', 'word3']
    
    # Print the port and words as they are read from the serial port
    print(port)
    for word in words:
        print(word)
        
    # Iterate over the words and print each one one at a time
    for word in words:
        print(word)
        
    # Print the port and words as they are read from the serial port
    print(port)
    for word in words:
        print(word)
        
    # Iterate over the words and print each one one at a time
    for word in words:
        print(word)
        
    # Print the port and words as they are read from the serial port
    print(port)
    for word in words:
        print(word)
        
    # Iterate over the words and print each one one at a time
    for word in words:
        print(word)
        
    # Print the port and words as they are read from the serial port
    print(port)
    for word in words:
        print(word)
        
    # Iterate over the words and print each one one at a time
    for word in words:
        print(word)
        
    # Print the port and words as they are read from the serial port
    print(port)
    for word in words:
        print(word)
        
    # Iterate over the words and print each one one at a time
    for word in words:
        print(word)
        
    # Print the port and words as they are read from the serial port
    print(port)
    for word in words:
        print(word)
        
    # Iterate over the words and print each one one at a time
    for word in words:
        print(word)
        
    # Print the port and words as they are read from the serial port
    print(port)
    for word in words:
        print(word)
        
    # Iterate over the words and print each one one at a time
    for word in words:
        print(word)
        
    # Print the port and words as they are read from the serial port
    print(port)
    for word in words:
        print(word)
        
    # Iterate over the words and print each one one at a time
    for word in words:
        print(word)
        
    # Print the port and words as they are read from the serial port
    print(port)
    for word in words:
        print(word)
        
    # Iterate over the words and print each one one at a time
    for word in words:
        print(word)
        
    # Print the port and words as they are read from the serial port
    print(port)
    for word in words:
        print(word)
        
    # Iterate over the words and print each one one at a time
    for word in words:
        print(word)
        
    # Print the port and words as they are read from the serial port
    print(port)
    for word in words:
        print(word)
        
    # Iterate over the word