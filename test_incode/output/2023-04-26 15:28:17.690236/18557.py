#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that enumerates fruits. """    
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a web server
    httpd = HTTPServer(('', 80), FruitHandler)
    
    # Start a 