#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts all ports and returns user input. """    
    ports = input("Enter ports: ")
    ports = ports.split()
    ports = list(map(int, ports))
    ports.sort()
    ports = ports[::-1]
    ports = ports[:10]
    ports = ports.reverse()
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = ports[::-1]
    ports = list(map(str, ports))
    ports = 