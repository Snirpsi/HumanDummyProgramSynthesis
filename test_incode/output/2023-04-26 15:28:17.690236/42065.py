#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that removes fruits or iterates over a port. """    
    port = '/dev/ttyACM0'
    
    # Open the port and read its contents.
    with serial.Serial(port, 9600) as ser:
        
        # Read the first line of the port.
        line = ser.readline()
        
        # Print the line to the screen.
        print(line)
        
        # Close the port.
        ser.close
        
    # Close the serial port.
    ser.close()
    
    # Print the line to the screen.
    print(line)
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
    ser.close()
    
    # Close the serial port.
