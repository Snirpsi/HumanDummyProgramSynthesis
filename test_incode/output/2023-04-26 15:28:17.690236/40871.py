#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that enumerates fruits or iterates over a list of words. """    
    
    fruits = ['apple', 'banana', 'cherry']
    
    # Iterate over fruits using enumerate and print each fruit and its index
    for index, fruit in enumerate(fruits):
        print('Fruit #{}: {}'.format(fruit, index))
    
    # Iterate over fruits using for loop and print each fruit and its index
    for fruit in fruits:
        print('Fruit #{}: {}'.format(fruit, index))
    
    # Iterate over fruits using while loop and print each fruit and its index
    while True:
        fruit = input('Enter a fruit: ')
        if fruit == 'quit':
            break
        print('Fruit #{}: {}'.format(fruit, index))
    
    # Iterate over fruits using for loop and print each fruit and its index
    for index, fruit in enumerate(fruits):
        print('Fruit #{}: {}'.format(fruit, index))
    
    # Iterate over fruits using while loop and print each fruit and its index
    while True:
        fruit = input('Enter a fruit: ')
        if fruit == 'quit':
            break
        print('Fruit #{}: {}'.format(fruit, index))
    
    # Iterate over fruits using for loop and print each fruit and its index
    for index, fruit in enumerate(fruits):
        print('Fruit #{}: {}'.format(fruit, index))
    
    # Iterate over fruits using while loop and print each fruit and its index
    while True:
        fruit = input('Enter a fruit: ')
        if fruit == 'quit':
            break
        print('Fruit #{}: {}'.format(fruit, index))
    
    # Iterate over fruits using for loop and print each fruit and its index
    for index, fruit in enumerate(fruits):
        print('Fruit #{}: {}'.format(fruit, index))
    
    # Iterate over fruits using while loop and print each fruit and its index
    while True:
        fruit = input('Enter a fruit: ')
        if fruit == 'quit':
            break
        print('Fruit #{}: {}'.format(fruit, index))
    
    # Iterate over fruits using for loop and print each fruit and its index
    for index, fruit in enumerate(fruits):
        print('Fruit #{}: {}'.format(fruit, index))
    
    # Iterate over fruits using while loop and print each fruit and its index
    while True:
        fruit = input('Enter a fruit: ')
        if fruit == 'quit':
            break