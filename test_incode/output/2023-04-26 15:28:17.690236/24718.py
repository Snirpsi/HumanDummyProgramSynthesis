#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that opens words or calculates words. """    
    while True:
        words = []
        
        # Open the word file
        with open('words.txt', 'r') as file:
            words = file.read().splitlines()
            
        # Calculate how many words are in the file
        wordCount = len(words)
        
        # Print the word count
        print('There are {} words in the file.'.format(wordCount))
        
        # Print the words in the file
        for word in words:
            print(word)
            
        # Calculate how many words are in the file
        wordCount = len(words)
        
        # Print the word count
        print('There are {} words in the file.'.format(wordCount))
        
        # Print the words in the file
        for word in words:
            print(word)
            
        # Calculate how many words are in the file
        wordCount = len(words)
        
        # Print the word count
        print('There are {} words in the file.'.format(wordCount))
        
        # Print the words in the file
        for word in words:
            print(word)
            
        # Calculate how many words are in the file
        wordCount = len(words)
        
        # Print the word count
        print('There are {} words in the file.'.format(wordCount))
        
        # Print the words in the file
        for word in words:
            print(word)
            
        # Calculate how many words are in the file
        wordCount = len(words)
        
        # Print the word count
        print('There are {} words in the file.'.format(wordCount))
        
        # Print the words in the file
        for word in words:
            print(word)
            
        # Calculate how many words are in the file
        wordCount = len(words)
        
        # Print the word count
        print('There are {} words in the file.'.format(wordCount))
        
        # Print the words in the file
        for word in words:
            print(word)
            
        # Calculate how many words are in the file
        wordCount = len(words)
        
        # Print the word count
        print('There are {} words in the file.'.format(wordCount))
        
        # Print the words in the file
        for word in words:
            print(word)
            
        # Calculate how many words are in the file
        wordCount = len(words)
        
        # Print the word count
        print('There are {} words in the file.'.format(wordCount))
        
        # Print the words in the file
        for word in words:
            print(word)
            
        # Calculate how many words are in the file
        wordCount = len(words)
        
        # Print the word count
        print('There are {} words in the file.'.format(wordCount))
        
        # Print the words in the file
        for word in words:
            print(word)
            
        # Calculate how many words are in the file
        wordCount = len(words)
        
        # Print the word count
