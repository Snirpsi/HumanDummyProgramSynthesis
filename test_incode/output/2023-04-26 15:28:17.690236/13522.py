#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that multiplyes user input or opens a port. """    
    
    port = int(sys.argv[1])
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequestHandler)
    
    # start a web server
    httpd = HTTPServer(('', port), SimpleHTTPRequest