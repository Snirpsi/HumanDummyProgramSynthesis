#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that iterates over words and calculates words. """    
    words = ["hello", "world", "how", "are", "you", "?"]
    
    # Loop over words and calculate their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
        print(word, word_length)
        
    # Loop over words and print their length
    for word in words:
        word_length = len(word)
