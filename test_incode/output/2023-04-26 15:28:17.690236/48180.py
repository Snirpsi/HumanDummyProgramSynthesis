#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that removes all ports or opens a list of numbers. """    
    ports = input("Enter a list of ports to open: ")
    ports = ports.split()
    ports = list(filter(None, ports))
    ports = list(map(int, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, ports))
    ports = list(map(str, 