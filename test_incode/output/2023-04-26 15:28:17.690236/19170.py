#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that converts user input and converts all ports. """    
    while True:
        ports = input("Enter a port to convert: ")
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports = list(set(ports))
        ports.sort()
        ports = [str(p) for p in ports]
        ports = " ".join(ports)
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports = list(set(ports))
        ports.sort()
        ports = [str(p) for p in ports]
        ports = " ".join(ports)
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports = list(set(ports))
        ports.sort()
        ports = [str(p) for p in ports]
        ports = " ".join(ports)
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports = list(set(ports))
        ports.sort()
        ports = [str(p) for p in ports]
        ports = " ".join(ports)
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports = list(set(ports))
        ports.sort()
        ports = [str(p) for p in ports]
        ports = " ".join(ports)
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports = list(set(ports))
        ports.sort()
        ports = [str(p) for p in ports]
        ports = " ".join(ports)
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports = list(set(ports))
        ports.sort()
        ports = [str(p) for p in ports]
        ports = " ".join(ports)
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports = list(set(ports))
        ports.sort()
        ports = [str(p) for p in ports]
        ports = " ".join(ports)
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports = list(set(ports))
        ports.sort()
        ports = [str(p) for p in ports]
        ports = " ".join(ports)
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports = list(set(ports))
        ports.sort()
        ports = [str(p) for p in ports]
        ports = " ".join(ports)
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports = list(set(ports))
        ports.sort()
        ports = [str(p) for p in ports]
        ports = " ".join(ports)
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports = list(set(ports))
        port