#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that stores a port or enumerates all ports. """    
    
    ports = []
    
    # enumerate all ports
    ports = enumeratePorts()
    
    # store all ports
    storePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(ports)
    
    # start webserver
    servePorts(port