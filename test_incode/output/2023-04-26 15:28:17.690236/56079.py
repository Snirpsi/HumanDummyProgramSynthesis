#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts a list of numbers. """    
    
    import sys
    
    numbers = sys.argv[1:]
    
    numbers = list(map(int, numbers))
    
    numbers = list(map(str, numbers))
    
    numbers = list(map(float, numbers))
    
    numbers = list(map(tuple, numbers))
    
    numbers = list(map(list, numbers))
    
    numbers = list(map(set, numbers))
    
    numbers = list(map(frozenset, numbers))
    
    numbers = list(map(list, numbers))
    
    numbers = list(map(tuple, numbers))
    
    numbers = list(map(set, numbers))
    
    numbers = list(map(frozenset, numbers))
    
    numbers = list(map(list, numbers))
    
    numbers = list(map(tuple, numbers))
    
    numbers = list(map(set, numbers))
    
    numbers = list(map(frozenset, numbers))
    
    numbers = list(map(list, numbers))
    
    numbers = list(map(tuple, numbers))
    
    numbers = list(map(set, numbers))
    
    numbers = list(map(frozenset, numbers))
    
    numbers = list(map(list, numbers))
    
    numbers = list(map(tuple, numbers))
    
    numbers = list(map(set, numbers))
    
    numbers = list(map(frozenset, numbers))
    
    numbers = list(map(list, numbers))
    
    numbers = list(map(tuple, numbers))
    
    numbers = list(map(set, numbers))
    
    numbers = list(map(frozenset, numbers))
    
    numbers = list(map(list, numbers))
    
    numbers = list(map(tuple, numbers))
    
    numbers = list(map(set, numbers))
    
    numbers = list(map(frozenset, numbers))
    
    numbers = list(map(list, numbers))
    
    numbers = list(map(tuple, numbers))
    
    numbers = list(map(set, numbers))
    
    numbers = list(map(frozenset, numbers))
    
    numbers = list(map(list, numbers))
    
    numbers = list(map(tuple, numbers))
    
    numbers = list(map(set, numbers))
    
    numbers = list(map(frozenset, numbers))
    
    numbers = list(map(list, numbers))
    
    numbers = list(map(tuple, numbers))
    
    numbers = list(map(set, numbers))
    
    numbers = list(map(frozenset, numbers))
    
    numbers = list(map(list, numbers))
    
    numbers = list(map(tuple, numbers))
    
    numbers = list(map(set, numbers))
    
    numbers = list(map(frozenset, numbers))
    
    numbers = list(map(list, numbers))
    
    numbers = list(map(tuple, 