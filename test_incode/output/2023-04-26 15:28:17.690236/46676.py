#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that iterates over a port or returns user input. """    
    while True:
        port = input("Enter a port number: ")
        try:
            port = int(port)
        except ValueError:
            print("Invalid port number")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    
    while True:
        command = input("Enter a command: ")
        try:
            command = str(command)
        except ValueError:
            print("Invalid command")
        else:
            break
    