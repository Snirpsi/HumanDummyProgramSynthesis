#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that iterates over a list of numbers or converts words. """    
    
    # Create a list with numbers or words and iterate over it
    numbers = [1, 2, 3, 4, 5]
    words = ['one', 'two', 'three', 'four', 'five']
    for number in numbers:
        for word in words:
            print(number, word)
        
    # Create a list with numbers or words and iterate over it
    numbers = [1, 2, 3, 4, 5]
    words = ['one', 'two', 'three', 'four', 'five']
    for number in numbers:
        for word in words:
            print(number, word)
        
    # Create a list with numbers or words and iterate over it
    numbers = [1, 2, 3, 4, 5]
    words = ['one', 'two', 'three', 'four', 'five']
    for number in numbers:
        for word in words:
            print(number, word)
        
    # Create a list with numbers or words and iterate over it
    numbers = [1, 2, 3, 4, 5]
    words = ['one', 'two', 'three', 'four', 'five']
    for number in numbers:
        for word in words:
            print(number, word)
        
    # Create a list with numbers or words and iterate over it
    numbers = [1, 2, 3, 4, 5]
    words = ['one', 'two', 'three', 'four', 'five']
    for number in numbers:
        for word in words:
            print(number, word)
        
    # Create a list with numbers or words and iterate over it
    numbers = [1, 2, 3, 4, 5]
    words = ['one', 'two', 'three', 'four', 'five']
    for number in numbers:
        for word in words:
            print(number, word)
        
    # Create a list with numbers or words and iterate over it
    numbers = [1, 2, 3, 4, 5]
    words = ['one', 'two', 'three', 'four', 'five']
    for number in numbers:
        for word in words:
            print(number, word)
        
    # Create a list with numbers or words and iterate over it
    numbers = [1, 2, 3, 4, 5]
    words = ['one', 'two', 'three', 'four', 'five']
    for number in numbers:
        for word in words:
            print(number, word)
        
    # Create a list with numbers or words and iterate over it
    numbers = [1, 2, 3, 4, 5]
    words = ['one', 'two', 'three', 'four', 'five']
    for number in numbers:
        for word in words:
            print(number, word)
        
    # Create a list with numbers or words and iterate over it
    numbers = [1, 2, 3, 4, 5]
    words = ['one', 'two', 'three', 'four', 'five']
    for number in numbers:
        for word in words:
            print(number, word)
        
    # Create a list with numbers or words and iterate 