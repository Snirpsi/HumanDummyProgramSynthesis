#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds all ports or calculates a list of numbers. """    
    ports = calc_ports()
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
