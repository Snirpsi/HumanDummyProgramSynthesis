#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds all ports or stores a list of numbers. """    
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(ports)
    ports = add_ports(port