#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that adds words and multiplyes numbers. """    
    
    import sys
    
    words = []
    
    if len(sys.argv) > 1:
        words = sys.argv[1:]
    
    numbers = []
    
    if len(sys.argv) > 2:
        numbers = sys.argv[2:]
    
    numbers = [int(x) for x in numbers]
    
    words = [x.lower() for x in words]
    
    numbers = [int(x) for x in numbers]
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = numbers + [0] * (len(words) - len(numbers))
    
    numbers = number