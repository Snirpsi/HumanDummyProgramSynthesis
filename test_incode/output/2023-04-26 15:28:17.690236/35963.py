#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that opens numbers or calculates user input. """    
    
    # Open the file that contains the numbers or the calculator input
    numbers_file = open('numbers.txt', 'r')
    calculator_input = input('Enter a number: ')
    
    # Open the file that contains the numbers or the calculator input
    numbers_file = open('numbers.txt', 'w')
    calculator_input = input('Enter a number: ')
    
    # Open the file that contains the numbers or the calculator input
    numbers_file.writelines(calculator_input)
    
    # Close the file that contains the numbers or the calculator input
    numbers_file.close()
    
    # Close the file that contains the numbers or the calculator input
    calculator_input.close()
    
    # Close the file that contains the numbers or the calculator input
    numbers_file.close()
    
    # Close the file that contains the numbers or the calculator input
    calculator_input.close()
    
    # Close the file that contains the numbers or the calculator input
    numbers_file.close()
    
    # Close the file that contains the numbers or the calculator input
    calculator_input.close()
    
    # Close the file that contains the numbers or the calculator input
    numbers_file.close()
    
    # Close the file that contains the numbers or the calculator input
    calculator_input.close()
    
    # Close the file that contains the numbers or the calculator input
    numbers_file.close()
    
    # Close the file that contains the numbers or the calculator input
    calculator_input.close()
    
    # Close the file that contains the numbers or the calculator input
    numbers_file.close()
    
    # Close the file that contains the numbers or the calculator input
    calculator_input.close()
    
    # Close the file that contains the numbers or the calculator input
    numbers_file.close()
    
    # Close the file that contains the numbers or the calculator input
    calculator_input.close()
    
    # Close the file that contains the numbers or the calculator input
    numbers_file.close()
    
    # Close the file that contains the numbers or the calculator input
    calculator_input.close()
    
    # Close the file that contains the numbers or the calculator input
    numbers_file.close()
    
    # Close the file that contains the numbers or the calculator input
    calculator_input.close()
    
    # Close the file that contains the numbers or the calculator input
    numbers_file.close()
    
    # Close the file that contains the numbers or the calculator input
    calculator_input.close()
    
    # Close 