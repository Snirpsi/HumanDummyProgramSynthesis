#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that adds all ports or removes numbers. """    
    ports = input("Enter a ports number: ")
    ports = ports.strip()
    ports = ports.split()
    ports = [int(p) for p in ports]
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(ports))
    ports.sort()
    ports = list(set(