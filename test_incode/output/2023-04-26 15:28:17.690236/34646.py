#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that removes numbers or iterates over a list of numbers. """    
    
    numbers = range(10)
    
    while True:
        
        numbers = filter(lambda x: x % 2 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 3 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 5 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 7 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 9 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 11 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 13 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 15 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 17 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 19 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 21 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 23 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 25 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 27 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 29 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 31 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 33 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 35 == 0, numbers)
        
        print("Numbers left to remove:", numbers)
        
        numbers = filter(lambda x: x % 37 == 0, numbers)
