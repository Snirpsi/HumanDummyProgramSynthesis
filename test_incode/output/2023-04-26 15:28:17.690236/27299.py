#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that multiplyes numbers or enumerates a port. """    
    
    # Create a port object
    port = Port()
    
    # Create a port enumerator object
    port_enumerator = PortEnumerator(port)
    
    # Enumerate ports
    port_enumerator.enumerate()
    
    # Print the port numbers
    for port in port_enumerator.ports:
        print(port)
    
    # Print the port names
    for port in port_enumerator.ports:
        print(port.name)
    
    # Print the port description
    for port in port_enumerator.ports:
        print(port.description)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the port state
    for port in port_enumerator.ports:
        print(port.state)
    
    # Print the 