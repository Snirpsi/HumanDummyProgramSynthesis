#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that enumerates a list of words or iterates over numbers. """    
    import sys
    
    # Iterate over numbers
    numbers = [int(i) for i in sys.argv[1:]]
    
    # Iterate over words
    words = sys.argv[2:]
    
    # Print the numbers and words
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for number, word in zip(numbers, words):
        print('{}: {}'.format(number, word))
    
    # Print the numbers and words again
    for 