#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that adds a list of words or opens user input. """    
    
    # Start a web server
    httpd = HTTPServer(("", 8000), WordsHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a web server
    httpd = HTTPServer(("", 8000), UserInputHandler)
    
    # Start a 