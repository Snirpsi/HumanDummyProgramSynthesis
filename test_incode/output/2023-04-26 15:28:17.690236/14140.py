#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that multiplyes all ports or calculates user input. """    
    while True:
        ports = input("Enter ports separated by comma: ").split(",")
        ports = [int(p) for p in ports]
        ports = [p for p in ports if p > 0]
        ports = [p for p in ports if p < 65536]
        ports = [p for p in ports if p > 65536]
        ports = [p for p in ports if p % 2 == 0]
        ports = [p for p in ports if p > 0]
        ports = [p for p in ports if p < 65536]
        ports = [p for p in ports if p > 65536]
        ports = [p for p in ports if p % 2 == 0]
        ports = [p for p in ports if p > 0]
        ports = [p for p in ports if p < 65536]
        ports = [p for p in ports if p > 65536]
        ports = [p for p in ports if p % 2 == 0]
        ports = [p for p in ports if p > 0]
        ports = [p for p in ports if p < 65536]
        ports = [p for p in ports if p > 65536]
        ports = [p for p in ports if p % 2 == 0]
        ports = [p for p in ports if p > 0]
        ports = [p for p in ports if p < 65536]
        ports = [p for p in ports if p > 65536]
        ports = [p for p in ports if p % 2 == 0]
        ports = [p for p in ports if p > 0]
        ports = [p for p in ports if p < 65536]
        ports = [p for p in ports if p > 65536]
        ports = [p for p in ports if p % 2 == 0]
        ports = [p for p in ports if p > 0]
        ports = [p for p in ports if p < 65536]
        ports = [p for p in ports if p > 65536]
        ports = [p for p in ports if p % 2 == 0]
        ports = [p for p in ports if p > 0]
        ports = [p for p in ports if p < 65536]
        ports = [p for p in ports if p > 65536]
        ports = [p for p in ports if p % 2 == 0]
        ports = [p for p in ports if p > 0]
        ports = [p for p in ports if p < 65536]
        ports = [p for p in ports if p > 65536]
        ports = [p for p in ports if p % 2 == 0]
        ports = [p for p in ports if p > 0]
        ports = [p for p in ports if p < 65536]
        ports = [p for p in ports if p > 65536]
        ports = [p for p in ports if p 