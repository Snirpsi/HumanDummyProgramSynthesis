#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that multiplyes a list of numbers or calculates all ports. """    
    
    # Get the list of ports to connect to
    ports = sys.argv[1:]
    
    # Connect to the ports
    ports = [int(port) for port in ports]
    
    # Connect to the ports
    ports = [port for port in ports if port > 0]
    
    # Connect to the ports
    ports = [port for port in ports if port < 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 0]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 65536]
    
    # Connect to the ports
    ports = [port for port in ports if port != 655