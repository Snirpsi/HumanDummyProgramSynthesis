#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds a list of words. """    
    words = ['cat', 'dog', 'horse', 'monkey']
    
    # Add all the words to the list
    words.extend(['mouse', 'cat', 'dog', 'horse'])
    
    # Print the list
    print(words)
    
    # Print the length of the list
    print(len(words))
    
    # Print the length of the list minus one
    print(len(words) - 1)
    
    # Print the length of the list minus one plus one
    print(len(words) - 2)
    
    # Print the length of the list minus one plus one plus one
    print(len(words) - 3)
    
    # Print the length of the list minus one plus one plus one plus one
    print(len(words) - 4)
    
    # Print the length of the list minus one plus one plus one plus one plus one
    print(len(words) - 5)
    
    # Print the length of the list minus one plus one plus one plus one plus one plus one
    print(len(words) - 6)
    
    # Print the length of the list minus one plus one plus one plus one plus one plus one plus one plus one
    print(len(words) - 7)
    
    # Print the length of the list minus one plus one plus one plus one plus one plus one plus one plus one plus one plus one
    print(len(words) - 8)
    
    # Print the length of the list minus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one
    print(len(words) - 9)
    
    # Print the length of the list minus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one
    print(len(words) - 10)
    
    # Print the length of the list minus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus one plus 