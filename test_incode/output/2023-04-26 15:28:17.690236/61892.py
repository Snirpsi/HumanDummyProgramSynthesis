#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts a list of words or opens user input. """    
    
    import sys
    
    if len(sys.argv) > 1:
        words = sys.argv[1:]
    else:
        words = input('Enter a list of words: ')
    
    words = list(filter(None, words.split(' ')))
    
    if len(words) == 0:
        print('No words given')
        exit()
    
    word_count = len(words)
    
    word_list = list(enumerate(words))
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])
    
    word_list.sort(key=lambda x:x[1])
    
    word_list.sort(key=lambda x:x[0])