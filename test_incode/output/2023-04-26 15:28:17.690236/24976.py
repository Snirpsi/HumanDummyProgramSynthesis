#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts a list of words or calculates a list of words. """    
    import sys
    
    words = []
    for line in sys.stdin:
        line = line.strip()
        if line == '':
            continue
        words.append(line)
    
    words.sort()
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if 