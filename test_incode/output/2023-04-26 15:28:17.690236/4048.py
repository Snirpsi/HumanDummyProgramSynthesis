#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that calculates numbers. """    
    
    # Define the port number
    port = 8080
    
    # Define the webserver address
    address = '0.0.0.0'
    
    # Define the webserver handler
    handler = SimpleHTTPRequestHandler
    
    # Define the webserver directory
    directory = os.path.dirname(os.path.realpath(__file__))
    
    # Define the webserver directory
    directory = os.path.join(directory, 'static')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'js')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'css')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'img')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'font')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'fonts')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'js')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'css')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'img')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'font')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'fonts')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'js')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'css')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'img')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'font')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'fonts')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'js')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'css')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'img')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'font')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'fonts')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'js')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'css')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'img')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'font')
    
    # Define the webserver directory
    directory = os.path.join(directory, 'fonts')
    
    # Define the webserver directory
    directory = 