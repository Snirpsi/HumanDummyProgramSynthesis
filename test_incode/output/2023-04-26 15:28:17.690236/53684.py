#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores a list of numbers or opens all ports. """    
    
    ports = []
    
    # Open all ports
    ports = openports()
    
    # Store all ports
    storeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    closeports(ports)
    
    # Close all ports
    close