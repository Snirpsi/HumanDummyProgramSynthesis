#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds a list of words. """    
    words = ['cat', 'dog', 'bird']
    
    # Add each word in the list to the list.
    for word in words:
        print(word)
        
    # Print the list again.
    print(words)
    
    # Print the length of the list.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
    print(len(words))
    
    # Print the length of the list minus 1.
