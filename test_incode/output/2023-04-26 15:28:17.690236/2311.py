#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that removes numbers or adds a list of words. """    
    
    # Remove all numbers from text
    words = re.sub('[0-9]', '', text)
    
    # Remove all words from text
    words = re.sub('[^a-zA-Z]+', ' ', text)
    
    # Remove all punctuation
    words = re.sub('[^a-zA-Z0-9]', ' ', text)
    
    # Remove all spaces
    words = re.sub('\s+', ' ', text)
    
    # Remove all punctuation except numbers
    words = re.sub('[^a-zA-Z0-9]', ' ', text)
    
    # Remove all spaces
    words = re.sub('\s+', ' ', text)
    
    # Remove all punctuation except numbers
    words = re.sub('[^a-zA-Z0-9]', ' ', text)
    
    # Remove all spaces
    words = re.sub('\s+', ' ', text)
    
    # Remove all punctuation except numbers
    words = re.sub('[^a-zA-Z0-9]', ' ', text)
    
    # Remove all spaces
    words = re.sub('\s+', ' ', text)
    
    # Remove all punctuation except numbers
    words = re.sub('[^a-zA-Z0-9]', ' ', text)
    
    # Remove all spaces
    words = re.sub('\s+', ' ', text)
    
    # Remove all punctuation except numbers
    words = re.sub('[^a-zA-Z0-9]', ' ', text)
    
    # Remove all spaces
    words = re.sub('\s+', ' ', text)
    
    # Remove all punctuation except numbers
    words = re.sub('[^a-zA-Z0-9]', ' ', text)
    
    # Remove all spaces
    words = re.sub('\s+', ' ', text)
    
    # Remove all punctuation except numbers
    words = re.sub('[^a-zA-Z0-9]', ' ', text)
    
    # Remove all spaces
    words = re.sub('\s+', ' ', text)
    
    # Remove all punctuation except numbers
    words = re.sub('[^a-zA-Z0-9]', ' ', text)
    
    # Remove all spaces
    words = re.sub('\s+', ' ', text)
    
    # Remove all punctuation except numbers
    words = re.sub('[^a-zA-Z0-9]', ' ', text)
    
    # Remove all spaces
    words = re.sub('\s+', ' ', text)
    
    # Remove all punctuation except numbers
    words = re.sub('[^a-zA-Z0-9]', ' ', text)
    
    # Remove all spaces
    words = re.sub('\s+', ' ', text)
    
    # Remove all punctuation except numbers
    words = re.sub('[^a-zA-Z0-9]', ' ', text)
    
    # Remove all spaces
    words = re.sub('\s+', ' ', text)
    
    # Remove all punctuation except numbers
    words = re.sub('[^a-zA-Z0-9]', ' 