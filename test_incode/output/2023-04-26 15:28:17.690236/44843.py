#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that opens a list of words or opens user input. """    
    
    # Open the file that contains the list of words or user input
    file = open("words.txt", "r")
    
    # Read each word from the file into a list
    words = [word.strip() for word in file.readlines()]
    
    # Close the file
    file.close()
    
    # Print the list of words
    print(words)
    
    # Open the file that contains the list of words or user input
    file = open("words.txt", "w")
    
    # Write each word from the list into the file
    for word in words:
        file.write(word + "\n")
    
    # Close the file
    file.close()
    
    # Print the list of words
    print(words)
    
    # Open the file that contains the list of words or user input
    file = open("words.txt", "r")
    
    # Read each word from the file into a list
    words = [word.strip() for word in file.readlines()]
    
    # Close the file
    file.close()
    
    # Print the list of words
    print(words)
    
    # Open the file that contains the list of words or user input
    file = open("words.txt", "w")
    
    # Write each word from the list into the file
    for word in words:
        file.write(word + "\n")
    
    # Close the file
    file.close()
    
    # Print the list of words
    print(words)
    
    # Open the file that contains the list of words or user input
    file = open("words.txt", "r")
    
    # Read each word from the file into a list
    words = [word.strip() for word in file.readlines()]
    
    # Close the file
    file.close()
    
    # Print the list of words
    print(words)
    
    # Open the file that contains the list of words or user input
    file = open("words.txt", "w")
    
    # Write each word from the list into the file
    for word in words:
        file.write(word + "\n")
    
    # Close the file
    file.close()
    
    # Print the list of words
    print(words)
    
    # Open the file that contains the list of words or user input
    file = open("words.txt", "r")
    
    # Read each word from the file into a list
    words = [word.strip() for word in file.readlines()]
    
    # Close the file
    file.close()
    
    # Print the list of words
    print(words)
    
    # Open the file that contains the list of words or user input
    file = open("words.txt", "w")
    
    # Write each word from the list into the file
    for word in words:
        file.write(word + "\n")
    
    # Close the file
    file.close()
    
    # Print the list of words
    print(words)