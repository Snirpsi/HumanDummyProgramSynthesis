#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that iterates over a list of words or enumerates numbers. """    
    words = ['cat', 'dog', 'bird', 'mouse']
    numbers = [0, 1, 2, 3]
    
    for index, word in enumerate(words):
        print('{}: {}'.format(index, word))
    
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    print('-----------')
    
    for index, word in enumerate(words):
        print('{}: {}'.format(index, word))
    
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    print('-----------')
    
    for index, word in enumerate(words):
        print('{}: {}'.format(index, word))
    
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    print('-----------')
    
    for index, word in enumerate(words):
        print('{}: {}'.format(index, word))
    
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    print('-----------')
    
    for index, word in enumerate(words):
        print('{}: {}'.format(index, word))
    
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    print('-----------')
    
    for index, word in enumerate(words):
        print('{}: {}'.format(index, word))
    
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    print('-----------')
    
    for index, word in enumerate(words):
        print('{}: {}'.format(index, word))
    
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    print('-----------')
    
    for index, word in enumerate(words):
        print('{}: {}'.format(index, word))
    
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    print('-----------')
    
    for index, word in enumerate(words):
        print('{}: {}'.format(index, word))
    
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    print('-----------')
    
    for index, word in enumerate(words):
        print('{}: {}'.format(index, word))
    
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    print('-----------')
    
    for index, word in enumerate(words):
        print('{}: {}'.format(index, word))
    
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    print('-----------')
    
    for index, word in enumerate(words):
        print('{}: {}'.format(index, word))
    
    for index, number in enumerate(numbers):
        print('{}: {}'.format(index, number))
    
    print('-----------')
    
    for index, word in enumerate(words):
        print('{}: {}'.format(index, word))
    
    for index, number in enumerate(numbers):
