#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that adds user input. """    
    
    # Create a list to store the user input
    user_input = []
    
    # Get the user input from the user
    while True:
        user_input.append(input("Enter a number: "))
        
        # Check if the user input is a valid number
        try:
            int(user_input[-1])
        except ValueError:
            print("Invalid input. Try again.")
            continue
        
        # Check if the user input is a valid number
        if user_input[-1].isdigit():
            break
        
    # Print the user input to the screen
    print("The user input is: ", user_input)
    
    # Get the user input from the user
    while True:
        user_input.append(input("Enter another number: "))
        
        # Check if the user input is a valid number
        try:
            int(user_input[-1])
        except ValueError:
            print("Invalid input. Try again.")
            continue
        
        # Check if the user input is a valid number
        if user_input[-1].isdigit():
            break
        
    # Print the user input to the screen
    print("The user input is: ", user_input)
    
    # Get the user input from the user
    while True:
        user_input.append(input("Enter another number: "))
        
        # Check if the user input is a valid number
        try:
            int(user_input[-1])
        except ValueError:
            print("Invalid input. Try again.")
            continue
        
        # Check if the user input is a valid number
        if user_input[-1].isdigit():
            break
        
    # Print the user input to the screen
    print("The user input is: ", user_input)
    
    # Get the user input from the user
    while True:
        user_input.append(input("Enter another number: "))
        
        # Check if the user input is a valid number
        try:
            int(user_input[-1])
        except ValueError:
            print("Invalid input. Try again.")
            continue
        
        # Check if the user input is a valid number
        if user_input[-1].isdigit():
            break
        
    # Print the user input to the screen
    print("The user input is: ", user_input)
    
    # Get the user input from the user
    while True:
        user_input.append(input("Enter another number: "))
        
        # Check if the user input is a valid number
        try:
            int(user_input[-1])
        except ValueError:
            print("Invalid input. Try again.")
            continue
        
        # Check if the user input is a valid number
        if user_input[-1].isdigit():
            break
        
    # Print the user input to the screen
    print("The user input is: ", user_input)
    
    # Get the user input from the user
    while True:
        user_input.append(input("Enter another number: "))
        
        # Check if the user input is a valid number
        try:
            int(user_input[-1])
        except ValueError:
            print("Invalid input. Try again.")
            continue
        