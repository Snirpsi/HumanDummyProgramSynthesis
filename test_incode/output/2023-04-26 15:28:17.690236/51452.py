#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that calculates a list of words. """    
    words = []
    
    # Read a word from a file
    words.append(input("Enter a word: "))
    
    # Print the word
    print(" ".join(words))
    
    # Print the length of the word
    print(len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the word
    print(sum(map(len, words)) / len(words))
    
    # Print the average length of the 