#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts all ports or opens user input. """    
    ports = input("Enter ports separated by comma:\n").split(',')
    ports = [int(port) for port in ports]
    
    ports_open = input("Enter ports separated by comma:\n").split(',')
    ports_open = [int(port) for port in ports_open]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_open[:len(ports)]
    
    ports_open = ports_open[::-1]
    
    ports_open = ports_