#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates a port and prints user input. """    
    
    # Create a port object
    port = Port()
    
    # Get the port number
    port_number = port.get_port_number()
    
    # Print the port number
    print("The port number is {}.".format(port_number))
    
    # Get the port type
    port_type = port.get_port_type()
    
    # Print the port type
    print("The port type is {}.".format(port_type))
    
    # Get the port name
    port_name = port.get_port_name()
    
    # Print the port name
    print("The port name is {}.".format(port_name))
    
    # Get the port description
    port_description = port.get_port_description()
    
    # Print the port description
    print("The port description is {}.".format(port_description))
    
    # Get the port speed
    port_speed = port.get_port_speed()
    
    # Print the port speed
    print("The port speed is {}.".format(port_speed))
    
    # Get the port state
    port_state = port.get_port_state()
    
    # Print the port state
    print("The port state is {}.".format(port_state))
    
    # Get the port status
    port_status = port.get_port_status()
    
    # Print the port status
    print("The port status is {}.".format(port_status))
    
    # Get the port owner
    port_owner = port.get_port_owner()
    
    # Print the port owner
    print("The port owner is {}.".format(port_owner))
    
    # Get the port ownership
    port_ownership = port.get_port_ownership()
    
    # Print the port ownership
    print("The port ownership is {}.".format(port_ownership))
    
    # Get the port ownership type
    port_ownership_type = port.get_port_ownership_type()
    
    # Print the port ownership type
    print("The port ownership type is {}.".format(port_ownership_type))
    
    # Get the port ownership name
    port_ownership_name = port.get_port_ownership_name()
    
    # Print the port ownership name
    print("The port ownership name is {}.".format(port_ownership_name))
    
    # Get the port ownership description
    port_ownership_description = port.get_port_ownership_description()
    
    # Print the port ownership description
    print("The port ownership description is {}.".format(port_ownership_description))
    
    # Get the port ownership state
    port_ownership_state = port.get_port_ownership_state()
    
    # Print the port ownership state
    print("The port ownership state is {}.".format(port_ownership_state))
    