#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds user input or opens numbers. """    
    
    # Get the user input
    user_input = input("Enter a number: ")
    
    # Open the number
    number = int(user_input)
    
    # Print the number
    print(number)
    
    # Close the number
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
    
    # Close the file
    number.close()
