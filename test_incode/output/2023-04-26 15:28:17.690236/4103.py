#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that multiplyes user input or enumerates a list of numbers. """    
    
    # Get the user input or enumerate a list of numbers
    numbers = input("Enter numbers: ")
    
    # Multiply user input or enumerate a list of numbers
    numbers = numbers * 2
    
    # Print the result
    print(numbers)
    
    # Enumerate a list of numbers
    numbers = enumerate(numbers)
    
    # Print the result
    for index, number in numbers:
        print("{}. {}".format(index, number))
    
    # Print the result
    for number, index in zip(numbers, range(1, len(numbers)+1)):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for number, index in zip(numbers, range(1, len(numbers)+1)):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
        print("{}. {}".format(index, number))
    
    # Print the result
    for index, number in enumerate(numbers):
