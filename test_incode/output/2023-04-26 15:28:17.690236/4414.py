#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that removes a list of words or opens a list of words. """    
    
    # Get the list of words to remove
    words_to_remove = input("Please enter the words to remove (separated by spaces): ")
    
    # Remove all words from the list
    words_to_remove = words_to_remove.split()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.strip()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.split()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.strip()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.split()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.strip()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.split()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.strip()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.split()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.strip()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.split()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.strip()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.split()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.strip()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.split()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.strip()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.split()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.strip()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.split()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.strip()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.split()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.strip()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.split()
    
    # Remove all words from the list
    words_to_remove = words_to_remove.strip()
    
    # 