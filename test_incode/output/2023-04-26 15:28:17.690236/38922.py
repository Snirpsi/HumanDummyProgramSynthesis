#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that adds words or converts numbers. """    
    import sys
    
    if len(sys.argv) < 2:
        print("Usage: python add.py <word> <number>", file=sys.stderr)
        sys.exit(1)
    
    word = sys.argv[1]
    number = int(sys.argv[2])
    
    if word == "quit":
        sys.exit(0)
    
    if number < 1:
        print("Usage: python add.py <word> <number>", file=sys.stderr)
        sys.exit(1)
    
    word = word.lower()
    
    if word == "quit":
        sys.exit(0)
    
    if number < 1:
        print("Usage: python add.py <word> <number>", file=sys.stderr)
        sys.exit(1)
    
    number = str(number)
    
    if number == "quit":
        sys.exit(0)
    
    number = int(number)
    
    if number < 1:
        print("Usage: python add.py <word> <number>", file=sys.stderr)
        sys.exit(1)
    
    number = str(number)
    
    if number == "quit":
        sys.exit(0)
    
    number = int(number)
    
    if number < 1:
        print("Usage: python add.py <word> <number>", file=sys.stderr)
        sys.exit(1)
    
    number = str(number)
    
    if number == "quit":
        sys.exit(0)
    
    number = int(number)
    
    if number < 1:
        print("Usage: python add.py <word> <number>", file=sys.stderr)
        sys.exit(1)
    
    number = str(number)
    
    if number == "quit":
        sys.exit(0)
    
    number = int(number)
    
    if number < 1:
        print("Usage: python add.py <word> <number>", file=sys.stderr)
        sys.exit(1)
    
    number = str(number)
    
    if number == "quit":
        sys.exit(0)
    
    number = int(number)
    
    if number < 1:
        print("Usage: python add.py <word> <number>", file=sys.stderr)
        sys.exit(1)
    
    number = str(number)
    
    if number == "quit":
        sys.exit(0)
    
    number = int(number)
    
    if number < 1:
        print("Usage: python add.py <word> <number>", file=sys.stderr)
        sys.exit(1)
    
    number = str(number)
    
    if number == "quit":
        sys.exit(0)
    
    number = int(number)
    
    if number < 1:
        print("Usage: python add.py <word> <number>", file=sys.stderr)
        sys.exit(1)
    
    number = str(number)
    
    if number == "quit":
        sys.exit(0)
    
    number = int(number)
    
    if number < 1:
        print("Usage: python add.py <word> <number>", file=sys.stderr)
        sys.exit(1)
    
    number = str(number)
    
    if number == "quit":
        sys.exit(0)
    
    number = int(number)
    
    if number < 1:
        print("Usage: python add