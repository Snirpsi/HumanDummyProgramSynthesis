#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that opens words or converts a list of words. """    
    while True:
        word = input("Enter a word: ")
        if word == 'quit':
            break
        
        word = word.lower()
        
        if word == 'help':
            print('''
            Usage: python3 wordlist.py [word]
            
            wordlist.py converts a list of words to lowercase.
            
            wordlist.py opens a wordlist from a file.
            
            wordlist.py opens a wordlist from stdin.
            
            wordlist.py opens a wordlist from stdin and converts it to lowercase.
            
            wordlist.py opens a wordlist from stdin and converts it to lowercase and opens a wordlist from a file.
            
            wordlist.py opens a wordlist from stdin and converts it to lowercase and opens a wordlist from a file and converts it to lowercase.
            
            wordlist.py opens a wordlist from stdin and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase.
            
            wordlist.py opens a wordlist from stdin and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase.
            
            wordlist.py opens a wordlist from stdin and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lowercase and opens a wordlist from a file and converts it to lower