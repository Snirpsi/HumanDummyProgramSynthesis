#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that iterates over numbers and prints a port. """    
    
    port = 8080
    
    # start a server
    httpd = HTTPServer(('localhost', port), MyHandler)
    
    # start a web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait for a second
    time.sleep(1)
    
    # stop the server
    httpd.server_close()
    
    # stop the web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait for a second
    time.sleep(1)
    
    # stop the server
    httpd.server_close()
    
    # stop the web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait for a second
    time.sleep(1)
    
    # stop the server
    httpd.server_close()
    
    # stop the web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait for a second
    time.sleep(1)
    
    # stop the server
    httpd.server_close()
    
    # stop the web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait for a second
    time.sleep(1)
    
    # stop the server
    httpd.server_close()
    
    # stop the web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait for a second
    time.sleep(1)
    
    # stop the server
    httpd.server_close()
    
    # stop the web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait for a second
    time.sleep(1)
    
    # stop the server
    httpd.server_close()
    
    # stop the web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait for a second
    time.sleep(1)
    
    # stop the server
    httpd.server_close()
    
    # stop the web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait for a second
    time.sleep(1)
    
    # stop the server
    httpd.server_close()
    
    # stop the web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait for a second
    time.sleep(1)
    
    # stop the server
    httpd.server_close()
    
    # stop the web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait for a second
    time.sleep(1)
    
    # stop the server
    httpd.server_close()
    
    # stop the web browser
    webbrowser.open('http://localhost:8080/')
    
    # wait for a second
    time.sleep(1)
    
    # stop the server
    httpd.server_close()
    
    # stop the web browser
    webbrowser.open('http://localhost:8080/')
