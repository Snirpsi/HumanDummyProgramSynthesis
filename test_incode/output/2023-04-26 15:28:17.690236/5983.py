#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that returns all ports and enumerates a list of words. """    
    ports = []
    words = []
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
    server = HTTPServer(('', 0), Handler)
    
    # start a server
