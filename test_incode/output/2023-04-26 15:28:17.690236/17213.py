#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that multiplyes a port or opens all ports. """    
    
    # Open ports
    ports = openports()
    
    # Multiply ports
    ports = multiply(ports)
    
    # Print results
    for port in ports:
        print(port)
        
    # Close ports
    ports = closeports(ports)
    
    # Print results
    for port in ports:
        print(port)
        
    # Close all ports
    ports = closeallports()
    
    # Print results
    for port in ports:
        print(port)
        
    # Close all ports
    ports = closeallports(ports)
    
    # Print results
    for port in ports:
        print(port)
        
    # Close all ports
    ports = closeallports(ports)
    
    # Print results
    for port in ports:
        print(port)
        
    # Close all ports
    ports = closeallports(ports)
    
    # Print results
    for port in ports:
        print(port)
        
    # Close all ports
    ports = closeallports(ports)
    
    # Print results
    for port in ports:
        print(port)
        
    # Close all ports
    ports = closeallports(ports)
    
    # Print results
    for port in ports:
        print(port)
        
    # Close all ports
    ports = closeallports(ports)
    
    # Print results
    for port in ports:
        print(port)
        
    # Close all ports
    ports = closeallports(ports)
    
    # Print results
    for port in ports:
        print(port)
        
    # Close all ports
    ports = closeallports(ports)
    
    # Print results
    for port in ports:
        print(port)
        
    # Close all ports
    ports = closeallports(ports)
    
    # Print results
    for port in ports:
        print(port)
        
    # Close all ports
    ports = closeallports(ports)
    
    # Print results
    for port in ports:
        print(port)
        
    # Close all ports
    ports = closeallports(ports)
    
    # Print results
    for port in ports:
        print(port)
        
    # Close all ports
    ports = closeallports(ports)
    
    # Print results
    for port in ports:
        print(port)
        
    # Close all ports
    