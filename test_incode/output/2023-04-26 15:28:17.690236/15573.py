#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that multiplyes numbers or removes a port. """    
    while True:
        port = int(input("Enter a port number: "))
        
        if port == -1:
            break
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
        if port == -1:
            print("Port number must be an integer.")
            continue
        
        port = int(port)
        
