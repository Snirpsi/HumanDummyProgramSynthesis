#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that removes numbers and opens user input. """    
    
    # Remove all numbers from user input
    numbers = input('Enter numbers: ')
    
    # Remove all numbers from user input
    numbers = numbers.replace(',', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all numbers from user input
    numbers = numbers.replace(' ', '')
    
    # Remove all number