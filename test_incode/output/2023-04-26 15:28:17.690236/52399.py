#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that removes user input or multiplyes all ports. """    
    
    ports = input("Enter ports separated by space: ")
    ports = ports.split()
    
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port = int(port)
        
        if port > 0 and port < 65535:
            ports.remove(port)
            
    for port in ports:
        port