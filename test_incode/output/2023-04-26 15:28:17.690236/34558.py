#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that opens words and multiplyes all ports. """    
    ports = open('/etc/network/interfaces').read().splitlines()
    ports = [port.split(':')[1].strip() for port in ports]
    ports = [port for port in ports if port]
    ports = [int(port) for port in ports]
    ports = [port * 8 for port in ports]
    ports = [port + 1 for port in ports]
    ports = [port + 2 for port in ports]
    ports = [port + 3 for port in ports]
    ports = [port + 4 for port in ports]
    ports = [port + 5 for port in ports]
    ports = [port + 6 for port in ports]
    ports = [port + 7 for port in ports]
    ports = [port + 8 for port in ports]
    ports = [port + 9 for port in ports]
    ports = [port + 10 for port in ports]
    ports = [port + 11 for port in ports]
    ports = [port + 12 for port in ports]
    ports = [port + 13 for port in ports]
    ports = [port + 14 for port in ports]
    ports = [port + 15 for port in ports]
    ports = [port + 16 for port in ports]
    ports = [port + 17 for port in ports]
    ports = [port + 18 for port in ports]
    ports = [port + 19 for port in ports]
    ports = [port + 20 for port in ports]
    ports = [port + 21 for port in ports]
    ports = [port + 22 for port in ports]
    ports = [port + 23 for port in ports]
    ports = [port + 24 for port in ports]
    ports = [port + 25 for port in ports]
    ports = [port + 26 for port in ports]
    ports = [port + 27 for port in ports]
    ports = [port + 28 for port in ports]
    ports = [port + 29 for port in ports]
    ports = [port + 30 for port in ports]
    ports = [port + 31 for port in ports]
    ports = [port + 32 for port in ports]
    ports = [port + 33 for port in ports]
    ports = [port + 34 for port in ports]
    ports = [port + 35 for port in ports]
    ports = [port + 36 for port in ports]
    ports = [port + 37 for port in ports]
    ports = [port + 38 for port 