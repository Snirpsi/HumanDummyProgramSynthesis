#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that removes words or returns a list of numbers. """    
    
    # Remove words from a string
    removeWords(input())
    
    # Return a list of numbers
    numbers = removeWords(input())
    
    # Print the list
    print(numbers)
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list
    print(len(numbers))
    