#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that multiplyes all ports and returns user input. """    
    ports = [int(i) for i in sys.argv[1:]]
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + ports
    ports = ports + 