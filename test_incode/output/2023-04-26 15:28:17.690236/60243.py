#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores a list of numbers. """    
    
    numbers = []
    
    # Add numbers to the list
    for i in range(0, 5):
        numbers.append(i)
    
    # Print the list
    print(numbers)
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the maximum number in the list
    print(max(numbers))
    
    # Print the minimum number in the list
    print(min(numbers))
    
    # Print the average of the numbers in the list
    print(sum(numbers) / len(numbers))
    
    # Print the median of the numbers in the list
    print(sorted(numbers)[len(numbers)//2])
    
    # Print the sum of the squares of the numbers in the list
    print(sum(numbers * numbers))
    
    # Print the square of the sum of the numbers in the list
    print(sum(numbers * numbers) ** 0.5)
    
    # Print the square root of the sum of the numbers in the list
    print(math.sqrt(sum(numbers * numbers)))
    
    # Print the square root of the sum of the numbers in the list
    print(math.sqrt(sum(numbers * numbers)) / math.sqrt(len(numbers)))
    
    # Print the square root of the sum of the numbers in the list
    print(math.sqrt(sum(numbers * numbers)) / math.sqrt(len(numbers)) * math.sqrt(len(numbers)))
    
    # Print the square root of the sum of the numbers in the list
    print(math.sqrt(sum(numbers * numbers)) / math.sqrt(len(numbers)) * math.sqrt(len(numbers)) * math.sqrt(len(numbers)))
    
    # Print the square root of the sum of the numbers in the list
    print(math.sqrt(sum(numbers * numbers)) / math.sqrt(len(numbers)) * math.sqrt(len(numbers)) * math.sqrt(len(numbers)) * math.sqrt(len(numbers)))
    
    # Print the square root of the sum of the numbers in the list
    print(math.sqrt(sum(numbers * numbers)) / math.sqrt(len(numbers)) * math.sqrt(len(numbers)) * math.sqrt(len(numbers)) * math.sqrt(len(numbers)))
    
    # Print the square root of the sum of the numbers in the list
    print(math.sqrt(sum(numbers * numbers)) / math.sqrt(len(numbers)) * math.sqrt(len(numbers)) * math.sqrt(len(numbers)) * math.sqrt(len(numbers)))
    
    # Print the square root of the sum of the numbers in the list
    print(math.sqrt(sum(numbers * numbers)) / math.sqrt(len(numbers)) * math.sqrt(len(numbers)) * math.sqrt(len(numbers)) * math.sqrt(len(numbers)))
    
    # Print the square root of the sum of the numbers in the list
    print(math.sqrt(sum(numbers * numbers)) / math.sqrt(len(numbers)) * math.sqrt(len(numbers)) * math.sqrt(len(numbers)) * math.sqrt(len(numbers)))
    
    # Print the square root of the sum of the numbers in the list
    print(math.sqrt(sum(numbers * numbers)) / math.sqrt(len(