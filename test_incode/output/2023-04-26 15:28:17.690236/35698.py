#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that multiplyes words or calculates numbers. """    
    
    import sys
    
    if len(sys.argv) < 2:
        print("Usage: %s <words>" % sys.argv[0])
        sys.exit()
    
    words = sys.argv[1:]
    
    numbers = map(lambda w: int(w), words)
    
    numbers = map(lambda w: w * 2, numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = map(lambda w: str(w), numbers)
    
    numbers = map(lambda w: int(w), numbers)
    
    numbers = 