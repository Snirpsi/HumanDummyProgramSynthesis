#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that removes a list of numbers and returns all ports. """    
    
    ports = []
    
    # Remove all numbers from the list
    for number in numbers:
        
        # Remove all numbers from the list
        ports.append(str(number))
        
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    ports = list(set(ports))
    
    # Remove all numbers from the list
    