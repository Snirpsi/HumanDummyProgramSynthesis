#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that multiplyes a port and removes words. """    
    import sys
    
    port = int(sys.argv[1])
    
    words = sys.argv[2:]
    
    words = [w for w in words if w != '']
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) <= 3]
    
    words = [w for w in words if len(w) >= 3]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) <= 3]
    
    words = [w for w in words if len(w) >= 3]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) <= 3]
    
    words = [w for w in words if len(w) >= 3]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) <= 3]
    
    words = [w for w in words if len(w) >= 3]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) <= 3]
    
    words = [w for w in words if len(w) >= 3]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) <= 3]
    
    words = [w for w in words if len(w) >= 3]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) <= 3]
    
    words = [w for w in words if len(w) >= 3]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) <= 3]
    
    words = [w for w in words if len(w) >= 3]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) <= 3]
    
    words = [w for w in words if len(w) >= 3]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) <= 3]
    
    words = [w for w in words if len(w) >= 3]
    
    words = [w for w in words if len(w) > 0]
    
    words = [w for w in words if len(w) <= 