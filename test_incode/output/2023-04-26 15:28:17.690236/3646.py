#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that iterates over fruits or calculates a list of numbers. """    
    fruits = ['apple', 'banana', 'cherry']
    numbers = []
    
    while True:
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(len(fruits))
        
        if len(fruits) == 0:
            break
        
        fruits.pop(0)
        numbers.append(