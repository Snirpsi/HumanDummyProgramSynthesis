#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores all ports and prints a port. """    
    ports = []
    
    # Open the ports file
    with open('ports.txt', 'r') as ports_file:
        
        # Read each line of the ports file
        for line in ports_file:
            
            # Split the line into its components
            components = line.strip().split(':')
            
            # Add the port to the list of ports
            ports.append(components[0])
            
            # Print the port to the screen
            print(components[0])
            
            # Print the port to the screen
            print(components[1])
            
            # Print the port to the screen
            print(components[2])
            
            # Print the port to the screen
            print(components[3])
            
            # Print the port to the screen
            print(components[4])
            
            # Print the port to the screen
            print(components[5])
            
            # Print the port to the screen
            print(components[6])
            
            # Print the port to the screen
            print(components[7])
            
            # Print the port to the screen
            print(components[8])
            
            # Print the port to the screen
            print(components[9])
            
            # Print the port to the screen
            print(components[10])
            
            # Print the port to the screen
            print(components[11])
            
            # Print the port to the screen
            print(components[12])
            
            # Print the port to the screen
            print(components[13])
            
            # Print the port to the screen
            print(components[14])
            
            # Print the port to the screen
            print(components[15])
            
            # Print the port to the screen
            print(components[16])
            
            # Print the port to the screen
            print(components[17])
            
            # Print the port to the screen
            print(components[18])
            
            # Print the port to the screen
            print(components[19])
            
            # Print the port to the screen
            print(components[20])
            
            # Print the port to the screen
            print(components[21])
            
            # Print the port to the screen
            print(components[22])
            
            # Print the port to the screen
            print(components[23])
            
            # Print the port to the screen
            print(components[24])
            
            # Print the port to the screen
            print(components[25])
            
            # Print the port to the screen
            print(components[26])
            
            # Print the port to the screen
            print(components[27])
            
            # Print the port to the screen
            print(components[28])
            
            # Print the port to the screen
            print(components[29])