#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that enumerates a port or removes a list of numbers. """    
    ports = sys.argv[1]
    ports = ports.split(',')
    ports = [int(port) for port in ports]
    ports.sort()
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    
    if len(ports) == 1:
        ports.remove(ports[0])
    
    ports = list(set(ports))
    