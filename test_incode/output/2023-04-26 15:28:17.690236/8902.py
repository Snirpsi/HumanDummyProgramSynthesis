#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that converts a list of numbers or enumerates user input. """    
    
    # Get the input from the user
    number = input("Enter a number: ")
    
    # Convert the input to a list
    numbers = number.split(',')
    
    # Print the list
    print(numbers)
    
    # Print the length of the list
    print(len(numbers))
    
    # Print the length of the list without the last element
    print(len(numbers) - 1)
    
    # Print the length of the list with one element
    print(len(numbers))
    
    # Print the length of the list with two elements
    print(len(numbers))
    
    # Print the length of the list with three elements
    print(len(numbers))
    
    # Print the length of the list with four elements
    print(len(numbers))
    
    # Print the length of the list with five elements
    print(len(numbers))
    
    # Print the length of the list with six elements
    print(len(numbers))
    
    # Print the length of the list with seven elements
    print(len(numbers))
    
    # Print the length of the list with eight elements
    print(len(numbers))
    
    # Print the length of the list with nine elements
    print(len(numbers))
    
    # Print the length of the list with ten elements
    print(len(numbers))
    
    # Print the length of the list with eleven elements
    print(len(numbers))
    
    # Print the length of the list with twelve elements
    print(len(numbers))
    
    # Print the length of the list with thirteen elements
    print(len(numbers))
    
    # Print the length of the list with fourteen elements
    print(len(numbers))
    
    # Print the length of the list with fifteen elements
    print(len(numbers))
    
    # Print the length of the list with sixteen elements
    print(len(numbers))
    
    # Print the length of the list with seventeen elements
    print(len(numbers))
    
    # Print the length of the list with eighteen elements
    print(len(numbers))
    
    # Print the length of the list with nineteen elements
    print(len(numbers))
    
    # Print the length of the list with twenty elements
    print(len(numbers))
    
    # Print the length of the list with twenty five elements
    print(len(numbers))
    
    # Print the length of the list with twenty six elements
    print(len(numbers))
    
    # Print the length of the list with twenty seven elements
    print(len(numbers))
    
    # Print the length of the list with twenty eight elements
    print(len(numbers))
    
    # Print the length of the list with twenty 