#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that converts a port or removes a port. """    
    
    # Get the port number from the command line
    port = int(sys.argv[1])
    
    # Remove port number from the command line
    sys.argv = sys.argv[0:1]
    
    # Convert the port number to a string
    port = str(port)
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]
    
    # Print the port number to the screen
    print(port)
    
    # Remove the port number from the command line
    sys.argv = sys.argv[1:]