#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that returns words or removes all ports. """    
    
    ports = [8080, 8081, 8082, 8083, 8084, 8085]
    
    ports = [int(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
    ports = ports[:]
    
    ports.sort()
    
    ports = [str(port) for port in ports]
    
