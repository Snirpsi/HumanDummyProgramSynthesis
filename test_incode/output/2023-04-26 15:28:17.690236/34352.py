#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that calculates a list of words and opens words. """    
    
    # Create a web server
    webserver = WebServer()
    
    # Start a web server
    webserver.start()
    
    # Wait for the server to start up
    webserver.join()
    
    # Print the list of words
    print("Words:")
    for word in words:
        print(word)
    
    # Open the word list
    webbrowser.open("wordlist.html")
    
    # Wait for the browser to finish loading
    webbrowser.open_new()
    
    # Close the browser
    webbrowser.close_new()
    
    # Shut down the web server
    webserver.stop()
    
    # Stop the server
    webserver.join()
    
    # Shut down the web server
    webserver.stop()
    
    # Shut down the web server
    webserver.join()
    
    # Shut down the web server
    webserver.stop()
    
    # Shut down the web server
    webserver.join()
    
    # Shut down the web server
    webserver.stop()
    
    # Shut down the web server
    webserver.join()
    
    # Shut down the web server
    webserver.stop()
    
    # Shut down the web server
    webserver.join()
    
    # Shut down the web server
    webserver.stop()
    
    # Shut down the web server
    webserver.join()
    
    # Shut down the web server
    webserver.stop()
    
    # Shut down the web server
    webserver.join()
    
    # Shut down the web server
    webserver.stop()
    
    # Shut down the web server
    webserver.join()
    
    # Shut down the web server
    webserver.stop()
    
    # Shut down the web server
    webserver.join()
    
    # Shut down the web server
    webserver.stop()
    
    # Shut down the web server
    webserver.join()
    
    # Shut down the web server
    webserver.stop()
    
    # Shut down the web server
    webserver.join()
    
    # Shut down the web server
    webserver.stop()
    
    # Shut down the web server
    webserver.join()
    
    # Shut down the web server
    webserver.stop()
    
    # Shut down the web server
    webserver.join()
    
    # Shut down the web server
    webserver.stop()
    
    # Shut down the web server
    webserver.join()
    
    # Shut down the web server
    