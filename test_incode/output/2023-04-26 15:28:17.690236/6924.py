#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that stores a list of words and adds user input. """    
    
    words = []
    
    # Handle GET requests
    
    if request.method == 'GET':
        
        # Handle GET requests for /
        
        if request.path == '/':
            
            # Handle GET requests for /
            
            form = cgi.FieldStorage()
            
            # Add all words to the list
            
            for key in form.keys():
                
                words.append(key)
            
            form = cgi.FieldStorage()
            
            # Add all words to the list
            
            for key in form.keys():
                
                words.append(key)
            
            form = cgi.FieldStorage()
            
            # Add all words to the list
            
            for key in form.keys():
                
                words.append(key)
            
            form = cgi.FieldStorage()
            
            # Add all words to the list
            
            for key in form.keys():
                
                words.append(key)
            
            form = cgi.FieldStorage()
            
            # Add all words to the list
            
            for key in form.keys():
                
                words.append(key)
            
            form = cgi.FieldStorage()
            
            # Add all words to the list
            
            for key in form.keys():
                
                words.append(key)
            
            form = cgi.FieldStorage()
            
            # Add all words to the list
            
            for key in form.keys():
                
                words.append(key)
            
            form = cgi.FieldStorage()
            
            # Add all words to the list
            
            for key in form.keys():
                
                words.append(key)
            
            form = cgi.FieldStorage()
            
            # Add all words to the list
            
            for key in form.keys():
                
                words.append(key)
            
            form = cgi.FieldStorage()
            
            # Add all words to the list
            
            for key in form.keys():
                
                words.append(key)
            
            form = cgi.FieldStorage()
            
            # Add all words to the list
            
            for key in form.keys():
                
                words.append(key)
            
            form = cgi.FieldStorage()
            
            # Add all words to the list
            
            for key in form.keys():
                
                words.append(key)
            
            form = cgi.FieldStorage()
            
            # Add all words to the list
            
            for key in form.keys():
                
                words.append(key)
            
            form = cgi.FieldStorage()
            
            # Add all words to the list
            
            for key in form.keys():
                
                words.append(key)
            