#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that adds a port or opens a list of words. """    
    
    # Get the arguments from the command line
    args = sys.argv[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port number from the command line
    port = int(args[0])
    
    # Get the words from the command line
    words = args[1:]
    
    # Get the port 