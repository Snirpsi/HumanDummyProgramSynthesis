#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that removes all ports. """    
    
    ports_to_remove = []
    
    # Loop over all ports we want to remove
    for port in ports_to_remove:
        
        # Remove all sockets on this port
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(("", port))
        sock.close()
        
        # Remove the port from the list of ports to remove
        ports_to_remove.remove(port)
        
    # Loop over all ports we want to remove
    for port in ports_to_remove:
        
        # Remove all sockets on this port
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(("", port))
        sock.close()
        
        # Remove the port from the list of ports to remove
        ports_to_remove.remove(port)
        
    # Loop over all ports we want to remove
    for port in ports_to_remove:
        
        # Remove all sockets on this port
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(("", port))
        sock.close()
        
        # Remove the port from the list of ports to remove
        ports_to_remove.remove(port)
        
    # Loop over all ports we want to remove
    for port in ports_to_remove:
        
        # Remove all sockets on this port
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(("", port))
        sock.close()
        
        # Remove the port from the list of ports to remove
        ports_to_remove.remove(port)
        
    # Loop over all ports we want to remove
    for port in ports_to_remove:
        
        # Remove all sockets on this port
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(("", port))
        sock.close()
        
        # Remove the port from the list of ports to remove
        ports_to_remove.remove(port)
        
    # Loop over all ports we want to remove
    for port in ports_to_remove:
        
        # Remove all sockets on this port
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(("", port))
        sock.close()
        
        # Remove the port from the list of ports to remove
        ports_to_remove.remove(port)
        
    # Loop over all ports we want to remove
    for port in ports_to_remove:
        
        # Remove all sockets on this port
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.bind(("", port))
        sock.close()
        
        # Remove the port from the list of ports to remove
        ports_to_remove.remove(port)
        
    # Loop over all ports we want to remove
    for port in ports_to_remove:
        
        # Remove all sockets on this 