#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores a list of numbers or adds fruits. """    
    fruits = ['apple', 'orange', 'banana', 'pear']
    
    # Store the fruits in the list
    fruits = list(fruits)
    
    # Add fruits to the list
    fruits.extend(['pear', 'kiwi', 'grape'])
    
    # Print the fruits list
    print(fruits)
    
    # Print the fruits list without duplicates
    print(list(fruits))
    
    # Print the fruits list with duplicates
    print(list(fruits), end='\n')
    
    # Print the fruits list with duplicates and a separator
    print(list(fruits), sep='\n')
    
    # Print the fruits list with duplicates and a separator and an ending line
    print(list(fruits), sep='\n', end='\n')
    
    # Print the fruits list with duplicates and a separator and an ending line
    print(list(fruits), sep='\n', end='\n', flush=True)
    
    # Print the fruits list with duplicates and a separator and an ending line
    print(list(fruits), sep='\n', end='\n', flush=True)
    
    # Print the fruits list with duplicates and a separator and an ending line
    print(list(fruits), sep='\n', end='\n', flush=True)
    
    # Print the fruits list with duplicates and a separator and an ending line
    print(list(fruits), sep='\n', end='\n', flush=True)
    
    # Print the fruits list with duplicates and a separator and an ending line
    print(list(fruits), sep='\n', end='\n', flush=True)
    
    # Print the fruits list with duplicates and a separator and an ending line
    print(list(fruits), sep='\n', end='\n', flush=True)
    
    # Print the fruits list with duplicates and a separator and an ending line
    print(list(fruits), sep='\n', end='\n', flush=True)
    
    # Print the fruits list with duplicates and a separator and an ending line
    print(list(fruits), sep='\n', end='\n', flush=True)
    
    # Print the fruits list with duplicates and a separator and an ending line
    print(list(fruits), sep='\n', end='\n', flush=True)
    
    # Print the fruits list with duplicates and a separator and an ending line
    print(list(fruits), sep='\n', end='\n', flush=True)
    
    # Print the fruits list with duplicates and a separator and an ending line
    print(list(fruits), sep='\n', end='\n', flush=True)
    
    # Print the fruits list 