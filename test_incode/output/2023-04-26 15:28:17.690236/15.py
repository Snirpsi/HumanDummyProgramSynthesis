#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that opens a list of words and iterates over all ports. """    
    
    # start a server
    server = HTTPServer(('', 80), WordsHandler)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = WebServer(("", 80), server)
    
    # start a web server
    webserver = Web