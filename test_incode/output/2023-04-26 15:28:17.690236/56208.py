#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that stores a list of words or opens user input. """    
    
    # Open the file the user wants to read in
    file = input("Enter the file you want to read: ")
    
    # Open the file the user wants to write to
    file = open("words.txt", "w")
    
    # Open the file the user wants to read from
    file = open("words.txt", "r")
    
    # Read each word in the file the user wants to read
    words = file.read().split()
    
    # Close the file the user wants to read from
    file.close()
    
    # Print the words in the file the user wants to read
    print(words)
    
    # Close the file the user wants to write to
    file.close()
    
    # Print the words in the file the user wants to write
    print(words)
    
    # Close the file the user wants to read in
    file.close()
    
    # Close the file the user wants to write to
    file.close()
    
    # Close the file the user wants to read in
    file.close()
    
    # Close the file the user wants to write to
    file.close()
    
    # Close the file the user wants to read in
    file.close()
    
    # Close the file the user wants to write to
    file.close()
    
    # Close the file the user wants to read in
    file.close()
    
    # Close the file the user wants to write to
    file.close()
    
    # Close the file the user wants to read in
    file.close()
    
    # Close the file the user wants to write to
    file.close()
    
    # Close the file the user wants to read in
    file.close()
    
    # Close the file the user wants to write to
    file.close()
    
    # Close the file the user wants to read in
    file.close()
    
    # Close the file the user wants to write to
    file.close()
    
    # Close the file the user wants to read in
    file.close()
    
    # Close the file the user wants to write to
    file.close()
    
    # Close the file the user wants to read in
    file.close()
    
    # Close the file the user wants to write to
    file.close()
    
    # Close the file the user wants to read in
    file.close()
    
    # Close the file the user wants to write to
    file.close()
    
    # Close the file the user wants to read in
    file.close()
    
    # Close the file the user wants to write to
    file.close()
    
    # Close the file the user wants to read in
    file.close()
    
    # Close the file the user wants to write to
    file.close()
    
    # Close the file the user want