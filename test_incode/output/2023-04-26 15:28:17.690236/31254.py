#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that opens a list of numbers or adds words. """    
    
    # Open the file that contains the list of numbers
    numbers = open('numbers.txt', 'r')
    
    # Read the numbers from the file and store them in a list
    numbers_list = numbers.readlines()
    
    # Close the file
    numbers.close()
    
    # Print the list of numbers to the screen
    print('\n'.join(numbers_list))
    
    # Open the file that contains the words to be added
    words = open('words.txt', 'r')
    
    # Read the words from the file and store them in a list
    words_list = words.readlines()
    
    # Close the file
    words.close()
    
    # Print the list of words to the screen
    print('\n'.join(words_list))
    
    # Close the file
    numbers.close()
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    
    # Close the file
    numbers.close()
    
    # Close the file
    words.close()
    