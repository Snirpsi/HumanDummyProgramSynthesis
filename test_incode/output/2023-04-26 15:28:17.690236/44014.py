#!/usr/bin/python3
if __name__ == '__main__':
    """ A programm that converts a list of words or iterates over a port. """    
    import sys
    
    if len(sys.argv) > 1:
        port = int(sys.argv[1])
        
        ports = [port]
        
        for arg in sys.argv[2:]:
            ports.append(int(arg))
        
        for port in ports:
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            port = str(port)
            
            