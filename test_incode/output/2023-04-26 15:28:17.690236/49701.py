#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that removes user input or iterates over user input. """    
    
    while(True):
        
        # Remove user input
        try: 
            line = input('Enter a number: ')
            
            # Remove user input if user enters anything other than a number
            if type(line) != int:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line < 0 or line > 1000:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 2 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 3 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 4 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 5 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 6 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 7 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 8 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 9 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 10 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 11 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 12 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 13 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 14 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 15 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if user enters anything other than a number
            if line % 16 == 0:
                print('Invalid input')
                continue
            
            # Remove user input if 