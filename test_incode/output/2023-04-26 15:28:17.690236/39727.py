#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that returns a list of numbers. """    
    numbers = [1, 2, 3, 4, 5]
    
    # Print the list of numbers:
    print(numbers)
    
    # Print the sum of all numbers:
    print(sum(numbers))
    
    # Print the average of all numbers:
    print(sum(numbers) / len(numbers))
    
    # Print the sum of all squares of all numbers:
    print(sum(map(lambda x: x**2, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**3, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**4, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**5, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**6, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**7, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**8, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**9, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**10, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**11, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**12, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**13, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**14, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**15, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**16, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**17, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**18, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**19, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**20, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**21, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**22, numbers)))
    
    # Print the sum of all cubes of all numbers:
    print(sum(map(lambda x: x**