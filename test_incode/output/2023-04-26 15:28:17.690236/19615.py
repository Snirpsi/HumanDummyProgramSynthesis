#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that prints a list of numbers and converts all ports. """    
    ports = [int(i) for i in input("Enter the ports separated by commas (1-65535): ").split(",")]
    
    while True:
        ports = list(map(int, input("Enter the ports separated by commas (1-65535): ").split(",")))
        
        for port in ports:
            
            if port not in ports:
                print("Port ", port, " is not valid.")
                continue
            
            print(port)
            
        ports = list(map(int, input("Enter the ports separated by commas (1-65535): ").split(",")))
        
        for port in ports:
            
            if port not in ports:
                print("Port ", port, " is not valid.")
                continue
            
            print(port)
            
        ports = list(map(int, input("Enter the ports separated by commas (1-65535): ").split(",")))
        
        for port in ports:
            
            if port not in ports:
                print("Port ", port, " is not valid.")
                continue
            
            print(port)
            
        ports = list(map(int, input("Enter the ports separated by commas (1-65535): ").split(",")))
        
        for port in ports:
            
            if port not in ports:
                print("Port ", port, " is not valid.")
                continue
            
            print(port)
            
        ports = list(map(int, input("Enter the ports separated by commas (1-65535): ").split(",")))
        
        for port in ports:
            
            if port not in ports:
                print("Port ", port, " is not valid.")
                continue
            
            print(port)
            
        ports = list(map(int, input("Enter the ports separated by commas (1-65535): ").split(",")))
        
        for port in ports:
            
            if port not in ports:
                print("Port ", port, " is not valid.")
                continue
            
            print(port)
            
        ports = list(map(int, input("Enter the ports separated by commas (1-65535): ").split(",")))
        
        for port in ports:
            
            if port not in ports:
                print("Port ", port, " is not valid.")
                continue
            
            print(port)
            
        ports = list(map(int, input("Enter the ports separated by commas (1-65535): ").split(",")))
        
        for port in ports:
            
            if port not in ports:
                print("Port ", port, " is not valid.")
                continue
            
            print(port)
            
        ports = list(map(int, input("Enter the ports separated by commas (1-65535): ").split(",")))
        
        for port in ports:
            
            if port not in port