#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that iterates over words or converts a port. """    
    
    while True:
        words = []
        port = int(input('Enter a port number: '))
        
        # Convert port to words
        for word in port2words(port):
            words.append(word)
        
        # Print the words
        print('\n'.join(words))
        
        # Wait for a keypress
        choice = input('Press enter to continue: ')
        
        # Exit the loop
        if choice == '':
            break
        
        # Wait for a keypress
        choice = input('Press enter to continue: ')
        
        # Exit the loop
        if choice == '':
            break
        
        # Wait for a keypress
        choice = input('Press enter to continue: ')
        
        # Exit the loop
        if choice == '':
            break
        
        # Wait for a keypress
        choice = input('Press enter to continue: ')
        
        # Exit the loop
        if choice == '':
            break
        
        # Wait for a keypress
        choice = input('Press enter to continue: ')
        
        # Exit the loop
        if choice == '':
            break
        
        # Wait for a keypress
        choice = input('Press enter to continue: ')
        
        # Exit the loop
        if choice == '':
            break
        
        # Wait for a keypress
        choice = input('Press enter to continue: ')
        
        # Exit the loop
        if choice == '':
            break
        
        # Wait for a keypress
        choice = input('Press enter to continue: ')
        
        # Exit the loop
        if choice == '':
            break
        
        # Wait for a keypress
        choice = input('Press enter to continue: ')
        
        # Exit the loop
        if choice == '':
            break
        
        # Wait for a keypress
        choice = input('Press enter to continue: ')
        
        # Exit the loop
        if choice == '':
            break
        
        # Wait for a keypress
        choice = input('Press enter to continue: ')
        
        # Exit the loop
        if choice == '':
            break
        
        # Wait for a keypress
        choice = input('Press enter to continue: ')
        
        # Exit the loop
        if choice == '':
            break
        
        # Wait for a keypress
        choice = input('Press enter to continue: ')
        
        # Exit the loop
        if choice == '':
            break
        
        # Wait for a keypress
        choice = input('Press enter to continue: ')
        
        # Exit the loop
        if choice == '':