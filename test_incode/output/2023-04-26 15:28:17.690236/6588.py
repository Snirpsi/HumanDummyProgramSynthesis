#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates all ports. """    
    
    ports = []
    
    for port in ports:
        
        port_name = port.get_name()
        
        if port_name == None:
            continue
        
        port_number = port.get_number()
        
        if port_number == None:
            continue
        
        port_type = port.get_type()
        
        if port_type == None:
            continue
        
        port_status = port.get_status()
        
        if port_status == None:
            continue
        
        port_speed = port.get_speed()
        
        if port_speed == None:
            continue
        
        port_type = port.get_type()
        
        if port_type == None:
            continue
        
        port_status = port.get_status()
        
        if port_status == None:
            continue
        
        port_speed = port.get_speed()
        
        if port_speed == None:
            continue
        
        port_type = port.get_type()
        
        if port_type == None:
            continue
        
        port_status = port.get_status()
        
        if port_status == None:
            continue
        
        port_speed = port.get_speed()
        
        if port_speed == None:
            continue
        
        port_type = port.get_type()
        
        if port_type == None:
            continue
        
        port_status = port.get_status()
        
        if port_status == None:
            continue
        
        port_speed = port.get_speed()
        
        if port_speed == None:
            continue
        
        port_type = port.get_type()
        
        if port_type == None:
            continue
        
        port_status = port.get_status()
        
        if port_status == None:
            continue
        
        port_speed = port.get_speed()
        
        if port_speed == None:
            continue
        
        port_type = port.get_type()
        
        if port_type == None:
            continue
        
        port_status = port.get_status()
        
        if port_status == None:
            continue
        
        port_speed = port.get_speed()
        
        if port_speed == None:
            continue
        
        port_type = port.get_type()
        
        if port_type == None:
            continue
        
        port_status = port.get_status()
        
        if port_status == None:
            continue
        
        port_speed = port.get_speed()
        
        if port_speed == None:
            continue
        
        port_type = port.get_type()
        
        if port_type == None:
            continue
        
        port_status = port.get_status()
        
        if port_status == None:
            continue
        
        port_speed = port.get_speed()
        
        if port_speed == None:
            continue
        
        port_