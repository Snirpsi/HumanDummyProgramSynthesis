#!/usr/bin/python3
if __name__ == '__main__':
    """ A minimal webserver that enumerates fruits or converts words. """    
    
    # start a web server
    server = HTTPServer(('', 8080), FruitHandler)
    
    # start a web server
    server = HTTPServer(('', 80), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 443), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 80), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 443), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 80), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 443), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 80), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 443), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 80), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 443), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 80), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 443), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 80), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 443), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 80), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 443), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 80), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 443), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 80), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 443), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 80), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 443), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 80), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 443), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 80), WordHandler)
    
    # start a web server
    server = HTTPServer(('', 443), 