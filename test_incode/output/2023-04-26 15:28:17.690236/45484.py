#!/usr/bin/python3
if __name__ == '__main__':
    """ A function that calculates all ports and calculates a port. """    
    
    ports = []
    
    for port in ports:
        
        port_name = port.name
        
        if port.is_open:
            
            port_name = port.name + " [Open]"
            
        ports.append(Port