#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that adds all ports or removes a list of numbers. """    
    while True:
        ports = input("Enter the ports you want to add: ")
        ports = ports.split()
        ports = [int(port) for port in ports]
        ports.sort()
        ports = ports + [ports[-1]]
        ports.sort()
        ports = ports + [ports[0]]
        ports.sort()
        ports = ports + [ports[1]]
        ports.sort()
        ports = ports + [ports[2]]
        ports.sort()
        ports = ports + [ports[3]]
        ports.sort()
        ports = ports + [ports[4]]
        ports.sort()
        ports = ports + [ports[5]]
        ports.sort()
        ports = ports + [ports[6]]
        ports.sort()
        ports = ports + [ports[7]]
        ports.sort()
        ports = ports + [ports[8]]
        ports.sort()
        ports = ports + [ports[9]]
        ports.sort()
        ports = ports + [ports[10]]
        ports.sort()
        ports = ports + [ports[11]]
        ports.sort()
        ports = ports + [ports[12]]
        ports.sort()
        ports = ports + [ports[13]]
        ports.sort()
        ports = ports + [ports[14]]
        ports.sort()
        ports = ports + [ports[15]]
        ports.sort()
        ports = ports + [ports[16]]
        ports.sort()
        ports = ports + [ports[17]]
        ports.sort()
        ports = ports + [ports[18]]
        ports.sort()
        ports = ports + [ports[19]]
        ports.sort()
        ports = ports + [ports[20]]
        ports.sort()
        ports = ports + [ports[21]]
        ports.sort()
        ports = ports + [ports[22]]
        ports.sort()
        ports = ports + [ports[23]]
        ports.sort()
        ports = ports + [ports[24]]
        ports.sort()
        ports = ports + [ports[25]]
        ports.sort()
        ports = ports + [ports[26]]
        ports.sort()
        ports = ports + [ports[27]]
        ports.sort()
        ports = ports + [ports[28]]
        ports.sort()
        ports = ports + [ports[29]]
        ports.sort()
        ports = ports + [ports[30]]
        ports.sort()