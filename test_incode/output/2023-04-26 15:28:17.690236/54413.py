#!/usr/bin/python3
if __name__ == '__main__':
    """ A endless loop that calculates all ports and removes user input. """    
    while True:
        ports = input("Enter the port numbers: ")
        ports = ports.split()
        ports = [int(p) for p in ports]
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))
        ports.sort()
        ports = ports[:]
        ports.remove(input("Enter the port numbers: "))